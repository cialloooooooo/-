import { t } from '@opentiny/vue-locale';
import '@opentiny/vue-theme/grid/index.css';
import { Interceptor, StoreMap } from '@opentiny/vue-renderless/grid/core';
import { debounce, PopupManager, PopperJS, extend, browserInfo, isObject, isEmptyObject, isServer, throttle, addClass, removeClass, getDataset, random, isNull, getActualTarget, fastdom, toDecimal, logger, tryToCurrency, tryToDecimal, tryToInt, toFileSize, truncate, toRate, isNumber as isNumber$1, toBoolValue, toDate, getDateWithNewTimezone, isDate, formatDateByPattern, toDateStr, on, Validator as Validator$1, getObj, getStyle, preventDefault, off, callInterceptor, hasClass } from '@opentiny/utils';
import { iconError, iconSortTriangleDescending, iconSortTriangleAscending, iconSortTriangle, iconUnfilter, iconEdit, iconConmentRefresh, iconMinscreen, iconFullscreen, iconSetting, iconChevronRight, iconChevronDown, iconGridNoData, iconRadio, iconRadioselected, iconSearch, iconCheckedSur, iconCheck, iconHalfselect, iconArrowBottom, IconEllipsis, iconClose, iconYes, iconExpand, iconPutAway, iconEllipsis, iconMarkOn } from '@opentiny/vue-icon';
import { $prefix, hooks, defineComponent, h, $install, isVue2, isVnode, $props, mergeClass, resolveTheme, useInstanceSlots, useRelation, emitter, resolveMode, useBreakpoint, setup as setup$1 } from '@opentiny/vue-common';
import { uniqueId, toNumber as toNumber$1, isBoolean, each, isObject as isObject$1, isFunction, isArray, findIndexOf, get, set, clone, clear, destructuring, mapTree, eachTree, has, isUndefined, isEqual, find as find$1, filterTree, arrayEach, isNumber, findTree as findTree$1, sum, template, assign, objectMap, sortBy, isString, remove, toStringJSON, toArray, arrayIndexOf } from '@opentiny/vue-renderless/grid/static/';
import { getFuncText, getDomNode, getRowid, getEventTargetNode, isScale, emitEvent, hasChildrenList, getListeners, getOffsetPos, getFilters, getColumnConfig, formatText, getRowkey, getRowUniqueId, setCellValue, getColumnList, repairFixed, rowToVisible, colToVisible, getCell, assemColumn, GlobalEvent, getCellValue, updateCellTitle, isPx, getClass, getRowNodes, getCellNodeIndex } from '@opentiny/vue-renderless/grid/utils';
import Tooltip$1 from '@opentiny/vue-tooltip';
import { createVNode, mergeProps, isVNode, createTextVNode, resolveComponent, openBlock, createElementBlock, normalizeClass, createBlock, createCommentVNode, normalizeStyle, Fragment, renderList } from 'vue';
import { toNumber, findTree, find, isEqual as isEqual$1, arrayEach as arrayEach$1 } from '@opentiny/vue-renderless/grid/static';
import { AutoTip } from '@opentiny/vue-directive';
import Button from '@opentiny/vue-button';
import Loading$1 from '@opentiny/vue-loading';
import Dropdown from '@opentiny/vue-dropdown';
import DropdownMenu from '@opentiny/vue-dropdown-menu';
import DropdownItem from '@opentiny/vue-dropdown-item';
import Exception from '@opentiny/vue-exception';
import Tag from '@opentiny/vue-tag';
import Methods$g from '@opentiny/vue-renderless/grid/plugins/export';
import Resize$1 from '@opentiny/vue-renderless/grid/plugins/resize';
import Modal from '@opentiny/vue-modal';
import Pager$1 from '@opentiny/vue-pager';

function _createForOfIteratorHelperLoose$1(r, e) {
  var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (t) return (t = t.call(r)).next.bind(t);
  if (Array.isArray(r) || (t = _unsupportedIterableToArray$1(r)) || e && r && "number" == typeof r.length) {
    t && (r = t);
    var o = 0;
    return function() {
      return o >= r.length ? { done: true } : { done: false, value: r[o++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray$1(r, a) {
  if (r) {
    if ("string" == typeof r) return _arrayLikeToArray$1(r, a);
    var t = {}.toString.call(r).slice(8, -1);
    return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray$1(r, a) : void 0;
  }
}
function _arrayLikeToArray$1(r, a) {
  (null == a || a > r.length) && (a = r.length);
  for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
  return n;
}
var _export_sfc = function _export_sfc2(sfc, props) {
  var target = sfc.__vccOpts || sfc;
  for (var _iterator = _createForOfIteratorHelperLoose$1(props), _step; !(_step = _iterator()).done; ) {
    var _step$value = _step.value, key = _step$value[0], val = _step$value[1];
    target[key] = val;
  }
  return target;
};

var _excluded = ["input", "change"], _excluded2 = ["field"];
function _defineProperties(e, r) {
  for (var t2 = 0; t2 < r.length; t2++) {
    var o = r[t2];
    o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e, _toPropertyKey(o.key), o);
  }
}
function _createClass(e, r, t2) {
  return r && _defineProperties(e.prototype, r), t2 && _defineProperties(e, t2), Object.defineProperty(e, "prototype", { writable: false }), e;
}
function _toPropertyKey(t2) {
  var i = _toPrimitive(t2, "string");
  return "symbol" == typeof i ? i : i + "";
}
function _toPrimitive(t2, r) {
  if ("object" != typeof t2 || !t2) return t2;
  var e = t2[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t2, r || "default");
    if ("object" != typeof i) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t2);
}
function _regenerator() {
  /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */
  var e, t2, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag";
  function i(r2, n2, o2, i2) {
    var c2 = n2 && n2.prototype instanceof Generator ? n2 : Generator, u2 = Object.create(c2.prototype);
    return _regeneratorDefine2(u2, "_invoke", (function(r3, n3, o3) {
      var i3, c3, u3, f2 = 0, p = o3 || [], y = false, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d2(t3, r4) {
        return i3 = t3, c3 = 0, u3 = e, G.n = r4, a;
      } };
      function d(r4, n4) {
        for (c3 = r4, u3 = n4, t2 = 0; !y && f2 && !o4 && t2 < p.length; t2++) {
          var o4, i4 = p[t2], d2 = G.p, l = i4[2];
          r4 > 3 ? (o4 = l === n4) && (u3 = i4[(c3 = i4[4]) ? 5 : (c3 = 3, 3)], i4[4] = i4[5] = e) : i4[0] <= d2 && ((o4 = r4 < 2 && d2 < i4[1]) ? (c3 = 0, G.v = n4, G.n = i4[1]) : d2 < l && (o4 = r4 < 3 || i4[0] > n4 || n4 > l) && (i4[4] = r4, i4[5] = n4, G.n = l, c3 = 0));
        }
        if (o4 || r4 > 1) return a;
        throw y = true, n4;
      }
      return function(o4, p2, l) {
        if (f2 > 1) throw TypeError("Generator is already running");
        for (y && 1 === p2 && d(p2, l), c3 = p2, u3 = l; (t2 = c3 < 2 ? e : u3) || !y; ) {
          i3 || (c3 ? c3 < 3 ? (c3 > 1 && (G.n = -1), d(c3, u3)) : G.n = u3 : G.v = u3);
          try {
            if (f2 = 2, i3) {
              if (c3 || (o4 = "next"), t2 = i3[o4]) {
                if (!(t2 = t2.call(i3, u3))) throw TypeError("iterator result is not an object");
                if (!t2.done) return t2;
                u3 = t2.value, c3 < 2 && (c3 = 0);
              } else 1 === c3 && (t2 = i3.return) && t2.call(i3), c3 < 2 && (u3 = TypeError("The iterator does not provide a '" + o4 + "' method"), c3 = 1);
              i3 = e;
            } else if ((t2 = (y = G.n < 0) ? u3 : r3.call(n3, G)) !== a) break;
          } catch (t3) {
            i3 = e, c3 = 1, u3 = t3;
          } finally {
            f2 = 1;
          }
        }
        return { value: t2, done: y };
      };
    })(r2, o2, i2), true), u2;
  }
  var a = {};
  function Generator() {
  }
  function GeneratorFunction() {
  }
  function GeneratorFunctionPrototype() {
  }
  t2 = Object.getPrototypeOf;
  var c = [][n] ? t2(t2([][n]())) : (_regeneratorDefine2(t2 = {}, n, function() {
    return this;
  }), t2), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c);
  function f(e2) {
    return Object.setPrototypeOf ? Object.setPrototypeOf(e2, GeneratorFunctionPrototype) : (e2.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e2, o, "GeneratorFunction")), e2.prototype = Object.create(u), e2;
  }
  return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function() {
    return this;
  }), _regeneratorDefine2(u, "toString", function() {
    return "[object Generator]";
  }), (_regenerator = function _regenerator2() {
    return { w: i, m: f };
  })();
}
function _regeneratorDefine2(e, r, n, t2) {
  var i = Object.defineProperty;
  try {
    i({}, "", {});
  } catch (e2) {
    i = 0;
  }
  _regeneratorDefine2 = function _regeneratorDefine(e2, r2, n2, t3) {
    function o(r3, n3) {
      _regeneratorDefine2(e2, r3, function(e3) {
        return this._invoke(r3, n3, e3);
      });
    }
    r2 ? i ? i(e2, r2, { value: n2, enumerable: !t3, configurable: !t3, writable: !t3 }) : e2[r2] = n2 : (o("next", 0), o("throw", 1), o("return", 2));
  }, _regeneratorDefine2(e, r, n, t2);
}
function asyncGeneratorStep(n, t2, e, r, o, a, c) {
  try {
    var i = n[a](c), u = i.value;
  } catch (n2) {
    return void e(n2);
  }
  i.done ? t2(u) : Promise.resolve(u).then(r, o);
}
function _asyncToGenerator(n) {
  return function() {
    var t2 = this, e = arguments;
    return new Promise(function(r, o) {
      var a = n.apply(t2, e);
      function _next(n2) {
        asyncGeneratorStep(a, r, o, _next, _throw, "next", n2);
      }
      function _throw(n2) {
        asyncGeneratorStep(a, r, o, _next, _throw, "throw", n2);
      }
      _next(void 0);
    });
  };
}
function _createForOfIteratorHelperLoose(r, e) {
  var t2 = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (t2) return (t2 = t2.call(r)).next.bind(t2);
  if (Array.isArray(r) || (t2 = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) {
    t2 && (r = t2);
    var o = 0;
    return function() {
      return o >= r.length ? { done: true } : { done: false, value: r[o++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray(r, a) {
  if (r) {
    if ("string" == typeof r) return _arrayLikeToArray(r, a);
    var t2 = {}.toString.call(r).slice(8, -1);
    return "Object" === t2 && r.constructor && (t2 = r.constructor.name), "Map" === t2 || "Set" === t2 ? Array.from(r) : "Arguments" === t2 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t2) ? _arrayLikeToArray(r, a) : void 0;
  }
}
function _arrayLikeToArray(r, a) {
  (null == a || a > r.length) && (a = r.length);
  for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
  return n;
}
function _objectWithoutPropertiesLoose(r, e) {
  if (null == r) return {};
  var t2 = {};
  for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
    if (-1 !== e.indexOf(n)) continue;
    t2[n] = r[n];
  }
  return t2;
}
function _extends() {
  return _extends = Object.assign ? Object.assign.bind() : function(n) {
    for (var e = 1; e < arguments.length; e++) {
      var t2 = arguments[e];
      for (var r in t2) ({}).hasOwnProperty.call(t2, r) && (n[r] = t2[r]);
    }
    return n;
  }, _extends.apply(null, arguments);
}
var GlobalConfig$1 = {
  validConfig: {
    message: "tooltip",
    highlightError: false,
    icon: iconError()
  },
  editConfig: {
    trigger: "click",
    mode: "cell",
    showStatus: true,
    validateOnActive: true
  },
  // 默认开启点击头部单元格触发排序
  sortConfig: {
    multipleColumnSort: false
  },
  // 默认不开启隔行换色和行高亮，不暴露此配置
  stripe: false,
  // saas下默认不开启隔行换色和行高亮
  stripeSaas: false,
  // saas下默认开启下划线
  borderSaas: true,
  // saas下通过borderVertical开启边框竖线
  borderVertical: false,
  // 鼠标移入表格行是否高亮显示
  highlightHoverRow: true,
  resizable: true,
  // 操作列（type为index或radio或selection的列）默认不可拖动列宽
  operationColumnResizable: false,
  fit: true,
  showHeader: true,
  defaultTreeIndent: 16,
  defaultTreeSpacing: 30,
  rowId: "_RID",
  // 行数据的唯一主键字段名
  $rowIndex: Symbol("$rowIndex"),
  // 虚拟滚动处理后的行索引
  version: 0,
  // 版本号，对于某些带数据缓存的功能有用到，上升版本号可以用于重置数据
  optimization: {
    animat: true,
    delayHover: 250,
    scrollDelay: 60,
    scrollX: {
      gt: 100
    },
    scrollY: {
      gt: 500
    }
  },
  icon: {
    sortAsc: iconSortTriangleDescending(),
    sortDesc: iconSortTriangleAscending(),
    sortDefault: iconSortTriangle(),
    required: "tiny-grid-required-icon",
    filter: iconUnfilter(),
    edit: iconEdit(),
    tree: "tiny-grid-icon__caret-right",
    refresh: iconConmentRefresh(),
    minscreen: iconMinscreen(),
    fullscreen: iconFullscreen(),
    custom: iconSetting(),
    jumpPrev: "tiny-grid-icon__d-arrow-left",
    jumpNext: "tiny-grid-icon__d-arrow-right",
    prevPage: "tiny-grid-icon__arrow-left",
    nextPage: "tiny-grid-icon__arrow-right",
    zoomIn: "tiny-grid-icon__zoomin",
    zoomOut: "tiny-grid-icon__zoomout",
    modalClose: "tiny-grid-icon__close",
    modalInfo: "tiny-grid-icon__info",
    modalSuccess: "tiny-grid-icon__success",
    modalWarning: "tiny-grid-icon__warning",
    modalError: "tiny-grid-icon__error",
    modalQuestion: "tiny-grid-icon__question",
    modalLoading: "tiny-grid-icon__refresh roll",
    caretBottom: "tiny-grid-icon__caret-bottom",
    dropdownBottom: "tiny-grid-icon__arrow-bottom",
    btnLoading: "tiny-grid-icon__refresh roll"
  },
  grid: {},
  menu: {},
  tooltip: {
    trigger: "hover",
    effect: "light"
  },
  pager: {},
  toolbar: {
    resizable: {
      storage: false
    },
    buttons: []
  },
  modal: {
    zIndex: 2e3,
    minWidth: 340,
    minHeight: 200,
    lockView: true,
    mask: true,
    duration: 3e3,
    marginSize: 10,
    animat: true
  },
  i18n: function i18n(key) {
    return key;
  },
  constant: {
    insertedField: "_$inserted",
    deletedField: "_$deleted",
    asyncPrefix: "_$async_"
  },
  TINY_TABLE_CUSTOM_COLUMN_WIDTH: "TINY_TABLE_CUSTOM_COLUMN_WIDTH",
  TINY_TABLE_CUSTOM_SETTING: "TINY_TABLE_CUSTOM_SETTING",
  viewConfig: {
    // 视图类型
    DEFAULT: "default",
    MF: "mf",
    CARD: "card",
    LIST: "list",
    GANTT: "gantt",
    CUSTOM: "custom",
    // 移动优先视图下展示类型
    MF_SHOW_LIST: "list",
    MF_SHOW_CARD: "card",
    // 在不同视图下表格模块的显示
    toolbar: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden sm:block"
    },
    pager: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden sm:block"
    },
    columnAnchor: {
      default: "flex",
      mf: "hidden sm:flex",
      card: "hidden"
    },
    hiddenColumn: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    tableHeader: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    emptyData: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    tableBody: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    tableFooter: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    fixedColumn: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    borderLine: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    resizeBar: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    gridLoading: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    tableWrapper: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    selectToolbar: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    mfTable: {
      default: "hidden",
      mf: "block sm:hidden",
      card: "block"
    },
    footerBorder: {
      default: "block",
      mf: "hidden sm:block",
      card: "hidden"
    },
    operButton: {
      default: "inline-block",
      mf: "hidden sm:inline-block",
      card: "hidden"
    }
  },
  themes: {
    TINY: "tiny",
    SAAS: "saas"
  },
  rowHeight: {
    tiny: {
      mini: 32,
      small: 40,
      default: 48,
      medium: 52
    },
    saas: {
      mini: 32,
      small: 36,
      default: 36,
      medium: 40
    }
  },
  headerRowHeight: {
    tiny: {
      mini: 28,
      small: 32,
      default: 40,
      medium: 40
    },
    saas: {
      mini: 32,
      small: 36,
      default: 36,
      medium: 40
    }
  },
  // 空数据最小表格高度
  emptyMinHeight: 200,
  columnLevelKey: "ColumnLevelProvideKey",
  defaultColumnName: $prefix + "GridColumn"
};
function addListConfig() {
  var viewConfig2 = GlobalConfig$1.viewConfig;
  for (var _i = 0, _Object$keys = Object.keys(viewConfig2); _i < _Object$keys.length; _i++) {
    var key = _Object$keys[_i];
    if (typeof viewConfig2[key] === "object") {
      viewConfig2[key].list = viewConfig2[key].card;
      viewConfig2[key].gantt = viewConfig2[key].card;
      viewConfig2[key].custom = viewConfig2[key].card;
    }
  }
}
addListConfig();
var setup = function setup2(options) {
  if (options === void 0) {
    options = {};
  }
  var icon = GlobalConfig$1.icon, menu = GlobalConfig$1.menu;
  if (options.menu) {
    menu = extend(true, {}, menu, options.menu);
  }
  if (options.icon) {
    icon = extend(true, {}, icon, options.icon);
  }
  extend(true, GlobalConfig$1, options, {
    icon,
    menu
  });
};
function getAttrs(_ref, params) {
  var name = _ref.name, attrs = _ref.attrs;
  var props = attrs;
  if (typeof attrs === "function") {
    props = attrs(params);
  }
  if (name === "input") {
    props = _extends({
      type: "text"
    }, props);
  }
  return props;
}
var isSyncCell = function isSyncCell2(renderOpts, params, context) {
  return renderOpts.type === "visible" || context.$type === "cell";
};
function autoRefresh(renderOpts, params) {
  var _renderOpts$refresh = renderOpts.refresh, refresh = _renderOpts$refresh === void 0 ? false : _renderOpts$refresh;
  var $table = params.$table, row2 = params.row;
  var _$table$editConfig = $table.editConfig, editConfig = _$table$editConfig === void 0 ? {} : _$table$editConfig;
  return $table.isTemporaryRow(row2) || !editConfig.showStatus || typeof refresh === "boolean" && refresh || typeof refresh === "function" && refresh(params);
}
function getEvents(renderOpts, params, context) {
  var _on;
  var _renderOpts$component = renderOpts.component, component = _renderOpts$component === void 0 ? {} : _renderOpts$component, _renderOpts$events = renderOpts.events, events = _renderOpts$events === void 0 ? {} : _renderOpts$events;
  var native = typeof component === "string";
  var editorModel = component.model || {};
  var type = component === "select" ? "change" : native ? "input" : editorModel.event || "update:modelValue";
  var input = events.input, change = events.change, other = _objectWithoutPropertiesLoose(events, _excluded);
  var $table = params.$table, column = params.column, row2 = params.row;
  var model = column.model;
  var on2 = (_on = {}, _on[type] = function(event) {
    var cellValue = native ? event.target.value : event;
    if (!isSyncCell(renderOpts, params, context)) {
      model.update = true;
      model.value = cellValue;
    }
    setCellValue(row2, column, cellValue);
    Promise.resolve().then(function() {
      $table.updateStatus(params, cellValue, renderOpts);
    });
    if (native) {
      input && input.apply(null, [params].concat.apply(params, arguments));
      change && change.apply(null, [params].concat.apply(params, arguments));
    }
    if (autoRefresh(renderOpts, params)) {
      $table.editStore.editorAutoRefreshKey++;
    }
  }, _on);
  var evts = native ? other : events;
  var mapHandler = function mapHandler2(cb) {
    return function() {
      cb.apply(null, [params].concat.apply(params, arguments));
    };
  };
  assign(on2, objectMap(evts, mapHandler));
  return on2;
}
function renderOptions(h2, options, renderOpts, params, context) {
  var _renderOpts$optionPro = renderOpts.optionProps, optionProps = _renderOpts$optionPro === void 0 ? {} : _renderOpts$optionPro;
  var labelProp = optionProps.label || "label";
  var valueProp = optionProps.value || "value";
  var column = params.column, row2 = params.row;
  var formatConfig = column.own.formatConfig;
  var cellValue = isSyncCell(renderOpts, params, context) ? getCellValue(row2, column) : column.model.value;
  if (!options && formatConfig && formatConfig.data) {
    options = formatConfig.data;
  }
  var hasSelected = false;
  var optionsList = options.map(function(item, index2) {
    var selected = item.value === cellValue;
    if (selected) {
      hasSelected = true;
    }
    var attrs = {
      domProps: {
        value: item[valueProp],
        selected
      },
      key: index2
    };
    return h2("option", attrs, item[labelProp]);
  });
  if (options.length && !hasSelected) {
    optionsList.unshift(h2("option", {
      style: "display:none",
      selected: true
    }, ""));
  }
  return optionsList;
}
function renderOptgroups(h2, options, params, context) {
  var optionGroups = options.optionGroups, _options$optionGroupP = options.optionGroupProps, optionGroupProps = _options$optionGroupP === void 0 ? {} : _options$optionGroupP;
  var groupLabel = optionGroupProps.label || "label";
  var groupOptions = optionGroupProps.options || "options";
  return optionGroups.map(function(group, key) {
    var attrs = {
      domProps: {
        label: group[groupLabel]
      },
      key
    };
    var children = renderOptions(h2, group[groupOptions], options, params, context);
    return h2("optgroup", attrs, children);
  });
}
function handleConfirmFilter(context, column, checked, item) {
  var key = column.filterMultiple ? "changeMultipleOption" : "changeRadioOption";
  var method = context[key];
  method({}, checked, item);
}
function getFilterEvents(item, renderOpts, params, context) {
  var _on2;
  var events = renderOpts.events, name = renderOpts.name;
  var column = params.column;
  var type = name === "select" ? "change" : "input";
  var on2 = (_on2 = {}, _on2[type] = function(event) {
    item.data = event.target.value;
    handleConfirmFilter(context, column, !!item.data, item);
  }, _on2);
  if (events) {
    var mapHandler = function mapHandler2(cb) {
      return function() {
        cb.apply(null, [params].concat.apply(params, arguments));
      };
    };
    assign(on2, objectMap(events, mapHandler));
  }
  return on2;
}
function defaultFilterRender(h2, renderOpts, params, context) {
  var name = renderOpts.name;
  var column = params.column;
  var attrs = getAttrs(renderOpts, params);
  return column.filters.map(function(item) {
    var props = {
      class: "tiny-grid-default-" + name,
      attrs,
      domProps: {
        value: item.data
      },
      on: getFilterEvents(item, renderOpts, params, context)
    };
    return h2(name, props);
  });
}
function defaultFilterMethod(_ref2) {
  var option = _ref2.option, row2 = _ref2.row, column = _ref2.column;
  var cellValue = get(row2, column.property);
  var data7 = option.data;
  return cellValue == data7;
}
function renderSelectEdit(h2, renderOpts, params, context) {
  var column = params.column, $table = params.$table, row2 = params.row;
  var editorKey = "editor-" + getRowid($table, row2) + "-" + column.id;
  var props = {
    ref: editorKey,
    class: "tiny-grid-default-select",
    on: getEvents(renderOpts, params, context)
  };
  var children = renderOpts.optionGroups ? renderOptgroups(h2, renderOpts, params, context) : renderOptions(h2, renderOpts.options, renderOpts, params, context);
  return [h2("select", props, children)];
}
function defaultEditRender(h2, renderOpts, params, context) {
  var _extends2;
  var $table = params.$table, column = params.column, row2 = params.row;
  var _column$formatValue = column.formatValue, formatValue = _column$formatValue === void 0 ? function(_ref3) {
    var cellValue2 = _ref3.cellValue;
    return cellValue2;
  } : _column$formatValue, own = column.own;
  var formatOpt = own.formatConfig || {};
  var children = renderOpts.children, _renderOpts$component2 = renderOpts.component, component = _renderOpts$component2 === void 0 ? {} : _renderOpts$component2;
  var attrs = getAttrs(renderOpts, params);
  var value = isSyncCell(renderOpts, params, context) ? getCellValue(row2, column) : column.model.value;
  var cellValue = formatValue(_extends({
    cellValue: value
  }, params));
  var isTag = ~["input", "textarea", "select"].indexOf(component);
  var editorModel = component.model || {};
  var modelProps = typeof component === "string" ? "value" : editorModel.prop || "modelValue";
  var editorKey = "editor-" + getRowid($table, row2) + "-" + column.id;
  var key = row2[$table.rowId];
  var options = {
    ref: editorKey,
    key,
    class: isTag ? "tiny-grid-default-" + component : "",
    attrs: _extends({
      formatOpt,
      options: formatOpt.data
    }, attrs, (_extends2 = {}, _extends2[modelProps] = cellValue, _extends2)),
    on: getEvents(renderOpts, params, context)
  };
  if (autoRefresh(renderOpts, params)) {
    options.attrs.editorAutoRefreshKey = $table.editStore.editorAutoRefreshKey;
  }
  var slot = children ? children(_extends({
    props: options
  }, params), h2) : null;
  var cell = [h2(hooks.toRaw(component), options, slot)];
  return isTag ? cell : [h2("div", {
    class: "tiny-grid-editor"
  }, cell)];
}
var buildRenderMap = function buildRenderMap2() {
  var renderMapInput = [["autofocus", "input"], ["renderEdit", defaultEditRender], ["renderDefault", defaultEditRender], ["renderFilter", defaultFilterRender], ["filterMethod", defaultFilterMethod]];
  var mapHandler = function mapHandler2(mapArr) {
    var obj = {};
    mapArr.forEach(function(item) {
      return obj[item[0]] = item[1];
    });
    return obj;
  };
  var renderMap2 = {};
  renderMap2.input = mapHandler(renderMapInput);
  renderMapInput[0][1] = "textarea";
  renderMap2.textarea = mapHandler(renderMapInput);
  renderMapInput.splice(0, 1);
  renderMapInput[0][1] = renderMapInput[1][1] = renderSelectEdit;
  renderMapInput[2][1] = function(h2, renderOpts, params, context) {
    var attrs = renderOpts.attrs;
    var column = params.column;
    return column.filters.map(function(item) {
      var props = {
        attrs,
        class: "tiny-grid-default-select",
        on: getFilterEvents(item, renderOpts, params, context)
      };
      var children = renderOpts.optionGroups ? renderOptgroups(h2, renderOpts, params) : renderOptions(h2, renderOpts.options, renderOpts, params);
      return h2("select", props, children);
    });
  };
  renderMap2.select = mapHandler(renderMapInput);
  return renderMap2;
};
var renderMap = buildRenderMap();
var buildRenderer = function buildRenderer2() {
  var Renderer2 = {};
  Renderer2.mixin = function(map) {
    each(map, function(options, name) {
      return Renderer2.add(name, options);
    });
    return Renderer2;
  };
  Renderer2.get = function(name) {
    return isObject$1(name) || isFunction(name) ? renderMap.input : renderMap[name] || null;
  };
  Renderer2.add = function(name, options) {
    var renders;
    var flag = name && options;
    if (!flag) {
      return Renderer2;
    }
    renders = renderMap[name];
    if (renders) {
      Object.assign(renders, options);
    } else {
      renderMap[name] = options;
    }
    return Renderer2;
  };
  Renderer2.delete = function(name) {
    delete renderMap[name];
    return Renderer2;
  };
  return Renderer2;
};
var Renderer = buildRenderer();
var installedPlugins = [];
var Buttons = StoreMap;
var Menus = StoreMap;
var Grid$1 = {
  use: function use(Plugin, options) {
    if (Plugin && Plugin.install) {
      if (!installedPlugins.includes(Plugin)) {
        Plugin.install(this, options);
        installedPlugins.push(Plugin);
      }
    }
    return this;
  },
  setup,
  interceptor: Interceptor,
  renderer: Renderer,
  buttons: Buttons,
  menus: Menus,
  _tooltip: true
};
var handleClearEvent = function handleClearEvent2(params, event) {
  if (params && params.$table) {
    var getTarget = params.$table.getEventTargetNode;
    var bodyEl = document.body;
    if (getTarget(event, bodyEl, "tiny-autocomplete-suggestion").flag || getTarget(event, bodyEl, "tiny-select-dropdown").flag || getTarget(event, bodyEl, "tiny-cascader__dropdown").flag || getTarget(event, bodyEl, "tiny-cascader-menus").flag || getTarget(event, bodyEl, "tiny-picker-panel").flag || getTarget(event, bodyEl, "tiny-popper").flag || getTarget(event, bodyEl, "tiny-dialog-box").flag) {
      return false;
    }
  }
};
Grid$1.interceptor.add("event.clear_actived", handleClearEvent);
Grid$1.interceptor.add("event.clear_filter", handleClearEvent);
var outLog = function outLog2(type) {
  return function(message, detail) {
    var msg = "[tiny-grid] " + (GlobalConfig$1.i18n(message) || message);
    if (detail) {
      msg += ": " + detail;
    }
    logger[type](msg);
    return msg;
  };
};
var warn = outLog("warn");
var error = outLog("error");
var DateFormat = {
  LONGDATETIME: "yyyy-MM-dd hh:mm:ss",
  DATETIME: "yyyy-MM-dd hh:mm",
  DATE: "yyyy-MM-dd",
  LONGTIME: "hh:mm:ss",
  TIME: "hh:mm",
  YEARMONTH: "yyyy-MM"
};
var dateFormat = function dateFormat2(value, formatString) {
  var userFormat = _extends({
    dateFormat: formatString
  }, this.own.formatConfig);
  if (value) {
    if (typeof value === "string") {
      if (userFormat.valueFormat) {
        value = toDate(value, userFormat.valueFormat);
      } else {
        warn("ui.grid.error.missingValueFormat");
        if (isNaN(Date.parse(value))) {
          return value;
        }
      }
    } else if (typeof value === "number") {
      value = new Date(value);
    }
  }
  if (userFormat.isutc8) {
    if (!value) {
      return value;
    }
    var currentTimezone = 0 - (/* @__PURE__ */ new Date()).getTimezoneOffset() / 60;
    var newDate = getDateWithNewTimezone(isDate(value) ? value : new Date(toDate(value)), currentTimezone, userFormat.timezone || 8);
    return formatDateByPattern(newDate, userFormat.dateFormat);
  }
  return toDateStr(value, userFormat.dateFormat, userFormat.timezone);
};
var Formatter = {
  money: function money(value) {
    var prefix = "$";
    var format2 = _extends({
      fraction: 2,
      flag: prefix + "{0}"
    }, this.own.formatConfig);
    return tryToCurrency(value, format2.fraction, format2.flag, "");
  },
  enum: function _enum(value) {
    var format2 = _extends({
      label: "label",
      value: "value"
    }, this.own.formatConfig);
    var data7 = Array.isArray(format2.data) && find$1(format2.data, function(item) {
      return item[format2.value] === value;
    });
    return data7 ? data7[format2.label] : "";
  },
  select: function select(cellValue) {
    var _this$editor = this.editor, options = _this$editor.options, optionGroups = _this$editor.optionGroups, _this$editor$optionPr = _this$editor.optionProps, optionProps = _this$editor$optionPr === void 0 ? {} : _this$editor$optionPr, _this$editor$optionGr = _this$editor.optionGroupProps, optionGroupProps = _this$editor$optionGr === void 0 ? {} : _this$editor$optionGr;
    var format2 = this.own.formatConfig;
    if (isNull(cellValue) || cellValue === "") {
      return "";
    }
    var selectItem;
    var labelProp = optionProps.label || "label";
    var valueProp = optionProps.value || "value";
    if (optionGroups) {
      var groupOptions = optionGroupProps.options || "options";
      for (var _index = 0; _index < optionGroups.length; _index++) {
        selectItem = find$1(optionGroups[_index][groupOptions], function(item) {
          return item[valueProp] === cellValue;
        });
        if (selectItem) {
          break;
        }
      }
      return selectItem ? selectItem[labelProp] : null;
    } else {
      if (!options && format2 && format2.async && format2.data) {
        options = format2.data;
      }
      selectItem = find$1(options, function(item) {
        return item[valueProp] === cellValue;
      });
      return selectItem ? selectItem[labelProp] : null;
    }
  },
  number: function number(value) {
    var format2 = _extends({
      fraction: 2
    }, this.own.formatConfig);
    return tryToDecimal(value, format2.fraction, false, "");
  },
  integer: function integer(value) {
    return tryToInt(value, "");
  },
  filesize: function filesize(value) {
    var format2 = _extends({}, this.own.formatConfig);
    return toFileSize(value, format2.unit, format2.currUnit);
  },
  date: function date(value) {
    return dateFormat.call(this, value, DateFormat.DATE);
  },
  dateTime: function dateTime(value) {
    return dateFormat.call(this, value, DateFormat.DATETIME);
  },
  longDateTime: function longDateTime(value) {
    return dateFormat.call(this, value, DateFormat.LONGDATETIME);
  },
  time: function time(value) {
    return dateFormat.call(this, value, DateFormat.TIME);
  },
  longTime: function longTime(value) {
    return dateFormat.call(this, value, DateFormat.LONGTIME);
  },
  yearMonth: function yearMonth(value) {
    return dateFormat.call(this, value, DateFormat.YEARMONTH);
  },
  ellipsis: function ellipsis(value) {
    var format2 = _extends({}, this.own.formatConfig);
    return function(h2) {
      return h2("div", {
        class: "data-ellipsis"
      }, [h2("span", {
        domProps: {
          title: value
        }
      }, truncate(value, format2.len))]);
    };
  },
  /**
   * rate 渲染器的计算规则：
   * 如果配置了 max，就按照 value / max 计算样式百分比，按照 value / 1 计算显示百分比；
   * 如果未配置 max，就按照 value / total 计算样式百分比和显示百分比；
   * @param {Number} value
   * @returns Function
   */
  rate: function rate(value) {
    var format2 = _extends({
      fraction: 2,
      total: 1,
      section: [30, 60, 90]
    }, this.own.formatConfig);
    var rateMethod = format2.rateMethod;
    var rate2;
    var formatTotal = format2.total;
    var getLengthRate = function getLengthRate2(value2, config2) {
      var lengthRate2;
      if (config2.max && typeof config2.max === "number" && typeof value2 === "number") {
        formatTotal = 1;
        lengthRate2 = toRate(value2 / config2.max, formatTotal, format2.fraction);
      }
      return lengthRate2;
    };
    var lengthRate = getLengthRate(value, format2);
    if (typeof rateMethod === "function") {
      rate2 = rateMethod.call(this, value, Object.assign({
        formatTotal
      }, format2));
    } else {
      rate2 = toRate(value, formatTotal, format2.fraction);
    }
    lengthRate = lengthRate || rate2;
    var section = format2.section;
    var css = "";
    if (section) {
      var rateValue = parseInt(lengthRate, 10);
      if (!isNumber$1(rateValue)) {
        return rate2;
      }
      if (rateValue === 100) {
        css = " tiny-grid__chart-completed";
      } else {
        var cssMap = {
          0: " tiny-grid__chart-danger",
          1: " tiny-grid__chart-warning",
          2: " tiny-grid__chart-normal"
        };
        Array.isArray(section) || (section = section.split(","));
        for (var i = 0; i < section.length; i++) {
          if (rateValue < section[i]) {
            css = cssMap[i];
            break;
          }
        }
      }
    }
    return function(h2) {
      return h2("div", {
        class: "tiny-grid__data-rate"
      }, [h2("div", {
        class: "tiny-grid__rate-chart" + css,
        style: {
          width: lengthRate
        }
      }), h2("span", {
        class: "tiny-grid__rate-text"
      }, rate2)]);
    };
  },
  boole: function boole(value) {
    var defaultFormat = {
      trueValue: true,
      falseValue: false,
      htmlView: true,
      noFork: false
    };
    var format2 = Object.assign(defaultFormat, this.own.formatConfig);
    var cellValue = !isNull(format2.trueValue) ? value === format2.trueValue : toBoolValue(value);
    if (!format2.htmlView) {
      return cellValue;
    }
    if (format2.noFork && !value) {
      return "";
    }
    var icon = !cellValue ? iconClose() : iconYes();
    return function(h2) {
      return h2("div", {
        class: "tiny-grid__data-boole"
      }, [h2(icon, {
        class: ["tiny-svg-size", !cellValue ? "icon-close" : "icon-yes"]
      })]);
    };
  }
};
function getRenderType(_ref4) {
  var editor = _ref4.editor, formatConfig = _ref4.formatConfig, formatText2 = _ref4.formatText;
  var renderType = formatText2 || formatConfig.type;
  if (!renderType && editor && editor.component === "select") {
    renderType = "select";
  }
  return renderType;
}
function getFormatData(_ref5) {
  var $table = _ref5.$table, cacheFormat = _ref5.cacheFormat, row2 = _ref5.row;
  var rest, formatData;
  if (cacheFormat) {
    rest = $table.fullDataRowMap.get(hooks.toRaw(row2));
    formatData = rest.formatData;
    if (!formatData) {
      formatData = $table.fullDataRowMap.get(hooks.toRaw(row2)).formatData = {};
    }
  }
  return {
    rest,
    formatData
  };
}
function hasCacheValue(_ref6) {
  var async = _ref6.async, cellValue = _ref6.cellValue, colid = _ref6.colid, formatData = _ref6.formatData, rest = _ref6.rest;
  return rest && formatData[colid] && formatData[colid].value === cellValue && (!async || formatData[colid].label);
}
function notHasCacheVal(_ref7) {
  var cellLabel = _ref7.cellLabel, cellValue = _ref7.cellValue, column = _ref7.column, formatText2 = _ref7.formatText, hasCacheVal = _ref7.hasCacheVal, params = _ref7.params, renderType = _ref7.renderType, row2 = _ref7.row;
  if (!hasCacheVal) {
    var cellText = row2[GlobalConfig$1.constant.insertedField + column.property];
    var formatParams = {
      cellValue
    };
    if (cellText) {
      formatParams.cellLabel = cellText;
    }
    cellLabel = isString(renderType) ? Formatter[renderType].call(column, cellValue) : formatText2(Object.assign(formatParams, params));
  }
  return cellLabel;
}
function setFormatData(_ref8) {
  var cellLabel = _ref8.cellLabel, cellValue = _ref8.cellValue, colid = _ref8.colid, formatData = _ref8.formatData;
  if (formatData) {
    formatData[colid] = {
      value: cellValue,
      label: cellLabel
    };
  }
}
var getCellLabel = function getCellLabel2(row2, column, params) {
  var formatText2 = column.formatText, editor = column.editor;
  var _column$own$formatCon = column.own.formatConfig, formatConfig = _column$own$formatCon === void 0 ? {} : _column$own$formatCon;
  var cellValue = getCellValue(row2, column);
  var cellLabel = cellValue;
  var renderType = getRenderType({
    editor,
    formatConfig,
    formatText: formatText2
  });
  if (params && renderType) {
    var $table = params.$table;
    var colid = column.id;
    var _column$own$formatCon2 = column.own.formatConfig, formatConfig2 = _column$own$formatCon2 === void 0 ? {} : _column$own$formatCon2;
    var async = formatConfig2.async === true;
    var cacheFormat = $table == null ? void 0 : $table.fullDataRowMap.has(hooks.toRaw(row2));
    var _getFormatData = getFormatData({
      $table,
      cacheFormat,
      row: row2
    }), rest = _getFormatData.rest, formatData = _getFormatData.formatData;
    var hasCacheVal = hasCacheValue({
      async,
      cellValue,
      colid,
      formatData,
      rest
    });
    if (hasCacheVal) {
      return formatData[colid].label;
    }
    cellLabel = notHasCacheVal({
      cellLabel,
      cellValue,
      column,
      formatText: formatText2,
      hasCacheVal,
      params,
      renderType,
      row: row2
    });
    setFormatData({
      cellLabel,
      cellValue,
      colid,
      formatData
    });
  }
  return cellLabel;
};
var index = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  Formatter,
  error,
  getCellLabel,
  warn
}, Symbol.toStringTag, {
  value: "Module"
}));
var initDrag = function initDrag2(dndProxyElement, dndElements, config2) {
  if (config2 === void 0) {
    config2 = {};
  }
  if (dndElements instanceof NodeList) {
    dndElements = Array.from(dndElements);
  }
  var onDragStart = function onDragStart2(e) {
    if (canDrag(e)) {
      e.dataTransfer.effectAllowed = "move";
      startDrag(e, config2.dragStart);
    }
  };
  var onDragOver = function onDragOver2(e) {
    return preventDefault(e);
  };
  var onDragEnter = function onDragEnter2(e) {
    if (canDrop(e)) {
      addDropClass(e);
    }
  };
  var onDragEnd = function onDragEnd2() {
    removeDropClass();
  };
  var onDrop = function onDrop2(e) {
    if (canDrop(e)) {
      startDrop(e, config2.drop);
    }
  };
  var groupNames = [];
  var elGroups = [];
  var elNodes = [];
  var parentMap = /* @__PURE__ */ new WeakMap();
  var childrenMap = /* @__PURE__ */ new WeakMap();
  if (dndProxyElement) {
    if (Array.isArray(dndElements) && dndElements.length > 0) {
      dndElements.forEach(function(el) {
        if (dndProxyElement.contains(el)) {
          var draggable = el.getAttribute("draggable") === "true";
          var groupName = el.dataset && el.dataset[config2.groupKey || "dndGroup"] || "";
          var dndId = el.dataset && el.dataset[config2.idKey || "dndId"];
          var dndPid = el.dataset && el.dataset[config2.pidKey || "dndPid"];
          if (draggable) {
            var _index2 = groupNames.indexOf(groupName);
            if (_index2 === -1) {
              groupNames.push(groupName);
              elGroups.push([el]);
            } else {
              elGroups[_index2].push(el);
            }
            elNodes.push({
              dndId,
              dndPid,
              el
            });
            childrenMap.set(el, []);
          }
        }
      });
      elNodes.forEach(function(_ref9) {
        _ref9.dndId; var dndPid = _ref9.dndPid, el = _ref9.el;
        if (dndPid) {
          var parent = elNodes.find(function(elNode) {
            return elNode.dndId === dndPid;
          }).el;
          parentMap.set(el, parent);
          childrenMap.get(parent).push(el);
        } else {
          parentMap.set(el, null);
        }
      });
      elNodes.splice(0, elNodes.length);
    }
    on(dndProxyElement, "dragstart", onDragStart);
    on(dndProxyElement, "dragover", onDragOver);
    on(dndProxyElement, "dragenter", onDragEnter);
    on(dndProxyElement, "dragend", onDragEnd);
    on(dndProxyElement, "drop", onDrop);
  }
  var dragTarget;
  var dndGroup2;
  var dropTarget;
  var isParent = function isParent2(el1, el2) {
    var parents = [];
    var p = parentMap.get(el2);
    if (p) {
      parents.push(p);
      while (true) {
        p = parentMap.get(p);
        if (p) {
          parents.push(p);
        } else {
          break;
        }
      }
    }
    return parents.includes(el1);
  };
  var isChild = function isChild2(el1, el2) {
    return isParent(el2, el1);
  };
  var isSibling = function isSibling2(el1, el2) {
    var p1 = parentMap.get(el1);
    var p2 = parentMap.get(el2);
    return !p1 && !p2 || p1 && p2 && p1 === p2;
  };
  var isOther = function isOther2(el1, el2) {
    return !isParent(el1, el2) && !isChild(el1, el2) && !isSibling(el1, el2);
  };
  var getClosestEl = function getClosestEl2(el) {
    for (var i = 0; i < elGroups.length; i++) {
      for (var j = 0; j < elGroups[i].length; j++) {
        var target = elGroups[i][j];
        if (target === el || target.contains(el)) {
          return target;
        }
      }
    }
    return el;
  };
  var canDrag = function canDrag2(e) {
    var found = false;
    var dragTarget2 = getClosestEl(e.target);
    for (var i = 0; i < elGroups.length; i++) {
      if (elGroups[i].includes(dragTarget2)) {
        found = true;
        break;
      }
    }
    return found;
  };
  var startDrag = function startDrag2(e, callback) {
    dragTarget = getClosestEl(e.target);
    for (var i = 0; i < elGroups.length; i++) {
      if (elGroups[i].includes(dragTarget)) {
        dndGroup2 = elGroups[i];
        break;
      }
    }
    if (typeof callback === "function") {
      callback(dragTarget, dndGroup2, e);
    }
  };
  var canDrop = function canDrop2(e) {
    if (!dragTarget || !dndGroup2) {
      return false;
    }
    var dropTarget2 = getClosestEl(e.target);
    return dragTarget && dropTarget2 && dragTarget !== dropTarget2 && // 必须在同一个组内
    dndGroup2.includes(dropTarget2) && // 不允许从父级向子级拖拽
    !isParent(dragTarget, dropTarget2);
  };
  var startDrop = function startDrop2(e, callback) {
    dropTarget = getClosestEl(e.target);
    if (typeof callback === "function") {
      var type = "";
      if (isChild(dragTarget, dropTarget)) {
        type = "child";
      } else if (isSibling(dragTarget, dropTarget)) {
        type = "sibling";
      } else if (isOther(dragTarget, dropTarget)) {
        type = "other";
      }
      callback(type, dragTarget, dropTarget, e);
    }
    dragTarget = dropTarget = dndGroup2 = null;
  };
  var enterTarget;
  var addDropClass = function addDropClass2(e) {
    if (!config2.dropClass) return;
    removeDropClass();
    enterTarget = getClosestEl(e.target);
    if (enterTarget) {
      enterTarget.classList.add(config2.dropClass);
    }
  };
  var removeDropClass = function removeDropClass2() {
    if (!config2.dropClass) return;
    if (enterTarget) {
      enterTarget.classList.remove(config2.dropClass);
      enterTarget = null;
    }
  };
  var destroy = function destroy2() {
    if (dndProxyElement) {
      off(dndProxyElement, "dragstart", onDragStart);
      off(dndProxyElement, "dragover", onDragOver);
      off(dndProxyElement, "dragenter", onDragEnter);
      off(dndProxyElement, "dragend", onDragEnd);
      off(dndProxyElement, "drop", onDrop);
    }
    removeDropClass();
    dndProxyElement = dndElements = null;
    groupNames = elGroups = null;
    parentMap = childrenMap = null;
  };
  return {
    destroy
  };
};
var headerTh = "th.tiny-grid-header__column:not(.col__gutter):not(.fixed__hidden)";
var groupKey = "dndGroup";
var idKey = "colid";
var pidKey = "pColid";
var timer = null;
var time2 = 2e3;
var dndGroup = 0;
var setDndAttribute = function setDndAttribute2(ths, colidMap, isColumnGroupLevel) {
  var pColidArr = [];
  var groupArr = [];
  ths.forEach(function(th) {
    var pColid = colidMap.get(th.dataset.colid).pColid || "";
    th.setAttribute("draggable", true);
    th.setAttribute("data-p-colid", pColid);
    var index2 = pColidArr.indexOf(pColid);
    if (index2 === -1) {
      pColidArr.push(pColid);
      groupArr.push([th]);
    } else {
      groupArr[index2].push(th);
    }
  });
  var groupId;
  if (!isColumnGroupLevel) {
    groupId = ++dndGroup;
  }
  groupArr.forEach(function(group) {
    if (isColumnGroupLevel) {
      groupId = ++dndGroup;
    }
    group.forEach(function(th) {
      return th.setAttribute("data-dnd-group", groupId);
    });
  });
};
var getColidMap = function getColidMap2(treeArray) {
  var result = /* @__PURE__ */ new Map();
  var parentMap = /* @__PURE__ */ new WeakMap();
  var colidMap = /* @__PURE__ */ new Map();
  var _traverse = function traverse(children, parent) {
    if (Array.isArray(children) && children.length > 0) {
      children.forEach(function(child) {
        parentMap.set(child, parent);
        colidMap.set(child.id, child);
        _traverse(child.children, child);
      });
    }
  };
  _traverse(treeArray, null);
  for (var _iterator = _createForOfIteratorHelperLoose(colidMap), _step; !(_step = _iterator()).done; ) {
    var _step$value = _step.value, colid = _step$value[0], column = _step$value[1];
    var pCol = parentMap.get(column);
    var pColid = pCol ? pCol.id : "";
    result.set(colid, {
      pColid,
      pCol,
      column
    });
  }
  return result;
};
var createDragHander = function createDragHander2(state, $table) {
  var dropConfig = state.dropConfig;
  var dropable = (dropConfig == null ? void 0 : dropConfig.column) && (dropConfig == null ? void 0 : dropConfig.scheme) === "v2";
  var dragStart = function dragStart2(dragTarget) {
    var dragColid = dragTarget.dataset.colid;
    var dragParentColumn = state.colidMap.get(dragColid).pCol;
    var dragParentChildren = dragParentColumn ? dragParentColumn.children : state.collectColumn;
    var dragColumn = state.colidMap.get(dragColid).column;
    var dragIndex = dragParentChildren.indexOf(dragColumn);
    $table.$emit("column-drag-start", {
      dragParentChildren,
      dragColumn,
      dragIndex
    });
    clearTimeout(timer);
  };
  var drop = function drop2(_, dragTarget, dropTarget) {
    var _state$dropConfig;
    var dragColid = dragTarget.dataset.colid;
    var dropColid = dropTarget.dataset.colid;
    var dragParentColumn = state.colidMap.get(dragColid).pCol;
    var dragParentChildren = dragParentColumn ? dragParentColumn.children : state.collectColumn;
    var dragColumn = state.colidMap.get(dragColid).column;
    var dragIndex = dragParentChildren.indexOf(dragColumn);
    var dropParentColumn = state.colidMap.get(dropColid).pCol;
    var dropParentChildren = dropParentColumn ? dropParentColumn.children : state.collectColumn;
    var dropColumn = state.colidMap.get(dropColid).column;
    var dropIndex = dropParentChildren.indexOf(dropColumn);
    var args = {
      dragParentChildren,
      dragColumn,
      dragIndex,
      dropParentChildren,
      dropColumn,
      dropIndex
    };
    callInterceptor((_state$dropConfig = state.dropConfig) == null ? void 0 : _state$dropConfig.columnBeforeDrop, {
      args: [args],
      done: function done() {
        dragParentChildren.splice(dragIndex, 1);
        dropParentChildren.splice(dropIndex, 0, dragColumn);
        var lastScrollLeft = $table.lastScrollLeft, lastScrollTop = $table.lastScrollTop, scrollXLoad = $table.scrollXLoad, scrollYLoad = $table.scrollYLoad;
        $table.loadColumn(state.collectColumn);
        $table.$emit("column-drop", args);
        setTimeout(function() {
          if (lastScrollLeft || lastScrollTop) {
            $table.scrollTo(lastScrollLeft, lastScrollTop);
            scrollXLoad && $table.triggerScrollXEvent();
            scrollYLoad && $table.triggerScrollYEvent({
              target: {
                scrollTop: lastScrollTop
              }
            });
          }
        });
        if ($table.getVm("toolbar") && dropable) {
          timer = setTimeout(function() {
            $table.getVm("toolbar").$refs.custom.saveSettings("drag");
          }, time2);
        }
      }
    });
  };
  return {
    dragStart,
    drop
  };
};
var createTableColumnWatch = function createTableColumnWatch2($table, state, isColumnGroupLevel, stopHandlerMap) {
  return debounce(100, function() {
    var dndProxy = $table.$el.querySelector("thead");
    if (dndProxy) {
      var _state$dropConfig2;
      var dndThs = dndProxy.querySelectorAll(headerTh);
      if (dndThs.length > 0) {
        dndThs = Array.from(dndThs);
      }
      setDndAttribute(dndThs, state.colidMap, isColumnGroupLevel);
      if (stopHandlerMap.has(dndProxy)) {
        stopHandlerMap.get(dndProxy).destroy();
        stopHandlerMap.delete(dndProxy);
      }
      var _createDragHander = createDragHander(state, $table), dragStart = _createDragHander.dragStart, drop = _createDragHander.drop;
      var dropClass = ((_state$dropConfig2 = state.dropConfig) == null ? void 0 : _state$dropConfig2.columnDropClass) || "";
      stopHandlerMap.set(dndProxy, initDrag(dndProxy, dndThs, {
        dragStart,
        drop,
        dropClass,
        groupKey,
        idKey,
        pidKey
      }));
    }
  });
};
var useDrag = function useDrag2(_ref0) {
  var _state$dropConfig3, _hooks$getCurrentInst, _state$dropConfig4;
  var props = _ref0.props, collectColumn2 = _ref0.collectColumn, tableColumn2 = _ref0.tableColumn;
  var state = hooks.reactive({
    dropConfig: hooks.toRef(props, "dropConfig"),
    collectColumn: collectColumn2,
    tableColumn: tableColumn2,
    colidMap: null
  });
  if (!state.dropConfig || ((_state$dropConfig3 = state.dropConfig) == null ? void 0 : _state$dropConfig3.scheme) !== "v2") return;
  var $table = (_hooks$getCurrentInst = hooks.getCurrentInstance()) == null ? void 0 : _hooks$getCurrentInst.proxy;
  if ((_state$dropConfig4 = state.dropConfig) != null && _state$dropConfig4.column) {
    var _state$dropConfig5, _state$dropConfig6;
    var isColumnGroupLevel = !((_state$dropConfig5 = state.dropConfig) != null && _state$dropConfig5.columnGroup) || ((_state$dropConfig6 = state.dropConfig) == null ? void 0 : _state$dropConfig6.columnGroup) === "level";
    var stopHandlerMap = /* @__PURE__ */ new Map();
    var tableColumnWatch = createTableColumnWatch($table, state, isColumnGroupLevel, stopHandlerMap);
    hooks.watch(collectColumn2, function() {
      state.colidMap = getColidMap(state.collectColumn);
    });
    hooks.watch(tableColumn2, function() {
      return tableColumnWatch();
    });
    hooks.onBeforeUnmount(function() {
      if (stopHandlerMap.size > 0) {
        var dndProxyList = [];
        for (var _iterator2 = _createForOfIteratorHelperLoose(stopHandlerMap), _step2; !(_step2 = _iterator2()).done; ) {
          var _step2$value = _step2.value, dndProxy = _step2$value[0], stopHander = _step2$value[1];
          dndProxyList.push(dndProxy);
          stopHander.destroy();
        }
        dndProxyList.forEach(function(dndProxy2) {
          return stopHandlerMap.delete(dndProxy2);
        });
      }
    });
  }
};
var useRowGroup = function useRowGroup2(_ref1) {
  var _hooks$getCurrentInst2;
  var props = _ref1.props, visibleColumn = _ref1.visibleColumn, tableFullColumn = _ref1.tableFullColumn, tableColumn2 = _ref1.tableColumn, columnStore = _ref1.columnStore;
  var state = hooks.reactive({
    rowGroup: hooks.toRef(props, "rowGroup"),
    visibleColumn,
    tableFullColumn,
    tableColumn: tableColumn2,
    columnStore
  });
  if (!state.rowGroup) return;
  var $table = (_hooks$getCurrentInst2 = hooks.getCurrentInstance()) == null ? void 0 : _hooks$getCurrentInst2.proxy;
  hooks.watch([visibleColumn, tableColumn2], function() {
    var targetColumn = find(state.visibleColumn, function(col) {
      return col.property === state.rowGroup.field;
    });
    if (!targetColumn) {
      targetColumn = find(state.visibleColumn, function(col) {
        return !!col.property;
      });
    }
    if (targetColumn) {
      $table._rowGroupTargetColumn = targetColumn;
      var _index3 = state.tableColumn.indexOf(targetColumn);
      var length = state.tableColumn.length;
      var targetColumnColspan = state.rowGroup.colspan || 1;
      targetColumnColspan = Math.max(targetColumnColspan, 1);
      if (targetColumnColspan > 1) {
        var leftIndex = -1;
        var rightIndex = -1;
        if ((leftIndex = state.columnStore.leftList.length) > 0) {
          leftIndex = leftIndex - 1;
        }
        if (state.columnStore.rightList.length > 0) {
          rightIndex = state.tableColumn.indexOf(state.columnStore.rightList[0]) - 1;
        }
        var max;
        if (leftIndex > -1 && _index3 <= leftIndex) {
          max = leftIndex - _index3 + 1;
        } else if (rightIndex > -1 && _index3 <= rightIndex) {
          max = rightIndex - _index3 + 1;
        } else {
          max = length - _index3;
        }
        targetColumnColspan = Math.min(targetColumnColspan, max);
      }
      for (var i = 0; i < length; i++) {
        var vCol = state.tableColumn[i];
        if (vCol === targetColumn) {
          vCol._rowGroupColspan = targetColumnColspan;
          vCol._stickyClass = "fixed-left-last__column";
        } else {
          vCol._rowGroupColspan = i > _index3 && i < _index3 + targetColumnColspan ? 0 : 1;
        }
      }
    }
  });
  hooks.onBeforeUnmount(function() {
    delete $table._rowGroupTargetColumn;
    state.tableFullColumn.forEach(function(column) {
      delete column._rowGroupColspan;
      delete column._stickyClass;
    });
  });
};
var isCellDirty = function isCellDirty2($table, row2, column) {
  var editConfig = $table.editConfig;
  var _ref10 = editConfig || {}, _ref10$showStatus = _ref10.showStatus, showStatus = _ref10$showStatus === void 0 ? false : _ref10$showStatus, _ref10$relationFields = _ref10.relationFields, relationFields = _ref10$relationFields === void 0 ? true : _ref10$relationFields;
  var canChange = relationFields === true || Array.isArray(relationFields) && relationFields.includes(column.property);
  var isDirty;
  if (editConfig && showStatus && column.property && (column.editor || relationFields && canChange)) {
    isDirty = $table.hasRowChange(row2, column.property);
  }
  return isDirty;
};
var getCellKey = function getCellKey2($table, row2, column) {
  var rowid2 = getRowid($table, row2);
  return rowid2 + "-" + column.id;
};
var updateCellStatus = function updateCellStatus2($table, row2, column) {
  var cellKey = getCellKey($table, row2, column);
  var isDirty = isCellDirty($table, row2, column);
  var map = $table.cellStatus;
  if (map.has(cellKey)) {
    map.get(cellKey).isDirty = isDirty;
  } else {
    map.set(cellKey, {
      isDirty
    });
  }
};
var _updateRowStatus = function updateRowStatus($table, row2) {
  $table.tableFullColumn.forEach(function(column) {
    return updateCellStatus($table, row2, column);
  });
};
var _getCellStatus = function getCellStatus($table, row2, column) {
  var cellKey = getCellKey($table, row2, column);
  var map = $table.cellStatus;
  if (map.has(cellKey)) {
    return map.get(cellKey);
  } else {
    return {
      isDirty: false
    };
  }
};
var isContented = function isContented2(array) {
  return Array.isArray(array) && array.length > 0;
};
var nid = 0;
var _structure = function structure(_ref11) {
  var array = _ref11.array, stack = _ref11.stack, tiled = _ref11.tiled, map = _ref11.map, customMappings = _ref11.customMappings, getID = _ref11.getID, childrenKey = _ref11.childrenKey, sizeKey = _ref11.sizeKey;
  if (!Array.isArray(array)) {
    return;
  }
  var level = stack.length;
  var nodes = [];
  for (var i = 0; i < array.length; i++) {
    var item = array[i];
    var node = {
      id: getID(item) || ++nid,
      payload: item,
      path: [].concat(stack, [item]),
      level,
      parentNode: level > 0 ? map.get(stack[stack.length - 1]) : void 0,
      childNodes: void 0,
      space: {
        originDistance: 0,
        size: item[sizeKey] || 36
      },
      mappings: customMappings ? Object.assign({}, customMappings({
        payload: item,
        viewIndex: tiled.length
      })) : {}
    };
    tiled.push(node);
    map.set(item, node);
    nodes.push(node);
    if (childrenKey) {
      stack.push(item);
      node.childNodes = _structure({
        array: item[childrenKey],
        stack,
        tiled,
        map,
        customMappings,
        getID,
        childrenKey,
        sizeKey
      });
      stack.pop();
    }
  }
  return nodes;
};
var makeTile = function makeTile2(_ref12) {
  var list = _ref12.list, getID = _ref12.getID, childrenKey = _ref12.childrenKey, sizeKey = _ref12.sizeKey, customMappings = _ref12.customMappings;
  var tiled = [];
  var map = /* @__PURE__ */ new WeakMap();
  if (isContented(list)) {
    _structure({
      array: list,
      stack: [],
      tiled,
      map,
      customMappings,
      getID,
      childrenKey,
      sizeKey
    });
  }
  return {
    tiled,
    map
  };
};
var getAncestors = function getAncestors2(node, map) {
  return node.parentNode ? node.path.slice(0, node.path.length - 1).map(function(row2) {
    return map.get(row2);
  }) : [];
};
var isExpand = function isExpand2(node, isRowExpand) {
  return isRowExpand(node.payload);
};
var isParentExpand = function isParentExpand2(node, isRowExpand, map) {
  return getAncestors(node, map).every(function(p) {
    return isExpand(p, isRowExpand);
  });
};
var makeGraph = function makeGraph2(_ref13) {
  var isRowExpand = _ref13.isRowExpand, tileInfo = _ref13.tileInfo;
  var tiled = tileInfo.tiled, map = tileInfo.map;
  var graphed = [];
  var scrollSize = 0;
  for (var i = 0; i < tiled.length; i++) {
    var node = tiled[i];
    if (!node.parentNode || isParentExpand(node, isRowExpand, map)) {
      node.space.originDistance = scrollSize;
      scrollSize += node.space.size;
      graphed.push(node);
    }
  }
  return {
    graphed,
    scrollSize
  };
};
var buildRenderGraph = function buildRenderGraph2($table) {
  tileFullData($table);
  graphFullData($table);
};
var tileFullData = function tileFullData2($table) {
  var treeConfig = $table.treeConfig, rowGroup = $table.rowGroup, groupFullData = $table.groupFullData, afterFullData = $table.afterFullData;
  var tileInfo = makeTile({
    list: !treeConfig && rowGroup != null && rowGroup.field ? groupFullData : afterFullData,
    getID: function getID(row2) {
      return getRowid($table, row2);
    },
    childrenKey: treeConfig ? treeConfig.children : void 0
  });
  $table._tileInfo = tileInfo;
};
var graphFullData = function graphFullData2($table) {
  var treeConfig = $table.treeConfig, treeExpandeds = $table.treeExpandeds, _tileInfo = $table._tileInfo;
  if (_tileInfo) {
    var graphInfo = makeGraph({
      isRowExpand: function isRowExpand(row2) {
        return treeConfig ? treeExpandeds.includes(row2) : true;
      },
      tileInfo: $table._tileInfo
    });
    $table._graphInfo = graphInfo;
  }
};
var _getTreeLength = function getTreeLength(tree, childrenKey) {
  if (childrenKey === void 0) {
    childrenKey = "children";
  }
  var length = tree.length;
  for (var _iterator3 = _createForOfIteratorHelperLoose(tree), _step3; !(_step3 = _iterator3()).done; ) {
    var node = _step3.value;
    var children = node[childrenKey];
    if (children && children.length > 0) {
      length += _getTreeLength(children, childrenKey);
    }
  }
  return length;
};
var getTiledLength = function getTiledLength2(props) {
  var data7 = props.data || [];
  var _ref14 = props.treeConfig || {}, childrenKey = _ref14.children;
  return props.treeConfig ? _getTreeLength(data7, childrenKey) : data7.length;
};
var useData = function useData2(props) {
  var _hooks$getCurrentInst3;
  var $table = (_hooks$getCurrentInst3 = hooks.getCurrentInstance()) == null ? void 0 : _hooks$getCurrentInst3.proxy;
  var tiledLength = hooks.ref(0);
  hooks.watch([function() {
    return props.data;
  }, function() {
    return getTiledLength(props);
  }], function(_ref15) {
    _ref15[0]; var length = _ref15[1];
    tiledLength.value = length;
    $table.updateRawData(props.data);
  });
  return {
    tiledLength
  };
};
var calcHeader = function calcHeader2(collectColumn2) {
  var maxLevel = 0;
  var leafColumns = [];
  var parentMap = /* @__PURE__ */ new WeakMap();
  var levelMap = /* @__PURE__ */ new WeakMap();
  var _traverseTree = function traverseTree(tree, level, parent) {
    if (Array.isArray(tree) && tree.length > 0) {
      if (level > maxLevel) {
        maxLevel = level;
      }
      tree.forEach(function(item) {
        if (parent) {
          parentMap.set(item, parent);
        }
        levelMap.set(item, level);
        _traverseTree(item.children, level + 1, item);
      });
    } else if (parent) {
      leafColumns.push(parent);
    }
  };
  _traverseTree(collectColumn2, 0, null);
  var headerTable = [];
  var rowspanMap = /* @__PURE__ */ new WeakMap();
  for (var i = 0; i <= maxLevel; i++) {
    headerTable[i] = new Array(leafColumns.length).fill(0);
  }
  leafColumns.forEach(function(column, index2) {
    var level = levelMap.get(column);
    rowspanMap.set(column, maxLevel - level + 1);
    headerTable[level][index2] = column;
    for (var l = level - 1; l >= 0; l--) {
      column = headerTable[l][index2] = parentMap.get(column);
    }
  });
  return {
    leafColumns,
    headerTable,
    rowspanMap,
    maxLevel
  };
};
var calcSpan = function calcSpan2(tableColumn2, header, headerRowHeight) {
  var indices = tableColumn2.map(function(c) {
    return header.leafColumns.indexOf(c);
  });
  var subTable = [];
  header.headerTable.forEach(function(cols, i) {
    var countMap = /* @__PURE__ */ new WeakMap();
    subTable[i] = indices.map(function(j) {
      return cols[j];
    }).reduce(function(p, col) {
      if (col) {
        if (!p.includes(col)) {
          p.push(col);
        }
        if (countMap.has(col)) {
          countMap.set(col, countMap.get(col) + 1);
        } else {
          countMap.set(col, 1);
        }
      }
      return p;
    }, []).map(function(column) {
      var rowspan = header.rowspanMap.get(column) || 1;
      return {
        id: column.id,
        column,
        colspan: countMap.get(column),
        rowspan,
        height: rowspan * headerRowHeight,
        top: i * headerRowHeight
      };
    });
  });
  return subTable;
};
var useHeader = function useHeader2(props, bodyVm, headerRowHeight) {
  var headerTable = hooks.ref([]);
  var $table = bodyVm.$parent;
  var showHeader = $table.showHeader;
  var header = hooks.ref();
  hooks.watch(function() {
    return props.collectColumn;
  }, function() {
    var head = header.value = calcHeader(props.collectColumn);
    if (showHeader) {
      $table.headerHeight = (head.maxLevel + 1) * headerRowHeight.value;
    }
  });
  hooks.watch([function() {
    return props.tableColumn;
  }, header], function() {
    var head = header.value;
    if (showHeader && head) {
      headerTable.value = calcSpan(props.tableColumn, head, headerRowHeight.value);
    }
  });
  return {
    headerTable
  };
};
var getEventSource = function getEventSource2(e, $table) {
  var _tableEl$dataset, _$table$getColumnNode, _rowEl$dataset, _rowid;
  var target = e.target;
  var tableEl = target.closest(".tiny-grid__body");
  if (((_tableEl$dataset = tableEl.dataset) == null ? void 0 : _tableEl$dataset.tableid) !== String($table.id)) return;
  var cellEl = target.closest(".tiny-grid-header__column");
  var rowEl, part, rowType, row2, column, rowid2;
  if (cellEl) {
    rowEl = cellEl.parentNode;
    part = "header";
  } else if (cellEl = target.closest(".tiny-grid-body__column")) {
    rowEl = cellEl.parentNode;
    part = "body";
  } else if (cellEl = target.closest(".tiny-grid-footer__column")) {
    rowEl = cellEl.parentNode;
    part = "footer";
  }
  if (!part || !cellEl || !rowEl) return;
  column = (_$table$getColumnNode = $table.getColumnNode(cellEl)) == null ? void 0 : _$table$getColumnNode.item;
  rowid2 = (_rowEl$dataset = rowEl.dataset) == null ? void 0 : _rowEl$dataset.rowid;
  if ((_rowid = rowid2) != null && _rowid.startsWith("row_g_")) {
    rowType = "virtual";
  } else if (part === "body") {
    rowType = "normal";
    if ($table.editStore.insertMap.has(rowid2)) {
      row2 = $table.editStore.insertMap.get(rowid2);
    } else {
      var _$table$getRowNode;
      row2 = (_$table$getRowNode = $table.getRowNode(rowEl)) == null ? void 0 : _$table$getRowNode.item;
    }
  }
  return {
    part,
    rowType,
    row: row2,
    column,
    cell: cellEl,
    tr: rowEl
  };
};
var parseNumber = function parseNumber2(numStr) {
  return isUndefined(numStr) ? numStr : parseInt(numStr, 10);
};
var normalOverflow = function normalOverflow2(overflow, _overflow) {
  overflow = isNull(overflow) ? _overflow : overflow;
  var overflowTitle = overflow === "title";
  var overflowTooltip = overflow === true || overflow === "tooltip";
  var overflowEllipsis = overflow === "ellipsis";
  var overflowHint = overflowTitle || overflowTooltip || overflowEllipsis;
  return {
    overflow,
    overflowTitle,
    overflowTooltip,
    overflowEllipsis,
    overflowHint
  };
};
var getConfigOverflow = function getConfigOverflow2(column, $table) {
  var _cellOverflow = $table.showOverflow, _headerOverflow = $table.showHeaderOverflow;
  var _ref16 = column || {}, showTip = _ref16.showTip, showOverflow = _ref16.showOverflow, showHeaderTip = _ref16.showHeaderTip, showHeaderOverflow = _ref16.showHeaderOverflow;
  var _normalOverflow = normalOverflow(showOverflow, _cellOverflow), cellOverflowTitle = _normalOverflow.overflowTitle, cellOverflowTooltip = _normalOverflow.overflowTooltip, cellOverflowEllipsis = _normalOverflow.overflowEllipsis, cellOverflowHint = _normalOverflow.overflowHint;
  var _normalOverflow2 = normalOverflow(showHeaderOverflow, _headerOverflow), headerOverflowTitle = _normalOverflow2.overflowTitle, headerOverflowTooltip = _normalOverflow2.overflowTooltip, headerOverflowEllipsis = _normalOverflow2.overflowEllipsis, headerOverflowHint = _normalOverflow2.overflowHint;
  return {
    cellTip: showTip,
    cellOverflowTitle,
    cellOverflowTooltip,
    cellOverflowEllipsis,
    cellOverflowHint,
    headerTip: showHeaderTip,
    headerOverflowTitle,
    headerOverflowTooltip,
    headerOverflowEllipsis,
    headerOverflowHint
  };
};
var getEventParams = function getEventParams2(_ref17, $table) {
  var row2 = _ref17.row, column = _ref17.column, cell = _ref17.cell, tr = _ref17.tr;
  var _ref18 = column || {}, showTip = _ref18.showTip, showHeaderTip = _ref18.showHeaderTip;
  var rowIndex = $table.getRowIndex(row2);
  var columnIndex = $table.getColumnIndex(column);
  var _ref19 = (cell == null ? void 0 : cell.dataset) || {}, seq = _ref19.seq, colindex = _ref19.colindex;
  var _ref20 = (tr == null ? void 0 : tr.dataset) || {}, rowindex = _ref20.rowindex, rowlevel = _ref20.rowlevel;
  return {
    $table,
    row: row2,
    column,
    cell,
    rowIndex,
    columnIndex,
    showTip,
    showHeaderTip,
    seq: parseNumber(seq),
    $rowIndex: parseNumber(rowindex),
    $columnIndex: parseNumber(colindex),
    level: parseNumber(rowlevel)
  };
};
var isOperateMouse = function isOperateMouse2($table) {
  return $table._isResize || $table.lastScrollTime && Date.now() < $table.lastScrollTime + $table.optimizeOpts.delayHover;
};
var hideAlignLines = function hideAlignLines2($table, cell) {
  var _$table$mouseConfig;
  if (!((_$table$mouseConfig = $table.mouseConfig) != null && _$table$mouseConfig.hover) || cell) {
    return;
  }
  var xBar = $table.elemStore["main-body-alignXBar"];
  var yBar = $table.elemStore["main-body-alignYBar"];
  if (xBar) {
    xBar.style.display = "none";
  }
  if (yBar) {
    yBar.style.display = "none";
  }
};
var showAlignLines = function showAlignLines2($table, cell) {
  var _$table$mouseConfig2;
  if (!((_$table$mouseConfig2 = $table.mouseConfig) != null && _$table$mouseConfig2.hover)) {
    return;
  }
  var xBar = $table.elemStore["main-body-alignXBar"];
  var yBar = $table.elemStore["main-body-alignYBar"];
  if (xBar) {
    xBar.style.display = "block";
    xBar.style.top = cell.offsetTop - 1 + "px";
    xBar.style.setProperty("--after-top-offset", cell.offsetHeight + "px");
  }
  if (yBar) {
    yBar.style.display = "block";
    yBar.style.left = cell.offsetLeft - 1 + "px";
    yBar.style.setProperty("--after-left-offset", cell.offsetWidth + "px");
  }
};
var useCellEvent = function useCellEvent2(_ref21) {
  var table = _ref21.table, $table = _ref21.$table;
  var isBound = false;
  var hoverCell = hooks.shallowRef();
  var hoverCellContext = hooks.shallowRef();
  var hoverRow = hooks.shallowRef();
  var hoverText = hooks.shallowRef();
  var textContent;
  var handleMouseEnter = function handleMouseEnter2(e) {
    if (isOperateMouse($table)) return;
    var source = getEventSource(e, $table);
    if (source) {
      var params = getEventParams(source, $table);
      if (hoverCell.value !== source.cell) {
        hoverCell.value = source.cell;
        hoverCellContext.value = {
          params,
          source,
          e
        };
        textContent = hoverCell.value.querySelector(".tiny-grid-cell-text") || hoverCell.value.querySelector(".tiny-grid-cell");
      }
      if (e.target === textContent) {
        hoverText.value = textContent;
      }
      if (source.part !== "body") {
        hoverRow.value = null;
      } else if (hoverRow.value !== source.row) {
        hoverRow.value = source.row;
      }
    }
  };
  var handleMouseLeave = function handleMouseLeave2(e) {
    var _target$dataset;
    if (isOperateMouse($table)) return;
    var target = e.target;
    if (target === hoverText.value) {
      hoverText.value = null;
    }
    if (target.localName === "table" && ((_target$dataset = target.dataset) == null ? void 0 : _target$dataset.tableid) === String($table.id)) {
      hoverRow.value = hoverCell.value = hoverCellContext.value = null;
    }
  };
  var handleMouseDown = function handleMouseDown2(e) {
    var source = getEventSource(e, $table);
    if (source) {
      var params = getEventParams(source, $table);
      var _$table$mouseConfig3 = $table.mouseConfig, mouseConfig = _$table$mouseConfig3 === void 0 ? {} : _$table$mouseConfig3;
      if (source.part === "header" && mouseConfig.checked) {
        $table.triggerHeaderCellMousedownEvent(e, params);
      }
      if (source.part === "body" && (mouseConfig.checked || mouseConfig.selected)) {
        $table.triggerCellMousedownEvent(e, params);
      }
    }
  };
  var handleClick2 = function handleClick3(e) {
    var source = getEventSource(e, $table);
    if (source) {
      var params = getEventParams(source, $table);
      var column = source.column || {};
      var editor = column.editor;
      var satisfy = function satisfy2(equal, trigger2) {
        return trigger2 === "row" || equal(column) && trigger2 === "cell";
      };
      var editConfig = $table.editConfig, _$table$expandConfig = $table.expandConfig, expandConfig = _$table$expandConfig === void 0 ? {} : _$table$expandConfig, highlightCurrentColumn = $table.highlightCurrentColumn, highlightCurrentRow = $table.highlightCurrentRow, _$table$mouseConfig4 = $table.mouseConfig, mouseConfig = _$table$mouseConfig4 === void 0 ? {} : _$table$mouseConfig4, _$table$radioConfig = $table.radioConfig, radioConfig = _$table$radioConfig === void 0 ? {} : _$table$radioConfig, _$table$selectConfig = $table.selectConfig, selectConfig = _$table$selectConfig === void 0 ? {} : _$table$selectConfig, sortOpts2 = $table.sortOpts, tableListeners = $table.tableListeners, _$table$treeConfig = $table.treeConfig, treeConfig = _$table$treeConfig === void 0 ? {} : _$table$treeConfig;
      if (source.part === "header" && (highlightCurrentColumn || tableListeners["header-cell-click"] || mouseConfig.checked || sortOpts2.trigger === "cell")) {
        $table.triggerHeaderCellClickEvent(e, params);
      }
      if (source.part === "body" && (highlightCurrentRow || tableListeners["cell-click"] || mouseConfig.checked || editor && editConfig || satisfy(function() {
        return true;
      }, expandConfig.trigger) || satisfy(function(_ref22) {
        var type = _ref22.type;
        return type === "radio";
      }, radioConfig.trigger) || satisfy(function(_ref23) {
        var type = _ref23.type;
        return type === "selection";
      }, selectConfig.trigger) || satisfy(function(_ref24) {
        var treeNode = _ref24.treeNode;
        return treeNode;
      }, treeConfig.trigger))) {
        var bubbling = treeConfig.bubbling, trigger = treeConfig.trigger;
        var isTreeNodeStopPropagation = !bubbling && (!trigger || trigger === "default") && $table.getEventTargetNode(e, hoverCell.value, "tiny-grid-tree__node-btn").flag;
        if (!isTreeNodeStopPropagation) {
          fastdom.mutate(function() {
            $table.triggerCellClickEvent(e, params);
          });
        }
      }
      if (source.part === "footer" && tableListeners["footer-cell-click"]) {
        emitEvent($table, "footer-cell-click", [params, e]);
      }
    }
  };
  var handleDoubleClick = function handleDoubleClick2(e) {
    var source = getEventSource(e, $table);
    if (source) {
      var params = getEventParams(source, $table);
      var column = source.column || {};
      var editor = column.editor;
      var editConfig = $table.editConfig, tableListeners = $table.tableListeners;
      var triggerDblclick = editor && editConfig && editConfig.trigger === "dblclick";
      if (source.part === "header" && tableListeners["header-cell-dblclick"]) {
        emitEvent($table, "header-cell-dblclick", [params, e]);
      }
      if (source.part === "body" && (triggerDblclick || tableListeners["cell-dblclick"])) {
        $table.triggerCellDBLClickEvent(e, params);
      }
      if (source.part === "footer" && tableListeners["footer-cell-dblclick"]) {
        emitEvent($table, "footer-cell-dblclick", [params, e]);
      }
    }
  };
  var bindMouseEvents = function bindMouseEvents2(target) {
    on(target, "mouseenter", handleMouseEnter, true);
    on(target, "mouseleave", handleMouseLeave, true);
    on(target, "mousedown", handleMouseDown, true);
    on(target, "click", handleClick2, true);
    on(target, "dblclick", handleDoubleClick, true);
  };
  var unbindMouseEvents = function unbindMouseEvents2(target) {
    off(target, "mouseenter", handleMouseEnter, true);
    off(target, "mouseleave", handleMouseLeave, true);
    off(target, "mousedown", handleMouseDown, true);
    off(target, "click", handleClick2, true);
    off(target, "dblclick", handleDoubleClick, true);
  };
  hooks.onBeforeUnmount(function() {
    if (isBound && table.value) {
      unbindMouseEvents(table.value);
      isBound = false;
      hoverRow.value = hoverCell.value = hoverCellContext.value = null;
      $table.hoverCell = null;
    }
  });
  hooks.watch(table, function(table2, old) {
    if (isBound && old) {
      unbindMouseEvents(old);
      isBound = false;
    }
    if (!isBound && table2) {
      bindMouseEvents(table2);
      isBound = true;
    }
  });
  hooks.watch(hoverText, function(curText, preText) {
    var _hoverCellContext$val = hoverCellContext.value, params = _hoverCellContext$val.params, source = _hoverCellContext$val.source, e = _hoverCellContext$val.e;
    var column = source.column || {};
    var _getConfigOverflow = getConfigOverflow(column, $table), headerOverflowTitle = _getConfigOverflow.headerOverflowTitle, headerOverflowTooltip = _getConfigOverflow.headerOverflowTooltip, headerTip = _getConfigOverflow.headerTip, cellOverflowTitle = _getConfigOverflow.cellOverflowTitle, cellOverflowTooltip = _getConfigOverflow.cellOverflowTooltip, cellTip = _getConfigOverflow.cellTip;
    $table.hoverText = curText;
    if (preText) {
      if (source.part === "header") {
        if (headerTip || headerOverflowTooltip) {
          $table.clostTooltip();
        }
      } else if (source.part === "body") {
        if (cellTip || cellOverflowTooltip) {
          $table.clostTooltip();
        }
      } else if (source.part === "footer") {
        if (cellOverflowTooltip) {
          $table.clostTooltip();
        }
      }
    }
    if (curText) {
      if (source.part === "header") {
        if (headerOverflowTitle) {
          updateCellTitle(e, source.cell);
        } else if (headerTip || headerOverflowTooltip) {
          $table.triggerHeaderTooltipEvent(e, params);
        }
      } else if (source.part === "body") {
        if (cellOverflowTitle) {
          updateCellTitle(e, source.cell);
        } else if (cellTip || cellOverflowTooltip) {
          $table.triggerTooltipEvent(e, params);
        }
      } else if (source.part === "footer") {
        if (cellOverflowTitle) {
          updateCellTitle(e, source.cell);
        } else if (cellOverflowTooltip) {
          $table.triggerFooterTooltipEvent(e, params);
        }
      }
    }
  });
  hooks.watch([hoverCell, hoverCellContext], function(_ref25, _ref26) {
    var curCell = _ref25[0], curCtx = _ref25[1];
    var preCell = _ref26[0], preCtx = _ref26[1];
    var tableListeners = $table.tableListeners;
    if (preCell) {
      var params = preCtx.params, source = preCtx.source, e = preCtx.e;
      if (source.part === "body") {
        if (tableListeners["cell-mouseleave"]) {
          emitEvent($table, "cell-mouseleave", [params, e]);
        }
        hideAlignLines($table, curCell);
      }
    }
    if (curCell) {
      var _params = curCtx.params, _source = curCtx.source, _e = curCtx.e;
      $table.hoverCell = curCell;
      if (_source.part === "body") {
        if (tableListeners["cell-mouseenter"]) {
          emitEvent($table, "cell-mouseenter", [_params, _e]);
        }
        showAlignLines($table, curCell);
      }
    }
  });
};
var useCellSpan = function useCellSpan2(bodyVm, bodyProps) {
  var $table = bodyVm.$parent;
  var normalRows = hooks.shallowRef({});
  var footerRows = hooks.shallowRef({});
  hooks.watch([function() {
    return $table.visibleColumn;
  }, function() {
    return bodyProps.tableData;
  }, function() {
    return $table.isColumnWidthAssigned;
  }, function() {
    return $table.columnStore.resizeList;
  }], function(_ref27) {
    var visibleColumn = _ref27[0], tableData = _ref27[1], isColumnWidthAssigned = _ref27[2];
    if (!Array.isArray(tableData) || !isColumnWidthAssigned) {
      return;
    }
    var tableNode = bodyProps.tableNode;
    var hasVirtualRow = $table.hasVirtualRow, treeConfig = $table.treeConfig, treeOrdered = $table.treeOrdered, scrollYLoad = $table.scrollYLoad, scrollYStore = $table.scrollYStore, afterFullData = $table.afterFullData, columnStore = $table.columnStore;
    var _ref28 = treeConfig || {}, hideMethod = _ref28.hideMethod;
    var isOrdered = treeConfig ? !!treeOrdered : false;
    var seqCount = {
      value: 0
    };
    var startIndex = scrollYStore.startIndex;
    var leftList = columnStore.leftList, rightList = columnStore.rightList;
    var normalState = {};
    var normalTable = [];
    for (var _index4 = 0; _index4 < tableData.length; _index4++) {
      var row2 = tableData[_index4];
      var rowLevel = tableNode[_index4].level;
      var rowIndex = $table.getRowIndex(row2);
      var virtualRow = false;
      if (hasVirtualRow) {
        virtualRow = isVirtualRow(row2);
      }
      var isSkipRowRender = hideMethod && hideMethod(row2, rowLevel) || virtualRow;
      if (!isSkipRowRender) {
        seqCount.value = seqCount.value + 1;
      }
      var seq = isOrdered ? seqCount.value : _index4 + 1;
      if (scrollYLoad) {
        seq += startIndex;
      }
      if (hasVirtualRow) {
        if (virtualRow) {
          continue;
        } else {
          seq = afterFullData.indexOf(row2) + 1;
        }
      }
      var params = {
        $table,
        data: tableData,
        row: row2,
        $rowIndex: row2[GlobalConfig$1.$rowIndex],
        rowIndex,
        level: rowLevel,
        $seq: "",
        seq
      };
      stateNormalCell(normalState, normalTable, params, $table);
    }
    if (leftList.length > 0) {
      adjustColspan(normalTable, leftList.length - 1, true);
    }
    if (rightList.length > 0) {
      adjustColspan(normalTable, visibleColumn.indexOf(rightList[0]) - 1, false);
    }
    normalRows.value = normalState;
  });
  hooks.watch([function() {
    return $table.visibleColumn;
  }, function() {
    return bodyProps.footerData;
  }, function() {
    return $table.isColumnWidthAssigned;
  }, function() {
    return $table.columnStore.resizeList;
  }], function(_ref29) {
    var visibleColumn = _ref29[0], footerData = _ref29[1], isColumnWidthAssigned = _ref29[2];
    if (!Array.isArray(footerData) || !isColumnWidthAssigned) {
      return;
    }
    var columnStore = $table.columnStore;
    var leftList = columnStore.leftList, rightList = columnStore.rightList;
    var footerState = {};
    var footerTable = [];
    for (var $rowIndex = 0; $rowIndex < footerData.length; $rowIndex++) {
      var params = {
        $table,
        $rowIndex,
        data: footerData
      };
      stateFooterCell(footerState, footerTable, params, $table);
    }
    if (leftList.length > 0) {
      adjustColspan(footerTable, leftList.length - 1, true);
    }
    if (rightList.length > 0) {
      adjustColspan(footerTable, visibleColumn.indexOf(rightList[0]) - 1, false);
    }
    footerRows.value = footerState;
  });
  return {
    normalRows,
    footerRows
  };
};
var stateNormalCell = function stateNormalCell2(state, table, params, $table) {
  var rowSpan = $table.rowSpan, spanMethod = $table.spanMethod, visibleColumn = $table.visibleColumn;
  var rowId = getRowid($table, params.row);
  var rowState = {};
  var rowAttrs = [];
  for (var $columnIndex = 0; $columnIndex < visibleColumn.length; $columnIndex++) {
    var column = visibleColumn[$columnIndex];
    var columnIndex = $table.getColumnIndex(column);
    var attrs = {
      rowspan: 1,
      colspan: 1,
      visible: true,
      _stickyClass: "",
      _stickyStyle: null
    };
    params = _extends({}, params, {
      column,
      $columnIndex,
      columnIndex
    });
    if (spanMethod || rowSpan) {
      doSpan({
        attrs,
        params,
        rowSpan,
        spanMethod
      });
    }
    rowState[column.id] = {
      attrs,
      params
    };
    rowAttrs.push({
      attrs,
      params
    });
  }
  state[rowId] = rowState;
  table.push(rowAttrs);
};
var stateFooterCell = function stateFooterCell2(state, table, params, $table) {
  var footerSpanMethod = $table.footerSpanMethod, visibleColumn = $table.visibleColumn;
  var rowState = {};
  var rowAttrs = [];
  for (var $columnIndex = 0; $columnIndex < visibleColumn.length; $columnIndex++) {
    var column = visibleColumn[$columnIndex];
    var columnIndex = $table.getColumnIndex(column);
    var attrs = {
      rowspan: 1,
      colspan: 1,
      visible: true
    };
    params = _extends({}, params, {
      column,
      $columnIndex,
      columnIndex
    });
    if (footerSpanMethod) {
      var _ref30 = footerSpanMethod(params) || {}, _ref30$rowspan = _ref30.rowspan, rowspan = _ref30$rowspan === void 0 ? 1 : _ref30$rowspan, _ref30$colspan = _ref30.colspan, colspan = _ref30$colspan === void 0 ? 1 : _ref30$colspan;
      attrs.rowspan = rowspan;
      attrs.colspan = colspan;
      attrs.visible = rowspan > 0 && colspan > 0;
    }
    rowState[column.id] = {
      attrs,
      params
    };
    rowAttrs.push({
      attrs,
      params
    });
  }
  state[params.$rowIndex] = rowState;
  table.push(rowAttrs);
};
var doSpan = function doSpan2(_ref31) {
  var attrs = _ref31.attrs, params = _ref31.params, rowSpan = _ref31.rowSpan, spanMethod = _ref31.spanMethod;
  var _ref32 = (spanMethod ? spanMethod(params) : rowSpanMethod(rowSpan, params)) || {}, _ref32$rowspan = _ref32.rowspan, rowspan = _ref32$rowspan === void 0 ? 1 : _ref32$rowspan, _ref32$colspan = _ref32.colspan, colspan = _ref32$colspan === void 0 ? 1 : _ref32$colspan;
  attrs.rowspan = rowspan;
  attrs.colspan = colspan;
  attrs.visible = rowspan > 0 && colspan > 0;
};
var rowSpanMethod = function rowSpanMethod2(rowSpan, _ref33) {
  var row2 = _ref33.row, $rowIndex = _ref33.$rowIndex, column = _ref33.column, data7 = _ref33.data;
  var fields = [];
  if (column.visible && rowSpan) {
    rowSpan.forEach(function(item) {
      return fields.push(item.field);
    });
    var cellVal = row2[column.property];
    if (cellVal && fields.includes(column.property)) {
      var prevSiblingRow = data7[$rowIndex - 1];
      var nextSiblingRow = data7[$rowIndex + 1];
      if ((prevSiblingRow == null ? void 0 : prevSiblingRow[column.property]) === cellVal) {
        return {
          rowspan: 0,
          colspan: 0
        };
      } else {
        var rowspanCount = 1;
        while (((_nextSiblingRow = nextSiblingRow) == null ? void 0 : _nextSiblingRow[column.property]) === cellVal) {
          var _nextSiblingRow;
          nextSiblingRow = data7[++rowspanCount + $rowIndex];
        }
        if (rowspanCount > 1) {
          return {
            rowspan: rowspanCount,
            colspan: 1
          };
        }
      }
    }
  }
};
var adjustColspan = function adjustColspan2(table, pos, isLeft) {
  if (pos < 0) {
    return;
  }
  for (var i = 0; i < table.length; i++) {
    var row2 = table[i];
    for (var j = 0; j <= pos && j < row2.length; j++) {
      var attrs = row2[j].attrs;
      var oldColspan = attrs.colspan;
      var k = void 0, posCol = void 0;
      var shouldAdjustColspan = oldColspan > 1 && (k = j + oldColspan - 1) > pos;
      if (shouldAdjustColspan) {
        attrs.colspan = pos - j + 1;
        if (isLeft) {
          attrs._stickyClass = "fixed-left-last__column";
        }
        posCol = row2[pos + 1];
        if (posCol) {
          posCol.attrs.colspan = k - pos;
          if (posCol.attrs.rowspan < 1) {
            posCol.attrs.rowspan = attrs.rowspan;
          }
          posCol.attrs.visible = true;
        }
      }
    }
    if (!isLeft) {
      var rightCols = row2.slice(pos + 1).reverse();
      var right = 0;
      for (var _j = 0; _j < rightCols.length; _j++) {
        var _rightCols$_j = rightCols[_j], _attrs = _rightCols$_j.attrs, params = _rightCols$_j.params;
        if (_attrs.visible) {
          _attrs._stickyStyle = {
            right: right + "px"
          };
          right += params.column.renderWidth;
        }
      }
    }
  }
};
var useNormalData = function useNormalData2(_ref34) {
  var _hooks$getCurrentInst4;
  var props = _ref34.props, tableFullColumn = _ref34.tableFullColumn;
  var $table = (_hooks$getCurrentInst4 = hooks.getCurrentInstance()) == null ? void 0 : _hooks$getCurrentInst4.proxy;
  var rawData = hooks.ref([]);
  var rawDataVersion = hooks.ref(0);
  var editorColumns = hooks.ref([]);
  var editorColumnKey = hooks.ref("");
  hooks.watch(tableFullColumn, function(_tableFullColumn) {
    var columns2 = [];
    var columnKeys = [];
    for (var i = 0; i < _tableFullColumn.length; i++) {
      var column = _tableFullColumn[i];
      if (column.property && column.editor) {
        columns2.push(column);
        columnKeys.push(column.propety);
      }
    }
    var keyString = columnKeys.sort().join();
    if (editorColumnKey.value !== keyString) {
      editorColumnKey.value = keyString;
      editorColumns.value = columns2;
    }
  });
  hooks.watch(editorColumns, function(_editorColumns) {
    if (props.editConfig && _editorColumns.length > 0) {
      var _insertData = $table.editStore.insertList || [];
      var _rawData = rawData.value || [];
      var _data = [].concat(_insertData, _rawData);
      if (Array.isArray(_data)) {
        _data.forEach(function(row2) {
          return $table.defineField(row2, false, _editorColumns, true);
        });
      }
    }
  });
  hooks.watch(rawData, function(_rawData) {
    var _editorColumns = editorColumns.value;
    if (props.editConfig && _editorColumns.length > 0) {
      var _data = _rawData || [];
      if (Array.isArray(_data)) {
        _data.forEach(function(row2) {
          return $table.defineField(row2, false, _editorColumns);
        });
      }
    }
    $table.operationMap.clear();
    rawDataVersion.value += 1;
  });
  hooks.watch(rawDataVersion, function() {
    var optimizeOpts = $table.optimizeOpts;
    var scrollY = optimizeOpts.scrollY;
    var _rawData = rawData.value || [];
    var tableFullData = Array.isArray(_rawData) ? _rawData.slice(0) : [];
    $table.tableFullData = tableFullData;
    $table.scrollYLoad = scrollY && scrollY.gt > 0 && scrollY.gt <= tableFullData.length;
    $table.updateCache(true, props.saveSource === "deep");
    $table.handleDataChange();
  });
  return {
    rawData,
    rawDataVersion
  };
};
var toRaw = hooks.toRaw;
var VIRTUAL_ROW_KEY = "_$virtual_";
var difference$1 = function difference$12(arr, other) {
  return arr.filter(function(i) {
    return other.findIndex(function(j) {
      return i.id === j.id;
    }) === -1;
  });
};
var sliceVisibleColumn = function sliceVisibleColumn2(args) {
  var lastStartIndex = args.lastStartIndex, renderSize = args.renderSize, scrollXLoad = args.scrollXLoad, startIndex = args.startIndex, tableColumn2 = args.tableColumn, columnStore = args.columnStore;
  var treeConfig = args.treeConfig, visibleColumn = args.visibleColumn, visibleColumnChanged = args.visibleColumnChanged;
  var leftList = columnStore.leftList, rightList = columnStore.rightList;
  var changeFrom = tableColumn2.reduce(function(p, c) {
    return p + c.id;
  }, "");
  var tableColumn22 = tableColumn2;
  var lastStartIndex2 = lastStartIndex;
  var visibleColumnChanged2 = visibleColumnChanged;
  var sliced = false;
  var addedColumn = [];
  if (scrollXLoad && treeConfig) {
    if (visibleColumnChanged || !~lastStartIndex || lastStartIndex !== startIndex) {
      tableColumn22 = visibleColumn.slice(startIndex, startIndex + renderSize);
      lastStartIndex2 = startIndex;
      visibleColumnChanged2 = false;
      sliced = true;
    }
  } else {
    tableColumn22 = visibleColumn.slice(startIndex, startIndex + renderSize);
    sliced = true;
  }
  tableColumn22 = Array.from(/* @__PURE__ */ new Set([].concat(leftList, tableColumn22, rightList)));
  var changeTo = tableColumn22.reduce(function(p, c) {
    return p + c.id;
  }, "");
  if (changeFrom === changeTo) {
    tableColumn22 = tableColumn2;
    sliced = false;
  } else {
    addedColumn = difference$1(tableColumn22, tableColumn2);
  }
  return {
    tableColumn: tableColumn22,
    lastStartIndex: lastStartIndex2,
    visibleColumnChanged: visibleColumnChanged2,
    sliced,
    addedColumn
  };
};
var isVirtualRow = function isVirtualRow2(row2) {
  return row2 && row2[VIRTUAL_ROW_KEY];
};
var orderingGroupBy = function orderingGroupBy2(arr, key, equals, active, rowKey) {
  var result = [];
  var virtualItems = [];
  var virtualRowId = 0;
  var createVirtualItem = function createVirtualItem2(vItem) {
    var _vItem;
    vItem = (_vItem = {}, _vItem[VIRTUAL_ROW_KEY] = true, _vItem[rowKey] = "row_g_" + ++virtualRowId, _vItem.value = null, _vItem.children = [], _vItem.fold = false, _vItem.hover = false, _vItem);
    result.push(vItem);
    virtualItems.push(vItem);
    return vItem;
  };
  var virtualItem = createVirtualItem();
  var prevSibling, equalsParam;
  arrayEach$1(arr, function(item, i) {
    if (i > 0) {
      prevSibling = arr[i - 1];
      equalsParam = {
        prevRow: prevSibling,
        row: item
      };
    }
    if (i > 0 && !equals(prevSibling[key], item[key], equalsParam)) {
      virtualItem = createVirtualItem();
    }
    virtualItem.children.push(item);
    result.push(item);
  });
  arrayEach$1(virtualItems, function(vItem) {
    vItem.value = vItem.children[0][key];
    if (typeof active === "function") {
      var expand = active(vItem);
      vItem.fold = typeof expand === "boolean" ? !expand : false;
    }
  });
  var rawResult = result.map(toRaw);
  arrayEach$1(virtualItems.slice().reverse(), function(vItem) {
    if (!vItem.fold) return;
    var index2 = rawResult.indexOf(toRaw(vItem.children[0]));
    if (index2 > -1) {
      result.splice(index2, vItem.children.length);
    }
  });
  return result;
};
var buildRowGroupFullData = function buildRowGroupFullData2(fullData, _vm) {
  var treeConfig = _vm.treeConfig, rowGroup = _vm.rowGroup;
  var _ref35 = rowGroup || {}, field = _ref35.field, equals = _ref35.equals, activeMethod = _ref35.activeMethod;
  _vm.hasVirtualRow = !treeConfig && rowGroup && field && fullData.length > 0;
  if (_vm.hasVirtualRow) {
    _vm.groupFullData = orderingGroupBy(fullData, field, equals || isEqual$1, activeMethod, getRowkey(_vm));
  }
};
var handleRowGroupFold = function handleRowGroupFold2(row2, _vm) {
  var hasVirtualRow = _vm.hasVirtualRow, groupFullData = _vm.groupFullData, _vm$rowGroup = _vm.rowGroup, rowGroup = _vm$rowGroup === void 0 ? {} : _vm$rowGroup;
  var _rowGroup$closeable = rowGroup.closeable, closeable = _rowGroup$closeable === void 0 ? true : _rowGroup$closeable;
  if (!hasVirtualRow || !closeable || !isVirtualRow(row2)) return;
  row2.fold = !row2.fold;
  var copy = groupFullData.slice(0).map(toRaw);
  var index2 = copy.indexOf(toRaw(row2.children[0]));
  if (row2.fold) {
    if (index2 > -1) {
      copy.splice(index2, row2.children.length);
    }
  } else if (index2 === -1) {
    copy.splice.apply(copy, [copy.indexOf(toRaw(row2)) + 1, 0].concat(row2.children));
  }
  _vm.groupFullData = copy;
  buildRenderGraph(_vm);
  _vm.handleTableData().then(_vm.recalculate);
};
var getFixedStyle = function getFixedStyle2(_ref36, _ref37) {
  var fixedDetails = _ref36.fixedDetails;
  var overflowX = _ref37.overflowX;
  return fixedDetails && overflowX ? fixedDetails.getStyle() : {};
};
var getFixedClass = function getFixedClass2(_ref38, _ref39) {
  var fixedDetails = _ref38.fixedDetails;
  var overflowX = _ref39.overflowX;
  return _extends({
    "fixed__column": fixedDetails && overflowX
  }, fixedDetails && overflowX ? fixedDetails.getClass() : {});
};
var difference = function difference2(arr, other) {
  return arr.filter(function(i) {
    return other.findIndex(function(j) {
      return i.id === j.id;
    }) === -1;
  });
};
var uid = 0;
var createPool = function createPool2(array) {
  if (!Array.isArray(array)) {
    return;
  }
  var context = {
    pool: [],
    idViewMap: /* @__PURE__ */ new Map(),
    unusedViews: [],
    array
  };
  array.forEach(function(item) {
    var view = {
      id: ++uid,
      used: true,
      item
    };
    context.pool.push(view);
    context.idViewMap.set(item.id, view);
  });
  return context;
};
var updatePool = function updatePool2(array, context) {
  if (!Array.isArray(array)) {
    return;
  }
  var expires = difference(context.array, array);
  var indices = /* @__PURE__ */ new WeakMap();
  expires.forEach(function(item) {
    var view = context.idViewMap.get(item.id);
    if (view) {
      view.used = false;
      context.idViewMap.delete(item.id);
      context.unusedViews.push(view);
    }
  });
  array.forEach(function(item, i) {
    indices.set(item, i);
    var view = context.idViewMap.get(item.id);
    if (!view) {
      if (context.unusedViews.length > 0) {
        view = context.unusedViews.shift();
      } else {
        view = {
          id: ++uid,
          used: true,
          item
        };
        context.pool.push(view);
      }
      context.idViewMap.set(item.id, view);
    }
    view.used = true;
    view.item = item;
  });
  context.array = array;
  context.pool.sort(function(a, b) {
    return a.used ? b.used ? indices.get(a.item) - indices.get(b.item) : -1 : b.used ? 1 : 0;
  });
  return context;
};
function addTableDataRowIndex(rowPool) {
  var treeTableData = [];
  var $rowIndex = GlobalConfig$1.$rowIndex;
  var tempChildren = Symbol("tempChildren");
  rowPool.forEach(function(rowPoolItem) {
    var _rowPoolItem$item = rowPoolItem.item, row2 = _rowPoolItem$item.payload, level = _rowPoolItem$item.level, parentNode = _rowPoolItem$item.parentNode, used = rowPoolItem.used;
    if (!used) {
      return;
    }
    if (level === 0) {
      treeTableData.push(row2);
      row2[$rowIndex] = treeTableData.length - 1;
      return;
    }
    var parent = parentNode == null ? void 0 : parentNode.payload;
    if (!parent) {
      return;
    }
    if (!parent[tempChildren]) {
      parent[tempChildren] = [];
    }
    parent[tempChildren].push(row2);
    row2[$rowIndex] = parent[tempChildren].length - 1;
  });
  rowPool.forEach(function(rowPoolItem) {
    delete rowPoolItem.item.payload[tempChildren];
  });
}
var usePool = function usePool2(props) {
  var columnPool = hooks.ref([]);
  var rowPool = hooks.ref([]);
  var isNoData = hooks.ref(true);
  var columnContext;
  hooks.watch(function() {
    return props.tableColumn;
  }, function() {
    if (columnContext) {
      updatePool(props.tableColumn, columnContext);
    } else {
      columnContext = createPool(props.tableColumn);
    }
    columnPool.value = columnContext.pool;
  });
  var rowContext;
  hooks.watch(function() {
    return props.tableNode;
  }, function() {
    var _rowContext, _props$tableNode;
    if (rowContext) {
      updatePool(props.tableNode, rowContext);
    } else {
      rowContext = createPool(props.tableNode);
    }
    rowPool.value = ((_rowContext = rowContext) == null ? void 0 : _rowContext.pool) || rowPool.value;
    addTableDataRowIndex(rowPool.value);
    isNoData.value = !(((_props$tableNode = props.tableNode) == null ? void 0 : _props$tableNode.length) > 0);
  });
  return {
    columnPool,
    rowPool,
    isNoData
  };
};
var ChevronRight = iconChevronRight();
var ChevronDown = iconChevronDown();
var GridNoData = iconGridNoData();
var renderRowFlag = true;
var renderBorder = function renderBorder2(type) {
  return createVNode("div", {
    "ref": type + "Borders",
    "class": "tiny-grid-" + type + "ed-borders"
  }, [createVNode("span", {
    "ref": type + "Top",
    "class": "tiny-grid-border-top"
  }, null), createVNode("span", {
    "ref": type + "Right",
    "class": "tiny-grid-border-right"
  }, null), createVNode("span", {
    "ref": type + "Bottom",
    "class": "tiny-grid-border-bottom"
  }, null), createVNode("span", {
    "ref": type + "Left",
    "class": "tiny-grid-border-left"
  }, null)]);
};
function renderColumn(_ref40) {
  var _normalRows$rowid, _ref42;
  var $columnIndex = _ref40.$columnIndex, $table = _ref40.$table, _vm = _ref40._vm, column = _ref40.column, id3 = _ref40.id, row2 = _ref40.row, rowid2 = _ref40.rowid, seq = _ref40.seq, used = _ref40.used;
  var allAlign = $table.align, cellClassName = $table.cellClassName, _$table$dropConfig = $table.dropConfig, dropConfig = _$table$dropConfig === void 0 ? {} : _$table$dropConfig, editConfig = $table.editConfig, editRules = $table.editRules, editStore = $table.editStore;
  var height2 = $table.height, rowId = $table.rowId, scrollXLoad = $table.scrollXLoad, scrollYLoad = $table.scrollYLoad, validOpts2 = $table.validOpts, validStore = $table.validStore, validatedMap = $table.validatedMap;
  var normalRows = _vm.normalRows;
  var _ref41 = ((_normalRows$rowid = normalRows[rowid2]) == null ? void 0 : _normalRows$rowid[column.id]) || {}, _ref41$attrs = _ref41.attrs, attrs = _ref41$attrs === void 0 ? {
    rowspan: 1,
    colspan: 1,
    visible: true
  } : _ref41$attrs, _ref41$params = _ref41.params, params = _ref41$params === void 0 ? {
    $table,
    row: row2,
    column
  } : _ref41$params;
  var isMessageDefault = validOpts2.isMessageDefault, isMessageInline = validOpts2.isMessageInline, highlightError = validOpts2.highlightError;
  var actived = editStore.actived;
  var validated = validatedMap[column.id + "-" + row2[rowId]];
  var validError = validStore.row === row2 && validStore.column === column;
  var hasDefaultTip = editRules && (isMessageDefault ? height2 || !_vm.isNoData : isMessageInline);
  var align = column.align, className = column.className, editor = column.editor;
  var cellAlign = align || allAlign;
  if (["radio", "selection", "index"].includes(column.type)) {
    cellAlign = cellAlign || "center";
  }
  var _getConfigOverflow2 = getConfigOverflow(column, $table), cellTip = _getConfigOverflow2.cellTip, cellOverflowTitle = _getConfigOverflow2.cellOverflowTitle, cellOverflowTooltip = _getConfigOverflow2.cellOverflowTooltip, cellOverflowEllipsis = _getConfigOverflow2.cellOverflowEllipsis, cellOverflowHint = _getConfigOverflow2.cellOverflowHint;
  if ((scrollXLoad || scrollYLoad) && !cellOverflowHint) {
    cellOverflowEllipsis = true;
  }
  var columnActived = editConfig && editor && actived.row === row2 && (actived.column === column || editConfig.mode === "row");
  var isDirty = $table.getCellStatus(row2, column).isDirty;
  params.$columnIndex = $columnIndex;
  return createVNode("td", {
    "key": id3,
    "style": [getFixedStyle(column, $table), attrs._stickyStyle, {
      display: used && attrs.visible ? void 0 : "none"
    }],
    "data-colid": column.id,
    "data-seq": seq,
    "data-colindex": $columnIndex,
    "rowspan": attrs.rowspan > 1 ? attrs.rowspan : void 0,
    "colspan": attrs.colspan > 1 ? attrs.colspan : void 0,
    "class": ["tiny-grid-body__column", column.id, (_ref42 = {}, _ref42["col__" + cellAlign] = cellAlign, _ref42["col__edit"] = editor, _ref42["col__index"] = column.type === "index", _ref42["col__radio"] = column.type === "radio", _ref42["col__selection"] = column.type === "selection", _ref42["col__ellipsis"] = cellOverflowHint, _ref42["edit__visible"] = editor && editor.type === "visible", _ref42["col__dirty"] = isDirty, _ref42["col__actived"] = columnActived, _ref42["col__highlight-error"] = highlightError && validated, _ref42["col__valid-error"] = validError && validated, _ref42["col__valid-success"] = columnActived ? !validError && !validated : isDirty && !validated, _ref42["col__treenode"] = column.treeNode, _ref42), getClass(className, params), getClass(cellClassName, params), getFixedClass(column, $table), attrs._stickyClass || ""]
  }, [[
    // 行拖拽手柄
    dropConfig.rowHandle === "index" && column.type === "index" ? createVNode("div", {
      "class": "row__drop-handle"
    }, null) : null,
    // 单元格主内容
    // 如果不是表格形态，就只保留表格结构（到tiny-grid-cell），不渲染具体的内容
    createVNode("div", {
      "class": {
        "tiny-grid-cell": true,
        "tiny-grid-cell__title": cellOverflowTitle,
        "tiny-grid-cell__tooltip": cellOverflowTooltip || cellTip,
        "tiny-grid-cell__ellipsis": cellOverflowEllipsis
      },
      "title": cellOverflowTitle ? getCellLabel(row2, column, params) : null
    }, [$table.isShapeTable ? column.renderCell(h, params) : null]),
    // 行内校验
    hasDefaultTip && validError ? createVNode("div", {
      "class": "tiny-grid-cell__valid",
      "style": validStore.rule && validStore.rule.width ? {
        width: validStore.rule.width + "px"
      } : null
    }, [createVNode("span", {
      "class": "tiny-grid-cell__valid-msg",
      "title": validStore.content
    }, [validStore.content])]) : null
  ]]);
}
function renderHeaderRows(_vm) {
  var $table = _vm.$parent, headerTable = _vm.headerTable;
  var headerCellClassName = $table.headerCellClassName, headerRowClassName = $table.headerRowClassName, headerSuffixIconAbsolute = $table.headerSuffixIconAbsolute;
  var allAlign = $table.align, border = $table.border, allHeaderAlign = $table.headerAlign, resizable = $table.resizable;
  var editConfig = $table.editConfig, operationColumnResizable = $table.operationColumnResizable, _$table$mouseConfig5 = $table.mouseConfig, mouseConfig = _$table$mouseConfig5 === void 0 ? {} : _$table$mouseConfig5, _$table$dropConfig2 = $table.dropConfig, dropConfig = _$table$dropConfig2 === void 0 ? {} : _$table$dropConfig2;
  return headerTable.map(function(cols, $rowIndex) {
    return createVNode("tr", {
      "data-rowindex": $rowIndex,
      "class": ["tiny-grid-header__row", headerRowClassName ? isFunction(headerRowClassName) ? headerRowClassName({
        $table,
        $rowIndex
      }) : headerRowClassName : ""]
    }, [cols.map(function(_ref43, $columnIndex) {
      var _column$children, _ref44;
      var id3 = _ref43.id, column = _ref43.column, colspan = _ref43.colspan, rowspan = _ref43.rowspan, height2 = _ref43.height, top = _ref43.top;
      var isColGroup = (_column$children = column.children) == null ? void 0 : _column$children.length;
      var headerAlign = column.headerAlign, align = column.align, headerClassName = column.headerClassName;
      var _getConfigOverflow3 = getConfigOverflow(column, $table), headerTip = _getConfigOverflow3.headerTip, headerOverflowTitle = _getConfigOverflow3.headerOverflowTitle, headerOverflowTooltip = _getConfigOverflow3.headerOverflowTooltip, headerOverflowEllipsis = _getConfigOverflow3.headerOverflowEllipsis, headerOverflowHint = _getConfigOverflow3.headerOverflowHint;
      var columnIndex = $table.getColumnIndex(column);
      var params = {
        $table,
        $rowIndex,
        column,
        columnIndex,
        $columnIndex,
        isHidden: false
      };
      var isColResize = ["index", "radio", "selection"].includes(column.type) ? operationColumnResizable : true;
      var headAlign = headerAlign || align || allHeaderAlign || allAlign;
      if (["radio", "selection", "index"].includes(column.type)) {
        headAlign = headAlign || "center";
      }
      return createVNode("th", {
        "key": id3,
        "data-colid": column.id,
        "data-colindex": $columnIndex,
        "colspan": colspan > 1 ? colspan : void 0,
        "rowspan": rowspan > 1 ? rowspan : void 0,
        "style": [getFixedStyle(column, $table), {
          height: rowspan > 1 ? height2 + "px" : void 0,
          top: top + "px",
          zIndex: column.fixed ? 20 : 10
        }],
        "class": ["tiny-grid-header__column", column.id, (_ref44 = {}, _ref44["col__" + headAlign] = headAlign, _ref44["col__fixed"] = column.fixed, _ref44["col__index"] = column.type === "index", _ref44["col__radio"] = column.type === "radio", _ref44["col__selection"] = column.type === "selection", _ref44["col__group"] = isColGroup, _ref44["col__ellipsis"] = headerOverflowHint, _ref44["is__sortable"] = !["index", "radio", "selection"].includes(column.type) && column.sortable, _ref44["is__editable"] = column.editor, _ref44["is__filter"] = isObject(column.filter), _ref44["filter__active"] = column.filter && column.filter.hasFilter, _ref44["is__multilevel"] = rowspan > 1, _ref44), getClass(headerClassName, params), getClass(headerCellClassName, params), getFixedClass(column, $table)]
      }, [[
        !isColGroup && !(isBoolean(column.resizable) ? column.resizable : resizable) && column.type !== "index" ? createVNode("div", {
          "class": {
            "tiny-grid-thead-partition": true,
            "is__line": !border
          }
        }, null) : null,
        // 如果不是表格形态，就只保留表格结构（到tiny-grid-cell），不渲染具体的内容
        createVNode("div", {
          "class": {
            "tiny-grid-cell": true,
            "tiny-grid-cell__title": headerOverflowTitle,
            "tiny-grid-cell__tooltip": headerTip || headerOverflowTooltip,
            "tiny-grid-cell__ellipsis": headerOverflowEllipsis,
            "tiny-grid-cell__header-suffix": headerSuffixIconAbsolute,
            "tiny-grid-cell__header-no-icon": editConfig && !editConfig.showIcon && !column.showIcon
          }
        }, [$table.isShapeTable ? column.renderHeader(h, params) : null]),
        mouseConfig.checked && dropConfig.column && !column.type && !column.fixed ? createVNode("div", {
          "class": "tiny-grid-cell__drag-bar",
          "onMousedown": function onMousedown(event) {
            return event.stopPropagation();
          }
        }, null) : null,
        !isColGroup && isColResize && (isBoolean(column.resizable) ? column.resizable : resizable) ? createVNode("div", {
          "class": {
            "tiny-grid-resizable": true,
            "is__line": !border
          },
          "onMousedown": function onMousedown(event) {
            return _vm.resizeMousedown(event, params);
          }
        }, null) : null
      ]]);
    })]);
  });
}
function renderFooterRows(_vm) {
  var $table = _vm.$parent, footerData = _vm.footerData, columnPool = _vm.columnPool, rowHeight = _vm.rowHeight, footerRows = _vm.footerRows;
  var allAlign = $table.align, allFooterAlign = $table.footerAlign, footerCellClassName = $table.footerCellClassName, footerRowClassName = $table.footerRowClassName;
  var footerDataLength = footerData.length;
  return footerData.map(function(list, $rowIndex) {
    var trBottom = (footerDataLength - 1 - $rowIndex) * rowHeight;
    return createVNode("tr", {
      "data-rowindex": $rowIndex,
      "class": ["tiny-grid-footer__row", footerRowClassName ? isFunction(footerRowClassName) ? footerRowClassName({
        $table,
        $rowIndex
      }) : footerRowClassName : ""]
    }, [columnPool.map(function(_ref45, $columnIndex) {
      var _footerRows$$rowIndex, _ref47;
      var id3 = _ref45.id, column = _ref45.item, used = _ref45.used;
      var footerAlign = column.footerAlign, align = column.align, footerClassName = column.footerClassName;
      var ftAlign = footerAlign || align || allFooterAlign || allAlign;
      var _getConfigOverflow4 = getConfigOverflow(column, $table), cellOverflowHint = _getConfigOverflow4.cellOverflowHint;
      var _ref46 = ((_footerRows$$rowIndex = footerRows[$rowIndex]) == null ? void 0 : _footerRows$$rowIndex[column.id]) || {}, _ref46$attrs = _ref46.attrs, attrs = _ref46$attrs === void 0 ? {
        rowspan: 1,
        colspan: 1,
        visible: true
      } : _ref46$attrs, _ref46$params = _ref46.params, params = _ref46$params === void 0 ? {
        $table,
        column
      } : _ref46$params;
      var rowspan = (attrs == null ? void 0 : attrs.rowspan) || 1;
      var tdBottom = trBottom - (rowspan > 1 ? (rowspan - 1) * rowHeight : 0);
      params.$columnIndex = $columnIndex;
      return createVNode("td", {
        "key": id3,
        "data-colid": column.id,
        "data-colindex": $columnIndex,
        "rowspan": attrs.rowspan > 1 ? attrs.rowspan : void 0,
        "colspan": attrs.colspan > 1 ? attrs.colspan : void 0,
        "style": [getFixedStyle(column, $table), attrs._stickyStyle, {
          bottom: tdBottom + "px",
          zIndex: column.fixed ? 20 : 10,
          display: used && attrs.visible ? void 0 : "none"
        }],
        "class": ["tiny-grid-footer__column", column.id, (_ref47 = {}, _ref47["col__" + ftAlign] = ftAlign, _ref47["col__ellipsis"] = cellOverflowHint, _ref47["filter__active"] = column.filter && column.filter.hasFilter, _ref47), getClass(footerClassName, params), getClass(footerCellClassName, params), getFixedClass(column, $table), attrs._stickyClass || ""]
      }, [createVNode("div", {
        "class": {
          "tiny-grid-cell": true,
          "cell__summary": $table.summaryConfig
        }
      }, [$table.isShapeTable ? formatText(list[$columnIndex]) : null])]);
    })]);
  });
}
function renderRows(_vm) {
  var $table = _vm.$parent, tableColumn2 = _vm.tableColumn, rowPool = _vm.rowPool;
  var afterFullData = $table.afterFullData, editConfig = $table.editConfig, editStore = $table.editStore, _$table$expandConfig2 = $table.expandConfig, expandConfig = _$table$expandConfig2 === void 0 ? {} : _$table$expandConfig2, expandeds = $table.expandeds, hasVirtualRow = $table.hasVirtualRow;
  var rowClassName = $table.rowClassName, rowGroup = $table.rowGroup, scrollYLoad = $table.scrollYLoad, scrollYStore = $table.scrollYStore, scrollXLoad = $table.scrollXLoad, selection = $table.selection, treeConfig = $table.treeConfig, treeOrdered = $table.treeOrdered, selectRow = $table.selectRow;
  var expandMethod = expandConfig.activeMethod;
  var startIndex = scrollYStore.startIndex;
  var isOrdered = treeConfig ? !!treeOrdered : false;
  var _ref48 = treeConfig || {}, hideMethod = _ref48.hideMethod;
  var actived = editStore.actived;
  var rows = [];
  var seqCount = {
    value: 0
  };
  var $seq = "";
  rowPool.forEach(function(_ref49) {
    var id3 = _ref49.id, _ref49$item = _ref49.item, row2 = _ref49$item.payload, rowLevel = _ref49$item.level, used = _ref49.used;
    var rowActived = editConfig && actived.row === row2;
    var virtualRow = isVirtualRow(row2);
    var isSkipRowRender = hideMethod && hideMethod(row2, rowLevel) || virtualRow;
    var rowid2 = getRowid($table, row2);
    var rowIndex = $table.getRowIndex(row2);
    var $rowIndex = row2[GlobalConfig$1.$rowIndex];
    if (!isSkipRowRender) {
      seqCount.value = seqCount.value + 1;
    }
    var seq = isOrdered ? seqCount.value : $rowIndex + 1;
    if (scrollYLoad) {
      seq += startIndex;
    }
    if (hasVirtualRow && !virtualRow) {
      seq = afterFullData.indexOf(row2) + 1;
    }
    renderRowGroupData({
      $table,
      _vm,
      id: id3,
      row: row2,
      rowGroup,
      rowid: rowid2,
      rows,
      used,
      virtualRow
    });
    var args = {
      $rowIndex,
      $seq,
      $table,
      _vm,
      editStore,
      id: id3,
      isSkipRowRender,
      row: row2,
      rowActived,
      rowClassName
    };
    Object.assign(args, {
      rowIndex,
      rowLevel,
      rowid: rowid2,
      rows,
      selection,
      seq,
      treeConfig,
      used,
      selectRow,
      scrollYLoad,
      scrollXLoad
    });
    renderRow(args);
    renderRowAfter({
      $table,
      _vm,
      id: id3,
      row: row2,
      rowIndex,
      rows,
      used
    });
    args = {
      $table,
      expandMethod,
      expandeds,
      id: id3,
      row: row2,
      rowIndex,
      rowLevel,
      rows,
      seq,
      tableColumn: tableColumn2,
      treeConfig,
      used
    };
    renderRowExpanded(args);
  });
  renderRowFlag = !renderRowFlag;
  return rows;
}
function renderRowExpanded(args) {
  var $table = args.$table, expandMethod = args.expandMethod, expandeds = args.expandeds, id3 = args.id, row2 = args.row, rowIndex = args.rowIndex;
  var rowLevel = args.rowLevel, rows = args.rows, seq = args.seq, tableColumn2 = args.tableColumn, treeConfig = args.treeConfig, used = args.used;
  if (expandeds.length && expandeds.includes(row2) && (typeof expandMethod === "function" ? expandMethod(row2, rowLevel) : true)) {
    var column = find$1(tableColumn2, function(column2) {
      return column2.type === "expand";
    });
    var columnIndex = $table.getColumnIndex(column);
    var cellStyle;
    if (treeConfig) {
      cellStyle = {
        paddingLeft: rowLevel * (treeConfig.indent || 16) + 30 + "px"
      };
    }
    if (column) {
      var options = {
        $table,
        seq,
        row: row2,
        rowIndex,
        column,
        columnIndex,
        level: rowLevel
      };
      rows.push(createVNode("tr", {
        "key": "expand_" + id3,
        "style": {
          display: used ? void 0 : "none"
        },
        "class": "tiny-grid-body__expanded-row"
      }, [createVNode("td", {
        "colspan": tableColumn2.length,
        "class": "tiny-grid-body__expanded-column"
      }, [createVNode("div", {
        "style": cellStyle,
        "class": "tiny-grid-body__expanded-cell"
      }, [column.renderData(h, options)])])]));
    }
  }
}
function renderRowAfter(_ref50) {
  var $table = _ref50.$table, _vm = _ref50._vm, row2 = _ref50.row, rowIndex = _ref50.rowIndex, rows = _ref50.rows, id3 = _ref50.id, used = _ref50.used;
  typeof $table.renderRowAfter === "function" && $table.renderRowAfter({
    rows,
    row: row2,
    data: _vm.tableData,
    rowIndex,
    renderColumn,
    id: id3,
    used
  }, h);
}
function renderRow(args) {
  var $rowIndex = args.$rowIndex, $seq = args.$seq, $table = args.$table, _vm = args._vm, editStore = args.editStore, id3 = args.id, isSkipRowRender = args.isSkipRowRender, row2 = args.row, rowActived = args.rowActived, rowClassName = args.rowClassName;
  var rowIndex = args.rowIndex, rowLevel = args.rowLevel, rowid2 = args.rowid, rows = args.rows, selection = args.selection, selectRow = args.selectRow, seq = args.seq, treeConfig = args.treeConfig, used = args.used, scrollYLoad = args.scrollYLoad, scrollXLoad = args.scrollXLoad;
  if (isSkipRowRender) {
    return;
  }
  var key = id3;
  if (row2._isDraging) {
    _vm.$nextTick(function() {
      delete row2._isDraging;
    });
    if (renderRowFlag) {
      key = "drag_" + key;
    }
  }
  var columnPool = _vm.columnPool;
  if (used || scrollYLoad) {
    var _ref51;
    rows.push(createVNode("tr", {
      "key": key,
      "data-rowid": rowid2,
      "data-rowindex": $rowIndex,
      "data-rowlevel": rowLevel,
      "style": {
        display: used ? void 0 : "none"
      },
      "class": ["tiny-grid-body__row", (_ref51 = {}, _ref51["row__level-" + rowLevel] = treeConfig, _ref51["row__new"] = editStore.insertList.includes(row2), _ref51["row__selected"] = selection.includes(row2), _ref51["row__radio"] = selectRow === row2, _ref51["row__actived"] = rowActived, _ref51), rowClassName ? isFunction(rowClassName) ? rowClassName({
        $table,
        $seq,
        seq,
        fixedType: void 0,
        rowLevel,
        row: row2,
        rowIndex,
        $rowIndex
      }) : rowClassName : ""]
    }, [columnPool.map(function(_ref52, $columnIndex) {
      var id4 = _ref52.id, column = _ref52.item, used2 = _ref52.used;
      return used2 || scrollXLoad ? renderColumn({
        $columnIndex,
        $table,
        _vm,
        column,
        id: id4,
        row: row2,
        rowid: rowid2,
        seq,
        used: used2
      }) : null;
    })]));
  }
}
function renderRowGroupTds(_ref53) {
  var $table = _ref53.$table, closeable = _ref53.closeable, render17 = _ref53.render, renderGroupCell = _ref53.renderGroupCell, row2 = _ref53.row, tds = _ref53.tds, title = _ref53.title, _vm = _ref53._vm;
  var targetColumn = $table._rowGroupTargetColumn;
  var value = row2.value || "";
  var columnPool = _vm.columnPool;
  for (var _iterator4 = _createForOfIteratorHelperLoose(columnPool), _step4; !(_step4 = _iterator4()).done; ) {
    var _step4$value = _step4.value, id3 = _step4$value.id, column = _step4$value.item, used = _step4$value.used;
    var columnIndex = $table.getColumnIndex(column);
    var header = title || formatText(getFuncText(column.title), 1) || value;
    var params = {
      value,
      header,
      children: row2.children,
      expand: !row2.fold,
      row: row2,
      column,
      columnIndex
    };
    var isTarget = column === targetColumn;
    tds.push(createVNode("td", {
      "key": id3,
      "style": [getFixedStyle(column, $table), {
        display: used && column._rowGroupColspan > 0 ? void 0 : "none"
      }],
      "class": ["tiny-grid-body__column", isTarget ? "td-group" : "td-placeholder", column.id, getFixedClass(column, $table), column._stickyClass || ""],
      "colspan": column._rowGroupColspan,
      "data-colid": column.id
    }, [createVNode("div", {
      "class": "tiny-grid-cell-group"
    }, [isTarget ? [closeable ? row2.fold ? createVNode(ChevronRight, {
      "class": "tiny-svg-size"
    }, null) : createVNode(ChevronDown, {
      "class": "tiny-svg-size"
    }, null) : null].concat(render17 ? render17(h, params) : [createVNode("span", {
      "class": "row-group-title"
    }, [header]), ":" + value, createVNode("span", {
      "class": "tiny-badge"
    }, [row2.children.length])]) : renderGroupCell ? renderGroupCell(h, params) : null])]));
  }
}
function renderRowGroupData(_ref54) {
  var $table = _ref54.$table, _vm = _ref54._vm, id3 = _ref54.id, row2 = _ref54.row, rowGroup = _ref54.rowGroup, rowid2 = _ref54.rowid, rows = _ref54.rows, used = _ref54.used, virtualRow = _ref54.virtualRow;
  if (!virtualRow) return;
  var title = rowGroup.title, _rowGroup$closeable2 = rowGroup.closeable, closeable = _rowGroup$closeable2 === void 0 ? true : _rowGroup$closeable2, render17 = rowGroup.render, renderGroupCell = rowGroup.renderGroupCell, className = rowGroup.className;
  var tds = [];
  renderRowGroupTds({
    $table,
    closeable,
    render: render17,
    renderGroupCell,
    row: row2,
    tds,
    title,
    _vm
  });
  var onClick = function onClick2(event) {
    handleRowGroupFold(row2, $table);
    if ($table.tableListeners["toggle-group-change"]) {
      emitEvent($table, "toggle-group-change", [{
        $table,
        row: row2
      }, event]);
    }
  };
  rows.push(createVNode("tr", {
    "key": id3,
    "class": ["tiny-grid-body__row", "group", className, {
      hover: row2.hover
    }],
    "style": {
      display: used ? void 0 : "none"
    },
    "data-rowid": rowid2,
    "onMouseout": function onMouseout() {
      return row2.hover = false;
    },
    "onMouseover": function onMouseover() {
      return row2.hover = true;
    },
    "onClick": onClick
  }, [tds]));
}
function renderTable(_ref55) {
  var $table = _ref55.$table, _vm = _ref55._vm;
  var tableLayout = $table.tableLayout, scrollXLoad = $table.scrollXLoad, scrollYLoad = $table.scrollYLoad, bodyTableWidth = $table.bodyTableWidth, isColumnWidthAssigned = $table.isColumnWidthAssigned;
  var columnPool = _vm.columnPool, isNoData = _vm.isNoData;
  if (!isColumnWidthAssigned) {
    return;
  }
  var tableVnode = createVNode("table", {
    "ref": "table",
    "class": "tiny-grid__body",
    "style": {
      tableLayout,
      width: bodyTableWidth ? bodyTableWidth + "px" : void 0
    },
    "cellspacing": 0,
    "cellpadding": 0,
    "border": 0,
    "data-tableid": $table.id
  }, [[
    // 列分组（用于指定列宽）
    createVNode("colgroup", {
      "ref": "colgroup"
    }, [columnPool.map(function(_ref56) {
      var id3 = _ref56.id, column = _ref56.item, used = _ref56.used;
      return createVNode("col", {
        "key": id3,
        "name": column.id,
        "width": String(column.renderWidth),
        "style": {
          display: used ? void 0 : "none"
        }
      }, null);
    })]),
    // 表头
    $table.showHeader ? createVNode("thead", {
      "ref": "thead"
    }, [renderHeaderRows(_vm)]) : null,
    // 表体内容
    createVNode("tbody", {
      "ref": "tbody"
    }, [renderRows(_vm)]),
    // 表尾
    $table.showFooter && !isNoData && typeof $table.renderFooter !== "function" ? createVNode("tfoot", {
      "ref": "tfoot"
    }, [renderFooterRows(_vm)]) : null
  ]]);
  return scrollXLoad || scrollYLoad ? createVNode("div", {
    "ref": "stickyWrapper",
    "class": "sticky-wrapper"
  }, [tableVnode]) : tableVnode;
}
var calcScrollLeft = function calcScrollLeft2($table, wrapperScrollLeft) {
  var visibleColumn = $table.visibleColumn, tableColumn2 = $table.tableColumn;
  var start, end, total, offset, column;
  start = end = total = offset = 0;
  for (var _iterator5 = _createForOfIteratorHelperLoose(visibleColumn), _step5; !(_step5 = _iterator5()).done; ) {
    var col = _step5.value;
    start = end;
    end = start + col.renderWidth;
    if (wrapperScrollLeft >= start && wrapperScrollLeft < end) {
      offset = wrapperScrollLeft - total;
      column = col;
      break;
    }
    total += col.renderWidth;
  }
  total = 0;
  for (var _iterator6 = _createForOfIteratorHelperLoose(tableColumn2), _step6; !(_step6 = _iterator6()).done; ) {
    var _col = _step6.value;
    if (_col === column) {
      total += offset;
      break;
    }
    total += _col.renderWidth;
  }
  return total;
};
var calcScrollTop = function calcScrollTop2($table, wrapperScrollTop) {
  var _graphInfo = $table._graphInfo, tableData = $table.tableData, headerHeight = $table.headerHeight, rowHeight = $table.rowHeight;
  var graphed = _graphInfo.graphed;
  var rows = graphed.map(function(node) {
    return node.payload;
  });
  var start, end, total, offset, row2;
  start = end = total = offset = 0;
  if (wrapperScrollTop < headerHeight) {
    total = wrapperScrollTop;
  } else {
    start = end = total = offset = headerHeight;
    for (var _iterator7 = _createForOfIteratorHelperLoose(rows), _step7; !(_step7 = _iterator7()).done; ) {
      var r = _step7.value;
      start = end;
      end = start + rowHeight;
      if (wrapperScrollTop >= start && wrapperScrollTop < end) {
        offset = wrapperScrollTop - total;
        row2 = r;
        break;
      }
      total += rowHeight;
    }
    total = headerHeight;
    for (var _iterator8 = _createForOfIteratorHelperLoose(tableData), _step8; !(_step8 = _iterator8()).done; ) {
      var _r = _step8.value;
      if (hooks.toRaw(_r) === hooks.toRaw(row2)) {
        total += offset;
        break;
      }
      total += rowHeight;
    }
  }
  return total;
};
var Body = /* @__PURE__ */ defineComponent({
  name: $prefix + "GridBody",
  props: {
    collectColumn: Array,
    tableColumn: Array,
    tableNode: Array,
    tableData: Array,
    footerData: Array
  },
  setup: function setup3(props, _ref57) {
    var _hooks$getCurrentInst5;
    var slots = _ref57.slots;
    var vm = (_hooks$getCurrentInst5 = hooks.getCurrentInstance()) == null ? void 0 : _hooks$getCurrentInst5.proxy;
    var $table = vm == null ? void 0 : vm.$parent;
    var rowHeight = hooks.computed(function() {
      return $table.rowHeight;
    });
    var headerRowHeight = hooks.computed(function() {
      return $table.headerRowHeight;
    });
    var _useHeader = useHeader(props, vm, headerRowHeight), headerTable = _useHeader.headerTable;
    var _usePool = usePool(props), columnPool = _usePool.columnPool, rowPool = _usePool.rowPool, isNoData = _usePool.isNoData;
    var wrapperScrollLeft = hooks.ref(0);
    var wrapperScrollTop = hooks.ref(0);
    var stickyWrapper = hooks.ref();
    var table = hooks.ref();
    var body = hooks.ref();
    var customFooter = hooks.ref();
    var colgroup = hooks.ref();
    var thead = hooks.ref();
    var tbody = hooks.ref();
    var ySpace = hooks.ref();
    var alignXBar = hooks.ref();
    var alignYBar = hooks.ref();
    hooks.watch(wrapperScrollLeft, function(wrapperScrollLeft2) {
      var el = stickyWrapper.value;
      if (!el) return;
      hooks.nextTick(function() {
        return el.scrollLeft = calcScrollLeft($table, wrapperScrollLeft2);
      });
    });
    hooks.watch(wrapperScrollTop, function(wrapperScrollTop2) {
      var el = stickyWrapper.value;
      if (!el) return;
      hooks.nextTick(function() {
        return el.scrollTop = calcScrollTop($table, wrapperScrollTop2);
      });
    });
    var resetStickyWrapperScrollPos = function resetStickyWrapperScrollPos2() {
      var el = stickyWrapper.value;
      if (!el) return;
      el.scrollLeft = calcScrollLeft($table, wrapperScrollLeft.value);
      el.scrollTop = calcScrollTop($table, wrapperScrollTop.value);
    };
    useCellEvent({
      table,
      $table
    });
    var _useCellSpan = useCellSpan(vm, props), normalRows = _useCellSpan.normalRows, footerRows = _useCellSpan.footerRows;
    hooks.watch([body, table, thead, tbody, ySpace, alignXBar, alignYBar], function() {
      var elemStore = $table.elemStore;
      elemStore["main-body-wrapper"] = body.value;
      elemStore["main-body-table"] = table.value;
      elemStore["main-body-headerList"] = thead.value;
      elemStore["main-body-list"] = tbody.value;
      elemStore["main-body-ySpace"] = ySpace.value;
      elemStore["main-body-alignXBar"] = alignXBar.value;
      elemStore["main-body-alignYBar"] = alignYBar.value;
    });
    var bodyClientWidth = hooks.ref(0);
    var resizeObserver = new ResizeObserver(function(entries) {
      for (var _iterator9 = _createForOfIteratorHelperLoose(entries), _step9; !(_step9 = _iterator9()).done; ) {
        var entry = _step9.value;
        var target = entry.target;
        switch (target) {
          case body.value:
            bodyClientWidth.value = target.clientWidth;
            break;
          case customFooter.value:
            $table.footerHeight = target.offsetHeight;
        }
      }
    });
    hooks.watch(body, function(body2) {
      return body2 && resizeObserver.observe(body2);
    });
    hooks.watch(tbody, function(tbody2) {
      if (tbody2 && $table.dropConfig) {
        var _vm$rowSortable, _vm$columnSortable;
        (_vm$rowSortable = vm.rowSortable) == null ? void 0 : _vm$rowSortable.destroy();
        (_vm$columnSortable = vm.columnSortable) == null ? void 0 : _vm$columnSortable.destroy();
        var _$table$dropConfig3 = $table.dropConfig, plugin = _$table$dropConfig3.plugin, _$table$dropConfig3$r = _$table$dropConfig3.row, row2 = _$table$dropConfig3$r === void 0 ? true : _$table$dropConfig3$r, _$table$dropConfig3$c = _$table$dropConfig3.column, column = _$table$dropConfig3$c === void 0 ? true : _$table$dropConfig3$c, scheme = _$table$dropConfig3.scheme;
        plugin && row2 && (vm.rowSortable = $table.rowDrop(body.value));
        if (scheme !== "v2") {
          plugin && column && (vm.columnSortable = $table.columnDrop(body.value));
        }
      }
    });
    hooks.watch(customFooter, function(customFooter2) {
      return customFooter2 && resizeObserver.observe(customFooter2);
    });
    hooks.watchEffect(function() {
      if ($table.isColumnWidthAssigned) {
        var columns2 = $table.scrollXLoad ? $table.visibleColumn : $table.tableColumn;
        var scrollWidth = columns2.reduce(function(total, col) {
          return total += col.renderWidth;
        }, 0);
        var clientWidth = bodyClientWidth.value;
        $table.horizonScroll.max = scrollWidth > clientWidth ? scrollWidth - clientWidth : 0;
      }
    });
    hooks.onMounted(function() {
      var _body$value;
      vm._throttleScrollHandler = throttle($table.optimizeOpts.scrollDelay, vm.handleScroll);
      (_body$value = body.value) == null ? void 0 : _body$value.addEventListener("scroll", vm._throttleScrollHandler);
    });
    hooks.onBeforeUnmount(function() {
      var _body$value2;
      var rowSortable = vm.rowSortable, columnSortable = vm.columnSortable;
      (_body$value2 = body.value) == null ? void 0 : _body$value2.removeEventListener("scroll", vm._throttleScrollHandler);
      vm._throttleScrollHandler = null;
      rowSortable == null ? void 0 : rowSortable.destroy();
      columnSortable == null ? void 0 : columnSortable.destroy();
      resizeObserver.disconnect();
    });
    return {
      slots,
      rowHeight,
      headerTable,
      columnPool,
      rowPool,
      isNoData,
      wrapperScrollLeft,
      wrapperScrollTop,
      stickyWrapper,
      table,
      body,
      customFooter,
      colgroup,
      thead,
      tbody,
      ySpace,
      alignXBar,
      alignYBar,
      normalRows,
      footerRows,
      resetStickyWrapperScrollPos
    };
  },
  render: function render() {
    var $table = this.$parent, isNoData = this.isNoData, tableColumn2 = this.tableColumn, footerData = this.footerData;
    var $grid = $table.$grid, keyboardConfig = $table.keyboardConfig, mouseConfig = $table.mouseConfig, scrollLoad = $table.scrollLoad, showFooter = $table.showFooter, renderFooter = $table.renderFooter;
    var containerScrollWidth = $table.containerScrollWidth, containerScrollHeight = $table.containerScrollHeight, scrollLoadScrollHeight = $table.scrollLoadScrollHeight;
    var bodyWrapperHeight = $table.bodyWrapperHeight, bodyWrapperMinHeight = $table.bodyWrapperMinHeight, bodyWrapperMaxHeight = $table.bodyWrapperMaxHeight;
    var $slots = $grid.slots;
    var _vm = this;
    return createVNode("div", {
      "ref": "body",
      "class": {
        "tiny-grid__body-wrapper body__wrapper": true,
        "is__scrollload": scrollLoad,
        "no-data": isNoData && $table.isShapeTable
      },
      "style": {
        height: bodyWrapperHeight ? bodyWrapperHeight + "px" : void 0,
        minHeight: bodyWrapperMinHeight ? bodyWrapperMinHeight + "px" : void 0,
        maxHeight: bodyWrapperMaxHeight ? bodyWrapperMaxHeight + "px" : void 0
      }
    }, [[
      mouseConfig != null && mouseConfig.hover ? [createVNode("div", {
        "ref": "alignYBar",
        "class": "tiny-grid-body__hover-align-y-bar",
        "style": {
          height: containerScrollHeight + "px"
        }
      }, null), createVNode("div", {
        "ref": "alignXBar",
        "class": "tiny-grid-body__hover-align-x-bar",
        "style": {
          width: containerScrollWidth + "px"
        }
      }, null)] : null,
      createVNode("div", {
        "class": "tiny-grid-body__x-space",
        "style": {
          width: containerScrollWidth + "px"
        }
      }, null),
      createVNode("div", {
        "ref": "ySpace",
        "class": "tiny-grid-body__y-space visual",
        "style": {
          height: containerScrollHeight + "px"
        },
        "onScroll": this.handleScrollLoad
      }, [scrollLoad ? createVNode("div", {
        "class": "tiny-grid-body__y-scrollbar",
        "style": {
          height: scrollLoadScrollHeight ? scrollLoadScrollHeight + "px" : void 0
        }
      }, null) : null]),
      // 内容表格
      renderTable({
        $table,
        _vm
      }),
      // 渲染自定义表尾
      showFooter && !isNoData && typeof renderFooter === "function" ? createVNode("div", {
        "ref": "customFooter",
        "class": "tiny-grid-custom-footer"
      }, [renderFooter({
        $table,
        columns: tableColumn2,
        footerData
      }, h)]) : null,
      // 选中边框线
      mouseConfig.checked || keyboardConfig.isCut ? createVNode("div", {
        "class": "tiny-grid__borders"
      }, [[mouseConfig.checked ? renderBorder("check") : null, keyboardConfig.isCut ? renderBorder("copy") : null]]) : null,
      // 空数据
      isNoData ? createVNode("div", {
        "ref": "emptyBlock",
        "class": "tiny-grid__empty-block"
      }, [$slots.empty ? $slots.empty({
        $table
      }, h) : $table.renderEmpty ? [$table.renderEmpty(h, $table)] : [createVNode(GridNoData, {
        "class": "tiny-grid__empty-img"
      }, null), createVNode("span", {
        "class": "tiny-grid__empty-text"
      }, [GlobalConfig$1.i18n("ui.grid.emptyText")])]]) : null
    ]]);
  },
  methods: {
    handleScroll: function handleScroll(event) {
      var $table = this.$parent, $el = this.$el;
      var lastScrollLeft = $table.lastScrollLeft, lastScrollTop = $table.lastScrollTop, scrollXLoad = $table.scrollXLoad, scrollYLoad = $table.scrollYLoad, horizonScroll = $table.horizonScroll;
      var max = horizonScroll.max, threshold = horizonScroll.threshold;
      var scrollLeft = $el.scrollLeft, scrollTop = $el.scrollTop;
      var isX = scrollLeft !== lastScrollLeft;
      var isY = scrollTop !== lastScrollTop;
      this.wrapperScrollLeft = scrollLeft;
      this.wrapperScrollTop = scrollTop;
      $table.lastScrollTime = Date.now();
      $table.lastScrollLeft = scrollLeft;
      $table.lastScrollTop = scrollTop;
      $table.scrollDirection = isX ? "X" : "Y";
      $table.horizonScroll.isLeft = scrollLeft < threshold;
      $table.horizonScroll.isRight = scrollLeft > max - threshold;
      if (isX && scrollXLoad) {
        $table.triggerScrollXEvent(event);
      }
      if (isY && scrollYLoad) {
        $table.triggerScrollYEvent(event);
      }
      emitEvent($table, "scroll", [{
        type: "body",
        scrollTop,
        scrollLeft,
        isX,
        isY,
        $table
      }, event]);
    },
    resizeMousedown: function resizeMousedown(event, params) {
      var $el = this.$el, $table = this.$parent;
      var dragClientX = event.clientX, dragBtnElem = event.target;
      var column = params.column;
      var _ref58 = {}, _ref58$dragLeft = _ref58.dragLeft, dragLeft = _ref58$dragLeft === void 0 ? 0 : _ref58$dragLeft, _ref58$minInterval = _ref58.minInterval, minInterval = _ref58$minInterval === void 0 ? 40 : _ref58$minInterval;
      var _$table$$refs = $table.$refs, resizeBarElem = _$table$$refs.resizeBar, tableBody = _$table$$refs.tableBody;
      var _ref59 = {}, _ref59$cell = _ref59.cell, cell = _ref59$cell === void 0 ? dragBtnElem.parentNode : _ref59$cell, _ref59$dragBtnWidth = _ref59.dragBtnWidth, dragBtnWidth = _ref59$dragBtnWidth === void 0 ? dragBtnElem.clientWidth : _ref59$dragBtnWidth;
      var _ref60 = {}, _ref60$pos = _ref60.pos, pos = _ref60$pos === void 0 ? getOffsetPos(dragBtnElem, $el) : _ref60$pos, _ref60$tableBodyElem = _ref60.tableBodyElem, tableBodyElem = _ref60$tableBodyElem === void 0 ? tableBody.$el : _ref60$tableBodyElem;
      var dragMinLeft = pos.left - cell.clientWidth + dragBtnWidth + minInterval;
      var dragPosLeft = pos.left + Math.floor(dragBtnWidth);
      var _ref61 = {}, _ref61$oldMousemove = _ref61.oldMousemove, oldMousemove = _ref61$oldMousemove === void 0 ? document.onmousemove : _ref61$oldMousemove, _ref61$oldMouseup = _ref61.oldMouseup, oldMouseup = _ref61$oldMouseup === void 0 ? document.onmouseup : _ref61$oldMouseup;
      var handleMousemoveEvent = function handleMousemoveEvent2(event2) {
        var _$table$resizableConf;
        event2.stopPropagation();
        event2.preventDefault();
        var _ref62 = {}, _ref62$offsetX = _ref62.offsetX, offsetX = _ref62$offsetX === void 0 ? event2.clientX - dragClientX : _ref62$offsetX, _ref62$left = _ref62.left, left = _ref62$left === void 0 ? offsetX + dragPosLeft : _ref62$left;
        var scrollLeft = tableBodyElem.scrollLeft;
        dragLeft = Math.max(left, dragMinLeft);
        if ((_$table$resizableConf = $table.resizableConfig) != null && _$table$resizableConf.limit) {
          var _$table$resizableConf2;
          var limitWidth = (_$table$resizableConf2 = $table.resizableConfig) == null ? void 0 : _$table$resizableConf2.limit({
            field: column.own.field,
            width: column.renderWidth + (dragLeft - dragPosLeft)
          });
          dragLeft = dragMinLeft - minInterval + limitWidth;
        }
        resizeBarElem.style.left = dragLeft - scrollLeft + "px";
      };
      resizeBarElem.style.display = "block";
      addClass($table.$el, "tiny-grid-cell__resize");
      $table._isResize = true;
      document.onmousemove = handleMousemoveEvent;
      document.onmouseup = function() {
        document.onmousemove = oldMousemove;
        document.onmouseup = oldMouseup;
        var resizeWidth = column.renderWidth + (dragLeft - dragPosLeft);
        resizeWidth = typeof resizeWidth === "number" ? resizeWidth : parseInt(resizeWidth, 10) || 40;
        column.resizeWidth = resizeWidth < 40 ? 40 : resizeWidth;
        resizeBarElem.style.display = "none";
        removeClass($table.$el, "tiny-grid-cell__resize");
        Object.assign($table, {
          _isResize: false,
          _lastResizeTime: Date.now()
        });
        $table.analyColumnWidth();
        $table.recalculate();
        var toolbarVm = $table.getVm("toolbar");
        if (toolbarVm) {
          toolbarVm.updateResizable();
        }
        emitEvent($table, "resizable-change", [params]);
        if ($table.horizonScroll.fixed) {
          setTimeout(function() {
            var _$table$$refs$tableBo = $table.$refs.tableBody.$el, scrollWidth = _$table$$refs$tableBo.scrollWidth, clientWidth = _$table$$refs$tableBo.clientWidth, scrollLeft = _$table$$refs$tableBo.scrollLeft;
            $table.horizonScroll.max = scrollWidth > clientWidth ? scrollWidth - clientWidth : 0;
            $table.horizonScroll.isRight = scrollLeft > $table.horizonScroll.max - $table.horizonScroll.threshold;
          }, 50);
        }
      };
      handleMousemoveEvent(event);
    },
    handleScrollLoad: function handleScrollLoad(e) {
      var $table = this.$parent;
      if ($table.scrollLoad) {
        $table.debounceScrollLoad(e);
      }
    }
  }
});
Body.install = function(Vue) {
  Vue.component(Body.name, Body);
};
var Radio = defineComponent({
  name: $prefix + "GridRadio",
  emits: ["update:modelValue", "change"],
  props: {
    modelValue: [String, Number],
    label: [String, Number],
    disabled: Boolean,
    name: String,
    size: String
  },
  directives: {
    AutoTip
  },
  computed: {
    vSize: function vSize() {
      return this.size || this.$parent.size || this.$parent.vSize;
    }
  },
  setup: function setup4(props, _ref63) {
    var slots = _ref63.slots;
    return {
      slots
    };
  },
  render: function render2() {
    var _ref64, _this2 = this;
    var disabled = this.disabled, vSize3 = this.vSize, modelValue = this.modelValue, label = this.label, name = this.name;
    var $slots = this.slots;
    var disableClass = "disabled";
    var labelContent = function labelContent2() {
      var content = h("span", {
        class: "tiny-grid-radio__label",
        directives: isVue2 ? [{
          name: "AutoTip"
        }] : null
      }, $slots.default());
      if (!isVue2) {
        hooks.withDirectives(content, [[AutoTip]]);
      }
      return content;
    };
    return h("label", {
      class: ["tiny-grid-radio", (_ref64 = {}, _ref64["size__" + vSize3] = vSize3, _ref64["is__" + disableClass] = disabled, _ref64)]
    }, [h("input", {
      attrs: {
        type: "radio",
        name,
        disabled
      },
      domProps: {
        checked: modelValue === label
      },
      on: {
        change: function change(event) {
          if (!disabled) {
            _this2.$emit("update:modelValue", label);
            _this2.$emit("change", label, event);
          }
        }
      }
    }), h("span", {
      class: "tiny-grid-radio__icon"
    }, [h(iconRadio(), {
      class: ["tiny-svg-size", "icon-radio"]
    }), h(iconRadioselected(), {
      class: ["tiny-svg-size", "icon-radio-selected"]
    })]), $slots.default ? labelContent() : null]);
  }
});
Radio.model = {
  prop: "modelValue",
  event: "update:modelValue"
};
$install(Radio);
function _isSlot(s) {
  return typeof s === "function" || Object.prototype.toString.call(s) === "[object Object]" && !isVNode(s);
}
var renderInputArgs = function renderInputArgs2(_ref65) {
  var _vm = _ref65._vm, inputFilter = _ref65.inputFilter;
  var isAddbyProgram = false;
  if (typeof inputFilter.relations_addby !== "undefined") {
    isAddbyProgram = inputFilter.relations_addby === "program";
  }
  var inputRelations;
  if (isAddbyProgram) {
    inputRelations = inputFilter.relations_backup;
  } else {
    inputRelations = inputFilter.relations;
  }
  if (!inputRelations) {
    inputRelations = [{
      label: _vm.i18n("ui.grid.filter.prefix"),
      value: "startwith"
    }, {
      label: _vm.i18n("ui.grid.filter.equal"),
      value: "equals"
    }, {
      label: _vm.i18n("ui.grid.filter.include"),
      value: "contains"
    }];
  }
  return {
    isAddbyProgram,
    inputRelations
  };
};
var renderInputProps = function renderInputProps2(_ref66) {
  var _on3;
  var InputComponent = _ref66.InputComponent, condition2 = _ref66.condition, event = _ref66.event, inputFilter = _ref66.inputFilter, isNativeInput = _ref66.isNativeInput, prop = _ref66.prop;
  var inputProps = {
    style: "width:100%",
    on: (_on3 = {}, _on3[event] = function(value) {
      condition2.input = value && value.target ? value.target.value : value;
    }, _on3),
    ref: "filterInputComponent"
  };
  if (isNativeInput) {
    var _domProps;
    inputProps = Object.assign(inputProps, {
      domProps: (_domProps = {}, _domProps[prop] = condition2.input, _domProps),
      attrs: _extends({}, inputFilter.attrs)
    });
  } else {
    var _extends3;
    InputComponent = hooks.toRaw(InputComponent);
    inputProps = Object.assign(inputProps, {
      props: _extends({}, inputFilter.attrs, (_extends3 = {}, _extends3[prop] = condition2.input, _extends3))
    });
  }
  return {
    inputProps,
    InputComponent
  };
};
var iconRender = function iconRender2(checked, index2, halfChecked) {
  var CheckedSur = iconCheckedSur();
  var Check = iconCheck();
  var HalfCheck = iconHalfselect();
  var props = {
    class: "tiny-svg-size",
    tabindex: index2
  };
  if (typeof halfChecked === "undefined") {
    return checked ? createVNode(CheckedSur, mergeProps(props, {
      "class": "is-checked"
    }), null) : createVNode(Check, props, null);
  } else {
    return checked ? createVNode(CheckedSur, mergeProps(props, {
      "class": "is-checked"
    }), null) : halfChecked ? createVNode(HalfCheck, mergeProps(props, {
      "class": "is-checked"
    }), null) : createVNode(Check, props, null);
  }
};
var renderEnumableOptions = function renderEnumableOptions2(_ref67) {
  var iconRender3 = _ref67.iconRender, _vm = _ref67._vm, filterStore = _ref67.filterStore, selectAll = _ref67.selectAll, searchable = _ref67.searchable;
  var resultList = filterStore.options.filter(function(value) {
    var _value$label;
    return (_value$label = value.label) == null ? void 0 : _value$label.toString().includes(filterStore.searchValue);
  });
  if (resultList.length) {
    var filterList = resultList.map(function(item, index2) {
      return createVNode("li", {
        "class": ["tiny-grid__filter-option", {
          selected: item.checked
        }],
        "onClick": function onClick() {
          if (!filterStore.multi) {
            filterStore.options.forEach(function(option) {
              option.checked = option === item;
            });
            _vm.filterEnum();
          } else {
            item.checked = !item.checked;
          }
        }
      }, [filterStore.multi ? iconRender3(item.checked, index2 + 1) : null, createVNode("a", {
        "title": item.label
      }, [createTextVNode(" "), item.label, createTextVNode(" ")])]);
    });
    var isShowAllSelect = selectAll && (!filterStore.multi || !searchable && filterStore.multi);
    return isShowAllSelect ? [createVNode("li", {
      "class": "tiny-grid__filter-option",
      "onClick": function onClick() {
        var isAllSelected = filterStore.options.every(function(item) {
          return item.checked;
        });
        filterStore.options.forEach(function(option) {
          option.checked = !isAllSelected;
        });
        if (!filterStore.multi) {
          _vm.filterEnum();
        }
      }
    }, [filterStore.multi ? iconRender3(filterStore.options.every(function(item) {
      return item.checked;
    }), 0, filterStore.options.some(function(item) {
      return item.checked;
    })) : null, createVNode("a", {
      "title": _vm.i18n("ui.grid.filter.allSelect")
    }, [createTextVNode(" "), _vm.i18n("ui.grid.filter.allSelect")])])].concat(filterList) : filterList;
  } else {
    return [createVNode("li", {
      "class": "tiny-grid__filter-empty"
    }, [_vm.i18n("ui.grid.emptyText")])];
  }
};
var Panel$1 = /* @__PURE__ */ defineComponent({
  name: $prefix + "GridFilter",
  props: {
    filterStore: Object,
    optimizeOpts: Object
  },
  inject: {
    $grid: {
      default: null
    }
  },
  watch: {
    id: function id() {
      this.updatePanel();
    },
    visible: function visible() {
      this.showAdvance = false;
      this.updatePanel();
    }
  },
  data: function data() {
    return {
      popperJS: null,
      showAdvance: false,
      showAdvItems: false,
      listPopper: null
    };
  },
  render: function render3() {
    var _this3 = this, _ref68;
    var filterStore = this.filterStore, $grid = this.$grid, $table = this.$parent, optimizeOpts = this.optimizeOpts, renderInput2 = this.renderInput, renderEnumable2 = this.renderEnumable, renderDefault2 = this.renderDefault, renderExtends2 = this.renderExtends, renderBase2 = this.renderBase, renderSimple2 = this.renderSimple;
    var args = filterStore.args, column = filterStore.column, options = filterStore.options, _filterStore$layout = filterStore.layout, layout2 = _filterStore$layout === void 0 ? "input,enum,default,extends,base" : _filterStore$layout;
    var layoutMap = {
      input: renderInput2,
      enum: renderEnumable2,
      default: renderDefault2,
      extends: renderExtends2,
      base: renderBase2,
      simple: renderSimple2
    };
    var quickFilter = layout2.split(",").map(function(item) {
      return item.trim();
    }).filter(Boolean).map(function(item) {
      return layoutMap[item] && layoutMap[item].call(_this3);
    });
    var map = {
      filterActive: "filter__active"
    };
    var wrapperAttrs = isVue2 ? {
      attrs: filterStore.attrs
    } : filterStore.attrs;
    return createVNode("div", mergeProps(wrapperAttrs, {
      "class": ["tiny-grid__wrapper", "tiny-grid__filter-wrapper", "filter__prevent-default", (_ref68 = {
        "tiny-grid__animat": optimizeOpts.animat
      }, _ref68[map.filterActive] = filterStore.visible, _ref68["tiny-grid__filter-simple"] = layout2.includes("simple"), _ref68["filter-wrapper-saas"] = $table.isThemeSaas, _ref68)],
      "style": filterStore.style
    }), [filterStore.visible ? createVNode("div", {
      "class": ["tiny-grid__filter-body", {
        "show-addvance": this.showAdvance
      }]
    }, [column.slots && column.slots.filter ? column.slots.filter({
      $grid,
      values: options,
      args,
      context: this
    }) : quickFilter]) : null]);
  },
  computed: {
    condition: function condition() {
      return this.filterStore.condition;
    },
    visible: function visible2() {
      return this.filterStore.visible;
    },
    id: function id2() {
      return this.filterStore.id;
    },
    i18n: function i18n2() {
      return t || this.$t;
    }
  },
  methods: {
    updatePanel: debounce(20, function() {
      var _this4 = this;
      this.correctRelations();
      var inputFilter = this.filterStore.inputFilter;
      if (inputFilter) {
        var relations = inputFilter.relations;
        if (inputFilter !== true && !relations.filter(function(r) {
          return r.value === _this4.condition.relation;
        }).length) {
          this.resetInput();
        }
      }
      if (this.popperJS) {
        this.popperJS.destroy();
        this.popperJS = null;
      }
      if (this.visible) {
        setTimeout(function() {
          var _this4$$grid;
          var _this4$filterStore = _this4.filterStore, targetElemParentTr = _this4$filterStore.targetElemParentTr, id3 = _this4$filterStore.id;
          var reference = targetElemParentTr && targetElemParentTr.querySelector("svg.tiny-grid-filter__btn." + id3);
          var popper = _this4.$el;
          popper.style.zIndex = PopupManager.nextZIndex();
          _this4.popperJS = new PopperJS(reference, popper, _extends({
            placement: "bottom-end",
            gpuAcceleration: false,
            bubbling: true
          }, ((_this4$$grid = _this4.$grid) == null ? void 0 : _this4$$grid.filterPopperOptions) || {}));
          popper.style.display = "block";
        });
      }
    }),
    // 基础清除选项
    renderBase: function renderBase() {
      return createVNode("ul", {
        "class": "tiny-grid__filter-panel filter-panel__clear"
      }, [createVNode("li", {
        "class": "tiny-grid__filter-option",
        "onClick": this.resetFilter
      }, [createVNode("a", {
        "class": "item-content"
      }, [this.i18n("ui.grid.filter.clear")])]), createVNode("li", {
        "class": "tiny-grid__filter-option",
        "onClick": this.clearFilter
      }, [createVNode("a", {
        "class": "item-content"
      }, [this.i18n("ui.grid.filter.clearAll")])])]);
    },
    // 设置筛选条件为空和不为空
    renderDefault: function renderDefault() {
      var _this5 = this;
      var condition2 = this.condition, filterStore = this.filterStore;
      if (!filterStore.defaultFilter) {
        return null;
      }
      return createVNode("ul", {
        "class": "tiny-grid__filter-panel filter-panel__default"
      }, [createVNode("li", {
        "class": ["tiny-grid__filter-option", {
          active: condition2.empty === true
        }],
        "onClick": function onClick() {
          _this5.filterNull(true);
        }
      }, [createVNode("a", {
        "class": "item-content"
      }, [this.i18n("ui.grid.filter.empty")])]), createVNode("li", {
        "class": ["tiny-grid__filter-option", {
          active: condition2.empty === false
        }],
        "onClick": function onClick() {
          _this5.filterNull(false);
        }
      }, [createVNode("a", {
        "class": "item-content"
      }, [this.i18n("ui.grid.filter.unempty")])])]);
    },
    // 筛选扩展项
    renderExtends: function renderExtends() {
      var _this6 = this;
      var filterStore = this.filterStore, condition2 = this.condition;
      if (!filterStore.extends) {
        return null;
      }
      return createVNode("ul", {
        "class": "tiny-grid__filter-panel filter-panel__clear"
      }, [filterStore.extends.map(function(item) {
        return createVNode("li", {
          "class": ["tiny-grid__filter-option", {
            active: condition2.value === (item.value || item.label)
          }],
          "onClick": function onClick() {
            _this6.filterExtends(item);
          }
        }, [createVNode("a", {
          "class": "item-content"
        }, [item.label])]);
      })]);
    },
    // 输入筛选项
    renderInput: function renderInput() {
      var _slot, _slot2, _slot3;
      this.correctRelations();
      var condition2 = this.condition, filterStore = this.filterStore;
      var inputFilter = filterStore.inputFilter;
      if (!inputFilter) return null;
      var _renderInputArgs = renderInputArgs({
        _vm: this,
        inputFilter
      }), isAddbyProgram = _renderInputArgs.isAddbyProgram, inputRelations = _renderInputArgs.inputRelations;
      var InputComponent = inputFilter.component || "input";
      var isNativeInput = InputComponent === "input";
      var model = InputComponent.model || {};
      var prop = isNativeInput ? "value" : model.prop || "modelValue";
      var event = isNativeInput ? "input" : model.event || "update:modelValue";
      var ret = renderInputProps({
        InputComponent,
        condition: condition2,
        event,
        inputFilter,
        isNativeInput,
        prop
      });
      var inputProps = ret.inputProps;
      InputComponent = ret.InputComponent;
      return createVNode("ul", {
        "class": "tiny-grid__filter-panel"
      }, [isAddbyProgram ? null : createVNode("li", {
        "class": "tiny-grid__filter-option filter-option__radios"
      }, [inputRelations.map(function(_ref69) {
        var label = _ref69.label, value = _ref69.value, method = _ref69.method;
        return createVNode(Radio, {
          "modelValue": condition2.relation,
          "label": value,
          "onChange": function onChange(value2) {
            condition2.relation = value2;
            condition2.method = method || function() {
              return true;
            };
          }
        }, _isSlot(label) ? label : {
          default: function _default29() {
            return [label];
          }
        });
      })]), createVNode("li", {
        "class": "filter-option__input"
      }, [h(InputComponent, inputProps)]), createVNode("li", {
        "class": "tiny-grid__filter-option filter-option__btns"
      }, [createVNode(Button, {
        "type": "primary",
        "onClick": this.filterInput
      }, _isSlot(_slot = this.i18n("ui.base.confirm")) ? _slot : {
        default: function _default29() {
          return [_slot];
        }
      }), createVNode(Button, {
        "onClick": this.resetInput
      }, _isSlot(_slot2 = this.i18n("ui.base.reset")) ? _slot2 : {
        default: function _default29() {
          return [_slot2];
        }
      }), createVNode(Button, {
        "onClick": this.close
      }, _isSlot(_slot3 = this.i18n("ui.base.cancel")) ? _slot3 : {
        default: function _default29() {
          return [_slot3];
        }
      })])]);
    },
    // 选择筛选项
    renderEnumable: function renderEnumable() {
      var _slot4, _slot5;
      var filterStore = this.filterStore;
      if (!filterStore.enumable) return null;
      return createVNode("div", {
        "class": "tiny-grid__filter-panel filter-panel__enum"
      }, [createVNode("ul", {
        "class": "tiny-grid__filter-options"
      }, [renderEnumableOptions({
        iconRender,
        _vm: this,
        filterStore
      })]), filterStore.multi ? createVNode("div", {
        "class": "tiny-grid__filter-option filter-option__btns"
      }, [createVNode(Button, {
        "type": "primary",
        "onClick": this.filterEnum
      }, _isSlot(_slot4 = this.i18n("ui.base.confirm")) ? _slot4 : {
        default: function _default29() {
          return [_slot4];
        }
      }), createVNode(Button, {
        "onClick": function onClick() {
          filterStore.visible = false;
        }
      }, _isSlot(_slot5 = this.i18n("ui.base.cancel")) ? _slot5 : {
        default: function _default29() {
          return [_slot5];
        }
      })]) : null]);
    },
    // 简化版筛选按钮对齐，对齐管理侧规范
    renderSimple: function renderSimple() {
      var filterStore = this.filterStore;
      var simpleFilter = filterStore.simpleFilter;
      if (!simpleFilter) {
        return null;
      }
      if (simpleFilter.isDatetime) {
        var _simpleFilter$datetim, _simpleFilter$datetim2, _simpleFilter$datetim3, _simpleFilter$datetim4, _simpleFilter$datetim5, _simpleFilter$datetim6;
        var _slot6, _slot7;
        var DatePickComponents = simpleFilter != null && (_simpleFilter$datetim = simpleFilter.datetimeConfig) != null && _simpleFilter$datetim.component ? hooks.toRaw(simpleFilter.datetimeConfig.component) : null;
        var format = simpleFilter == null ? void 0 : (_simpleFilter$datetim2 = simpleFilter.datetimeConfig) == null ? void 0 : _simpleFilter$datetim2.format;
        var valueFormat = simpleFilter == null ? void 0 : (_simpleFilter$datetim3 = simpleFilter.datetimeConfig) == null ? void 0 : _simpleFilter$datetim3.valueFormat;
        var type = simpleFilter == null ? void 0 : (_simpleFilter$datetim4 = simpleFilter.datetimeConfig) == null ? void 0 : _simpleFilter$datetim4.type;
        var max = simpleFilter == null ? void 0 : (_simpleFilter$datetim5 = simpleFilter.datetimeConfig) == null ? void 0 : _simpleFilter$datetim5.max;
        var min = simpleFilter == null ? void 0 : (_simpleFilter$datetim6 = simpleFilter.datetimeConfig) == null ? void 0 : _simpleFilter$datetim6.min;
        var pickerOptions = {
          disabledDate: function disabledDate(time3) {
            return time3.getTime() < min || time3.getTime() > max;
          }
        };
        return createVNode("div", {
          "class": "tiny-grid__filter-panel filter-panel__enum filter-panel__simple"
        }, [createVNode("div", {
          "class": "tiny-grid__filter-date-title"
        }, [this.i18n("ui.grid.filter.dateTips")]), createVNode("ul", {
          "class": "tiny-grid__filter-date"
        }, [createVNode("li", {
          "class": "tiny-grid__filter-date-item"
        }, [createVNode("span", null, [this.i18n("ui.grid.filter.startDate")]), createVNode(DatePickComponents, {
          "format": format,
          "type": type,
          "value-format": valueFormat,
          "picker-options": pickerOptions,
          "class": "tiny-grid__filter-date-pick",
          "onChange": function onChange(value) {
            if (filterStore.datetimeConfig) {
              filterStore.datetimeConfig.startDate = value;
            }
          },
          "modelValue": filterStore.startDate,
          "onUpdate:modelValue": function onUpdateModelValue($event) {
            return filterStore.startDate = $event;
          }
        }, null)]), createVNode("li", {
          "class": "tiny-grid__filter-date-item"
        }, [createVNode("span", null, [this.i18n("ui.grid.filter.endDate")]), createVNode(DatePickComponents, {
          "format": format,
          "type": type,
          "value-format": valueFormat,
          "picker-options": pickerOptions,
          "class": "tiny-grid__filter-date-pick",
          "modelValue": filterStore.endDate,
          "onUpdate:modelValue": function onUpdateModelValue($event) {
            return filterStore.endDate = $event;
          },
          "onChange": function onChange(value) {
            if (filterStore.datetimeConfig) {
              filterStore.datetimeConfig.endDate = value;
            }
          }
        }, null)])]), createVNode("div", {
          "class": "tiny-grid__filter-option filter-option__btns filter-option__date-button"
        }, [createVNode(Button, {
          "type": "primary",
          "onClick": this.filterDate
        }, _isSlot(_slot6 = this.i18n("ui.base.confirm")) ? _slot6 : {
          default: function _default29() {
            return [_slot6];
          }
        }), createVNode(Button, {
          "onClick": function onClick() {
            filterStore.visible = false;
          }
        }, _isSlot(_slot7 = this.i18n("ui.base.cancel")) ? _slot7 : {
          default: function _default29() {
            return [_slot7];
          }
        })])]);
      } else {
        var _simpleFilter$searchC;
        var _slot8, _slot9;
        var IconSearch = iconSearch();
        var InputComponents = simpleFilter != null && (_simpleFilter$searchC = simpleFilter.searchConfig) != null && _simpleFilter$searchC.component ? hooks.toRaw(simpleFilter.searchConfig.component) : null;
        return createVNode("div", {
          "class": "tiny-grid__filter-panel filter-panel__enum filter-panel__simple"
        }, [InputComponents ? createVNode(InputComponents, {
          "modelValue": filterStore.searchValue,
          "onUpdate:modelValue": function onUpdateModelValue($event) {
            return filterStore.searchValue = $event;
          },
          "onChange": function onChange(value) {
            if (filterStore.searchConfig) {
              filterStore.searchConfig.searchValue = value;
            }
          },
          "class": "tiny-grid__filter-search",
          "clearable": true
        }, {
          suffix: function suffix() {
            return createVNode(IconSearch, null, null);
          }
        }) : null, createVNode("ul", {
          "class": "tiny-grid__filter-options"
        }, [renderEnumableOptions({
          iconRender,
          _vm: this,
          filterStore,
          selectAll: simpleFilter.selectAll,
          searchable: Boolean(InputComponents)
        })]), filterStore.multi ? createVNode("div", {
          "class": "tiny-grid__filter-option filter-option__btns"
        }, [createVNode(Button, {
          "type": "primary",
          "onClick": this.filterEnum
        }, _isSlot(_slot8 = this.i18n("ui.base.confirm")) ? _slot8 : {
          default: function _default29() {
            return [_slot8];
          }
        }), createVNode(Button, {
          "onClick": function onClick() {
            filterStore.visible = false;
          }
        }, _isSlot(_slot9 = this.i18n("ui.base.cancel")) ? _slot9 : {
          default: function _default29() {
            return [_slot9];
          }
        })]) : null]);
      }
    },
    renderSlot: function renderSlot(h2) {
      var $table = this.$parent, filterStore = this.filterStore;
      var args = filterStore.args, column = filterStore.column;
      var slots = column.slots;
      if (slots && slots.filter) {
        return slots.filter.call($table, _extends({
          $table,
          context: this
        }, args), h2);
      }
    },
    // 全部筛选事件
    filterCheckAllEvent: function filterCheckAllEvent(event, value) {
      var filterStore = this.filterStore;
      filterStore.options.forEach(function(item) {
        item.checked = value;
      });
      filterStore.isAllSelected = value;
      filterStore.isIndeterminate = false;
    },
    // 筛选发生改变 check 是否检查默认值的有效性，当自定义relations时，需要检查
    resetInput: function resetInput() {
      this.correctRelations();
      var _this$filterStore = this.filterStore, inputFilter = _this$filterStore.inputFilter, onResetInputFilter = _this$filterStore.onResetInputFilter;
      if (inputFilter) {
        var _this$filterStore$inp = this.filterStore.inputFilter, relations = _this$filterStore$inp.relations, relation = _this$filterStore$inp.relation;
        this.condition.input = "";
        if (typeof onResetInputFilter === "function") {
          onResetInputFilter(this.$refs.filterInputComponent);
        }
        var defaultrelation = "equals";
        var relationValues;
        var relationValue;
        if (Array.isArray(relations) && relations.length) {
          this.condition.method = relations[0].method;
          defaultrelation = relations[0].value || "equals";
          for (var i = 0; i < relations.length; i++) {
            if (relations[i].value === "equals") {
              defaultrelation = "equals";
              break;
            }
          }
          relationValues = relations.map(function(r) {
            return r.value;
          });
          if (relation && ~relationValues.indexOf(relation)) {
            relationValue = relation;
          }
        }
        this.condition.relation = relationValue || defaultrelation;
      }
    },
    // 确认筛选
    confirmFilter: function confirmFilter(type) {
      var column = this.filterStore.column;
      this.condition.type = type;
      if (typeof this.condition.input === "string") {
        var _this$condition$input;
        this.condition.input = (_this$condition$input = this.condition.input) == null ? void 0 : _this$condition$input.trim();
      }
      column.filter.condition = extend(true, {}, this.condition);
      this.$parent.confirmFilterEvent();
    },
    // 对外的暴露自定义提交方法
    commitFilter: function commitFilter() {
      var column = this.filterStore.column;
      column.filter.condition.type = "custom";
      this.$parent.confirmFilterEvent();
    },
    // 重置当前列筛选
    resetFilter: function resetFilter() {
      this.condition.value = null;
      this.condition.input = "";
      this.condition.empty = null;
      this.condition.value = [];
      this.condition.type = null;
      this.resetInput();
      this.confirmFilter();
    },
    // 清除所有列筛选
    clearFilter: function clearFilter() {
      this.$parent.clearFilter(true);
    },
    filterNull: function filterNull(empty) {
      this.condition.value = [];
      this.condition.input = "";
      this.condition.empty = empty;
      this.confirmFilter("empty");
    },
    filterExtends: function filterExtends(item) {
      this.condition.input = "";
      this.condition.empty = null;
      this.condition.value = item.value || item.label;
      this.condition.method = item.method;
      this.confirmFilter("extend");
    },
    filterInput: function filterInput() {
      this.condition.value = [];
      this.condition.empty = null;
      this.confirmFilter("input");
    },
    filterDate: function filterDate() {
      var _this$filterStore2 = this.filterStore, startDate = _this$filterStore2.startDate, endDate = _this$filterStore2.endDate;
      this.condition.input = "";
      this.condition.value = [];
      this.condition.empty = null;
      this.condition.dateList = [startDate, endDate];
      if (startDate && !endDate) {
        this.condition.relation = "greaterThan";
      } else if (!startDate && endDate) {
        this.condition.relation = "greaterThan";
      } else if (startDate && endDate) {
        this.condition.relation = "interveningBetween";
      }
      this.confirmFilter("date");
    },
    filterEnum: function filterEnum() {
      this.condition.input = "";
      this.condition.empty = null;
      this.condition.value = this.filterStore.options.filter(function(item) {
        return item.checked;
      }).map(function(check) {
        return check.value;
      });
      this.confirmFilter("enum");
    },
    close: function close() {
      var filterStore = this.filterStore;
      filterStore.visible = false;
    },
    correctRelations: function correctRelations() {
      if (this.filterStore && typeof this.filterStore.inputFilter === "object") {
        var relations = this.filterStore.inputFilter.relations;
        var relation = this.filterStore.inputFilter.relation || "equals";
        if (!relations || Array.isArray(relations) && relations.length === 0) {
          this.filterStore.inputFilter.relations = [{
            label: relation,
            value: relation
          }];
          this.filterStore.inputFilter.relations_addby = "program";
          this.filterStore.inputFilter.relations_backup = relations;
        }
      }
    }
  },
  setup: function setup5() {
    var instance = hooks.getCurrentInstance().proxy;
    hooks.onBeforeUnmount(function() {
      instance.popperJS && instance.popperJS.destroy() && (instance.popperJS = null);
    });
  }
});
function handleFilterConditionCustom(_ref70) {
  var column = _ref70.column, condition2 = _ref70.condition, method = _ref70.method, property = _ref70.property, row2 = _ref70.row;
  var ret = {
    flag: false,
    result: null
  };
  if (method && condition2.type === "custom") {
    ret.result = method({
      property,
      row: row2,
      column
    });
    ret.flag = true;
  }
  return ret;
}
function handleFilterConditionExtend(_ref71) {
  var column = _ref71.column, condition2 = _ref71.condition, property = _ref71.property, row2 = _ref71.row;
  var ret = {
    flag: false,
    result: null
  };
  if (condition2.type === "extend") {
    var extendMethod = condition2.method;
    if (typeof extendMethod !== "function") {
      extendMethod = function extendMethod2() {
        return true;
      };
    }
    ret.result = extendMethod({
      value: get(row2, property),
      row: row2,
      column
    });
    ret.flag = true;
  }
  return ret;
}
function findRelationMethod(relation, relations) {
  if (Array.isArray(relations)) {
    var method;
    for (var i = 0; i < relations.length; i++) {
      if (relations[i] && relations[i].value === relation) {
        method = relations[i].method;
        break;
      }
    }
    return method;
  }
}
function handleFilterRelations(_ref72) {
  var inputFilter = _ref72.inputFilter;
  var relations = [];
  if (typeof inputFilter === "object" && Array.isArray(inputFilter.relations)) {
    relations = inputFilter.relations;
  }
  return relations;
}
function modifyValueCheckStr(value) {
  if (!value && typeof value !== "number") {
    value = "";
  }
  return value;
}
function handleDefaultCheckStr(_ref73) {
  var column = _ref73.column, input = _ref73.input, relation = _ref73.relation, relationMethod = _ref73.relationMethod, relations = _ref73.relations, result = _ref73.result, row2 = _ref73.row, value = _ref73.value;
  if (typeof relationMethod !== "function") {
    relationMethod = findRelationMethod(relation, relations);
  }
  if (typeof relationMethod === "function") {
    result = relationMethod({
      value,
      input,
      row: row2,
      column
    });
  }
  return {
    relationMethod,
    result
  };
}
function handleFilterCheckStr(_ref74) {
  var column = _ref74.column, relationMethod = _ref74.relationMethod, relations = _ref74.relations, row2 = _ref74.row;
  return function(value, input, relation, dateList) {
    var result = false;
    value = modifyValueCheckStr(value);
    switch (relation) {
      case "equals":
        result = value === input;
        break;
      case "unequal":
        result = value !== input;
        break;
      case "greaterThan":
        result = value > input;
        break;
      case "lessThan":
        result = value < input;
        break;
      case "equalToGreaterThan":
        result = value >= input;
        break;
      case "equalToLessThan":
        result = value <= input;
        break;
      case "interveningBetween":
        result = value >= dateList[0] && value <= dateList[1];
        break;
      case "contains":
        result = value.toString().includes(input);
        break;
      case "startwith":
        result = value.toString().indexOf(input) === 0;
        break;
      case "endwith":
        result = value.toString().match(new RegExp(input + "$"));
        break;
      default: {
        var ret = handleDefaultCheckStr({
          column,
          input,
          relation,
          relationMethod,
          relations,
          result,
          row: row2,
          value
        });
        relationMethod = ret.relationMethod;
        result = ret.result;
      }
    }
    return result;
  };
}
function handleFilterCheck(_ref75) {
  var checkStr = _ref75.checkStr, empty = _ref75.empty, input = _ref75.input, property = _ref75.property, relation = _ref75.relation, row2 = _ref75.row, valueList = _ref75.valueList, dateList = _ref75.dateList;
  return function() {
    var value = get(row2, property);
    if (empty === true) {
      return !value;
    }
    if (empty === false) {
      return !!value;
    }
    if (dateList) {
      if (!dateList[0] && !dateList[1]) {
        return true;
      }
      return checkStr(value, dateList[0] || dateList[1], relation, dateList);
    }
    var checkInput = !input && input !== 0 || checkStr(value, input, relation);
    var checkEnum = !valueList.length || valueList.includes(value);
    return checkInput && checkEnum;
  };
}
function getClassName(elem) {
  if (elem && elem.nodeType) {
    if (elem instanceof SVGElement) {
      return elem.className.baseVal;
    }
    if (elem instanceof HTMLElement) {
      return elem.className;
    }
  }
  return "";
}
function closest(elem, parentClassName) {
  if (elem && elem.nodeType) {
    var clsReg = new RegExp("\\b" + parentClassName + "\\b");
    var matches;
    do {
      matches = getClassName(elem).match(clsReg);
      if (matches) {
        return elem;
      }
      if (elem === document.body) {
        break;
      }
      elem = elem.parentNode;
    } while (elem);
  }
  return null;
}
var columnfilters = function columnfilters2(visibleColumn) {
  var filters = {};
  visibleColumn.forEach(function(column) {
    var property = column.property, filter2 = column.filter;
    if (filter2) {
      var _filter$condition = filter2.condition, type = _filter$condition.type, input = _filter$condition.input, relation = _filter$condition.relation, value = _filter$condition.value, empty = _filter$condition.empty, dateList = _filter$condition.dateList;
      if (type === "input") {
        filters[property] = {
          type,
          value: {
            text: input,
            relation
          }
        };
      } else if (type === "enum") {
        filters[property] = {
          type,
          value
        };
      } else if (type === "empty") {
        filters[property] = {
          type,
          value: empty
        };
      } else if (type === "custom") {
        filters[property] = {
          type
        };
      } else if (type === "date") {
        filters[property] = {
          type,
          value: {
            dateList,
            relation
          }
        };
      } else if (type === "extend") {
        filters[property] = {
          type,
          value
        };
      } else if (type === "extend1") {
        filters[property] = {
          type,
          value
        };
      } else if (type === "extend2") {
        filters[property] = {
          type,
          value
        };
      }
      filter2.hasFilter = value.length || input || (dateList == null ? void 0 : dateList.some(Boolean)) || input === 0 || empty !== null || type === "custom";
    }
  });
  return filters;
};
var Methods$f = {
  filter: function filter(field, callback) {
    var column = this.getColumnByField(field);
    var filters = column.filter.options;
    if (callback) {
      var rest = callback(filters);
      if (isArray(rest)) {
        column.filters = getFilters(rest);
      }
    }
    return Promise.resolve(filters);
  },
  // 关闭筛选
  closeFilter: function closeFilter() {
    var filterStore = this.filterStore;
    Object.assign(filterStore, {
      visible: false,
      targetElem: null,
      targetElemParentTr: null
    });
    return this.$nextTick();
  },
  handleLocalFilter: function handleLocalFilter(row2, column) {
    var property = column.property;
    var _column$filter = column.filter, condition2 = _column$filter.condition, method = _column$filter.method, inputFilter = _column$filter.inputFilter;
    var ret = handleFilterConditionCustom({
      column,
      condition: condition2,
      method,
      property,
      row: row2
    });
    if (ret.flag) {
      return ret.result;
    }
    ret = handleFilterConditionExtend({
      column,
      condition: condition2,
      property,
      row: row2
    });
    if (ret.flag) {
      return ret.result;
    }
    var empty = condition2.empty, input = condition2.input, relation = condition2.relation, value = condition2.value, dateList = condition2.dateList;
    var relationMethod = condition2.method;
    var relations = handleFilterRelations({
      inputFilter
    });
    var checkStr = handleFilterCheckStr({
      column,
      relationMethod,
      relations,
      row: row2
    });
    var check = handleFilterCheck({
      checkStr,
      empty,
      input,
      property,
      relation,
      row: row2,
      valueList: value,
      dateList
    });
    return check();
  },
  getOptions: function getOptions(_ref76) {
    var property = _ref76.property, filter2 = _ref76.filter;
    var values = filter2.values, _filter$value = filter2.value, value = _filter$value === void 0 ? "value" : _filter$value, _filter$label = filter2.label, label = _filter$label === void 0 ? "label" : _filter$label, dataset = filter2.dataset;
    if (typeof values === "function") {
      return values({
        property,
        filter: filter2
      });
    } else if (dataset) {
      return getDataset({
        dataset,
        service: this.$service
      });
    } else {
      var newOptions = (values || []).map(function(item) {
        return {
          label: item[label],
          value: item[value],
          checked: !!item.checked
        };
      });
      if (!values) {
        var optionMap = {};
        var tableData = this.getTableData();
        tableData.fullData.forEach(function(row2) {
          var key = row2[property];
          if (!optionMap[key]) {
            newOptions.push({
              value: key,
              label: key,
              checked: false
            });
          }
          optionMap[key] = true;
        });
      }
      return Promise.resolve(newOptions);
    }
  },
  /**
   * 点击筛选事件
   */
  triggerFilterEvent: function triggerFilterEvent(event, column, params) {
    var filterStore = this.filterStore;
    var filter2 = column.filter;
    var inputFilterRelation;
    if (filterStore.id !== column.id && filterStore.visible) {
      this.closeFilter();
    }
    if (filter2 && typeof filter2.inputFilter === "object" && filter2.inputFilter.relation) {
      if (filter2.condition.relation === "equals" && filter2.inputFilter.relation && !column.isInitTriggerFlag) {
        inputFilterRelation = filter2.inputFilter.relation;
        column.isInitTriggerFlag = true;
      }
      filter2.condition.relation = inputFilterRelation || filter2.condition.relation;
    }
    if (filterStore.column === column && filterStore.visible) {
      filterStore.visible = false;
    } else {
      var targetElem = event.target;
      var targetElemParentTr = closest(targetElem, "tiny-grid-header__row");
      this.getOptions(column).then(function(options) {
        var _filter$simpleFilter, _filter$simpleFilter2, _filter$simpleFilter3, _filter$simpleFilter4, _filter$simpleFilter5, _filter$simpleFilter6, _filter$simpleFilter7, _filter$simpleFilter8;
        Object.assign(filterStore, _extends({
          args: params
        }, filter2, {
          defaultFilter: isBoolean(filter2.defaultFilter) ? filter2.defaultFilter : true,
          options: options.map(function(_ref77) {
            var value = _ref77.value, label = _ref77.label;
            return {
              value,
              label,
              checked: filter2.condition.value.includes(value)
            };
          }),
          condition: _extends({}, filter2.condition),
          multi: isBoolean(filter2.multi) ? filter2.multi : true,
          column,
          visible: true,
          targetElem: hooks.markRaw(targetElem),
          targetElemParentTr: hooks.markRaw(targetElemParentTr),
          id: column.id,
          searchConfig: (_filter$simpleFilter = filter2.simpleFilter) == null ? void 0 : _filter$simpleFilter.searchConfig,
          datetimeConfig: (_filter$simpleFilter2 = filter2.simpleFilter) == null ? void 0 : _filter$simpleFilter2.datetimeConfig,
          searchValue: ((_filter$simpleFilter3 = filter2.simpleFilter) == null ? void 0 : (_filter$simpleFilter4 = _filter$simpleFilter3.searchConfig) == null ? void 0 : _filter$simpleFilter4.searchValue) || "",
          startDate: ((_filter$simpleFilter5 = filter2.simpleFilter) == null ? void 0 : (_filter$simpleFilter6 = _filter$simpleFilter5.datetimeConfig) == null ? void 0 : _filter$simpleFilter6.startDate) || "",
          endDate: ((_filter$simpleFilter7 = filter2.simpleFilter) == null ? void 0 : (_filter$simpleFilter8 = _filter$simpleFilter7.datetimeConfig) == null ? void 0 : _filter$simpleFilter8.endDate) || ""
        }));
      });
    }
  },
  // 确认筛选
  confirmFilterEvent: function confirmFilterEvent() {
    var _this7 = this;
    var visibleColumn = this.visibleColumn, filterStore = this.filterStore, scrollXLoad = this.scrollXLoad, scrollYLoad = this.scrollYLoad, remoteFilter = this.remoteFilter, lastScrollLeft = this.lastScrollLeft;
    filterStore.visible = false;
    if (!remoteFilter) {
      this.handleTableData(true).then(this.refreshStyle);
    }
    var filters = columnfilters(visibleColumn);
    if (remoteFilter) {
      if (this.$grid.pagerConfig) {
        this.$grid.pagerConfig.currentPage = 1;
      }
    }
    emitEvent(this, "filter-change", [{
      filters,
      $table: this
    }]);
    this.updateFooter();
    var restoreScrollLeft = function restoreScrollLeft2() {
      return _this7.scrollTo(lastScrollLeft);
    };
    if (scrollXLoad || scrollYLoad) {
      scrollXLoad && this.clearScroll();
      if (scrollYLoad) {
        this.updateScrollYSpace();
      }
    }
    this.closeFilter();
    this.clearSelection();
    this.$nextTick().then(this.recalculate).then(restoreScrollLeft);
  },
  clearFilter: function clearFilter2(field) {
    var _this$$grid;
    var column = arguments.length ? this.getColumnByField(field) : null;
    var filterStore = this.filterStore;
    var filterData = ((_this$$grid = this.$grid) == null ? void 0 : _this$$grid.filterData) || {};
    var handleClear = function handleClear2(column2) {
      var filter2 = column2.filter;
      if (filter2) {
        filter2.condition = {
          input: "",
          relation: "equals",
          empty: null,
          value: []
        };
        filter2.hasFilter = false;
        filter2.custom = null;
        delete filterData[column2.property];
      }
    };
    if (column) {
      handleClear(column);
    } else {
      this.visibleColumn.forEach(handleClear);
    }
    if (!column || column !== filterStore.column) {
      Object.assign(filterStore, {
        style: null,
        options: [],
        column: null,
        multi: false,
        visible: false
      });
    }
    if (field === true || column) {
      var filters = columnfilters(this.visibleColumn);
      emitEvent(this, "filter-change", [{
        filters,
        $table: this
      }]);
    }
    this.clearSelection();
    return this.updateData();
  },
  getAllFilter: function getAllFilter() {
    return columnfilters(this.visibleColumn);
  }
};
Panel$1.install = function(Table2) {
  Grid$1._filter = 1;
  Object.assign(Table2.methods, Methods$f);
};
var Children = defineComponent({
  name: $prefix + "GridContextMenuChild",
  props: {
    item: Object,
    gIndex: Number,
    index: Number,
    show: Boolean,
    ctxMenuStore: Object,
    tableInstance: Object
  },
  render: function render4() {
    var tableInstance = this.tableInstance, item = this.item, gIndex = this.gIndex, index2 = this.index, show2 = this.show, ctxMenuStore = this.ctxMenuStore;
    var toKebab2 = tableInstance.toKebab, getEventTargetNode2 = tableInstance.getEventTargetNode;
    return h("ul", {
      class: ["tiny-grid-menu__clild-wrapper", {
        show: show2
      }]
    }, item.children.map(function(child, cIndex) {
      return child.visible === false ? [null] : h("li", {
        class: {
          "link__disabled": child.disabled,
          "link__active": child === ctxMenuStore.selectChild
        },
        key: gIndex + "_" + index2 + "_" + cIndex
      }, [h("span", {
        class: "tiny-grid-menu__link " + toKebab2(item.code),
        on: {
          click: function click(event) {
            tableInstance.ctxMenuLinkEvent(event, child);
          },
          mouseover: function mouseover(event) {
            tableInstance.ctxMenuMouseoverEvent(event, item, child);
          },
          mouseout: function mouseout(event) {
            if (!getEventTargetNode2(event, document.body, toKebab2(item.code)).flag) {
              tableInstance.ctxMenuMouseoutEvent(event, item, child);
            }
          }
        }
      }, [child.prefixIcon ? h(child.prefixIcon, {
        class: "tiny-grid-menu__link-prefix"
      }) : [null], h("span", {
        class: "tiny-grid-menu__link-content"
      }, getFuncText(child.name))])]);
    }));
  },
  watch: {
    show: function show(value) {
      var _this8 = this;
      if (value) {
        this.$el.style.top = "0px";
        this.$nextTick(function() {
          var tableInstance = _this8.tableInstance, $el = _this8.$el;
          var _$el$getBoundingClien = $el.getBoundingClientRect(), width = _$el$getBoundingClien.width, height2 = _$el$getBoundingClien.height, bottom = _$el$getBoundingClien.bottom;
          var _getDomNode = getDomNode(), visibleHeight = _getDomNode.visibleHeight, visibleWidth = _getDomNode.visibleWidth;
          var ctxElem = tableInstance.$refs.ctxWrapper.$el;
          var target = tableInstance.ctxMenuStore.target;
          var _ctxElem$getBoundingC = ctxElem.getBoundingClientRect(), ctxWidth = _ctxElem$getBoundingC.width, ctxRight = _ctxElem$getBoundingC.right;
          if (width + ctxRight > visibleWidth - 6) {
            $el.style.left = -6 - width + "px";
          } else {
            $el.style.left = ctxWidth + "px";
          }
          if (bottom > visibleHeight) {
            var offset = target && target.clientHeight || 0;
            $el.style.top = offset - height2 + "px";
          }
        });
      }
    }
  }
});
var onMenuItem = function onMenuItem2(_ref78) {
  var tableInstance = _ref78.tableInstance, item = _ref78.item, getEventTargetNode2 = _ref78.getEventTargetNode, toKebab2 = _ref78.toKebab;
  return {
    click: function click(event) {
      tableInstance.ctxMenuLinkEvent(event, item);
    },
    mouseover: function mouseover(event) {
      tableInstance.ctxMenuMouseoverEvent(event, item);
    },
    mouseout: function mouseout(event) {
      if (!getEventTargetNode2(event, document.body, toKebab2(item.code)).flag) {
        tableInstance.ctxMenuMouseoutEvent(event, item);
      }
    }
  };
};
function createMenuItem(args) {
  var ctxMenuStore = args.ctxMenuStore, gIndex = args.gIndex, getEventTargetNode2 = args.getEventTargetNode, hasChild = args.hasChild;
  var index2 = args.index, item = args.item, show2 = args.show, tableInstance = args.tableInstance, toKebab2 = args.toKebab;
  var res = [null];
  var classMap = {
    linkDisabled: "link__disabled",
    linkActive: "link__active"
  };
  if (item.visible !== false) {
    var _class;
    res = h("li", {
      class: (_class = {}, _class[classMap.linkDisabled] = item.disabled, _class[classMap.linkActive] = item === ctxMenuStore.selected, _class),
      key: gIndex + "_" + index2
    }, [h("a", {
      class: "tiny-grid-menu__link " + toKebab2(item.code),
      on: onMenuItem({
        tableInstance,
        item,
        getEventTargetNode: getEventTargetNode2,
        toKebab: toKebab2
      })
    }, [item.prefixIcon ? h(item.prefixIcon, {
      class: "tiny-grid-menu__link-prefix"
    }) : [null], h("span", {
      class: "tiny-grid-menu__link-content"
    }, getFuncText(item.name)), h("i", {
      class: ["tiny-grid-menu__link-suffix", hasChild ? item.suffixIcon || "suffix__haschild" : item.suffixIcon]
    })]), hasChild ? h(Children, {
      props: {
        item,
        gIndex,
        index: index2,
        show: show2,
        tableInstance,
        ctxMenuStore
      }
    }) : [null]]);
  }
  return res;
}
function getMenuItemCreator(_ref79) {
  var ctxMenuStore = _ref79.ctxMenuStore, getEventTargetNode2 = _ref79.getEventTargetNode, tableInstance = _ref79.tableInstance, toKebab2 = _ref79.toKebab;
  return function(options, gIndex) {
    return h("ul", {
      class: "tiny-grid-menu__option-wrapper",
      key: gIndex
    }, options.map(function(item, index2) {
      var hasChild = item.children && item.children.length;
      var show2 = item === ctxMenuStore.selected && ctxMenuStore.showChild;
      var args = {
        ctxMenuStore,
        gIndex,
        getEventTargetNode: getEventTargetNode2,
        hasChild
      };
      Object.assign(args, {
        index: index2,
        item,
        show: show2,
        tableInstance,
        toKebab: toKebab2
      });
      return createMenuItem(args);
    }));
  };
}
var Panel = defineComponent({
  name: $prefix + "GridContextMenu",
  props: {
    ctxMenuStore: Object
  },
  render: function render5() {
    var ctxMenuStore = this.ctxMenuStore, tableInstance = this.$parent;
    var toKebab2 = tableInstance.toKebab, getEventTargetNode2 = tableInstance.getEventTargetNode;
    var hasChild = Array.isArray(ctxMenuStore.list) && ctxMenuStore.list.flat().some(function(item) {
      return item.visible !== false;
    });
    return h("div", {
      class: ["tiny-grid__wrapper", "tiny-grid-menu__wrapper", {
        show: ctxMenuStore.visible && hasChild
      }],
      style: ctxMenuStore.style
    }, ctxMenuStore.list.map(getMenuItemCreator({
      ctxMenuStore,
      getEventTargetNode: getEventTargetNode2,
      tableInstance,
      toKebab: toKebab2
    })));
  }
});
var getSelectItem = function getSelectItem2(subList) {
  for (var i = 0; i < subList.length; i++) {
    var menu = subList[i];
    if (menu.visible !== false) {
      return menu;
    }
  }
};
var Methods$e = {
  // 关闭快捷菜单
  _closeMenu: function _closeMenu() {
    Object.assign(this.ctxMenuStore, {
      visible: false,
      selected: null,
      selectChild: null,
      showChild: false
    });
    return this.$nextTick();
  },
  // 处理菜单的移动
  moveCtxMenu: function moveCtxMenu(_ref80) {
    var _this9 = this;
    var event = _ref80.event, keyCode = _ref80.keyCode, ctxMenuStore = _ref80.ctxMenuStore, property = _ref80.property, operKey = _ref80.operKey, operRest = _ref80.operRest, menuList = _ref80.menuList;
    var selectIndex = findIndexOf(menuList, function(item) {
      return ctxMenuStore[property] === item;
    });
    var selectItem;
    var ruleChains = [{
      match: function match() {
        return keyCode === operKey;
      },
      action: function action() {
        if (!operRest || !hasChildrenList(ctxMenuStore.selected)) {
          ctxMenuStore.showChild = false;
          ctxMenuStore.selectChild = null;
        } else {
          ctxMenuStore.showChild = true;
        }
      }
    }, {
      match: function match() {
        return keyCode === 38;
      },
      action: function action() {
        var subList = menuList.slice(0, selectIndex);
        subList = subList.reverse();
        selectItem = getSelectItem(subList);
        ctxMenuStore[property] = selectItem || menuList[menuList.length - 1];
      }
    }, {
      match: function match() {
        return keyCode === 40;
      },
      action: function action() {
        var subList = menuList.slice(selectIndex + 1);
        selectItem = getSelectItem(subList);
        ctxMenuStore[property] = selectItem || menuList[0];
      }
    }, {
      match: function match() {
        return ctxMenuStore[property] && (keyCode === 13 || keyCode === 32);
      },
      action: function action() {
        _this9.ctxMenuLinkEvent(event, ctxMenuStore[property]);
      }
    }];
    for (var i = 0; i < ruleChains.length; i++) {
      var ruleChain = ruleChains[i];
      if (ruleChain.match()) {
        ruleChain.action();
        break;
      }
    }
  },
  // 快捷菜单事件处理
  handleGlobalContextmenuEvent: function handleGlobalContextmenuEvent(event) {
    var ctxMenuOpts2 = this.ctxMenuOpts, ctxMenuStore = this.ctxMenuStore, isCtxMenu2 = this.isCtxMenu;
    if (!isCtxMenu2) {
      this.closeMenu();
      this.closeFilter();
      return;
    }
    if (ctxMenuStore.visible && this.$refs.ctxWrapper && this.getEventTargetNode(event, this.$refs.ctxWrapper.$el).flag) {
      event.preventDefault();
      return;
    }
    var eventTargetNode;
    var eventParams = {
      $table: this,
      columns: this.visibleColumn.slice(0)
    };
    for (var _i2 = 0, _arr = ["header", "body", "footer"]; _i2 < _arr.length; _i2++) {
      var layout2 = _arr[_i2];
      eventTargetNode = this.getEventTargetNode(event, this.$el, "tiny-grid-" + layout2 + "__column");
      eventParams.type = layout2;
      if (eventTargetNode.flag) {
        var cell = eventTargetNode.targetElem;
        var column = this.getColumnNode(cell).item;
        var typePrefix = layout2 + "-";
        Object.assign(eventParams, {
          cell,
          column,
          columnIndex: this.getColumnIndex(column)
        });
        if (layout2 === "body") {
          var row2 = this.getRowNode(cell.parentNode).item;
          typePrefix = "";
          Object.assign(eventParams, {
            row: row2,
            rowIndex: this.getRowIndex(row2)
          });
        }
        this.openContextMenu(event, layout2, eventParams);
        emitEvent(this, typePrefix + "cell-context-menu", [eventParams, event]);
        return;
      }
    }
    eventTargetNode = this.getEventTargetNode(event, this.$el, "tiny-grid__body-wrapper");
    if (eventTargetNode.flag) {
      if (ctxMenuOpts2.trigger === "cell") {
        event.preventDefault();
      } else {
        eventParams.type = "body";
        this.openContextMenu(event, layout, eventParams);
      }
      return;
    }
    this.closeMenu();
    this.closeFilter();
  },
  // 显示快捷菜单
  openContextMenu: function openContextMenu(event, type, params) {
    var _this0 = this;
    var ctxMenuOpts2 = this.ctxMenuOpts, ctxMenuStore = this.ctxMenuStore;
    var visibleMethod = ctxMenuOpts2.visibleMethod;
    var config2 = ctxMenuOpts2[type];
    if (!config2) {
      this.closeFilter();
      return;
    }
    var disabled = config2.disabled, options = config2.options;
    if (disabled) {
      event.preventDefault();
      this.closeFilter();
      return;
    }
    if (!options || !options.length) {
      this.closeFilter();
      return;
    }
    params.options = options;
    var end = function end2() {
      if (visibleMethod && !visibleMethod(params, event)) {
        _this0.closeMenu();
        return;
      }
      event.preventDefault();
      var _getDomNode2 = getDomNode(), scrollLeft = _getDomNode2.scrollLeft, scrollTop = _getDomNode2.scrollTop, visibleHeight = _getDomNode2.visibleHeight, visibleWidth = _getDomNode2.visibleWidth;
      var clientX = event.clientX, clientY = event.clientY;
      var left = clientX + scrollLeft;
      var top = clientY + scrollTop;
      var style = {
        left: left + "px",
        top: top + "px"
      };
      Object.assign(ctxMenuStore, {
        args: params,
        list: options,
        selected: null,
        selectChild: null,
        showChild: false,
        style,
        visible: true
      });
      var next = function next2() {
        var ctxElem = _this0.$refs.ctxWrapper.$el;
        var clientWidth = ctxElem.clientWidth, clientHeight = ctxElem.clientHeight;
        var offsetLeft = clientX + clientWidth - visibleWidth;
        var offsetTop = clientY + clientHeight - visibleHeight;
        offsetLeft > -10 && (ctxMenuStore.style.left = left - clientWidth + "px");
        offsetTop > -10 && (ctxMenuStore.style.top = top - clientHeight + "px");
      };
      _this0.$nextTick(next);
    };
    this.preventEvent(event, "event.show_menu", params, null, end);
    this.closeFilter();
  },
  ctxMenuMouseoverEvent: function ctxMenuMouseoverEvent(event, item, child) {
    var ctxMenuStore = this.ctxMenuStore;
    event.preventDefault();
    event.stopPropagation();
    Object.assign(ctxMenuStore, {
      selectChild: child,
      selected: item,
      target: event.target
    });
    !child && (ctxMenuStore.showChild = hasChildrenList(item));
  },
  ctxMenuMouseoutEvent: function ctxMenuMouseoutEvent(event, item) {
    var ctxMenuStore = this.ctxMenuStore;
    !item.children && (ctxMenuStore.selected = null);
    Object.assign(ctxMenuStore, {
      selectChild: null,
      showChild: null,
      target: null
    });
  },
  // 快捷菜单点击事件
  ctxMenuLinkEvent: function ctxMenuLinkEvent(event, menu) {
    if (menu.disabled || menu.children && menu.children.length) {
      return;
    }
    var ctxMenuStore = this.ctxMenuStore;
    var ctxMenuMethod = Menus.get(menu.code);
    var params = _extends({
      $table: this,
      menu
    }, ctxMenuStore.args);
    ctxMenuMethod && ctxMenuMethod.call(this, params, event);
    emitEvent(this, "context-menu-click", [params, event]);
    this.closeMenu();
  },
  toKebab: function toKebab(code) {
    return typeof code === "string" ? code.replace(/\B([A-Z])/g, "-$1").toLowerCase() : "menu-code";
  }
};
Panel.install = function(Table2) {
  Grid$1._menu = 1;
  Object.assign(Table2.methods, Methods$e);
};
var Loading = defineComponent({
  name: $prefix + "GridLoading",
  directives: {
    loading: Loading$1.directive
  },
  props: _extends({}, $props, {
    visible: Boolean,
    loadingComponent: Object
  }),
  render: function render6() {
    var loadingSpinner = h("div", {
      class: "tiny-grid-loading__wrap tiny-loading__spinner",
      directives: isVue2 ? [{
        name: "loading",
        value: true
      }] : null
    });
    if (!isVue2) {
      loadingSpinner = hooks.withDirectives(loadingSpinner, [[Loading$1.directive, true]]);
    }
    var loadingComponent;
    if (this.loadingComponent) {
      loadingComponent = isVnode(this.loadingComponent) ? this.loadingComponent : h(this.loadingComponent);
    }
    return h("div", {
      class: "tiny-grid-loading",
      style: {
        display: this.visible ? "block" : "none"
      }
    }, [loadingComponent || loadingSpinner]);
  }
});
$install(Loading);
function handleActivedCheckCell(_ref81) {
  var actived = _ref81.actived, column = _ref81.column, editConfig = _ref81.editConfig, row2 = _ref81.row;
  return actived.row !== row2 || (editConfig.mode === "cell" ? actived.column !== column : false);
}
function handleActivedCanActive(_ref82) {
  var editConfig = _ref82.editConfig, params = _ref82.params;
  return !(editConfig != null && editConfig.activeMethod) || editConfig.activeMethod(params);
}
function handleActivedDoActive(_ref83) {
  var _vm = _ref83._vm, actived = _ref83.actived, canActive = _ref83.canActive, cell = _ref83.cell, column = _ref83.column, editConfig = _ref83.editConfig, event = _ref83.event, params = _ref83.params, row2 = _ref83.row, tableColumn2 = _ref83.tableColumn, type = _ref83.type;
  if (canActive) {
    if (_vm.keyboardConfig || _vm.mouseConfig) {
      _vm.clearCopyed(event);
      _vm.clearChecked();
      _vm.clearSelected(event);
    }
    _vm.clostTooltip();
    _vm.clearActived(event);
    type = "edit-actived";
    column.renderHeight = cell.offsetHeight;
    actived.args = params;
    actived.row = row2;
    actived.column = column;
    if (editConfig.mode === "row") {
      tableColumn2.forEach(function(column2) {
        return _vm._getColumnModel(row2, column2);
      });
    } else {
      _vm._getColumnModel(row2, column);
    }
    _vm.$nextTick(function() {
      _vm.handleFocus(params, event);
    });
  }
  return type;
}
function handleActivedClearActive(_ref84) {
  var _vm = _ref84._vm, canActive = _ref84.canActive, event = _ref84.event;
  if (!canActive) {
    _vm.clearActived(event);
  }
}
function handleActivedTryActive(_ref85) {
  var _vm = _ref85._vm, actived = _ref85.actived, cell = _ref85.cell, column = _ref85.column, editor = _ref85.editor, event = _ref85.event, isActiveCell = _ref85.isActiveCell, params = _ref85.params, row2 = _ref85.row;
  if (editor && cell && !isActiveCell) {
    var oldColumn = actived.column;
    if (oldColumn !== column) {
      var oldModel = oldColumn.model;
      if (oldModel.update) {
        setCellValue(row2, oldColumn, oldModel.value);
      }
      _vm.clearValidate();
    }
    column.renderHeight = cell.offsetHeight;
    actived.args = params;
    actived.column = column;
    setTimeout(function() {
      _vm.handleFocus(params, event);
    });
  }
}
var insertedField = GlobalConfig$1.constant.insertedField;
var getCellRender = function getCellRender2(isTreeNode, treeCellRender, treeRender, context) {
  return context[isTreeNode ? treeCellRender : treeRender];
};
function processRenderer(_ref86) {
  var h2 = _ref86.h, params = _ref86.params, renderer = _ref86.renderer, value = _ref86.value;
  var result = {
    flag: false,
    vnodes: null
  };
  if (renderer) {
    if (typeof renderer === "function") {
      result.flag = true;
      result.vnodes = [renderer(h2, params)];
    }
    if (renderer.component) {
      var component = renderer.component, attrs = renderer.attrs, events = renderer.events;
      result.flag = true;
      result.vnodes = [h2(hooks.toRaw(component), {
        class: "grid-cell",
        on: events,
        attrs: _extends({
          value,
          modelValue: value
        }, typeof attrs === "function" ? attrs(params) : attrs)
      })];
    }
  }
  return result;
}
function processAsyncColumn(_ref87) {
  var $table = _ref87.$table, column = _ref87.column, format = _ref87.format, h2 = _ref87.h, params = _ref87.params, row2 = _ref87.row;
  var result = {
    flag: false,
    vnodes: null
  };
  var _ref88 = format.async || {}, fetch = _ref88.fetch, loadingText = _ref88.loadingText;
  if ($table.isAsyncColumn && row2[insertedField] !== true && typeof fetch === "function") {
    var cellText = row2[$table.getAsyncColumnName(column.property)];
    var loadingRender = loadingText || "loading ...";
    if (typeof cellText === "undefined") {
      cellText = typeof loadingRender === "function" ? loadingRender(h2, params) : loadingRender;
    }
    if (typeof cellText === "undefined" || typeof column.formatText === "undefined") {
      result.flag = true;
      result.vnodes = [cellText];
    }
  }
  return result;
}
function getColumnRuleTypeIndex(_ref89) {
  var _vm = _ref89._vm, isTreeNode = _ref89.isTreeNode, renMaps = _ref89.renMaps, type = _ref89.type;
  return {
    match: function match() {
      return type === "index";
    },
    action: function action() {
      renMaps.renderHeader = _vm.renderIndexHeader;
      renMaps.renderCell = getCellRender(isTreeNode, "renderTreeIndexCell", "renderIndexCell", _vm);
    }
  };
}
function getColumnRuleTypeRadio(_ref90) {
  var _vm = _ref90._vm, isTreeNode = _ref90.isTreeNode, renMaps = _ref90.renMaps, type = _ref90.type;
  return {
    match: function match() {
      return type === "radio";
    },
    action: function action() {
      renMaps.renderHeader = _vm.renderRadioHeader;
      renMaps.renderCell = getCellRender(isTreeNode, "renderTreeRadioCell", "renderRadioCell", _vm);
    }
  };
}
function getColumnRuleTypeSelection(_ref91) {
  var _vm = _ref91._vm, isTreeNode = _ref91.isTreeNode, renMaps = _ref91.renMaps, selectConfig = _ref91.selectConfig, type = _ref91.type;
  return {
    match: function match() {
      return type === "selection";
    },
    action: function action() {
      renMaps.renderHeader = _vm.renderSelectionHeader;
      renMaps.renderCell = getCellRender(isTreeNode, "renderTreeSelectionCell", "renderSelectionCell", _vm);
      if (selectConfig && selectConfig.checkField) {
        renMaps.renderCell = getCellRender(isTreeNode, "renderTreeSelectionCellByProp", "renderSelectionCellByProp", _vm);
      }
    }
  };
}
function getColumnRuleTypeExpand(_ref92) {
  var _vm = _ref92._vm, renMaps = _ref92.renMaps, type = _ref92.type;
  return {
    match: function match() {
      return type === "expand";
    },
    action: function action() {
      renMaps.renderCell = _vm.renderExpandCell;
      renMaps.renderData = _vm.renderExpandData;
    }
  };
}
function getColumnRuleTypeOperation(_ref93) {
  var _vm = _ref93._vm, renMaps = _ref93.renMaps, type = _ref93.type;
  return {
    match: function match() {
      return type === "operation";
    },
    action: function action() {
      renMaps.renderCell = _vm.renderOperationCell;
    }
  };
}
function getColumnRuleTypeOther(_ref94) {
  var $table = _ref94.$table, _vm = _ref94._vm, colProps = _ref94.colProps, editor = _ref94.editor, filter2 = _ref94.filter, isTreeNode = _ref94.isTreeNode, renMaps = _ref94.renMaps, type = _ref94.type;
  return {
    match: function match() {
      return !["index", "radio", "selection", "expand", "operation"].includes(type);
    },
    action: function action() {
      var sortable = colProps.sortable, remoteSort = colProps.remoteSort;
      var isSortable = $table.sortable && sortable;
      var isSortColumn = isSortable || remoteSort;
      if (editor) {
        renMaps.renderHeader = _vm.renderEditHeader;
        renMaps.renderCell = getCellRender(isTreeNode, "renderTreeRadioCell", "renderRowEdit", _vm);
        if ($table.editConfig && $table.editConfig.mode === "row") {
          renMaps.renderCell = getCellRender(isTreeNode, "renderTreeRowEdit", "renderRowEdit", _vm);
        }
        if ($table.editConfig && $table.editConfig.mode === "cell") {
          renMaps.renderCell = getCellRender(isTreeNode, "renderTreeCellEdit", "renderCellEdit", _vm);
        }
      } else if (filter2) {
        renMaps.renderHeader = _vm.renderFilterHeader;
        if (isSortColumn) {
          renMaps.renderHeader = _vm.renderSortAndFilterHeader;
        }
      } else if (isSortColumn) {
        renMaps.renderHeader = _vm.renderSortHeader;
      }
    }
  };
}
var isCheckStrictly = function isCheckStrictly2(selectConfig) {
  return selectConfig && selectConfig.checkStrictly && !selectConfig.showHeader || selectConfig && !selectConfig.checkStrictly && selectConfig.showHeader === false;
};
var runRender = function runRender2(render17) {
  var vnode;
  try {
    for (var _len = arguments.length, params = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      params[_key - 1] = arguments[_key];
    }
    vnode = render17.apply(void 0, params);
  } catch (e) {
    warn("ui.grid.error.renderParamError");
  } finally {
    if (!vnode || !isVnode(vnode)) {
      try {
        vnode = hooks.h(render17);
      } catch (e) {
        vnode = null;
        warn("ui.grid.error.classComponentError");
      }
    }
  }
  return vnode;
};
var Cell = {
  createColumn: function createColumn($table, colProps) {
    var type = colProps.type, filter2 = colProps.filter, editor = colProps.editor, treeNode = colProps.treeNode;
    var selectConfig = $table.selectConfig, treeConfig = $table.treeConfig;
    var isTreeNode = treeConfig && treeNode;
    var renMaps = {
      renderHeader: this.renderHeader,
      renderCell: getCellRender(isTreeNode, "renderTreeCell", "renderCell", this)
    };
    var _vm = this;
    var ruleChains = [getColumnRuleTypeIndex({
      _vm,
      isTreeNode,
      renMaps,
      type
    }), getColumnRuleTypeRadio({
      _vm,
      isTreeNode,
      renMaps,
      type
    }), getColumnRuleTypeSelection({
      _vm,
      isTreeNode,
      renMaps,
      selectConfig,
      type
    }), getColumnRuleTypeExpand({
      _vm,
      renMaps,
      type
    }), getColumnRuleTypeOperation({
      _vm,
      renMaps,
      type
    }), getColumnRuleTypeOther({
      $table,
      _vm,
      colProps,
      editor,
      filter: filter2,
      isTreeNode,
      renMaps,
      type
    })];
    for (var j = 0; j < ruleChains.length; j++) {
      if (ruleChains[j].match()) {
        ruleChains[j].action();
        break;
      }
    }
    return getColumnConfig(colProps, renMaps, GlobalConfig$1);
  },
  // 单元格
  renderHeader: function renderHeader(h2, params, type) {
    var column = params.column;
    var slots = column.slots, own = column.own, title = column.title;
    if (slots && slots.header) {
      if (type === "card") {
        return [slots.header(params, h2)];
      } else {
        return [h2("div", {
          class: "tiny-grid-cell-text"
        }, [slots.header(params, h2)])];
      }
    }
    if (typeof title === "function") {
      return [h2("div", {
        class: "tiny-grid-cell-text"
      }, [title(h2, params)])];
    }
    if (type === "card") {
      return [formatText(getFuncText(own.title), 1)];
    }
    return [h2("div", {
      class: "tiny-grid-cell-text"
    }, [formatText(getFuncText(own.title), 1)])];
  },
  renderCell: function renderCell(h2, params) {
    var $table = params.$table, row2 = params.row, column = params.column;
    var slots = column.slots, renderer = column.renderer;
    var format = column.format || {};
    $table.columnSlotsWeakMap || ($table.columnSlotsWeakMap = /* @__PURE__ */ new WeakMap());
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    var value = get(row2, column.property);
    var result = processRenderer({
      h: h2,
      params,
      renderer,
      value
    });
    if (result.flag) {
      return result.vnodes;
    }
    result = processAsyncColumn({
      $table,
      column,
      format,
      h: h2,
      params,
      row: row2
    });
    if (result.flag) {
      return result.vnodes;
    }
    var cellValue = getCellLabel(row2, column, params);
    if (typeof cellValue === "function") {
      return [cellValue(h2, params)];
    }
    return [formatText(cellValue)];
  },
  renderTreeCell: function renderTreeCell(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderCell.call(this, h2, params));
  },
  // 树节点
  renderTreeIcon: function renderTreeIcon(h2, params) {
    var _ref96, _$table$$grid, _$table$$grid$designC, _$table$$grid$designC2, _ref97;
    var $table = params.$table, level = params.level, row2 = params.row;
    var treeConfig = $table.treeConfig, treeExpandeds = $table.treeExpandeds;
    var children = treeConfig.children, indent = treeConfig.indent, renderIcon = treeConfig.renderIcon, trigger = treeConfig.trigger, bubbling = treeConfig.bubbling;
    var isActive = ~treeExpandeds.indexOf(row2);
    var rowChildren = row2[children];
    var listeners = {
      click: function click(event) {
        if (!bubbling) {
          event.stopPropagation();
        }
        $table.triggerTreeExpandEvent(event, params);
      }
    };
    var icon = GlobalConfig$1.icon;
    var defaultIcon = function defaultIcon2(h22, _ref95) {
      var active = _ref95.active;
      var IconExpand = iconExpand();
      var IconPutAway = iconPutAway();
      return active ? h22(IconExpand) : h22(IconPutAway);
    };
    var customExpandIcon = (_ref96 = renderIcon || ((_$table$$grid = $table.$grid) == null ? void 0 : (_$table$$grid$designC = _$table$$grid.designConfig) == null ? void 0 : (_$table$$grid$designC2 = _$table$$grid$designC.treeConfig) == null ? void 0 : _$table$$grid$designC2.renderIcon)) != null ? _ref96 : defaultIcon;
    if (trigger && trigger !== "default") {
      listeners = {};
    }
    var iconVNode = [];
    if (rowChildren && rowChildren.length) {
      iconVNode = [customExpandIcon ? customExpandIcon(h2, _extends({
        active: isActive
      }, params)) : h2(iconArrowBottom(), {
        class: ["tiny-grid-tree__node-btn", icon.tree, {
          "is__active": isActive
        }]
      })];
    }
    var map = {
      isActive: "is__active"
    };
    return [h2("span", {
      class: "tiny-grid-tree__indent",
      style: {
        width: level * (indent || 16) + "px"
      }
    }), h2("span", {
      class: ["tiny-grid-tree-wrapper", (_ref97 = {}, _ref97[map.isActive] = isActive, _ref97)],
      on: listeners
    }, iconVNode)];
  },
  // 索引
  renderIndexHeader: function renderIndexHeader(h2, params) {
    var column = params.column;
    var slots = column.slots;
    var own = column.own;
    if (slots && slots.header) {
      return slots.header(params, h2);
    }
    var value = getFuncText(own.title || own.label || "");
    return [formatText(value, 1)];
  },
  renderTreeIndexCell: function renderTreeIndexCell(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderIndexCell(h2, params));
  },
  renderIndexCell: function renderIndexCell(h2, params) {
    var $table = params.$table, column = params.column, row2 = params.row, seq = params.seq; params.level;
    var startIndex = $table.startIndex, treeConfig = $table.treeConfig, treeOrdered = $table.treeOrdered;
    var indexMethod = column.indexMethod, slots = column.slots;
    var _ref98 = treeConfig || {}, _ref98$temporaryIndex = _ref98.temporaryIndex, temporaryIndex = _ref98$temporaryIndex === void 0 ? "_$index_" : _ref98$temporaryIndex;
    var isTreeOrderedFalse = treeConfig && !treeOrdered;
    var indexValue = startIndex + seq;
    if (isTreeOrderedFalse) {
      indexValue = row2[temporaryIndex];
    }
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    return [formatText(indexMethod ? indexMethod(params) : indexValue, 1)];
  },
  // 单选
  renderRadioHeader: function renderRadioHeader(h2, params) {
    var _params$column = params.column, own = _params$column.own, slots = _params$column.slots;
    if (slots && slots.header) {
      return slots.header(params, h2);
    }
    var value = getFuncText(own.title || own.label);
    return [formatText(value, 1)];
  },
  renderRadioCell: function renderRadioCell(h2, params) {
    var _ref99;
    var $table = params.$table, slots = params.column.slots, row2 = params.row;
    var _$table$radioConfig2 = $table.radioConfig, radioConfig = _$table$radioConfig2 === void 0 ? {} : _$table$radioConfig2, selectRow = $table.selectRow, vSize3 = $table.vSize;
    var labelField = radioConfig.labelField, checkMethod = radioConfig.checkMethod;
    var disabled = !!checkMethod;
    var options = {};
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    options.attrs = {
      name: "tiny-grid-radio__" + $table.id,
      type: "radio"
    };
    if (checkMethod) {
      options.attrs.disabled = disabled = !checkMethod(params);
    }
    options.domProps = {
      checked: row2 === selectRow
    };
    options.on = {
      change: function change(event) {
        $table.triggerRadioRowEvent(event, params);
      }
    };
    return [h2("label", {
      class: ["tiny-grid-radio", (_ref99 = {}, _ref99["size__" + vSize3] = vSize3, _ref99["is__disabled"] = disabled, _ref99)]
    }, [h2("input", options), h2("span", {
      class: "tiny-grid-radio__icon"
    }, [h2(iconRadio(), {
      class: ["tiny-svg-size", "icon-radio"]
    }), h2(iconRadioselected(), {
      class: ["tiny-svg-size", "icon-radio-selected"]
    })]), labelField ? h2("span", {
      class: "tiny-grid-radio__label"
    }, get(row2, labelField)) : null])];
  },
  renderTreeRadioCell: function renderTreeRadioCell(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderRadioCell(h2, params));
  },
  renderSelectionHeader: function renderSelectionHeader(h2, params) {
    var _ref100;
    var $table = params.$table, column = params.column;
    var slots = column.slots, own = column.own;
    var headerCheckDisabled = $table.headerCheckDisabled, isAllSelected = $table.isAllSelected, isIndeterminate = $table.isIndeterminate, selectConfig = $table.selectConfig, vSize3 = $table.vSize;
    var headerTitle = own.title || own.label;
    var options = {
      attrs: {
        type: "checkbox"
      }
    };
    if (slots && slots.header) {
      return slots.header(params, h2);
    }
    if (isCheckStrictly(selectConfig)) {
      return [];
    }
    options.domProps = {
      disabled: headerCheckDisabled,
      checked: isAllSelected && !headerCheckDisabled
    };
    options.on = {
      change: function change(event) {
        $table.triggerCheckAllEvent(event, event.target.checked);
        $table.showSelectToolbar();
      }
    };
    var vnode = h2("label", {
      class: ["tiny-grid-checkbox tiny-select-header", (_ref100 = {}, _ref100["size__" + vSize3] = vSize3, _ref100["is__disabled"] = headerCheckDisabled, _ref100["is__indeterminate"] = isIndeterminate, _ref100)],
      key: random()
    }, [h2("input", options), h2("span", {
      class: "tiny-grid-checkbox__icon"
    }, [h2(iconCheck(), {
      class: ["tiny-svg-size", "icon-check"]
    }), isAllSelected ? h2(iconCheckedSur(), {
      class: ["tiny-svg-size", "icon-checked-sur"]
    }) : "", isIndeterminate ? h2(iconHalfselect(), {
      class: ["tiny-svg-size", "icon-half-select"]
    }) : ""]), headerTitle ? h2("span", {
      class: "tiny-grid-checkbox__label"
    }, getFuncText(headerTitle)) : null]);
    var dropdownVnode = null;
    if (slots && slots.dropdown) {
      dropdownVnode = h2("div", {
        class: "selection-dropdown-wrapper"
      }, [slots.dropdown(params, h2)]);
    }
    return [vnode, dropdownVnode];
  },
  renderSelectionCell: function renderSelectionCell(h2, params) {
    var _ref102;
    var $table = params.$table, column = params.column, row2 = params.row;
    var slots = column.slots;
    var _$table$selectConfig2 = $table.selectConfig, selectConfig = _$table$selectConfig2 === void 0 ? {} : _$table$selectConfig2, treeConfig = $table.treeConfig, treeIndeterminates = $table.treeIndeterminates, vSize3 = $table.vSize;
    var labelField = selectConfig.labelField, checkMethod = selectConfig.checkMethod;
    var _ref101 = {}, _ref101$indeterminate = _ref101.indeterminate, indeterminate = _ref101$indeterminate === void 0 ? false : _ref101$indeterminate, _ref101$isDisabled = _ref101.isDisabled, isDisabled = _ref101$isDisabled === void 0 ? !!checkMethod : _ref101$isDisabled;
    var options = {
      attrs: {
        type: "checkbox"
      }
    };
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    checkMethod && (options.attrs.disabled = isDisabled = !checkMethod(params));
    treeConfig && (indeterminate = ~treeIndeterminates.indexOf(row2));
    options.domProps = {
      checked: ~$table.selection.indexOf(row2)
    };
    var twcls = params.twcls;
    var labelCls = ["tiny-grid-checkbox tiny-select-cell", (_ref102 = {}, _ref102["size__" + vSize3] = vSize3, _ref102["is__indeterminate"] = indeterminate, _ref102["is__disabled"] = isDisabled, _ref102)];
    var inputCls = null;
    var spanCls = "tiny-grid-checkbox__icon";
    var svgCls = "tiny-svg-size icon-checked-sur";
    if (twcls) {
      labelCls = twcls.label || labelCls;
      inputCls = twcls.input || inputCls;
      spanCls = twcls.span || spanCls;
      svgCls = twcls.svg || svgCls;
      options.class = inputCls;
    }
    var checkboxEvent = {
      click: function click(event) {
        var _event$target;
        if ((event == null ? void 0 : (_event$target = event.target) == null ? void 0 : _event$target.tagName.toLowerCase()) === "input") {
          return;
        }
        var isChecked = $table.selection.includes(row2);
        setTimeout(function() {
          $table.triggerCheckRowEvent(event, params, !isChecked);
          $table.showSelectToolbar();
        }, 0);
      }
    };
    var vnode = h2("label", {
      class: labelCls,
      key: random(),
      on: checkboxEvent
    }, [h2("input", options), h2("span", {
      class: spanCls
    }, [h2(iconCheck(), {
      class: ["tiny-svg-size", "icon-check"]
    }), h2(iconCheckedSur(), {
      class: svgCls
    }), indeterminate ? h2(iconHalfselect(), {
      class: "tiny-svg-size icon-half-select"
    }) : ""]), labelField ? h2("span", {
      class: "tiny-grid-checkbox__label"
    }, get(row2, labelField)) : null]);
    return [vnode];
  },
  renderTreeSelectionCell: function renderTreeSelectionCell(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderSelectionCell(h2, params));
  },
  // TODO: 与renderSelectionCell代码方法高度相似，待提取公共逻辑。
  renderSelectionCellByProp: function renderSelectionCellByProp(h2, params) {
    var _ref104;
    var $table = params.$table, column = params.column, row2 = params.row;
    var slots = column.slots;
    var _$table$selectConfig3 = $table.selectConfig, selectConfig = _$table$selectConfig3 === void 0 ? {} : _$table$selectConfig3, treeConfig = $table.treeConfig, treeIndeterminates = $table.treeIndeterminates, vSize3 = $table.vSize;
    var property = selectConfig.checkField, checkMethod = selectConfig.checkMethod, labelField = selectConfig.labelField;
    var _ref103 = {}, _ref103$indeterminate = _ref103.indeterminate, indeterminate = _ref103$indeterminate === void 0 ? false : _ref103$indeterminate, _ref103$isDisabled = _ref103.isDisabled, isDisabled = _ref103$isDisabled === void 0 ? !!checkMethod : _ref103$isDisabled;
    var options = {
      attrs: {
        type: "checkbox"
      }
    };
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    checkMethod && (options.attrs.disabled = isDisabled = !checkMethod(params));
    treeConfig && (indeterminate = ~treeIndeterminates.indexOf(row2));
    options.domProps = {
      checked: get(row2, property)
    };
    options.on = {
      change: function change(event) {
        $table.triggerCheckRowEvent(event, params, event.target.checked);
      }
    };
    var vnode = h2("label", {
      key: random(),
      class: ["tiny-grid-checkbox", (_ref104 = {}, _ref104["size__" + vSize3] = vSize3, _ref104["is__indeterminate"] = indeterminate, _ref104["is__disabled"] = isDisabled, _ref104)]
    }, [h2("input", options), h2("span", {
      class: "tiny-grid-checkbox__icon"
    }, [h2(iconCheck(), {
      class: ["tiny-svg-size", "icon-check"]
    }), !indeterminate ? "" : h2(iconHalfselect(), {
      class: ["tiny-svg-size", "icon-half-select"]
    }), h2(iconCheckedSur(), {
      class: ["tiny-svg-size", "icon-checked-sur"]
    })]), labelField ? h2("span", {
      class: "tiny-grid-checkbox__label"
    }, get(row2, labelField)) : null]);
    return [vnode];
  },
  renderTreeSelectionCellByProp: function renderTreeSelectionCellByProp(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderSelectionCellByProp(h2, params));
  },
  // 展开行
  renderExpandCell: function renderExpandCell(h2, params) {
    var _column$slots, _ref105;
    var $table = params.$table, row2 = params.row, column = params.column;
    var _$table$expandConfig3 = $table.expandConfig, expandConfig = _$table$expandConfig3 === void 0 ? {} : _$table$expandConfig3;
    var _expandConfig$showIco = expandConfig.showIcon, showIcon = _expandConfig$showIco === void 0 ? true : _expandConfig$showIco, expandMethod = expandConfig.activeMethod;
    var hideExpand = typeof expandMethod === "function" ? expandMethod(row2) : true;
    var expandActive = $table.expandeds.includes(params.row);
    if (!showIcon) return null;
    var expandTrigger = (_column$slots = column.slots) == null ? void 0 : _column$slots["expand-trigger"];
    var triggerContent = expandTrigger ? expandTrigger(params, h2) : h2("i", {
      class: "tiny-grid__expand-icon"
    });
    var map = {
      expandActive: "expand__active"
    };
    return [h2("span", {
      class: ["tiny-grid__expanded", (_ref105 = {}, _ref105[map.expandActive] = expandActive, _ref105)],
      on: {
        click: function click(event) {
          if (!hideExpand) {
            return;
          }
          $table.triggerRowExpandEvent(event, params);
        }
      }
    }, [hideExpand && triggerContent])];
  },
  renderExpandData: function renderExpandData(h2, params) {
    var slots = params.column.slots;
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    return [];
  },
  // 排序和筛选
  renderSortAndFilterHeader: function renderSortAndFilterHeader(h2, params) {
    var suffixCls = Cell.getSuffixCls(params);
    return Cell.renderHeader(h2, params).concat(Cell.renderFilterIcon(h2, params, suffixCls[0])).concat(Cell.renderSortIcon(h2, params, suffixCls[1]));
  },
  // 排序
  renderSortHeader: function renderSortHeader(h2, params) {
    var suffixCls = Cell.getSuffixCls(params);
    return Cell.renderHeader(h2, params).concat(Cell.renderSortIcon(h2, params, suffixCls[1]));
  },
  renderSortIcon: function renderSortIcon(h2, params, cls) {
    if (cls === void 0) {
      cls = "";
    }
    var icon = GlobalConfig$1.icon;
    var $table = params.$table, column = params.column;
    var isColGroup = column.children && column.children.length;
    return [h2("span", {
      class: "tiny-grid-sort-wrapper " + cls
    }, isColGroup ? [] : [column.order === "desc" || !icon.sortDefault ? h2(icon.sortAsc, {
      class: ["tiny-grid-sort__btn", {
        "sort__active": column.order === (!icon.sortDefault ? "asc" : "desc")
      }],
      on: {
        click: function click(event) {
          $table.triggerSortEvent(event, column, !icon.sortDefault ? "asc" : "");
        }
      }
    }) : "", column.order === "asc" || !icon.sortDefault ? h2(icon.sortDesc, {
      class: ["tiny-grid-sort__btn", {
        "sort__active": column.order === (!icon.sortDefault ? "desc" : "asc")
      }],
      on: {
        click: function click(event) {
          $table.triggerSortEvent(event, column, "desc");
        }
      }
    }) : "", !column.order && icon.sortDefault ? h2(icon.sortDefault, {
      class: ["tiny-grid-sort__btn"],
      on: {
        click: function click(event) {
          $table.triggerSortEvent(event, column, "asc");
        }
      }
    }) : ""])];
  },
  // 筛选
  renderFilterHeader: function renderFilterHeader(h2, params) {
    var suffixCls = Cell.getSuffixCls(params);
    return Cell.renderHeader(h2, params).concat(Cell.renderFilterIcon(h2, params, suffixCls[1]));
  },
  renderFilterIcon: function renderFilterIcon(h2, params, cls) {
    var _ref106, _ref107;
    if (cls === void 0) {
      cls = "";
    }
    var $table = params.$table, column = params.column;
    var filterStore = $table.filterStore;
    var icon = GlobalConfig$1.icon;
    var map = {
      isActive: "is__active",
      hasFilter: "has__Filter"
    };
    return [h2("span", {
      class: ["tiny-grid-filter-wrapper " + cls, (_ref106 = {}, _ref106[map.isActive] = filterStore.visible && filterStore.column === column, _ref106)]
    }, [h2(icon.filter, {
      class: ["tiny-svg-size tiny-grid-filter__btn " + column.id, (_ref107 = {}, _ref107[map.hasFilter] = column.filter && column.filter.hasFilter, _ref107)],
      on: {
        click: function click(event) {
          $table.triggerFilterEvent(event, params.column, params);
        }
      }
    })])];
  },
  // 可编辑
  renderEditHeader: function renderEditHeader(h2, params) {
    var $table = params.$table, column = params.column;
    var editConfig = $table.editConfig, editRules = $table.editRules, validOpts2 = $table.validOpts;
    var filter2 = column.filter, remoteSort = column.remoteSort, sortable = column.sortable, type = column.type, own = column.own;
    var icon = GlobalConfig$1.icon;
    var isRequired;
    sortable = $table.sortable && !type && sortable;
    var suffixCls = Cell.getSuffixCls(params);
    var isRenderSortIcon = sortable || remoteSort;
    var columnRules;
    if (editRules) {
      columnRules = get(editRules, params.column.property);
    }
    if (editRules && columnRules && !Array.isArray(columnRules)) {
      columnRules = [columnRules];
    }
    if (editRules && columnRules) {
      isRequired = columnRules.some(function(rule) {
        return rule.required;
      });
    }
    var showAsterisk = typeof validOpts2.asteriskMethod !== "function" || validOpts2.asteriskMethod(params);
    var vNodes = [isRequired && showAsterisk ? h2("i", {
      class: "tiny-icon " + icon.required
    }) : null, !editConfig || !own.showIcon ? null : h2(icon.edit, {
      class: "tiny-grid-edit-icon tiny-svg-size"
    })];
    vNodes = vNodes.concat(Cell.renderHeader(h2, params));
    vNodes = vNodes.concat(filter2 ? Cell.renderFilterIcon(h2, params, isRenderSortIcon ? suffixCls[0] : suffixCls[1]) : []);
    vNodes = vNodes.concat(isRenderSortIcon ? Cell.renderSortIcon(h2, params, suffixCls[1]) : []);
    return vNodes;
  },
  renderTreeRowEdit: function renderTreeRowEdit(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderRowEdit(h2, params));
  },
  // 行格编辑模式
  renderRowEdit: function renderRowEdit(h2, params) {
    var actived = params.$table.editStore.actived;
    var editConfig = params.$table.editConfig;
    var isActiveCell = function isActiveCell2() {
      return editConfig != null && editConfig.activeStrictly ? handleActivedCanActive({
        editConfig,
        params
      }) : true;
    };
    return Cell.runRenderer(h2, params, this, (actived == null ? void 0 : actived.row) === params.row && isActiveCell());
  },
  renderTreeCellEdit: function renderTreeCellEdit(h2, params) {
    return Cell.renderTreeIcon(h2, params).concat(Cell.renderCellEdit(h2, params));
  },
  // 单元格编辑模式
  renderCellEdit: function renderCellEdit(h2, params) {
    var actived = params.$table.editStore.actived;
    return Cell.runRenderer(h2, params, this, actived && actived.row === params.row && actived.column === params.column);
  },
  runRenderer: function runRenderer(h2, params, _vm, isEdit) {
    var $table = params.$table, column = params.column, row2 = params.row;
    var formatText2 = column.formatText, own = column.own, slots = column.slots;
    var editor = own.editor;
    var compConf = Renderer.get(editor.component);
    var showEdit = $table.editConfig && editor.type === "visible" || isEdit;
    if (showEdit && slots && slots.edit) {
      return slots.edit(params, h2);
    }
    if (showEdit && typeof editor === "function") {
      return [editor(h2, params)];
    }
    if (showEdit && compConf && compConf.renderEdit) {
      return compConf.renderEdit.call($table, h2, editor, params, {
        $type: "edit",
        $excel: $table.$parent,
        $table,
        $column: column
      });
    }
    if (showEdit) {
      return [];
    }
    if (slots && slots.default) {
      return slots.default(params, h2);
    }
    if (formatText2) {
      var cellValue = getCellLabel(row2, column, params);
      if (typeof cellValue === "function") {
        return [cellValue(h2)];
      }
      return [cellValue];
    }
    return Cell.renderCell.call(_vm, h2, params);
  },
  getSuffixCls: function getSuffixCls(params) {
    return params.$table.headerSuffixIconAbsolute ? ["suffix-icon-1", "suffix-icon-0"] : ["", ""];
  },
  renderOperationCell: function renderOperationCell(h2, params) {
    var column = params.column, $table = params.$table, row2 = params.row;
    var _column$operationConf = column.operationConfig, operationConfig = _column$operationConf === void 0 ? {} : _column$operationConf, slots = column.slots;
    var rowId = getRowid($table, row2);
    var operationCell;
    var cached = $table.operationMap.get(rowId);
    if (cached) {
      var cachedCell = cached.cell, cachedRow = cached.row;
      var _$table$visibleColumn = $table.visibleColumn, visibleColumn = _$table$visibleColumn === void 0 ? [] : _$table$visibleColumn;
      var rowUnchanged = visibleColumn.every(function(col) {
        return !col.property || $table.compareRow(row2, cachedRow, col.property);
      });
      if (rowUnchanged) {
        return cachedCell;
      }
    }
    var setOperationCache = function setOperationCache2(cell) {
      var rowSnapshot = _extends({}, row2);
      $table.operationMap.set(rowId, {
        cell,
        row: rowSnapshot
      });
    };
    if (slots && slots.default) {
      operationCell = slots.default(params, h2);
      setOperationCache(operationCell);
      return operationCell;
    }
    var _operationConfig$butt = operationConfig.buttons, buttons = _operationConfig$butt === void 0 ? [] : _operationConfig$butt, render17 = operationConfig.render, _operationConfig$max = operationConfig.max, max = _operationConfig$max === void 0 ? 3 : _operationConfig$max, _operationConfig$disa = operationConfig.disabledClass, disabledClass = _operationConfig$disa === void 0 ? "" : _operationConfig$disa;
    var viewClass = $table.viewCls("operButton");
    if (render17) {
      operationCell = render17({
        h: h2,
        buttons,
        params
      });
      setOperationCache(operationCell);
      return operationCell;
    }
    var renderBase2 = function renderBase3(buttonConfig, flag, classes, attrs) {
      var mergeParams = _extends({
        buttonConfig
      }, params);
      var on2 = {
        click: function click(e) {
          if (!isDisabled(buttonConfig) && isFunction(buttonConfig.click)) {
            buttonConfig.click(e, mergeParams);
          }
        }
      };
      classes = classes.join(" ");
      var clazz = isDisabled(buttonConfig) ? [classes, "tiny-grid__oper-col-button--disabled", disabledClass] : classes;
      var childNodes = isFunction(buttonConfig.icon) ? [runRender(buttonConfig.icon, h2, mergeParams)] : [h2(buttonConfig.icon)];
      return flag ? h2("span", {
        class: clazz,
        attrs,
        on: on2
      }, childNodes) : null;
    };
    var renderBig = function renderBig2(buttonConfig, cls) {
      if (cls === void 0) {
        cls = "";
      }
      var classes = ["tiny-grid__oper-col-button", buttonConfig.class || "", cls];
      var attrs = {
        title: buttonConfig.name || ""
      };
      return renderBase2(buttonConfig, buttonConfig.icon, classes, attrs);
    };
    var isDisabled = function isDisabled2(buttonConfig) {
      var _buttonConfig$disable = buttonConfig.disabled, disabled = _buttonConfig$disable === void 0 ? false : _buttonConfig$disable;
      return typeof disabled === "boolean" && disabled || typeof disabled === "function" && disabled(row2);
    };
    var isHidden = function isHidden2(buttonConfig) {
      var _buttonConfig$hidden = buttonConfig.hidden, hidden = _buttonConfig$hidden === void 0 ? false : _buttonConfig$hidden;
      return typeof hidden === "boolean" && hidden || typeof hidden === "function" && hidden(row2);
    };
    var handleItemClick = function handleItemClick2(itemData) {
      var realName = (itemData == null ? void 0 : itemData.name) || itemData;
      var buttonConfig = visibleButtons.find(function(_ref108) {
        var buttonName = _ref108.name;
        return buttonName === realName;
      });
      buttonConfig.click(window.event || {}, _extends({
        buttonConfig
      }, params));
    };
    var visibleButtons = [];
    buttons.forEach(function(buttonConfig) {
      return !isHidden(buttonConfig) && visibleButtons.push(buttonConfig);
    });
    var groupBig;
    if (visibleButtons.length > max) {
      var end = max - 1;
      var dropdownProps = {
        trigger: "hover",
        showIcon: false
      };
      groupBig = visibleButtons.slice(0, end).map(function(buttonConfig) {
        return renderBig(buttonConfig, viewClass);
      });
      var scopedSlots = {
        default: function _default29() {
          return h2(iconEllipsis(), {
            class: "tiny-grid__oper-col-elps"
          });
        },
        dropdown: function dropdown() {
          return h2(DropdownMenu, {
            slot: "dropdown"
          }, visibleButtons.slice(end).map(function(buttonConfig) {
            var _class2;
            return h2(DropdownItem, {
              class: (_class2 = {}, _class2[disabledClass || ""] = isDisabled(buttonConfig), _class2),
              props: {
                itemData: buttonConfig,
                disabled: isDisabled(buttonConfig)
              }
            }, buttonConfig.name);
          }));
        }
      };
      groupBig.push(h2(Dropdown, {
        on: {
          "item-click": handleItemClick
        },
        props: dropdownProps,
        scopedSlots
      }));
    } else {
      groupBig = visibleButtons.map(function(buttonConfig) {
        return renderBig(buttonConfig, viewClass);
      });
    }
    operationCell = [h2("span", {
      class: "tiny-grid__oper-col-wrapper",
      key: getRowid($table, row2),
      attrs: {
        "data-tag": "operation-cell-buttons"
      }
    }, groupBig)];
    setOperationCache(operationCell);
    return operationCell;
  }
};
function handleLayout(_vm) {
  var tableFullData = _vm.tableFullData, height2 = _vm.height, parentHeight2 = _vm.parentHeight, scrollXLoad = _vm.scrollXLoad; _vm.scrollYLoad; var tableColumn2 = _vm.tableColumn;
  var maxHeight = _vm.maxHeight, minHeight = _vm.minHeight, totalWidth = _vm.totalWidth;
  var customHeight, scaleToPx;
  if (height2 === "auto") {
    customHeight = parentHeight2;
  } else {
    scaleToPx = Math.floor(parseInt(height2) / 100 * parentHeight2);
    customHeight = isScale(height2) ? scaleToPx : toNumber$1(height2);
  }
  if (customHeight > 0) {
    if ((tableFullData == null ? void 0 : tableFullData.length) <= 0) {
      if (customHeight < GlobalConfig$1.emptyMinHeight) {
        _vm.bodyWrapperHeight = GlobalConfig$1.emptyMinHeight;
      } else {
        _vm.bodyWrapperHeight = customHeight;
      }
    } else {
      _vm.bodyWrapperHeight = customHeight;
    }
  }
  if (maxHeight) {
    maxHeight = isScale(maxHeight) ? Math.floor(parseInt(maxHeight) / 100 * parentHeight2) : toNumber$1(maxHeight);
    _vm.bodyWrapperMaxHeight = maxHeight;
  }
  if (minHeight) {
    minHeight = isScale(minHeight) ? Math.floor(parseInt(minHeight) / 100 * parentHeight2) : toNumber$1(minHeight);
    if (maxHeight && minHeight > maxHeight) {
      minHeight = maxHeight;
    }
    _vm.bodyWrapperMinHeight = minHeight;
  }
  _vm.bodyTableWidth = scrollXLoad ? tableColumn2.reduce(function(previous, column) {
    return previous + column.renderWidth;
  }, 0) : totalWidth;
}
function isTargetRadioOrCheckbox(event, column, colType, targetType) {
  var target = event.target;
  return target && column.type === colType && target.tagName.toLowerCase() === "input" && target.type === (targetType || colType);
}
function onClickExpandColumn(_ref109) {
  var $el = _ref109.$el, _vm = _ref109._vm, column = _ref109.column, event = _ref109.event, expandConfig = _ref109.expandConfig, params = _ref109.params;
  if ((expandConfig.trigger === "row" || column.type === "expand" && expandConfig.trigger === "cell") && !_vm.getEventTargetNode(event, $el, "tiny-grid__expanded").flag) {
    _vm.triggerRowExpandEvent(event, params);
  }
}
function onClickTreeNodeColumn(_ref110) {
  var _vm = _ref110._vm, column = _ref110.column, event = _ref110.event, params = _ref110.params, treeConfig = _ref110.treeConfig;
  if (treeConfig.trigger === "row" || column.treeNode && treeConfig.trigger === "cell") {
    _vm.triggerTreeExpandEvent(event, params);
  }
}
function onHighlightCurrentRow(_ref111) {
  var $el = _ref111.$el, _vm = _ref111._vm, event = _ref111.event, highlightCurrentRow = _ref111.highlightCurrentRow, params = _ref111.params, radioConfig = _ref111.radioConfig;
  if (highlightCurrentRow) {
    if (radioConfig.trigger === "row" || !_vm.getEventTargetNode(event, $el, "tiny-grid-checkbox").flag && !_vm.getEventTargetNode(event, $el, "tiny-grid-radio").flag) {
      _vm.triggerCurrentRowEvent(event, params);
    }
  }
}
function onClickRadioColumn(_ref112) {
  var $el = _ref112.$el, _vm = _ref112._vm, column = _ref112.column, event = _ref112.event, params = _ref112.params, radioConfig = _ref112.radioConfig;
  if ((radioConfig.trigger === "row" || column.type === "radio" && radioConfig.trigger === "cell") && !_vm.getEventTargetNode(event, $el, "tiny-grid-radio").flag) {
    _vm.triggerRadioRowEvent(event, params);
  }
}
function onClickSelectColumn(_ref113) {
  var _vm = _ref113._vm, column = _ref113.column, event = _ref113.event, params = _ref113.params, selectConfig = _ref113.selectConfig;
  if ((selectConfig.trigger === "row" || column.type === "selection" && selectConfig.trigger === "cell") && !_vm.getEventTargetNode(event, params.cell, "tiny-grid-checkbox").flag) {
    _vm.handleToggleCheckRowEvent(params, event);
  }
}
function onClickCellSelect(_ref114) {
  var _vm = _ref114._vm, actived = _ref114.actived, cell = _ref114.cell, column = _ref114.column, editConfig = _ref114.editConfig, event = _ref114.event, mouseConfig = _ref114.mouseConfig, params = _ref114.params, row2 = _ref114.row;
  if (!mouseConfig.checked && editConfig) {
    if (editConfig.trigger === "manual") {
      if (actived.args && actived.row === row2 && column !== actived.column) {
        _vm.handleChangeCell(event, params);
      }
    } else if (!actived.args || cell !== actived.args.cell) {
      if (editConfig.trigger === "click") {
        _vm.handleChangeCell(event, params);
      } else if (editConfig.trigger === "dblclick") {
        if (editConfig.mode === "row" && actived.row === params.row) {
          _vm.handleChangeCell(event, params);
        } else {
          _vm.handleSelected(params, event);
        }
      }
    }
  }
}
function onGroupHeader(_ref115) {
  var _vm = _ref115._vm, isGroup2 = _ref115.isGroup, headerProps2 = _ref115.headerProps;
  if (isGroup2) {
    eachTree(_vm.collectColumn, function(column) {
      if (column.children && column.children.length) {
        column.visible = !!findTree$1(column.children, function(subColumn) {
          return subColumn.children && subColumn.children.length ? 0 : subColumn.visible;
        }, headerProps2);
      }
    }, headerProps2);
  }
}
function reassignNotFixed(_ref116) {
  var centerList = _ref116.centerList, column = _ref116.column;
  if (!column.fixed) {
    centerList.push(column);
  }
}
function reassignFixedRight(_ref117) {
  var column = _ref117.column, columnIndex = _ref117.columnIndex, isColspan = _ref117.isColspan, rightEndIndex = _ref117.rightEndIndex, rightList = _ref117.rightList;
  if (column.fixed === "right") {
    if (!isColspan) {
      if (rightEndIndex === null) {
        rightEndIndex = columnIndex;
      }
      if (columnIndex - rightEndIndex !== 0) {
        isColspan = true;
      } else {
        rightEndIndex++;
      }
    }
    rightList.push(column);
  }
  return {
    isColspan,
    rightEndIndex
  };
}
function reassignFixedLeft(_ref118) {
  var column = _ref118.column, columnIndex = _ref118.columnIndex, isColspan = _ref118.isColspan, leftList = _ref118.leftList, leftStartIndex = _ref118.leftStartIndex, letIndex = _ref118.letIndex;
  if (column.fixed === "left") {
    if (leftStartIndex === null) {
      leftStartIndex = letIndex;
    }
    if (!isColspan) {
      if (columnIndex - letIndex !== 0) {
        isColspan = true;
      } else {
        letIndex++;
      }
    }
    leftList.push(column);
  }
  return {
    leftStartIndex,
    letIndex,
    isColspan
  };
}
function showGroupFixedError(_ref119) {
  var isColspan = _ref119.isColspan, isGroup2 = _ref119.isGroup, leftStartIndex = _ref119.leftStartIndex, rightEndIndex = _ref119.rightEndIndex, visibleColumn = _ref119.visibleColumn;
  if (isGroup2 && (isColspan || leftStartIndex || rightEndIndex !== null && rightEndIndex !== visibleColumn.length)) {
    error("ui.grid.error.groupFixed");
  }
}
function onScrollXLoad(_ref120) {
  var _vm = _ref120._vm, scrollX = _ref120.scrollX, scrollXLoad = _ref120.scrollXLoad, scrollXStore = _ref120.scrollXStore, tableColumn2 = _ref120.tableColumn, visibleColumn = _ref120.visibleColumn;
  if (scrollXLoad) {
    if (_vm.resizable || visibleColumn.some(function(column) {
      return column.resizable;
    })) {
      warn("ui.grid.error.notResizable");
    }
    var renderSize = toNumber$1(scrollX.rSize || scrollXStore.renderSize);
    var offsetSize = toNumber$1(scrollX.oSize || scrollXStore.offsetSize);
    Object.assign(scrollXStore, {
      startIndex: 0,
      visibleIndex: 0,
      renderSize,
      offsetSize
    });
    tableColumn2 = visibleColumn.slice(scrollXStore.startIndex, scrollXStore.startIndex + scrollXStore.renderSize);
  }
  return tableColumn2;
}
function mapFetchColumnPromise(_ref121) {
  var _vm = _ref121._vm, fetchColumns = _ref121.fetchColumns, tableColumn2 = _ref121.tableColumn;
  return fetchColumns.map(function(_ref122) {
    var format = _ref122.format, columnValues = _ref122.columnValues;
    return format.async.fetch({
      columns: tableColumn2,
      columnValues,
      $table: _vm
    });
  });
}
function preprocessDataObjectFormat(_ref123) {
  var columnCount = _ref123.columnCount, columnValues = _ref123.columnValues, columnValuesMap = _ref123.columnValuesMap, fields = _ref123.fields;
  if (columnCount) {
    columnValues.forEach(function(col) {
      if (typeof col === "object") {
        var label = get(col, fields.text || "label");
        var value = get(col, fields.value || "value");
        col.label = label;
        columnValuesMap[value] = col;
      }
    });
  }
}
function preventDupRender(_ref124) {
  var asyncColumnName = _ref124.asyncColumnName, cellTexts = _ref124.cellTexts, cellValuesCount = _ref124.cellValuesCount, columnData = _ref124.columnData, columnValuesMap = _ref124.columnValuesMap, isRender = _ref124.isRender, property = _ref124.property, renderCount = _ref124.renderCount, row2 = _ref124.row, splitConfig = _ref124.splitConfig;
  var cellEachIndex = 0;
  if (!isRender && cellValuesCount) {
    var cellLabel;
    var cellValues = [row2[property]];
    if (splitConfig.enabled === true) {
      cellValues = (row2[property] || "").split(splitConfig.valueSplit || ",");
    }
    while (cellEachIndex < cellValuesCount) {
      var activeValue = cellValues[cellEachIndex];
      var currentRow = columnValuesMap[activeValue];
      cellLabel = typeof currentRow === "object" ? currentRow.label : currentRow;
      cellTexts.push(cellLabel);
      columnData.push({
        label: cellLabel,
        value: cellValues[cellEachIndex],
        row: currentRow
      });
      cellEachIndex++;
      renderCount++;
    }
    row2[asyncColumnName] = cellTexts.join(splitConfig.textSplit || ",");
  }
  return renderCount;
}
function handleResolveColumnComplete(_ref125) {
  var _vm = _ref125._vm, columnData = _ref125.columnData, complete = _ref125.complete;
  if (typeof complete === "function") {
    complete({
      columnData,
      $table: _vm
    });
  }
}
var isWebkit = browserInfo["-webkit"];
function computeScrollYLoad(_ref126) {
  var _vm = _ref126._vm, scrollLoad = _ref126.scrollLoad, scrollY = _ref126.scrollY, scrollYLoad = _ref126.scrollYLoad, scrollYStore = _ref126.scrollYStore, tableBodyElem = _ref126.tableBodyElem;
  if (scrollYLoad || scrollLoad) {
    scrollYStore.rowHeight = _vm.rowHeight;
  }
  if (scrollYLoad) {
    var _tableBodyElem$style, _tableBodyElem$style2;
    var bodyHeight = toNumber$1(((_tableBodyElem$style = tableBodyElem.style) == null ? void 0 : _tableBodyElem$style.height) || ((_tableBodyElem$style2 = tableBodyElem.style) == null ? void 0 : _tableBodyElem$style2.maxHeight) || tableBodyElem.clientHeight);
    var visibleYSize = toNumber$1(scrollY.vSize || Math.ceil(bodyHeight / scrollYStore.rowHeight));
    scrollYStore.visibleSize = visibleYSize;
    if (!scrollY.oSize) {
      scrollYStore.offsetSize = visibleYSize;
    }
    if (!scrollY.rSize) {
      scrollYStore.renderSize = visibleYSize + (isWebkit ? 2 : visibleYSize);
    }
    _vm.updateScrollYData();
  } else {
    _vm.updateScrollYSpace();
  }
}
function computeScrollXLoad(_ref127) {
  var _vm = _ref127._vm, scrollX = _ref127.scrollX, scrollXLoad = _ref127.scrollXLoad, scrollXStore = _ref127.scrollXStore, tableBodyElem = _ref127.tableBodyElem, visibleColumn = _ref127.visibleColumn;
  if (scrollXLoad) {
    var clientWidth = tableBodyElem.clientWidth;
    var width = 0;
    var visibleXSize = 0;
    var len = visibleColumn.length;
    var colsWidth = (visibleColumn == null ? void 0 : visibleColumn.map(function(i2) {
      return i2.renderWidth;
    }).sort(function(a, b) {
      return a - b;
    })) || [];
    for (var i = 0; i < len; i++) {
      width += colsWidth[i];
      if (width > clientWidth || i === len - 1) {
        visibleXSize = i + 1;
        break;
      }
    }
    visibleXSize = toNumber$1(scrollX.vSize || visibleXSize);
    scrollXStore.visibleSize = visibleXSize;
    if (!scrollX.oSize) {
      scrollXStore.offsetSize = visibleXSize;
    }
    if (!scrollX.rSize) {
      scrollXStore.renderSize = visibleXSize + 2;
    }
    _vm.updateScrollXData();
  }
}
var calcTableWidth = function calcTableWidth2($table) {
  var _$table$$grid2, _$table$$grid2$design;
  var minCellWidth = ((_$table$$grid2 = $table.$grid) == null ? void 0 : (_$table$$grid2$design = _$table$$grid2.designConfig) == null ? void 0 : _$table$$grid2$design.minWidth) || 72;
  var columnStore = $table.columnStore, fit = $table.fit, elemStore = $table.elemStore;
  var bodyWrapper = elemStore["main-body-wrapper"];
  var bdoyClientWidth = bodyWrapper.clientWidth, offsetWidth = bodyWrapper.offsetWidth, offsetHeight = bodyWrapper.offsetHeight;
  var clientWidth = bdoyClientWidth - 1;
  var bodyWidth = clientWidth;
  var pxArr = columnStore.pxList, scaleArr = columnStore.scaleList, pxMinArr = columnStore.pxMinList, scaleMinArr = columnStore.scaleMinList, autoArr = columnStore.autoList, resizeArr = columnStore.resizeList;
  var remainWidth = clientWidth;
  var totalWidth = 0;
  var minArr = new Array(pxMinArr.length + scaleMinArr.length);
  pxMinArr.forEach(function(column, index2) {
    var width = parseInt(column.minWidth);
    totalWidth += width;
    minArr[index2 + scaleMinArr.length] = width;
  });
  var meanWidth = remainWidth / 100;
  scaleMinArr.forEach(function(column, index2) {
    var width = Math.floor(parseInt(column.minWidth) * meanWidth);
    totalWidth += width;
    minArr[index2] = width;
  });
  scaleArr.forEach(function(column) {
    var width = Math.floor(parseInt(column.width) * meanWidth);
    totalWidth += width;
    column.renderWidth = width;
  });
  pxArr.forEach(function(column) {
    var width = parseInt(column.width);
    totalWidth += width;
    column.renderWidth = width;
  });
  resizeArr.forEach(function(column) {
    var width = parseInt(column.resizeWidth);
    totalWidth += width;
    column.renderWidth = width;
  });
  remainWidth -= totalWidth;
  meanWidth = remainWidth > 0 ? Math.floor(remainWidth / (scaleMinArr.length + pxMinArr.length + autoArr.length)) : 0;
  if (fit) {
    if (remainWidth > 0) {
      scaleMinArr.concat(pxMinArr).forEach(function(column, index2) {
        totalWidth += meanWidth;
        minArr[index2] += meanWidth;
      });
    }
  } else {
    meanWidth = minCellWidth;
  }
  autoArr.forEach(function(column, index2) {
    var width = Math.max(meanWidth, minCellWidth);
    var renderWidth = width;
    totalWidth += width;
    if (fit && index2 === autoArr.length - 1) {
      var odiffer = clientWidth - totalWidth;
      if (odiffer > 0) {
        renderWidth += odiffer;
        totalWidth = clientWidth;
      }
    }
    column.renderWidth = renderWidth;
  });
  var remainingSpace = bodyWidth - totalWidth;
  if (fit && remainingSpace > 0) {
    scaleMinArr.concat(pxMinArr).slice(0, remainingSpace).forEach(function(column, index2) {
      totalWidth += 1;
      minArr[index2] += 1;
    });
  }
  scaleMinArr.concat(pxMinArr).forEach(function(column, index2) {
    column.renderWidth = minArr[index2];
  });
  return {
    totalWidth,
    offsetWidth,
    offsetHeight
  };
};
function calcFixedDetails(_vm) {
  var collectColumn2 = _vm.collectColumn, visibleColumn = _vm.visibleColumn, columnStore = _vm.columnStore, isGroup2 = _vm.isGroup;
  var leftList = columnStore.leftList, rightList = columnStore.rightList;
  visibleColumn.forEach(function(_ref128) {
    var fixedDetails = _ref128.fixedDetails;
    if (fixedDetails) {
      fixedDetails.isLeftLast = false;
      fixedDetails.isRightFirst = false;
      fixedDetails.left = 0;
      fixedDetails.right = 0;
    }
  });
  var length = leftList.length;
  var value = 0;
  if (Array.isArray(leftList) && length > 0) {
    leftList.forEach(function(_ref129, i) {
      var fixedDetails = _ref129.fixedDetails, renderWidth = _ref129.renderWidth;
      fixedDetails.isLeftLast = i === length - 1;
      fixedDetails.left = value;
      value += renderWidth;
    });
  }
  length = rightList.length;
  value = 0;
  if (Array.isArray(rightList) && length > 0) {
    var reversed = [].concat(rightList).reverse();
    reversed.forEach(function(_ref130, i) {
      var fixedDetails = _ref130.fixedDetails, renderWidth = _ref130.renderWidth;
      fixedDetails.isRightFirst = i === length - 1;
      fixedDetails.right = value;
      value += renderWidth;
    });
  }
  if (isGroup2) {
    var header = calcHeader(collectColumn2);
    var leftWidth = [];
    var rightWidth = [];
    var leftIndices = leftList.map(function(col) {
      return leftWidth.push(col.renderWidth), header.leafColumns.indexOf(col);
    });
    var rightIndices = rightList.map(function(col) {
      return rightWidth.push(col.renderWidth), header.leafColumns.indexOf(col);
    });
    var leftTable = [];
    var rightTable = [];
    header.headerTable.forEach(function(levelCols, level) {
      if (level < header.maxLevel) {
        leftTable.push(leftIndices.map(function(i) {
          return levelCols[i];
        }));
        rightTable.push(rightIndices.map(function(i) {
          return levelCols[i];
        }));
      }
    });
    leftTable.forEach(function(cols) {
      var row2 = cols.reduce(function(p, c) {
        return c && !p.includes(c) ? p.push(c) : null, p;
      }, []);
      if (row2.length > 0) {
        row2[row2.length - 1].fixedDetails.isLeftLast = true;
        row2.forEach(function(c) {
          var s = cols.indexOf(c);
          var e = cols.lastIndexOf(c);
          c.fixedDetails.left = leftWidth.slice(0, s).reduce(function(t2, c2) {
            return t2 += c2;
          }, 0);
          c.renderWidth = leftWidth.slice(s, e + 1).reduce(function(t2, c2) {
            return t2 += c2;
          }, 0);
        });
      }
    });
    rightTable.forEach(function(cols) {
      var row2 = cols.reduce(function(p, c) {
        return c && !p.includes(c) ? p.push(c) : null, p;
      }, []).reverse();
      if (row2.length > 0) {
        row2[row2.length - 1].fixedDetails.isRightFirst = true;
        var right = 0;
        row2.forEach(function(c) {
          var s = cols.indexOf(c);
          var e = cols.lastIndexOf(c);
          c.renderWidth = rightWidth.slice(s, e + 1).reduce(function(t2, c2) {
            return t2 += c2;
          }, 0);
          c.fixedDetails.right = right;
          right += c.renderWidth;
        });
      }
    });
  }
}
var funcs = ["closeMenu", "getMouseSelecteds", "getMouseCheckeds", "clearCopyed", "clearChecked", "clearHeaderChecked", "clearIndexChecked", "clearSelected", "insert", "insertAt", "remove", "removeSelecteds", "revert", "revertData", "getRecordset", "getInsertRecords", "getRemoveRecords", "getUpdateRecords", "clearActived", "getActiveRow", "hasActiveRow", "setActiveRow", "setActiveCell", "setSelectCell", "clearValidate", "fullValidate", "validate", "exportCsv", "exportExcel"];
var headerProps = {
  children: "children"
};
var handleAllColumnPromises = function handleAllColumnPromises2(opt, ctx) {
  var startIndex = opt.startIndex, fetchColumns = opt.fetchColumns, tableData = opt.tableData, asyncRenderMap = opt.asyncRenderMap, isScrollLoad = opt.isScrollLoad;
  return function(data7) {
    if (data7.length) {
      ctx._isUpdateData = true;
      data7.forEach(function(item, i) {
        var columnValues = [];
        var columnValuesMap = {};
        var k = startIndex;
        var renderCount = 0;
        var columnCount = 0;
        var columnData = [];
        var _fetchColumns$i = fetchColumns[i], _fetchColumns$i$forma = _fetchColumns$i.format, format = _fetchColumns$i$forma === void 0 ? {} : _fetchColumns$i$forma, property = _fetchColumns$i.property;
        var _ref131 = format.async || {}, _ref131$splitConfig = _ref131.splitConfig, splitConfig = _ref131$splitConfig === void 0 ? {} : _ref131$splitConfig, _ref131$fields = _ref131.fields, fields = _ref131$fields === void 0 ? {} : _ref131$fields, complete = _ref131.complete;
        columnValues = isArray(item) ? item : get(item, fields.data || "values");
        columnCount = columnValues.length;
        preprocessDataObjectFormat({
          columnCount,
          columnValues,
          columnValuesMap,
          fields
        });
        for (var len = tableData.length; k < len; k++) {
          var row2 = tableData[k];
          var cellTexts = [];
          var uniqueKey = ctx.getAsyncColumnUniqueKey(property, row2);
          var cellValuesCount = asyncRenderMap[uniqueKey];
          var asyncColumnName = ctx.getAsyncColumnName(property);
          var isRender = !!row2[asyncColumnName];
          var args = {
            asyncColumnName,
            cellTexts,
            cellValuesCount,
            columnData
          };
          Object.assign(args, {
            columnValuesMap,
            isRender,
            property,
            renderCount,
            row: row2,
            splitConfig
          });
          renderCount = preventDupRender(args);
          if (isScrollLoad && renderCount >= columnCount) {
            break;
          }
        }
        format.data = columnData;
        handleResolveColumnComplete({
          _vm: ctx,
          columnData,
          complete
        });
      });
      ctx.tableData = ctx.tableData.slice(0);
      ctx.$nextTick(function() {
        ctx._isUpdateData = false;
      });
    } else {
      warn("Unknown error：the query data is empty.");
    }
  };
};
function handleGlobalMousedownOnFilterWrapper(_ref132) {
  var $el = _ref132.$el, _vm = _ref132._vm, event = _ref132.event, filterStore = _ref132.filterStore, filterWrapper = _ref132.filterWrapper;
  if (filterWrapper) {
    if (_vm.getEventTargetNode(event, $el, "tiny-grid-filter-wrapper").flag) ;
    else if (_vm.getEventTargetNode(event, filterWrapper.$el).flag) ;
    else if (_vm.getEventTargetNode(event, document.body, "tiny-popper").flag) ;
    else {
      _vm.closeFilter();
      _vm.preventEvent(event, "event.clear_filter", filterStore.args, _vm.closeFilter);
    }
  }
}
function handleGlobalMousedownOnCtxMenu(_ref133) {
  var _vm = _ref133._vm, ctxMenuStore = _ref133.ctxMenuStore, event = _ref133.event;
  if (ctxMenuStore.visible && _vm.$refs.ctxWrapper && !_vm.getEventTargetNode(event, _vm.$refs.ctxWrapper.$el).flag) {
    _vm.closeMenu();
  }
}
function handleGlobalBlurOutside(_ref134) {
  var _vm = _ref134._vm, actived = _ref134.actived, event = _ref134.event;
  var custblur = _vm.blurOutside(actived, event);
  if (typeof custblur === "boolean") {
    custblur || setTimeout(function() {
      return _vm.clearActived(event);
    });
    return true;
  }
  return false;
}
function handleGlobalIsClear(_ref135) {
  var $el = _ref135.$el, _vm = _ref135._vm, actived = _ref135.actived, editConfig = _ref135.editConfig, event = _ref135.event, isClear = _ref135.isClear, isReadonlyCol = _ref135.isReadonlyCol;
  if (editConfig.mode === "row") {
    var rowNode = _vm.getEventTargetNode(event, $el, "tiny-grid-body__row");
    var isOtherRow = rowNode.flag ? rowNode.targetElem !== actived.args.cell.parentNode : 0;
    if (editConfig.trigger === "manual") {
      isClear = !_vm.getEventTargetNode(event, $el, "row__actived").flag && isOtherRow;
    } else {
      isClear = isOtherRow && isReadonlyCol;
    }
  } else {
    isClear = isReadonlyCol;
  }
  return isClear;
}
function handleGlobalClearActived(_ref136) {
  var _vm$$refs$tableBody;
  var $el = _ref136.$el, _vm = _ref136._vm, event = _ref136.event, isClear = _ref136.isClear;
  var tableContent = (_vm$$refs$tableBody = _vm.$refs.tableBody) == null ? void 0 : _vm$$refs$tableBody.$refs.table;
  var actualTarget = getActualTarget(event);
  if (isClear || !_vm.getEventTargetNode(event, $el).flag || _vm.$refs.tableHeader && _vm.$refs.tableHeader.$el.contains(actualTarget) || tableContent && !tableContent.contains(actualTarget)) {
    setTimeout(function() {
      return _vm.clearActived(event);
    });
  }
}
function checkOtherKey(keyCode) {
  return keyCode >= 48 && keyCode <= 57 || keyCode >= 65 && keyCode <= 90 || keyCode >= 96 && keyCode <= 111 || keyCode >= 186 && keyCode <= 192 || keyCode >= 219 && keyCode <= 222 || keyCode === 32;
}
function rule1(_ref137) {
  var isKeyEsc = _ref137.isKeyEsc, _vm = _ref137._vm, event = _ref137.event, actived = _ref137.actived, mouseConfig = _ref137.mouseConfig;
  return {
    match: function match() {
      return isKeyEsc;
    },
    action: function action() {
      return _vm.handleEscKeyDown({
        event,
        actived,
        mouseConfig
      });
    }
  };
}
function rule2(_ref138) {
  var isKeySpacebar = _ref138.isKeySpacebar, keyboardConfig = _ref138.keyboardConfig, selected = _ref138.selected, _vm = _ref138._vm, event = _ref138.event;
  return {
    match: function match() {
      return isKeySpacebar && (keyboardConfig.isArrow || keyboardConfig.isTab) && selected.row && selected.column && (selected.column.type === "selection" || selected.column.type === "radio");
    },
    action: function action() {
      return _vm.handleSpaceKeyDown({
        event,
        keyboardConfig,
        selected
      });
    }
  };
}
function rule3(args) {
  var isKeyEnter = args.isKeyEnter, keyboardConfig = args.keyboardConfig, selected = args.selected, actived = args.actived;
  var treeConfig = args.treeConfig, highlightCurrentRow = args.highlightCurrentRow, currentRow = args.currentRow, _vm = args._vm, event = args.event;
  return {
    match: function match() {
      return isKeyEnter && (keyboardConfig.isArrow || keyboardConfig.isTab) && (selected.row || actived.row || treeConfig && highlightCurrentRow && currentRow);
    },
    action: function action() {
      return _vm.handleEnterKeyDown({
        event,
        selected,
        actived
      });
    }
  };
}
function rule4(_ref139) {
  var isOperCtxMenu = _ref139.isOperCtxMenu, _vm = _ref139._vm, event = _ref139.event;
  return {
    match: function match() {
      return isOperCtxMenu;
    },
    action: function action() {
      return _vm.handleCtxMenu({
        event
      });
    }
  };
}
function rule5(_ref140) {
  var isKeyF2 = _ref140.isKeyF2, _vm = _ref140._vm, event = _ref140.event, selected = _ref140.selected;
  return {
    match: function match() {
      return isKeyF2;
    },
    action: function action() {
      return _vm.handleF2KeyDown({
        event,
        selected
      });
    }
  };
}
function rule6(_ref141) {
  var isOperArrowKeys = _ref141.isOperArrowKeys, keyboardConfig = _ref141.keyboardConfig, _vm = _ref141._vm, event = _ref141.event, selected = _ref141.selected;
  return {
    match: function match() {
      return isOperArrowKeys && keyboardConfig.isArrow;
    },
    action: function action() {
      return _vm.handleArrowKeyDown({
        event,
        selected
      });
    }
  };
}
function rule7(_ref142) {
  var isKeyTab = _ref142.isKeyTab, keyboardConfig = _ref142.keyboardConfig, _vm = _ref142._vm, event = _ref142.event, selected = _ref142.selected, actived = _ref142.actived;
  return {
    match: function match() {
      return isKeyTab && keyboardConfig.isTab;
    },
    action: function action() {
      return _vm.handleTabKeyDown({
        event,
        selected,
        actived
      });
    }
  };
}
function rule8(args) {
  var isKeyDel = args.isKeyDel, treeConfig = args.treeConfig, highlightCurrentRow = args.highlightCurrentRow, currentRow = args.currentRow;
  var isKeyBack = args.isKeyBack, keyboardConfig = args.keyboardConfig, _vm = args._vm, event = args.event, selected = args.selected;
  return {
    match: function match() {
      return isKeyDel || (treeConfig && highlightCurrentRow && currentRow ? isKeyBack && keyboardConfig.isArrow : isKeyBack);
    },
    action: function action() {
      return _vm.handleDelKeyDown({
        event,
        selected
      });
    }
  };
}
function rule9(_ref143) {
  var keyboardConfig = _ref143.keyboardConfig, isKeyWithCtrl = _ref143.isKeyWithCtrl, isKeyA = _ref143.isKeyA, isKeyX = _ref143.isKeyX, isKeyC = _ref143.isKeyC, isKeyV = _ref143.isKeyV, _vm = _ref143._vm, event = _ref143.event;
  return {
    match: function match() {
      return keyboardConfig.isCut && isKeyWithCtrl && (isKeyA || isKeyX || isKeyC || isKeyV);
    },
    action: function action() {
      return _vm.handleCopyKeyDown({
        event
      });
    }
  };
}
function rule10(_ref144) {
  var keyboardConfig = _ref144.keyboardConfig, isKeyWithCtrl = _ref144.isKeyWithCtrl, _vm = _ref144._vm, event = _ref144.event, selected = _ref144.selected, actived = _ref144.actived;
  return {
    match: function match() {
      return keyboardConfig.isEdit && !isKeyWithCtrl && !(selected.row === actived.row && selected.column === actived.column);
    },
    action: function action() {
      return _vm.handleOtherKeyDown({
        event,
        selected
      });
    }
  };
}
function onGlobalKeydown(event, _vm) {
  var isCtxMenu2 = _vm.isCtxMenu, ctxMenuStore = _vm.ctxMenuStore, _vm$mouseConfig = _vm.mouseConfig, mouseConfig = _vm$mouseConfig === void 0 ? {} : _vm$mouseConfig, _vm$keyboardConfig = _vm.keyboardConfig, keyboardConfig = _vm$keyboardConfig === void 0 ? {} : _vm$keyboardConfig;
  var treeConfig = _vm.treeConfig, highlightCurrentRow = _vm.highlightCurrentRow, currentRow = _vm.currentRow;
  var _vm$editStore = _vm.editStore, selected = _vm$editStore.selected, actived = _vm$editStore.actived;
  var eventKeyCode = event.keyCode;
  var isKeyWithCtrl = event.ctrlKey;
  var isKeyF2 = eventKeyCode === 113;
  var isKeyX = eventKeyCode === 88;
  var isKeyV = eventKeyCode === 86;
  var isKeyC = eventKeyCode === 67;
  var isKeyA = eventKeyCode === 65;
  var isKeyDel = eventKeyCode === 46;
  var isKeyDwArrow = eventKeyCode === 40;
  var isKeyRightArrow = eventKeyCode === 39;
  var isKeyUpArrow = eventKeyCode === 38;
  var isKeyLeftArrow = eventKeyCode === 37;
  var isKeySpacebar = eventKeyCode === 32;
  var isKeyEsc = eventKeyCode === 27;
  var isKeyEnter = eventKeyCode === 13;
  var isKeyTab = eventKeyCode === 9;
  var isKeyBack = eventKeyCode === 8;
  var isOperArrowKeys = isKeyLeftArrow || isKeyUpArrow || isKeyRightArrow || isKeyDwArrow;
  var isOperCtxMenu = isCtxMenu2 && ctxMenuStore.visible && (isKeyEnter || isKeySpacebar || isOperArrowKeys);
  var rules = [
    rule1({
      isKeyEsc,
      _vm,
      event,
      actived,
      mouseConfig
    }),
    // 空格键支持选中复选列
    rule2({
      isKeySpacebar,
      keyboardConfig,
      selected,
      _vm,
      event
    }),
    // 如果是激活状态，退则出到下一行
    rule3({
      isKeyEnter,
      keyboardConfig,
      selected,
      actived,
      treeConfig,
      highlightCurrentRow,
      currentRow,
      _vm,
      event
    }),
    // 如果配置了右键菜单; 支持方向键操作、回车
    rule4({
      isOperCtxMenu,
      _vm,
      event
    }),
    // 如果按下了 F2 键
    rule5({
      isKeyF2,
      _vm,
      event,
      selected
    }),
    // 如果按下了方向键
    rule6({
      isOperArrowKeys,
      keyboardConfig,
      _vm,
      event,
      selected
    }),
    // 如果按下了 Tab 键切换
    rule7({
      isKeyTab,
      keyboardConfig,
      _vm,
      event,
      selected,
      actived
    }),
    // 如果是删除键
    rule8({
      isKeyDel,
      treeConfig,
      highlightCurrentRow,
      currentRow,
      isKeyBack,
      keyboardConfig,
      _vm,
      event,
      selected
    }),
    rule9({
      keyboardConfig,
      isKeyWithCtrl,
      isKeyA,
      isKeyX,
      isKeyC,
      isKeyV,
      _vm,
      event
    }),
    // 如果是按下非功能键之外允许直接编辑
    rule10({
      keyboardConfig,
      isKeyWithCtrl,
      _vm,
      event,
      selected,
      actived
    })
  ];
  for (var i = 0; i < rules.length; i++) {
    if (rules[i].match()) {
      return rules[i].action();
    }
  }
}
function handleGlobalMousedownEvent(event) {
  var _this1 = this;
  var $el = this.$el, ctxMenuStore = this.ctxMenuStore, _this$editConfig = this.editConfig, editConfig = _this$editConfig === void 0 ? {} : _this$editConfig, filterStore = this.filterStore;
  var _this$$refs = this.$refs, filterWrapper = _this$$refs.filterWrapper, validTip = _this$$refs.validTip;
  var actived = this.editStore.actived;
  handleGlobalMousedownOnFilterWrapper({
    $el,
    _vm: this,
    event,
    filterStore,
    filterWrapper
  });
  if (actived.row && !(editConfig.autoClear === false)) {
    if (handleGlobalBlurOutside({
      _vm: this,
      actived,
      event
    })) {
      return;
    }
    if (validTip && this.getEventTargetNode(event, validTip.$el).flag) ;
    else if (!this.lastCallTime || this.lastCallTime + 50 < Date.now()) {
      this.preventEvent(event, "event.clear_actived", actived.args, function() {
        var isClear;
        var isReadonlyCol = !_this1.getEventTargetNode(event, $el, "col__edit").flag;
        isClear = handleGlobalIsClear({
          $el,
          _vm: _this1,
          actived,
          editConfig,
          event,
          isClear,
          isReadonlyCol
        });
        handleGlobalClearActived({
          $el,
          _vm: _this1,
          event,
          isClear
        });
      });
    }
  }
  handleGlobalMousedownOnCtxMenu({
    _vm: this,
    ctxMenuStore,
    event
  });
}
function handleGlobalMousedownCaptureEvent(event) {
  this.handleClearMouseChecked(event);
}
function handleGlobalBlurEvent() {
  this.closeFilter();
  this.closeMenu();
}
function handleGlobalMousewheelEvent(event) {
  this.updateScrollLoadBar(event);
  this.clostTooltip();
  this.closeMenu();
}
function handleEscKeyDown(_ref145) {
  var _this10 = this;
  var event = _ref145.event, actived = _ref145.actived, mouseConfig = _ref145.mouseConfig;
  this.closeMenu();
  this.closeFilter();
  if (actived.row) {
    this.clearActived(event);
    if (mouseConfig.selected) {
      this.$nextTick(function() {
        return _this10.handleSelected(actived.args, event);
      });
    }
  }
}
function handleEnterKeyDown(_ref146) {
  var _this11 = this;
  var event = _ref146.event, selected = _ref146.selected, actived = _ref146.actived;
  var highlightCurrentRow = this.highlightCurrentRow, currentRow = this.currentRow, treeConfig = this.treeConfig;
  var isLeftArrow = event.keyCode === 37;
  var isUpArrow = event.keyCode === 38;
  var isRightArrow = event.keyCode === 39;
  if (selected.row || actived.row) {
    this.moveSelected({
      args: selected.row ? selected.args : actived.args,
      isLeftArrow,
      isUpArrow,
      isRightArrow,
      isDownArrow: true,
      event
    });
  } else if (treeConfig && highlightCurrentRow && currentRow) {
    var children = currentRow[treeConfig.children];
    if (children && children.length) {
      event.preventDefault();
      var targetRow = children[0];
      this.setTreeExpansion(currentRow, true).then(function() {
        return _this11.scrollToRow(targetRow);
      }).then(function() {
        return _this11.triggerCurrentRowEvent(event, {
          $table: _this11,
          row: targetRow
        });
      });
    }
  }
}
function handleCtxMenu(_ref147) {
  var event = _ref147.event;
  var ctxMenuStore = this.ctxMenuStore;
  event.preventDefault();
  if (ctxMenuStore.showChild && hasChildrenList(ctxMenuStore.selected)) {
    this.moveCtxMenu({
      event,
      keyCode: event.keyCode,
      ctxMenuStore,
      property: "selectChild",
      operKey: 37,
      operRest: false,
      menuList: ctxMenuStore.selected.children
    });
  } else {
    this.moveCtxMenu({
      event,
      keyCode: event.keyCode,
      ctxMenuStore,
      property: "selected",
      operKey: 39,
      operRest: true,
      menuList: this.ctxMenuList
    });
  }
}
function handleArrowKeyDown(_ref148) {
  var event = _ref148.event, selected = _ref148.selected;
  var highlightCurrentRow = this.highlightCurrentRow, currentRow = this.currentRow;
  var isLeftArrow = event.keyCode === 37;
  var isUpArrow = event.keyCode === 38;
  var isRightArrow = event.keyCode === 39;
  var isDownArrow = event.keyCode === 40;
  if (selected.row && selected.column) {
    this.moveSelected({
      args: selected.args,
      isLeftArrow,
      isUpArrow,
      isRightArrow,
      isDownArrow,
      event
    });
  } else if ((isUpArrow || isDownArrow) && highlightCurrentRow && currentRow) {
    this.moveCurrentRow(isUpArrow, isDownArrow, event);
  }
}
function handleDelKeyDown(_ref149) {
  var _this12 = this;
  var event = _ref149.event, selected = _ref149.selected;
  var _this$keyboardConfig = this.keyboardConfig, keyboardConfig = _this$keyboardConfig === void 0 ? {} : _this$keyboardConfig, treeConfig = this.treeConfig, highlightCurrentRow = this.highlightCurrentRow, currentRow = this.currentRow;
  var isBack = event.keyCode === 8;
  if (keyboardConfig.isDel && (selected.row || selected.column)) {
    setCellValue(selected.row, selected.column, null);
    if (isBack) {
      this.handleActived(selected.args, event);
    }
  } else if (isBack && keyboardConfig.isArrow && treeConfig && highlightCurrentRow && currentRow) {
    var _findTree = findTree$1(this.afterFullData, function(item) {
      return item === currentRow;
    }, treeConfig), parentRow = _findTree.parent;
    if (parentRow) {
      event.preventDefault();
      this.setTreeExpansion(parentRow, false).then(function() {
        return _this12.scrollToRow(parentRow);
      }).then(function() {
        return _this12.triggerCurrentRowEvent(event, {
          $table: _this12,
          row: parentRow
        });
      });
    }
  }
}
function handleSpaceKeyDown(_ref150) {
  var event = _ref150.event, selected = _ref150.selected;
  event.preventDefault();
  if (selected.column.type === "selection") {
    this.handleToggleCheckRowEvent(selected.args, event);
  } else {
    this.triggerRadioRowEvent(event, selected.args);
  }
}
function handleTabKeyDown(_ref151) {
  var event = _ref151.event, selected = _ref151.selected, actived = _ref151.actived;
  var isShiftKey = event.shiftKey;
  var useTab = this.editConfig.useTab;
  if (selected.row || selected.column) {
    this.moveTabSelected(selected.args, isShiftKey, event, useTab);
  } else if (actived.row || actived.column) {
    this.moveTabSelected(actived.args, isShiftKey, event, useTab);
  }
}
function handleCopyKeyDown(_ref152) {
  var event = _ref152.event;
  if (event.keyCode === 65) {
    this.handleAllChecked(event);
  } else if (event.keyCode === 88 || event.keyCode === 67) {
    this.handleCopyed(event.keyCode === 88, event);
  } else {
    this.handlePaste(event);
  }
}
function handleF2KeyDown(_ref153) {
  var event = _ref153.event, selected = _ref153.selected;
  if (selected.row && selected.column) {
    event.preventDefault();
    this.handleActived(selected.args, event);
  }
}
function handleOtherKeyDown(_ref154) {
  var event = _ref154.event, selected = _ref154.selected;
  var _this$keyboardConfig2 = this.keyboardConfig, keyboardConfig = _this$keyboardConfig2 === void 0 ? {} : _this$keyboardConfig2;
  var keyCode = event.keyCode;
  var isOtherKey = checkOtherKey(keyCode);
  if (isOtherKey && selected.column && selected.row && selected.column.editor && (!keyboardConfig.editMethod || !(keyboardConfig.editMethod(selected.args, event) === false))) {
    this.handleActived(selected.args, event);
  }
}
function handleGlobalKeydownEvent(event) {
  var _this13 = this;
  this.preventEvent(event, "event.keydown", {
    $table: this
  }, function() {
    onGlobalKeydown(event, _this13);
  });
}
function handleGlobalResizeEvent() {
  this.updateParentHeight();
  this.recalculate();
}
var run = function run2(names, $table) {
  return names.forEach(function(name) {
    return $table[name].apply($table);
  });
};
var debounceScrollLoadDuration = 200;
var AsyncCollectTimeout = 100;
var sortMultiple = function sortMultiple2(rows, columns2, _vm) {
  var greaterThan = function greaterThan2(valueP, valueQ) {
    var typeP = typeof valueP;
    var typeQ = typeof valueQ;
    if (typeP === typeQ && ["number", "string", "boolean"].includes(typeP)) {
      return valueP > valueQ;
    } else {
      return String(valueP) > String(valueQ);
    }
  };
  var multipleColumnSort = _vm.sortOpts.multipleColumnSort;
  if (typeof multipleColumnSort === "function") {
    rows = multipleColumnSort({
      $table: _vm,
      tableData: rows,
      sortColumns: columns2
    });
  } else {
    rows = rows.sort(function(p, q) {
      for (var i = 0; i < columns2.length; i++) {
        var _columns$i = columns2[i], property = _columns$i.property, order = _columns$i.order;
        var flag = order === "asc" ? 1 : -1;
        var valueP = p[property];
        var valueQ = q[property];
        if (!Object.is(valueP, valueQ)) {
          return greaterThan(valueP, valueQ) ? flag : -flag;
        }
      }
      return 0;
    });
  }
  return rows;
};
var Methods$d = {
  // 监听某个元素是否出现在视口中
  addIntersectionObserver: function addIntersectionObserver() {
    var _this14 = this;
    if (typeof IntersectionObserver === "undefined") {
      return;
    }
    this.intersectionObserver = new IntersectionObserver(function(entries) {
      var entry = entries[0];
      if (entries.length > 1) {
        var intersectingEntry = entries.find(function(entry2) {
          return entry2.isIntersecting;
        });
        if (intersectingEntry) {
          entry = intersectingEntry;
        }
      }
      _this14.handleVisibilityChange(entry.isIntersecting, entry);
    }, this.intersectionOption);
    this.intersectionObserver.observe(this.$refs.tableBody.$el);
  },
  removeIntersectionObserver: function removeIntersectionObserver() {
    if (this.intersectionObserver) {
      this.intersectionObserver.unobserve(this.$refs.tableBody.$el);
      this.intersectionObserver.disconnect();
      this.intersectionObserver = null;
    }
  },
  getParentElem: function getParentElem() {
    var $el = this.$grid ? this.$grid.$el : this.$el;
    return $el.parentNode;
  },
  updateParentHeight: function updateParentHeight() {
    if (this.$grid) {
      this.$grid.updateParentHeight();
    } else {
      this.parentHeight = this.getParentElem().clientHeight;
    }
  },
  getParentHeight: function getParentHeight() {
    return this.parentHeight;
  },
  clearAll: function clearAll(silent) {
    var _this$$grid$fetchOpti = this.$grid.fetchOption, fetchOption = _this$$grid$fetchOpti === void 0 ? {} : _this$$grid$fetchOpti;
    var isReloadFilter = fetchOption.isReloadFilter, _fetchOption$isReload = fetchOption.isReloadScroll, isReloadScroll = _fetchOption$isReload === void 0 ? false : _fetchOption$isReload;
    this.clearSort();
    this.clearCurrentRow();
    this.clearCurrentColumn();
    this.clearSelection();
    this.clearRowExpand();
    this.clearTreeExpand();
    this.clearValidateMap();
    if (!isReloadScroll) {
      this.clearScroll();
    }
    this.cellStatus.clear();
    if (typeof isReloadFilter === "undefined" ? Grid$1._filter : !isReloadFilter) {
      this.clearFilter(silent);
    }
    if (this.keyboardConfig || this.mouseConfig) {
      this.clearIndexChecked();
      this.clearHeaderChecked();
      this.clearChecked();
      this.clearSelected();
      this.clearCopyed();
    }
    return this.clearActived();
  },
  refreshData: function refreshData(data7) {
    var _this15 = this;
    var next = function next2() {
      _this15.tableData = [];
      _this15.cellStatus.clear();
      _this15.clearValidateMap();
      return _this15.updateRawData(data7 || _this15.tableFullData);
    };
    return this.$nextTick().then(next);
  },
  refreshStyle: function refreshStyle() {
    var $el = this.$el, rowSpan = this.rowSpan, spanMethod = this.spanMethod;
    if ($el && (rowSpan || spanMethod)) {
      var transform = $el.style.transform;
      var restore = function restore2() {
        return setTimeout(function() {
          $el.style.transform = transform;
        });
      };
      $el.style.transform = "scale(0.99999)";
      return this.$nextTick().then(restore);
    }
    return this.$nextTick();
  },
  updateData: function updateData() {
    var _this16 = this;
    return this.handleTableData(true).then(function() {
      return _this16.updateFooter();
    }).then(function() {
      return _this16.recalculate();
    });
  },
  // 处理表格数据（过滤，排序，虚拟滚动需要渲染数据的条数）
  handleTableData: function handleTableData(force) {
    var _this$tableFullColumn, _this$tableFullColumn2;
    if (force && (((_this$tableFullColumn = this.tableFullColumn) == null ? void 0 : _this$tableFullColumn.length) > 0 || this.viewType !== GlobalConfig$1.viewConfig.DEFAULT)) {
      this.updateAfterFullData();
      this.updateSelectionStatus();
      this.handleSelectionHeader();
    }
    var scrollYStore = this.scrollYStore, _graphInfo = this._graphInfo, scrollYLoad = this.scrollYLoad;
    var renderSize = scrollYStore.renderSize, startIndex = scrollYStore.startIndex;
    var graphed = _graphInfo == null ? void 0 : _graphInfo.graphed;
    var tableNode = scrollYLoad ? graphed == null ? void 0 : graphed.slice(startIndex, startIndex + renderSize) : graphed;
    var tableData = (tableNode == null ? void 0 : tableNode.map(function(node) {
      return node.payload;
    })) || [];
    this.tableNode = tableNode;
    this.tableData = tableData;
    this.updateScrollStatus();
    if (force && ((_this$tableFullColumn2 = this.tableFullColumn) == null ? void 0 : _this$tableFullColumn2.length) > 0) {
      this.handleDefault();
    }
    return this.$nextTick();
  },
  // 全量加载表格数据
  loadTableData: function loadTableData(datas, notRefresh) {
    var _this17 = this;
    var editStore = this.editStore, height2 = this.height, maxHeight = this.maxHeight, lastScrollLeft = this.lastScrollLeft, lastScrollTop = this.lastScrollTop, scrollYLoad = this.scrollYLoad;
    editStore.insertList = [];
    editStore.insertMap = /* @__PURE__ */ new Map();
    editStore.removeList = [];
    if (scrollYLoad && !(height2 || maxHeight)) {
      error("ui.grid.error.scrollYHeight");
    }
    if (!notRefresh) {
      this.clearScroll();
    }
    return this.handleTableData(true).then(function() {
      return !notRefresh && _this17.recalculate();
    }).then(function() {
      if (lastScrollLeft || lastScrollTop) {
        return _this17.attemptRestoreScroll({
          lastScrollLeft,
          lastScrollTop
        });
      }
    }).then(function() {
      var _this17$resolveMap$lo;
      if (((_this17$resolveMap$lo = _this17.resolveMap.loadDataResolve) == null ? void 0 : _this17$resolveMap$lo.data) === datas) {
        _this17.resolveMap.loadDataResolve.resolveQueue.forEach(function(resolve) {
          return resolve();
        });
        _this17.resolveMap.loadDataResolve = null;
      }
    });
  },
  // 重新加载数据
  reloadData: function reloadData(datas) {
    var _this18 = this;
    return this.clearAll().then(function() {
      return _this18.updateRawData(datas);
    });
  },
  // 加载全量数据
  loadData: function loadData(datas) {
    var _this19 = this;
    return new Promise(function(resolve) {
      _this19.updateRawData(datas);
      if (_this19.resolveMap.loadDataResolve) {
        _this19.resolveMap.loadDataResolve.data = datas;
        _this19.resolveMap.loadDataResolve.resolveQueue.push(resolve);
      } else {
        _this19.resolveMap.loadDataResolve = {
          data: datas,
          resolveQueue: [resolve]
        };
      }
    });
  },
  updateRawData: function updateRawData(datas) {
    this.rawData = datas;
    this.rawDataVersion += 1;
  },
  getOriginRow: function getOriginRow(row2) {
    var rowid2 = getRowid(this, row2);
    return rowid2 ? this.rowidCacheMap.get(rowid2) : null;
  },
  setOriginRow: function setOriginRow(row2, record) {
    var rowid2 = getRowid(this, row2);
    if (rowid2 && record) {
      this.rowidCacheMap.set(rowid2, record);
    }
  },
  reloadRow: function reloadRow(row2, record, field) {
    var tableData = this.tableData, treeConfig = this.treeConfig, treeOrdered = this.treeOrdered;
    var _ref155 = treeConfig || {}, childrenKey = _ref155.children, _ref155$temporaryInde = _ref155.temporaryIndex, temporaryIndex = _ref155$temporaryInde === void 0 ? "_$index_" : _ref155$temporaryInde;
    var rowKey = getRowkey(this);
    var originRow = this.getOriginRow(row2);
    var hasSrc = originRow && row2;
    var hasSrcNoField = hasSrc && !field;
    if (hasSrc && field) {
      set(originRow, field, get(record || row2, field));
    }
    if (hasSrcNoField && record) {
      var _extends4, _ref156;
      var backupRow = this.defineField(_extends({}, record, (_extends4 = {}, _extends4[rowKey] = originRow[rowKey], _extends4)));
      var rowChildren, clonedRow;
      if (treeConfig) {
        backupRow[childrenKey] = void 0;
        rowChildren = row2[childrenKey];
        if (!treeOrdered) {
          backupRow[temporaryIndex] = originRow[temporaryIndex];
        }
      }
      clonedRow = clone(backupRow, true);
      if (treeConfig) {
        backupRow[childrenKey] = originRow[childrenKey];
      }
      this.setOriginRow(row2, backupRow);
      clear(row2, void 0);
      Object.assign(row2, clonedRow, treeConfig ? (_ref156 = {}, _ref156[childrenKey] = rowChildren, _ref156) : null);
      this.updateCache();
    }
    if (hasSrcNoField && !record) {
      var _clonedRow;
      if (treeConfig) {
        var _extends5;
        _clonedRow = clone(_extends({}, row2, (_extends5 = {}, _extends5[childrenKey] = void 0, _extends5)), true);
        _clonedRow[childrenKey] = originRow[childrenKey];
      } else {
        _clonedRow = clone(row2, true);
      }
      destructuring(originRow, _clonedRow);
    }
    this.tableData = tableData.slice(0);
    return this.$nextTick();
  },
  // 从新加载列配置
  reloadColumn: function reloadColumn(columns2) {
    var _this20 = this;
    return this.clearAll().then(function() {
      return _this20.loadColumn(columns2);
    });
  },
  loadColumn: function loadColumn(columns2) {
    var _this21 = this;
    return new Promise(function(resolve) {
      _this21.collectColumn = mapTree(columns2, function(column) {
        return Cell.createColumn(_this21, column);
      }, headerProps);
      resolve();
    }).then(function() {
      return _this21.$nextTick();
    });
  },
  /** 设置数据查找缓存，对数据进行备份，深度克隆  */
  updateCache: function updateCache(backup, deepCopy) {
    var _this22 = this;
    if (backup === void 0) {
      backup = false;
    }
    if (deepCopy === void 0) {
      deepCopy = false;
    }
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig, treeOrdered = this.treeOrdered, editConfig = this.editConfig, _this$saveSource = this.saveSource, saveSource = _this$saveSource === void 0 ? false : _this$saveSource;
    var rowKey = getRowkey(this);
    var _ref157 = treeConfig || {}, childrenKey = _ref157.children, _ref157$temporaryInde = _ref157.temporaryIndex, temporaryIndex = _ref157$temporaryInde === void 0 ? "_$index_" : _ref157$temporaryInde;
    var isTreeOrderedFalse = treeConfig && !treeOrdered;
    var fullDataRowIdData = this.fullDataRowIdData = {};
    var fullDataRowMap = this.fullDataRowMap;
    fullDataRowMap.clear();
    eachTree(tableFullData, function(row2, index2, _array, _path, parent) {
      var rowId = getRowid(_this22, row2);
      if (!rowId) {
        rowId = getRowUniqueId();
        set(row2, rowKey, rowId);
      }
      var rowCache = {
        row: row2,
        rowid: rowId,
        index: index2
      };
      if (fullDataRowIdData[rowId]) {
        warn("ui.grid.error.duplicateRowId", rowId);
      }
      fullDataRowIdData[rowId] = rowCache;
      fullDataRowMap.set(_this22.getRaw(row2), rowCache);
      if (isTreeOrderedFalse) {
        var parentIndex;
        if (parent) {
          parentIndex = get(parent, temporaryIndex);
        }
        set(row2, temporaryIndex, (parentIndex ? parentIndex + "." : "") + (index2 + 1));
      }
    }, treeConfig);
    if (backup && (editConfig || saveSource)) {
      requestIdleCallback(function() {
        var rowidCacheMap = /* @__PURE__ */ new Map();
        var callback = function callback2(row2) {
          var _extends6;
          return rowidCacheMap.set(getRowid(_this22, row2), clone(childrenKey ? _extends({}, row2, (_extends6 = {}, _extends6[childrenKey] = null, _extends6)) : _extends({}, row2), deepCopy));
        };
        eachTree(tableFullData, callback, treeConfig);
        _this22.rowidCacheMap = rowidCacheMap;
      });
    }
  },
  getRaw: function getRaw(object) {
    var hooks_vue = hooks;
    return !isVue2 && hooks_vue.isProxy(object) ? hooks.toRaw(object) : object;
  },
  /**
   * 递归地将 Proxy 对象（如 Vue 3 的响应式对象）转换为普通对象。
   * @param {any} data 待处理的数据
   * @returns {any} 转换后的普通对象或原始数据
   */
  deepUnwrap: function deepUnwrap(data7) {
    if (data7 === null || typeof data7 !== "object") {
      return data7;
    }
    if (data7 instanceof Map) {
      return this.cloneMapAndUnwrap(data7);
    }
    if (data7 instanceof Set) {
      return new Set(Array.from(data7).map(this.deepUnwrap));
    }
    if (Array.isArray(data7)) {
      return data7.map(this.deepUnwrap);
    }
    var result = {};
    for (var key in data7) {
      if (Object.prototype.hasOwnProperty.call(data7, key)) {
        result[key] = this.deepUnwrap(data7[key]);
      }
    }
    return result;
  },
  /**
   * 复制 Map，并递归地将 Map 的值中包含的 Proxy 转换为普通对象。
   * @param {Map<any, any>} originalMap 原始 Map
   * @returns {Map<any, any>} 复制并解包后的新 Map
   */
  cloneMapAndUnwrap: function cloneMapAndUnwrap(originalMap) {
    if (!(originalMap instanceof Map)) {
      return originalMap;
    }
    var newMap = /* @__PURE__ */ new Map();
    for (var _iterator0 = _createForOfIteratorHelperLoose(originalMap.entries()), _step0; !(_step0 = _iterator0()).done; ) {
      var _step0$value = _step0.value, key = _step0$value[0], value = _step0$value[1];
      var unwrappedValue = this.deepUnwrap(value);
      newMap.set(key, unwrappedValue);
    }
    return newMap;
  },
  // 更新列的 Map
  cacheColumnMap: function cacheColumnMap(options) {
    var fullColumnMap = this.fullColumnMap;
    var fullColumnIdData = {};
    this.fullColumnIdData = fullColumnIdData;
    fullColumnMap.clear();
    options.columnCaches.forEach(function(cache) {
      fullColumnIdData[cache.colid] = cache;
      fullColumnMap.set(cache.column, cache);
    });
  },
  // 通过tr的dom元素获取行数据等相关信息
  getRowNode: function getRowNode(tr) {
    if (!tr) return null;
    var fullDataRowIdData = this.fullDataRowIdData, tableFullData = this.tableFullData;
    var dataRowid = tr.dataset.rowid;
    var rowCache = fullDataRowIdData[dataRowid];
    if (rowCache) {
      return {
        item: rowCache.row,
        index: rowCache.index,
        items: tableFullData
      };
    }
    return null;
  },
  getColumnNode: function getColumnNode(cell) {
    if (!cell) return null;
    var fullColumnIdData = this.fullColumnIdData, tableFullColumn = this.tableFullColumn;
    var dataColid = cell.dataset.colid;
    var colCache = fullColumnIdData[dataColid];
    if (colCache) {
      return {
        index: colCache.index,
        item: colCache.column,
        items: tableFullColumn
      };
    }
    return null;
  },
  getRowIndex: function getRowIndex(row2) {
    var fullDataRowMap = this.fullDataRowMap;
    row2 = this.getRaw(row2);
    return fullDataRowMap.has(row2) ? fullDataRowMap.get(row2).index : -1;
  },
  getColumnIndex: function getColumnIndex(column) {
    var fullColumnMap = this.fullColumnMap;
    return fullColumnMap.has(column) ? fullColumnMap.get(column).columnIndex : -1;
  },
  hasIndexColumn: function hasIndexColumn(column) {
    return (column == null ? void 0 : column.type) === "index";
  },
  defineField: function defineField(row2, copy, editorColumns, cache) {
    var _this23 = this;
    if (!row2 || typeof row2 !== "object") return row2;
    if (copy) {
      row2 = clone(row2, true);
    }
    var rowKey = getRowkey(this);
    var columns2 = editorColumns || this.visibleColumn;
    columns2.forEach(function(column) {
      var property = column.property, editor = column.editor;
      var propNotExist = property && !has(row2, property);
      var propDefaultValue = editor && !isUndefined(editor.defaultValue) ? editor.defaultValue : null;
      if (propNotExist) {
        if (cache) {
          var originRow = _this23.getOriginRow(row2);
          if (originRow) {
            originRow[property] = propDefaultValue;
          }
        }
        if (isVue2) {
          _this23.$set(row2, property, propDefaultValue);
        } else {
          set(row2, property, propDefaultValue);
        }
      }
    });
    var rowId = get(row2, rowKey);
    if (isNull(rowId) || rowId === "") {
      set(row2, rowKey, getRowUniqueId());
    }
    return row2;
  },
  isTemporaryRow: function isTemporaryRow(row2) {
    var rowid2 = getRowid(this, row2);
    return this.editStore.insertMap.has(rowid2);
  },
  createData: function createData(records, copy) {
    var _this24 = this;
    var isArr = isArray(records);
    if (!isArr) {
      records = [records];
    }
    var tmp = records.map(function(record) {
      return _this24.defineField(record, copy);
    });
    return new Promise(function(resolve) {
      resolve(isArr ? tmp : tmp[0]);
    });
  },
  createRow: function createRow(records) {
    return this.createData(records, true);
  },
  /**
   * 清空单元格内容：
   * 如果不传参数，则清空整个表格内容；
   * 如果传row，则清空一行内容；
   * 如果传rows，则清空多行内容；
   * 如果还额外传了field，则清空指定单元格内容；
   */
  clearData: function clearData(rows, field) {
    var _this25 = this;
    rows = !arguments.length ? this.tableFullData : rows && !isArray(rows) ? [rows] : rows;
    rows.forEach(function(row2) {
      if (field) {
        set(row2, field, null);
      } else {
        _this25.visibleColumn.forEach(function(column) {
          column.property && setCellValue(row2, column, null);
        });
      }
    });
    return this.$nextTick();
  },
  hasRowInsert: function hasRowInsert(row2) {
    return this.isTemporaryRow(row2);
  },
  compareRow: function compareRow(row2, originalRow, field) {
    var value = get(row2, field);
    var originalValue = get(originalRow, field);
    var column = this.getColumnByField(field);
    var equals = (column == null ? void 0 : column.equals) || this.equals;
    var result;
    if (equals) {
      result = equals({
        value,
        originalValue,
        field,
        row: row2,
        originalRow,
        column,
        $table: this
      });
    }
    if (typeof result !== "boolean") {
      result = isEqual(originalValue, value);
    }
    return result;
  },
  hasRowChange: function hasRowChange(row2, field) {
    var _editConfig$insertCha;
    var treeConfig = this.treeConfig, visibleColumn = this.visibleColumn, _this$editConfig2 = this.editConfig, editConfig = _this$editConfig2 === void 0 ? {} : _this$editConfig2;
    var insertChanged = (_editConfig$insertCha = editConfig == null ? void 0 : editConfig.insertChanged) != null ? _editConfig$insertCha : false;
    var argsLength = arguments.length;
    var originRow;
    if (this.isTemporaryRow(row2)) {
      return insertChanged;
    }
    var cacheRow = this.getOriginRow(row2);
    if (cacheRow) {
      if (treeConfig) {
        var _extends7, _extends8;
        var children = treeConfig.children;
        row2 = _extends({}, row2, (_extends7 = {}, _extends7[children] = null, _extends7));
        originRow = _extends({}, cacheRow, (_extends8 = {}, _extends8[children] = null, _extends8));
      } else {
        originRow = _extends({}, cacheRow);
      }
    }
    if (originRow) {
      if (argsLength > 1) {
        if (!this.getColumnByField(field)) {
          warn("ui.grid.error.fieldNotExist", field);
          return false;
        }
        return !this.compareRow(row2, originRow, field);
      }
      for (var i = 0; i < visibleColumn.length; i++) {
        var property = visibleColumn[i].property;
        if (property && !this.compareRow(row2, originRow, property)) {
          return true;
        }
      }
    }
    return false;
  },
  // 获取表格所有列
  getColumns: function getColumns(columnIndex) {
    var columns2 = this.visibleColumn;
    var argsLength = arguments.length;
    return argsLength ? columns2[columnIndex] : columns2.slice(0);
  },
  getColumnById: function getColumnById(colid) {
    var fullColumnIdData = this.fullColumnIdData;
    var colCache = fullColumnIdData[colid];
    return colCache ? colCache.column : null;
  },
  getColumnByField: function getColumnByField(field) {
    var columns2 = this.visibleColumn;
    return typeof field === "string" && field ? find$1(columns2, function(column) {
      return column.property === field;
    }) : null;
  },
  // 获取当前表格的列（完整的全量表头列、处理条件之后的全量表头列、当前渲染中的表头列）
  getTableColumn: function getTableColumn() {
    var collectColumn2 = this.collectColumn, tableColumn2 = this.tableColumn, tableFullColumn = this.tableFullColumn, visibleColumn = this.visibleColumn;
    return {
      fullColumn: tableFullColumn.slice(0),
      visibleColumn: visibleColumn.slice(0),
      tableColumn: tableColumn2.slice(0),
      collectColumn: collectColumn2.slice(0)
    };
  },
  // 获取表格所有数据
  getData: function getData(rowIndex) {
    var allRows = this.rawData || [];
    if (!arguments.length) {
      return allRows.slice(0);
    }
    if (typeof rowIndex === "number" && rowIndex > -1) {
      return allRows[rowIndex];
    }
    return void 0;
  },
  // 获取选中数据。notCopy为true不返回数据副本，表格内部要继续处理其返回值时设置为true
  getSelectRecords: function getSelectRecords(notCopy) {
    var _this$selectConfig = this.selectConfig, selectConfig = _this$selectConfig === void 0 ? {} : _this$selectConfig, selection = this.selection;
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    var checkField = selectConfig.checkField;
    var _ref158 = {}, _ref158$rowList = _ref158.rowList, rowList = _ref158$rowList === void 0 ? [] : _ref158$rowList;
    if (checkField && treeConfig) {
      rowList = filterTree(tableFullData, function(row2) {
        return get(row2, checkField);
      }, treeConfig);
    }
    if (checkField && !treeConfig) {
      rowList = tableFullData.filter(function(row2) {
        return get(row2, checkField);
      });
    }
    if (!checkField && treeConfig) {
      rowList = filterTree(tableFullData, function(row2) {
        return ~selection.indexOf(row2);
      }, treeConfig);
    }
    if (!checkField && !treeConfig) {
      rowList = tableFullData.filter(function(row2) {
        return ~selection.indexOf(row2);
      });
    }
    return notCopy ? rowList : clone(rowList, true);
  },
  // 对数据进行筛选和排序，获取处理后数据。服务端筛选和排序，在接口调用时已传入参数
  updateAfterFullData: function updateAfterFullData() {
    var _this26 = this;
    var tableData = this.tableFullData;
    var updateFilter = function updateFilter2(tableData2) {
      var remoteFilter = _this26.remoteFilter, visibleColumn = _this26.visibleColumn;
      var filterColumn = visibleColumn.filter(function(_ref159) {
        var filter2 = _ref159.filter;
        return !!filter2;
      });
      return tableData2.filter(function(row2) {
        return filterColumn.every(function(column) {
          return remoteFilter ? true : _this26.handleLocalFilter(row2, column);
        });
      });
    };
    var updateSort = function updateSort2(tableData2) {
      var visibleColumn = _this26.visibleColumn, sortOpts2 = _this26.sortOpts, remoteSort = _this26.remoteSort;
      var sortColumn = visibleColumn.find(function(column) {
        return column.order;
      });
      var isRemote = isBoolean(sortColumn == null ? void 0 : sortColumn.remoteSort) ? sortColumn == null ? void 0 : sortColumn.remoteSort : remoteSort;
      if (sortColumn != null && sortColumn.order && !isRemote) {
        if (_this26.sortMethod) {
          var order = sortColumn.order, property = sortColumn.property;
          var params = {
            $table: _this26,
            column: sortColumn,
            data: tableData2,
            order,
            property
          };
          tableData2 = _this26.sortMethod(params) || tableData2;
        } else {
          var sortedFlag = false;
          if (sortOpts2.multipleColumnSort) {
            var sortColumns = visibleColumn.filter(function(_ref160) {
              var order2 = _ref160.order;
              return !!order2;
            });
            if (sortColumns.length > 1) {
              tableData2 = sortMultiple(tableData2, sortColumns, _this26);
              sortedFlag = true;
            }
          }
          if (!sortedFlag) {
            var columnSortMethod = sortColumn.sortMethod;
            var sorted = columnSortMethod ? tableData2.sort(columnSortMethod) : sortBy(tableData2, sortColumn.sortBy ? sortColumn.sortBy : sortColumn.property);
            tableData2 = sortColumn.order === "desc" ? sorted.reverse() : sorted;
          }
        }
      }
      return tableData2;
    };
    tableData = updateFilter(tableData);
    tableData = updateSort(tableData);
    this.afterFullData = tableData;
    this.handleAfterFullData();
    return tableData;
  },
  /** 处理后数据，构建分组数据和构建渲染图 */
  handleAfterFullData: function handleAfterFullData() {
    var afterFullData = this.afterFullData;
    buildRowGroupFullData(afterFullData, this);
    buildRenderGraph(this);
  },
  /** 处理外部过滤和重置，区别于表头过滤 */
  handleExternalFilter: function handleExternalFilter(value, filter2) {
    var _this27 = this;
    return new Promise(function(resolve) {
      if (value) {
        var afterFullData = _this27.afterFullData;
        if (typeof filter2 === "function") {
          _this27.afterFullData = filter2(afterFullData) || [];
          _this27.handleAfterFullData();
          _this27.handleTableData(false).then(function() {
            return _this27.scrollTo(0, 0);
          }).then(function() {
            return resolve(true);
          });
        }
      } else {
        _this27.handleTableData(true).then(function() {
          return _this27.scrollTo(0, 0);
        }).then(function() {
          return resolve(false);
        });
      }
    });
  },
  updateSelectionStatus: function updateSelectionStatus() {
    this.reserveCheckSelection();
    this.checkSelectionStatus();
  },
  getRowCount: function getRowCount() {
    var _this$_graphInfo;
    return this.scrollLoad ? this.scrollLoad.pageSize || 10 : ((_this$_graphInfo = this._graphInfo) == null ? void 0 : _this$_graphInfo.graphed.length) || 0;
  },
  getRowById: function getRowById(rowid2) {
    var _fullDataRowIdData$ro, _editStore$insertMap;
    var fullDataRowIdData = this.fullDataRowIdData, editStore = this.editStore;
    var rowCache = ((_fullDataRowIdData$ro = fullDataRowIdData[rowid2]) == null ? void 0 : _fullDataRowIdData$ro.row) || (editStore == null ? void 0 : (_editStore$insertMap = editStore.insertMap) == null ? void 0 : _editStore$insertMap.get(rowid2));
    return rowCache || null;
  },
  // 获取处理后的表格数据
  getTableData: function getTableData() {
    var afterFullData = this.afterFullData, footerData = this.footerData, tableData = this.tableData, tableFullData = this.tableFullData;
    return {
      visibleData: afterFullData.slice(0),
      footerData: footerData.slice(0),
      tableData: tableData.slice(0),
      fullData: tableFullData.slice(0)
    };
  },
  handleDefault: function handleDefault() {
    this.handleAsyncColumn(this.tableData);
    this.isCheckable && this.selectConfig && this.handleSelectionDefChecked();
    this.radioConfig && this.handleRadioDefChecked();
    this.expandConfig && this.handleDefaultRowExpand();
    this.treeConfig && this.handleDefaultTreeExpand();
    this.updateFooter();
    this.$nextTick(this.recalculate);
  },
  // 动态列处理
  mergeCustomColumn: function mergeCustomColumn(customColumns, sort2, colWidth) {
    var isGroup2 = this.isGroup, fullColumn = this.tableFullColumn;
    var mergeWidth = function mergeWidth2(col) {
      return colWidth && Object.assign(col, colWidth[col.property]);
    };
    var getCustomCol = function getCustomCol2(col) {
      return findTree$1(customColumns, function(item) {
        return col.property && item.property === col.property;
      });
    };
    var hasCustomsOrColWidth = customColumns.length || colWidth;
    var isGroupOrNonSort = isGroup2 || !sort2;
    var matches;
    this.isUpdateCustoms = true;
    if (hasCustomsOrColWidth && isGroupOrNonSort) {
      fullColumn.forEach(function(column) {
        mergeWidth(column);
        matches = getCustomCol(column);
        matches && Object.assign(column, matches.item);
      });
    }
    if (hasCustomsOrColWidth && !isGroupOrNonSort) {
      var _ref161 = {}, _ref161$collectColumn = _ref161.collectColumn, collectColumn2 = _ref161$collectColumn === void 0 ? [] : _ref161$collectColumn, _ref161$customMap = _ref161.customMap, customMap = _ref161$customMap === void 0 ? {} : _ref161$customMap, _ref161$orderColumn = _ref161.orderColumn, orderColumn = _ref161$orderColumn === void 0 ? [] : _ref161$orderColumn;
      mapTree(customColumns, function(customCol) {
        var targetCol = find$1(fullColumn, function(item) {
          return customCol.property && item.property === customCol.property;
        });
        if (targetCol) {
          Object.assign(targetCol, customCol);
          customMap[customCol.property] = targetCol;
          orderColumn.push(targetCol);
        }
      });
      fullColumn.forEach(function(col) {
        var newColumn = customMap[col.property] ? orderColumn.shift() : col;
        if (newColumn) {
          mergeWidth(newColumn);
          collectColumn2.push(newColumn);
        }
      });
      this.collectColumn = collectColumn2;
    }
    var toolbarVm = this.getVm("toolbar");
    if (toolbarVm) {
      toolbarVm.updateColumn(fullColumn);
    }
    this.$emit("update:customs", fullColumn);
  },
  resetAll: function resetAll() {
    run(["resetCustoms", "resetResizable"], this);
  },
  showColumn: function showColumn(column) {
    return this.handleVisibleColumn(column, true);
  },
  hideColumn: function hideColumn(column) {
    return this.handleVisibleColumn(column, false);
  },
  handleVisibleColumn: function handleVisibleColumn(column, visible3) {
    var _arguments = arguments;
    var tableFullColumn = this.tableFullColumn;
    var toolbarVm = this.getVm("toolbar");
    var columns2 = arguments.length ? [column] : tableFullColumn;
    columns2.forEach(function(column2) {
      column2.visible = _arguments.length ? visible3 : true;
    });
    if (toolbarVm) {
      toolbarVm.updateSetting();
    }
    return this.$nextTick();
  },
  resetCustoms: function resetCustoms() {
    return this.handleVisibleColumn();
  },
  // 初始化加载动态列：customColumns 列信息，sort 是否按顺序加载
  reloadCustoms: function reloadCustoms(customColumns, sort2, colWidth) {
    var _this28 = this;
    this.mergeCustomColumn(customColumns, sort2, colWidth);
    this.handleTableData(true);
    return this.refreshColumn().then(function() {
      return _this28.tableFullColumn.slice(0);
    });
  },
  watchColumn: function watchColumn(value) {
    var customs2 = this.customs, treeConfig = this.treeConfig;
    var toolbarVm = this.getVm("toolbar");
    var options = {
      columnCaches: []
    };
    this.markColumnIndex = 0;
    var fullColumn = getColumnList(this, value, options);
    if (options.isGroup && options.hasFixed) {
      value.forEach(function(root) {
        return repairFixed(root);
      });
    }
    this.isCheckable = !!options.isCheckable;
    this.tableFullColumn = fullColumn;
    this.cacheColumnMap(options);
    if (!this.isColumnInitReady) {
      this.isColumnInitReady = true;
      customs2 && this.mergeCustomColumn(customs2);
      this.$emit("column-init-ready");
    }
    this.refreshColumn();
    if (this.remoteFilter) {
      this.$grid.filterData = this.getAllFilter();
    }
    if (!this.isColumnReady) {
      this.isColumnReady = true;
      this.$emit("column-ready");
    }
    this.handleTableData(true);
    toolbarVm == null ? void 0 : toolbarVm.updateColumn(fullColumn);
    if (treeConfig && fullColumn.some(function(column) {
      return column.fixed;
    }) && fullColumn.some(function(column) {
      return column.type === "expand";
    })) {
      warn("ui.grid.error.treeFixedExpand");
    }
  },
  /*
   * 刷新列信息
   * 将固定的列左边、右边分别靠边
   * 如果使用了分组表头，固定列必须在左侧或者右侧
   */
  refreshColumn: function refreshColumn() {
    var _this29 = this;
    var leftList = [], centerList = [], rightList = [];
    var letIndex = 0, leftStartIndex = null, rightEndIndex = null, isColspan = void 0;
    var columnStore = this.columnStore, isGroup2 = this.isGroup;
    var scrollXStore = this.scrollXStore, tableFullColumn = this.tableFullColumn;
    var scrollX = this.optimizeOpts.scrollX;
    onGroupHeader({
      _vm: this,
      isGroup: isGroup2,
      headerProps
    });
    tableFullColumn.filter(function(column) {
      return column.visible;
    }).forEach(function(column, columnIndex) {
      var ret = reassignFixedLeft({
        column,
        columnIndex,
        isColspan,
        leftList,
        leftStartIndex,
        letIndex
      });
      leftStartIndex = ret.leftStartIndex;
      letIndex = ret.letIndex;
      isColspan = ret.isColspan;
      ret = reassignFixedRight({
        column,
        columnIndex,
        isColspan,
        rightEndIndex,
        rightList
      });
      isColspan = ret.isColspan;
      rightEndIndex = ret.rightEndIndex;
      reassignNotFixed({
        centerList,
        column
      });
    });
    var visibleColumn = leftList.concat(centerList).concat(rightList);
    var scrollXLoad = scrollX && scrollX.gt && scrollX.gt < tableFullColumn.length;
    var tableColumn2 = visibleColumn;
    Object.assign(columnStore, {
      leftList,
      centerList,
      rightList
    });
    this.analyColumnWidth();
    showGroupFixedError({
      isColspan,
      isGroup: isGroup2,
      leftStartIndex,
      rightEndIndex,
      visibleColumn
    });
    tableColumn2 = onScrollXLoad({
      _vm: this,
      scrollX,
      scrollXLoad,
      scrollXStore,
      tableColumn: tableColumn2,
      visibleColumn
    });
    this.scrollXLoad = scrollXLoad;
    this.tableColumn = tableColumn2;
    this.visibleColumn = visibleColumn;
    this.visibleColumnChanged = true;
    this.horizonScroll.fixed = leftList.length > 0 || rightList.length > 0;
    this.columnAnchor && this.$grid.buildColumnAnchorParams();
    this.updateFooter();
    this.recalculate();
    return this.$nextTick().then(function() {
      _this29.$emit("after-refresh-column");
      if (_this29.isColumnReady) {
        _this29.attemptRestoreScroll();
      }
    });
  },
  // 指定列宽的列进行拆分
  analyColumnWidth: function analyColumnWidth() {
    var columnMinWidth = this.columnMinWidth, columnStore = this.columnStore, columnWidth = this.columnWidth, tableFullColumn = this.tableFullColumn;
    var autoList = [], pxList = [], pxMinList = [], resizeList = [], scaleList = [], scaleMinList = [];
    var ruleChains = [{
      match: function match(col) {
        return col.resizeWidth;
      },
      action: function action(col) {
        return resizeList.push(col);
      }
    }, {
      match: function match(col) {
        return isPx(col.width);
      },
      action: function action(col) {
        return pxList.push(col);
      }
    }, {
      match: function match(col) {
        return isScale(col.width);
      },
      action: function action(col) {
        return scaleList.push(col);
      }
    }, {
      match: function match(col) {
        return isPx(col.minWidth);
      },
      action: function action(col) {
        return pxMinList.push(col);
      }
    }, {
      match: function match(col) {
        return isScale(col.minWidth);
      },
      action: function action(col) {
        return scaleMinList.push(col);
      }
    }, {
      match: function match() {
        return true;
      },
      action: function action(col) {
        return autoList.push(col);
      }
    }];
    for (var i = 0; i < tableFullColumn.length; i++) {
      var column = tableFullColumn[i];
      columnWidth && !column.width && (column.width = columnWidth);
      columnMinWidth && !column.minWidth && (column.minWidth = columnMinWidth);
      if (!column.visible) {
        continue;
      }
      for (var j = 0; j < ruleChains.length; j++) {
        var ruleChain = ruleChains[j];
        if (ruleChain.match(column)) {
          ruleChain.action(column);
          break;
        }
      }
    }
    Object.assign(columnStore, {
      autoList,
      pxList,
      pxMinList,
      resizeList,
      scaleList,
      scaleMinList
    });
  },
  /**
   * 计算单元格列宽，动态分配可用剩余空间
   * 支持（width=?、width=?px、width=?%、min-width=?、min-width=?px、min-width=?%）
   */
  recalculate: function recalculate() {
    var _this30 = this;
    var elemStore = this.elemStore, scrollXLoad = this.scrollXLoad, scrollYLoad = this.scrollYLoad, scrollLoad = this.scrollLoad, _tableVisible = this._tableVisible;
    var bodyWrapper = elemStore["main-body-wrapper"];
    if (!_tableVisible) {
      return;
    }
    if (!bodyWrapper) {
      return this.computeScrollLoad();
    }
    this.autoCellWidth();
    if (scrollXLoad || scrollYLoad || scrollLoad) {
      return this.computeScrollLoad().then(function() {
        _this30.autoCellWidth();
      });
    }
    return this.computeScrollLoad();
  },
  // 列宽计算
  autoCellWidth: function autoCellWidth() {
    var horizonScroll = this.horizonScroll, headerHeight = this.headerHeight, footerHeight = this.footerHeight, rowHeight = this.rowHeight;
    var _calcTableWidth = calcTableWidth(this), totalWidth = _calcTableWidth.totalWidth, offsetWidth = _calcTableWidth.offsetWidth, offsetHeight = _calcTableWidth.offsetHeight;
    horizonScroll.fixed && calcFixedDetails(this);
    var scrollbarSize = 10;
    var containerWidth = offsetWidth;
    var containerHeight = offsetHeight;
    var containerScrollWidth = totalWidth;
    var containerScrollHeight = this.getRowCount() * rowHeight + headerHeight + footerHeight;
    var isScrollX, isScrollY;
    if (containerScrollWidth <= containerWidth) {
      if (containerScrollHeight <= containerHeight) {
        isScrollX = isScrollY = false;
      } else {
        isScrollY = true;
        isScrollX = containerScrollWidth + scrollbarSize > containerWidth;
      }
    } else {
      isScrollX = true;
      isScrollY = containerScrollHeight + scrollbarSize > containerHeight;
    }
    if (!this.height) {
      this.bodyWrapperHeight = containerScrollHeight > this.bodyWrapperMaxHeight ? this.bodyWrapperMaxHeight : null;
    }
    Object.assign(this, {
      overflowX: isScrollX,
      overflowY: isScrollY,
      scrollbarWidth: isScrollY ? scrollbarSize : 0,
      scrollbarHeight: isScrollX ? scrollbarSize : 0,
      totalWidth,
      tableHeight: offsetHeight,
      containerScrollWidth,
      containerScrollHeight
    });
    if (!this.isColumnWidthAssigned) {
      this.isColumnWidthAssigned = true;
    }
  },
  resetResizable: function resetResizable() {
    var toolbarVm = this.getVm("toolbar");
    this.visibleColumn.forEach(function(col) {
      return col.resizeWidth = 0;
    });
    toolbarVm == null ? void 0 : toolbarVm.resetResizable();
    this.analyColumnWidth();
    return this.recalculate();
  },
  updateStyle: function updateStyle() {
    handleLayout(this);
    this.currentRow && this.setCurrentRow(this.currentRow);
  },
  preventEvent: function preventEvent(event, type, args, next, end) {
    var _this31 = this;
    var eventList = Interceptor.get(type);
    if (!eventList.some(function(listener) {
      return listener(args, event, _this31) === false;
    })) {
      next && next();
    }
    end && end();
  },
  blurOutside: function blurOutside(_ref162, event) {
    var row2 = _ref162.row, args = _ref162.args, column = _ref162.column;
    var editConfig = this.editConfig, getEventTargetNode2 = this.getEventTargetNode, $el = this.$el;
    if (column && row2) {
      var editor = column.editor;
      if (typeof editor.blurOutside === "function") {
        return Boolean(editor.blurOutside({
          cell: args.cell,
          event
        }));
      }
      if (typeof editConfig.blurOutside === "function") {
        var bodyEl = document.body;
        if (getEventTargetNode2(event, bodyEl, "tiny-autocomplete-suggestion").flag || getEventTargetNode2(event, bodyEl, "tiny-select-dropdown").flag || getEventTargetNode2(event, bodyEl, "tiny-cascader__dropdown").flag || getEventTargetNode2(event, bodyEl, "tiny-cascader-menus").flag || getEventTargetNode2(event, bodyEl, "tiny-picker-panel").flag || getEventTargetNode2(event, bodyEl, "tiny-popper").flag || getEventTargetNode2(event, bodyEl, "tiny-dialog-box").flag) {
          return true;
        }
        return Boolean(editConfig.blurOutside({
          cell: args.cell,
          event,
          $table: this
        }));
      }
      var blurClassConfig = editor.blurClass || editConfig.blurClass;
      if (blurClassConfig) {
        var blurClass = [];
        if (typeof blurClassConfig === "string") {
          blurClass.push(blurClassConfig);
        } else if (isArray(blurClassConfig)) {
          blurClass = blurClassConfig.slice(0);
        }
        if (args != null && args.cell.contains(getActualTarget(event))) {
          return true;
        }
        if (editConfig.mode === "row" && getEventTargetNode2(event, $el, "tiny-grid-body__column").flag) {
          return true;
        }
        return blurClass.every(function(cls) {
          return !getEventTargetNode2(event, document.body, cls).flag;
        });
      }
    }
  },
  handleGlobalMousedownEvent,
  handleGlobalBlurEvent,
  handleGlobalMousewheelEvent,
  handleEscKeyDown,
  handleEnterKeyDown,
  handleCtxMenu,
  handleArrowKeyDown,
  handleDelKeyDown,
  handleSpaceKeyDown,
  handleTabKeyDown,
  handleCopyKeyDown,
  handleF2KeyDown,
  handleOtherKeyDown,
  handleGlobalKeydownEvent,
  handleGlobalResizeEvent,
  handleGlobalMousedownCaptureEvent,
  // 处理单选框默认勾选
  handleRadioDefChecked: function handleRadioDefChecked() {
    var fullDataRowIdData = this.fullDataRowIdData;
    var _ref163 = this.radioConfig || {}, checkRowKey = _ref163.checkRowKey;
    var rowid2 = checkRowKey && encodeURIComponent(checkRowKey);
    var rowCache = fullDataRowIdData[rowid2];
    if (rowid2 && rowCache) {
      this.setRadioRow(rowCache.row);
    }
  },
  // 单选，行选中事件
  triggerRadioRowEvent: function triggerRadioRowEvent(event, params) {
    var selectRow = this.selectRow;
    var _ref164 = this.radioConfig || {}, checkMethod = _ref164.checkMethod;
    if (checkMethod && !checkMethod(params)) {
      return;
    }
    this.setRadioRow(params.row);
    this.setCurrentRow(params.row);
    if (selectRow !== params.row) {
      emitEvent(this, "radio-change", [params, event]);
    }
  },
  triggerCurrentRowEvent: function triggerCurrentRowEvent(event, params) {
    var currentRow = this.currentRow;
    this.setCurrentRow(params.row);
    if (currentRow !== params.row) {
      emitEvent(this, "current-change", [params, event]);
    }
  },
  // 高亮行，设置某一行为高亮状态，如果调不加参数，则会取消目前高亮行的选中状态
  setCurrentRow: function setCurrentRow(row2) {
    run(["clearCurrentRow", "clearCurrentColumn"], this);
    this.currentRow = row2;
    if (this.highlightCurrentRow) {
      var rowElems = this.$el.querySelectorAll('[data-rowid="' + getRowid(this, row2) + '"]');
      arrayEach(rowElems, function(elem) {
        return addClass(elem, "row__current");
      });
    }
    return this.$nextTick();
  },
  setRadioRow: function setRadioRow(row2) {
    row2 !== this.selectRow && this.clearRadioRow();
    this.selectRow = row2;
    return this.$nextTick();
  },
  clearCurrentRow: function clearCurrentRow() {
    Object.assign(this, {
      currentRow: null,
      hoverRow: null
    });
    var rowElems = this.$el.querySelectorAll(".row__current");
    arrayEach(rowElems, function(elem) {
      return removeClass(elem, "row__current");
    });
    return this.$nextTick();
  },
  clearRadioRow: function clearRadioRow() {
    var radioRow = this.selectRow;
    radioRow && (this.selectRow = null);
    return this.$nextTick();
  },
  getCurrentRow: function getCurrentRow() {
    var currentRow = this.currentRow;
    return currentRow;
  },
  getRadioRow: function getRadioRow() {
    var radioRow = this.selectRow;
    return radioRow;
  },
  triggerHeaderCellClickEvent: function triggerHeaderCellClickEvent(event, params) {
    var lastTime = this._lastResizeTime, highlightCurrentColumn = this.highlightCurrentColumn;
    var cell = params.cell, column = params.column;
    var trigger = this.sortOpts.trigger;
    var isResizable2 = lastTime && lastTime > Date.now() - 300;
    var isSort = this.getEventTargetNode(event, cell, "tiny-grid-sort-wrapper").flag;
    var isFilter = this.getEventTargetNode(event, cell, "tiny-grid-filter-wrapper").flag;
    if (trigger === "cell" && !(isResizable2 || isSort || isFilter)) {
      var nextOrder = this.toggleColumnOrder(column);
      this.triggerSortEvent(event, column, nextOrder);
    }
    var eventParams = _extends({
      triggerResizable: isResizable2,
      triggerSort: isSort,
      triggerFilter: isFilter
    }, params);
    emitEvent(this, "header-cell-click", [eventParams, event]);
    if (highlightCurrentColumn) {
      return this.setCurrentColumn(column, true);
    }
    return this.$nextTick();
  },
  setCurrentColumn: function setCurrentColumn(column) {
    run(["clearCurrentRow", "clearCurrentColumn"], this);
    this.currentColumn = column;
    var colElems = this.$el.querySelectorAll("." + column.id);
    arrayEach(colElems, function(elem) {
      return addClass(elem, "col__current");
    });
    return this.$nextTick();
  },
  clearCurrentColumn: function clearCurrentColumn() {
    this.currentColumn = null;
    var colElems = this.$el.querySelectorAll(".col__current");
    arrayEach(colElems, function(elem) {
      return removeClass(elem, "col__current");
    });
    return this.$nextTick();
  },
  // 当单元格发生改变时，如果存在规则，则校验
  handleChangeCell: function handleChangeCell(event, params) {
    var _this32 = this;
    this.triggerValidate("blur").catch(function(e) {
      return e;
    }).then(function() {
      _this32.handleActived(params, event).then(function() {
        var _this32$editConfig;
        if ((_this32$editConfig = _this32.editConfig) != null && _this32$editConfig.validateOnActive) {
          return _this32.triggerValidate("change");
        }
      }).catch(function(e) {
        return e;
      });
    });
  },
  // 列点击事件：如果是单击模式，则激活为编辑状态；如果是双击模式，则单击后选中状态
  triggerCellClickEvent: function triggerCellClickEvent(event, params) {
    var $el = this.$el, highlightCurrentRow = this.highlightCurrentRow, editStore = this.editStore;
    var _this$radioConfig = this.radioConfig, radioConfig = _this$radioConfig === void 0 ? {} : _this$radioConfig, _this$selectConfig2 = this.selectConfig, selectConfig = _this$selectConfig2 === void 0 ? {} : _this$selectConfig2, _this$expandConfig = this.expandConfig, expandConfig = _this$expandConfig === void 0 ? {} : _this$expandConfig;
    var _this$treeConfig = this.treeConfig, treeConfig = _this$treeConfig === void 0 ? {} : _this$treeConfig, editConfig = this.editConfig, _this$mouseConfig = this.mouseConfig, mouseConfig = _this$mouseConfig === void 0 ? {} : _this$mouseConfig;
    var actived = editStore.actived;
    var row2 = params.row, column = params.column, cell = params.cell;
    if (isTargetRadioOrCheckbox(event, column, "radio") || isTargetRadioOrCheckbox(event, column, "selection", "checkbox")) {
      return;
    }
    onClickExpandColumn({
      $el,
      _vm: this,
      column,
      event,
      expandConfig,
      params
    });
    onClickTreeNodeColumn({
      _vm: this,
      column,
      event,
      params,
      treeConfig
    });
    if ((!column.treeNode || !this.getEventTargetNode(event, $el, "tiny-grid-tree-wrapper").flag) && (column.type !== "expand" || !this.getEventTargetNode(event, $el, "tiny-grid__expanded").flag)) {
      onHighlightCurrentRow({
        $el,
        _vm: this,
        event,
        highlightCurrentRow,
        params,
        radioConfig
      });
      onClickRadioColumn({
        $el,
        _vm: this,
        column,
        event,
        params,
        radioConfig
      });
      onClickSelectColumn({
        _vm: this,
        column,
        event,
        params,
        selectConfig
      });
      onClickCellSelect({
        _vm: this,
        actived,
        cell,
        column,
        editConfig,
        event,
        mouseConfig,
        params,
        row: row2
      });
    }
    emitEvent(this, "cell-click", [params, event]);
  },
  // 列双击点击事件：如果是双击模式，则激活为编辑状态
  triggerCellDBLClickEvent: function triggerCellDBLClickEvent(event, params) {
    var _this33 = this;
    var editStore = this.editStore, editConfig = this.editConfig;
    var actived = editStore.actived;
    var isActiveNewCell = !actived.args || event.currentTarget !== actived.args.cell;
    var isDblclickNewCell = editConfig && editConfig.trigger === "dblclick" && isActiveNewCell;
    if (isDblclickNewCell && editConfig.mode === "row") {
      this.triggerValidate("blur").catch(function(e) {
        return e;
      }).then(function() {
        _this33.handleActived(params, event).then(function() {
          return _this33.triggerValidate("change");
        }).catch(function(e) {
          return e;
        });
      });
    }
    if (isDblclickNewCell && editConfig.mode === "cell") {
      this.handleActived(params, event).then(function() {
        return _this33.triggerValidate("change");
      }).catch(function(e) {
        return e;
      });
    }
    emitEvent(this, "cell-dblclick", [params, event]);
  },
  // 点击排序事件
  triggerSortEvent: function triggerSortEvent(event, column, order) {
    var property = column.property;
    var isColumnSortable = column.sortable || column.remoteSort;
    if (this.sortable && isColumnSortable) {
      var evntParams = {
        $table: this,
        column,
        order,
        property
      };
      evntParams.prop = property;
      evntParams.field = evntParams.prop;
      if (order === column.order) {
        evntParams.order = null;
        this.clearSort(column.property);
      } else {
        this.sort(property, order);
      }
      emitEvent(this, "sort-change", [evntParams, event]);
    }
  },
  sort: function sort(field, order) {
    var remoteSort = this.remoteSort, tableFullColumn = this.tableFullColumn, visibleColumn = this.visibleColumn;
    var column = find$1(visibleColumn, function(item) {
      return item.property === field;
    });
    var isRemote = isBoolean(column.remoteSort) ? column.remoteSort : remoteSort;
    var isColumnSortable = column.sortable || column.remoteSort;
    if (this.sortable && isColumnSortable) {
      if (column.order !== order) {
        tableFullColumn.forEach(function(column2) {
          return column2.order = null;
        });
        column.order = order;
        !isRemote && this.handleTableData(true).then(this.refreshStyle);
      }
      return this.$nextTick().then(this.updateStyle);
    }
    return this.$nextTick();
  },
  clearSort: function clearSort() {
    arrayEach(this.tableFullColumn, function(column) {
      return column.order = null;
    });
    this.$grid && (this.$grid.sortData = {});
    return this.handleTableData(true).then(this.refreshStyle);
  },
  toggleGroupExpansion: function toggleGroupExpansion(row2) {
    this.groupExpandeds.push(row2);
  },
  // 展开行事件
  triggerRowExpandEvent: function triggerRowExpandEvent(event, _ref165) {
    var row2 = _ref165.row;
    var rest = this.toggleRowExpansion(row2);
    var eventParams = {
      $table: this,
      row: row2,
      rowIndex: this.getRowIndex(row2)
    };
    emitEvent(this, "toggle-expand-change", [eventParams, event]);
    return rest;
  },
  // 切换展开行
  toggleRowExpansion: function toggleRowExpansion(row2) {
    return this.setRowExpansion(row2);
  },
  // 处理默认展开行
  handleDefaultRowExpand: function handleDefaultRowExpand() {
    var fullDataRowIdData = this.fullDataRowIdData, tableFullData = this.tableFullData;
    var _ref166 = this.expandConfig || {}, expandAll = _ref166.expandAll, expandRowKeys = _ref166.expandRowKeys;
    if (expandAll) {
      this.expandeds = tableFullData.slice(0);
      return;
    }
    if (expandRowKeys) {
      var defExpandeds = [];
      expandRowKeys.forEach(function(rowid2) {
        var rowCache = fullDataRowIdData[rowid2];
        rowCache && defExpandeds.push(rowCache.row);
      });
      this.expandeds = defExpandeds;
    }
  },
  setAllRowExpansion: function setAllRowExpansion(expanded) {
    this.expandeds = !expanded ? [] : this.tableFullData.slice(0);
    return this.$nextTick().then(this.recalculate);
  },
  // 设置展开行，二个参数设置这一行展开与否；支持单行；支持多行
  setRowExpansion: function setRowExpansion(rows, expanded) {
    var expandeds = this.expandeds;
    var _ref167 = this.expandConfig || {}, accordion = _ref167.accordion;
    var isToggle = arguments.length === 1;
    var isAccordionCloseAll = false;
    if (!rows) {
      return this.$nextTick().then(this.recalculate);
    }
    if (!isArray(rows)) {
      rows = [rows];
    }
    if (accordion) {
      rows = rows.slice(rows.length - 1, rows.length);
      if (rows.length && isToggle) {
        isAccordionCloseAll = expandeds.includes(rows[0]);
      }
      expandeds.length = 0;
    }
    rows.forEach(function(row2) {
      var index2 = expandeds.indexOf(row2);
      if (isToggle && ~index2 || !isToggle && !expanded && ~index2) {
        expandeds.splice(index2, 1);
        return;
      }
      if (isToggle && !isAccordionCloseAll && !~index2 || !isToggle && expanded && !~index2) {
        expandeds.push(row2);
      }
    });
    return this.$nextTick().then(this.recalculate);
  },
  hasRowExpand: function hasRowExpand(row2) {
    return ~this.expandeds.indexOf(row2);
  },
  clearRowExpand: function clearRowExpand() {
    var _this34 = this;
    var hasExpand = this.expandeds.length;
    this.expandeds = [];
    return this.$nextTick().then(function() {
      return hasExpand ? _this34.recalculate() : 0;
    });
  },
  // 获取虚拟滚动状态
  getVirtualScroller: function getVirtualScroller() {
    var scrollXLoad = this.scrollXLoad, scrollYLoad = this.scrollYLoad;
    var _this$$refs$tableBody = this.$refs.tableBody.$el, scrollLeft = _this$$refs$tableBody.scrollLeft, scrollTop = _this$$refs$tableBody.scrollTop;
    return {
      scrollX: scrollXLoad,
      scrollY: scrollYLoad,
      scrollLeft,
      scrollTop
    };
  },
  // 横向 X 可视渲染事件处理
  triggerScrollXEvent: function triggerScrollXEvent(event) {
    this.loadScrollXData(event);
  },
  debounceScrollX: function debounceScrollX(event) {
    var _this35 = this;
    if (!this.tasks.debounceScrollX) {
      this.tasks.debounceScrollXHandler = null;
      this.tasks.debounceScrollX = function() {
        return requestAnimationFrame(function() {
          _this35.tasks.debounceScrollXHandler = null;
          _this35.loadScrollXData(event);
        });
      };
    }
    if (this.tasks.debounceScrollXHandler) {
      cancelAnimationFrame(this.tasks.debounceScrollXHandler);
      this.tasks.debounceScrollXHandler = null;
    }
    this.tasks.debounceScrollXHandler = this.tasks.debounceScrollX();
  },
  // 处理x轴滚动时，虚拟滚动数据计算
  loadScrollXData: function loadScrollXData() {
    var scrollXStore = this.scrollXStore, visibleColumn = this.visibleColumn;
    var offsetSize = scrollXStore.offsetSize, renderSize = scrollXStore.renderSize, startIndex = scrollXStore.startIndex, visibleIndex = scrollXStore.visibleIndex, visibleSize = scrollXStore.visibleSize;
    var scrollLeft = this.$refs.tableBody.$el.scrollLeft;
    var _ref168 = {}, _ref168$preload = _ref168.preload, preload = _ref168$preload === void 0 ? false : _ref168$preload, _ref168$toVisibleInde = _ref168.toVisibleIndex, toVisibleIndex = _ref168$toVisibleInde === void 0 ? 0 : _ref168$toVisibleInde, _ref168$width = _ref168.width, width = _ref168$width === void 0 ? 0 : _ref168$width;
    for (var i = 0; i < visibleColumn.length; i++) {
      width += visibleColumn[i].renderWidth;
      if (scrollLeft < width) {
        toVisibleIndex = i;
        break;
      }
    }
    if (visibleIndex === toVisibleIndex) {
      this.clostTooltip();
      return;
    }
    var marginSize = Math.min(Math.floor((renderSize - visibleSize) / 2), visibleSize);
    marginSize = Math.max(0, marginSize);
    if (visibleIndex > toVisibleIndex) {
      preload = startIndex >= toVisibleIndex - offsetSize;
      if (preload) {
        scrollXStore.startIndex = Math.max(0, toVisibleIndex - Math.max(marginSize, renderSize - visibleSize));
        this.updateScrollXData();
      }
    } else {
      preload = startIndex + renderSize <= toVisibleIndex + visibleSize + offsetSize;
      if (preload) {
        scrollXStore.startIndex = Math.max(0, Math.min(visibleColumn.length - renderSize, toVisibleIndex - marginSize));
        this.updateScrollXData();
      }
    }
    scrollXStore.visibleIndex = toVisibleIndex;
    this.clostTooltip();
  },
  // 纵向 Y 可视渲染事件处理
  triggerScrollYEvent: function triggerScrollYEvent(event) {
    this.loadScrollYData(event);
  },
  // 处理滚动分页相关逻辑
  debounceScrollLoad: function debounceScrollLoad(event) {
    var _this36 = this;
    if (!this.tasks.debounceScrollLoad) {
      this.tasks.debounceScrollLoad = debounce(debounceScrollLoadDuration, function() {
        var _this36$scrollLoadSto = _this36.scrollLoadStore, scrollHeight = _this36$scrollLoadSto.scrollHeight, bodyHeight = _this36$scrollLoadSto.bodyHeight;
        var _this36$$grid$tablePa = _this36.$grid.tablePage, currentPage = _this36$$grid$tablePa.currentPage, pageSize = _this36$$grid$tablePa.pageSize;
        var max = scrollHeight - bodyHeight;
        var scrollTop = getActualTarget(event).scrollTop;
        if (scrollTop > max) {
          scrollTop = max;
        }
        var rowHeight = _this36.scrollYStore.rowHeight;
        var visibleIndex = Math.ceil(scrollTop / rowHeight);
        var page = Math.ceil(visibleIndex / pageSize) + 1;
        if (currentPage !== page) {
          _this36.$grid.pageCurrentChange(page);
        }
      });
    }
    this.tasks.debounceScrollLoad();
  },
  // 纵向 Y 可视渲染处理
  loadScrollYData: function loadScrollYData(event) {
    var _this37 = this;
    var scrollYStore = this.scrollYStore;
    var startIndex = scrollYStore.startIndex, renderSize = scrollYStore.renderSize, offsetSize = scrollYStore.offsetSize, visibleIndex = scrollYStore.visibleIndex, visibleSize = scrollYStore.visibleSize, rowHeight = scrollYStore.rowHeight;
    var _getActualTarget = getActualTarget(event), scrollTop = _getActualTarget.scrollTop;
    var toVisibleIndex = Math.ceil(scrollTop / rowHeight);
    var preload = false;
    if (visibleIndex === toVisibleIndex) {
      return;
    }
    var marginSize = Math.min(Math.floor((renderSize - visibleSize) / 2), visibleSize);
    if (toVisibleIndex < visibleIndex) {
      preload = startIndex >= toVisibleIndex - offsetSize;
      if (preload) {
        scrollYStore.startIndex = Math.max(0, toVisibleIndex - Math.max(marginSize, renderSize - visibleSize));
        this.updateScrollYData();
      }
    } else {
      preload = startIndex + renderSize <= toVisibleIndex + visibleSize + offsetSize;
      if (preload) {
        var totalRows = this.getRowCount();
        scrollYStore.startIndex = Math.max(0, Math.min(totalRows - renderSize, toVisibleIndex - marginSize));
        this.updateScrollYData();
      }
    }
    scrollYStore.visibleIndex = toVisibleIndex;
    this.$nextTick(function() {
      _this37.updateSelectedCls(true);
    });
  },
  getRowHeight: function getRowHeight() {
    return this.rowHeight;
  },
  // 计算可视渲染相关数据
  computeScrollLoad: function computeScrollLoad() {
    var _this38 = this;
    return this.$nextTick().then(function() {
      var $refs = _this38.$refs, optimizeOpts = _this38.optimizeOpts, visibleColumn = _this38.visibleColumn;
      var scrollLoad = _this38.scrollLoad, scrollXLoad = _this38.scrollXLoad, scrollXStore = _this38.scrollXStore, scrollYLoad = _this38.scrollYLoad, scrollYStore = _this38.scrollYStore;
      var scrollX = optimizeOpts.scrollX, scrollY = optimizeOpts.scrollY;
      var tableBody = $refs.tableBody;
      var bodyElem = tableBody ? tableBody.$el : null;
      if (bodyElem) {
        computeScrollXLoad({
          _vm: _this38,
          scrollX,
          scrollXLoad,
          scrollXStore,
          tableBodyElem: bodyElem,
          visibleColumn
        });
        computeScrollYLoad({
          _vm: _this38,
          scrollLoad,
          scrollY,
          scrollYLoad,
          scrollYStore,
          tableBodyElem: bodyElem
        });
      }
      _this38.$nextTick(_this38.updateStyle);
    });
  },
  updateScrollXData: function updateScrollXData() {
    var scrollXLoad = this.scrollXLoad, scrollXStore = this.scrollXStore, tableColumn2 = this.tableColumn, treeConfig = this.treeConfig, visibleColumn = this.visibleColumn, visibleColumnChanged = this.visibleColumnChanged, columnStore = this.columnStore;
    var _scrollXStore$lastSta = scrollXStore.lastStartIndex, lastStartIndex = _scrollXStore$lastSta === void 0 ? -1 : _scrollXStore$lastSta, renderSize = scrollXStore.renderSize, startIndex = scrollXStore.startIndex;
    var args = {
      lastStartIndex,
      renderSize,
      scrollXLoad,
      startIndex,
      tableColumn: tableColumn2,
      columnStore
    };
    Object.assign(args, {
      treeConfig,
      visibleColumn,
      visibleColumnChanged
    });
    var ret = sliceVisibleColumn(args);
    this.updateScrollStatus();
    if (ret.sliced) {
      scrollXStore.lastStartIndex = ret.lastStartIndex;
      this.tableColumn = ret.tableColumn;
      this.visibleColumnChanged = ret.visibleColumnChanged;
      this.addedColumn = ret.addedColumn;
      this.$nextTick(this.updateStyle);
    }
  },
  updateScrollYData: function updateScrollYData() {
    var _this39 = this;
    this.updateScrollYSpace();
    this.handleTableData().then(function() {
      return _this39.updateStyle();
    });
  },
  // 更新纵向 Y 可视渲染上下剩余空间大小
  updateScrollYSpace: function updateScrollYSpace() {
    var $grid = this.$grid, elemStore = this.elemStore, scrollLoad = this.scrollLoad, scrollLoadStore = this.scrollLoadStore, headerHeight = this.headerHeight, footerHeight = this.footerHeight, rowHeight = this.rowHeight;
    var totalRows = this.getRowCount();
    var scrollHeight = $grid.pagerConfig ? $grid.pagerConfig.total * rowHeight : 0;
    var ySpaceElem = elemStore["main-body-ySpace"];
    scrollLoadStore.bodyHeight = totalRows * rowHeight + headerHeight + footerHeight;
    scrollLoadStore.scrollHeight = scrollHeight;
    if (ySpaceElem && scrollLoad) {
      this.scrollLoadScrollHeight = scrollHeight;
    }
  },
  updateScrollLoadBar: function updateScrollLoadBar(event) {
    var $el = this.$el, elemStore = this.elemStore, scrollLoad = this.scrollLoad, scrollLoadStore = this.scrollLoadStore;
    if (scrollLoad && $el.contains(getActualTarget(event))) {
      var wheelDelta = event.wheelDelta ? event.wheelDelta : -event.detail * 40;
      var scrollElm = elemStore["main-body-ySpace"];
      var scrollHeight = scrollLoadStore.scrollHeight, bodyHeight = scrollLoadStore.bodyHeight;
      var max = scrollHeight - bodyHeight;
      var top = scrollElm.scrollTop - wheelDelta;
      top = max < top ? max : top;
      top = top < 0 ? 0 : top;
      scrollElm.scrollTop = top;
    }
  },
  scrollTo: function scrollTo(scrollLeft, scrollTop) {
    var elemStore = this.elemStore;
    var tableBodyElem = elemStore["main-body-wrapper"];
    if (isNumber(scrollLeft)) {
      tableBodyElem && (tableBodyElem.scrollLeft = scrollLeft);
    }
    if (isNumber(scrollTop)) {
      tableBodyElem && (tableBodyElem.scrollTop = scrollTop);
    }
    return this.$nextTick();
  },
  scrollToRow: function scrollToRow(row2, column, isDelay, move) {
    var hasRowCache = this.fullDataRowMap.has(this.getRaw(row2));
    var isDelayArg = isDelay || isBoolean(column);
    row2 && hasRowCache && rowToVisible(this, row2);
    return this.scrollToColumn(column, isDelayArg, move);
  },
  scrollToTreeRow: function scrollToTreeRow(row2) {
    var _this40 = this;
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    if (!treeConfig) {
      return this.$nextTick();
    }
    var matchObj = findTree$1(tableFullData, function(item) {
      return item === row2;
    }, treeConfig);
    if (!matchObj) {
      return this.$nextTick();
    }
    var nodes = matchObj.nodes;
    nodes.forEach(function(row22, index2) {
      if (index2 === nodes.length - 1 || _this40.hasTreeExpand(row22)) {
        return;
      }
      _this40.setTreeExpansion(row22, true);
    });
    return this.$nextTick();
  },
  scrollToColumn: function scrollToColumn(column, isDelay, move) {
    var _this41 = this;
    var hasColCache = this.fullColumnMap.has(column);
    column && hasColCache && colToVisible(this, column, move);
    return isDelay && (this.scrollXLoad || this.scrollYLoad) ? new Promise(function(resolve) {
      return setTimeout(function() {
        return resolve(_this41.$nextTick());
      }, 50);
    }) : this.$nextTick();
  },
  resetScrollTop: function resetScrollTop() {
    this.lastScrollTop = 0;
  },
  clearScroll: function clearScroll() {
    var _this42 = this;
    var $grid = this.$grid, scrollXStore = this.scrollXStore, scrollYStore = this.scrollYStore, elemStore = this.elemStore;
    var _$grid$fetchOption = $grid.fetchOption, fetchOption = _$grid$fetchOption === void 0 ? {} : _$grid$fetchOption;
    var _fetchOption$isReload2 = fetchOption.isReloadScroll, isReloadScroll = _fetchOption$isReload2 === void 0 ? false : _fetchOption$isReload2;
    if (isReloadScroll) {
      return;
    }
    Object.assign(this, {
      lastScrollLeft: 0,
      lastScrollTop: 0
    });
    Object.assign(scrollXStore, {
      startIndex: 0,
      visibleIndex: 0
    });
    Object.assign(scrollYStore, {
      startIndex: 0,
      visibleIndex: 0
    });
    return this.$nextTick(function() {
      var tableBodyElem = elemStore["main-body-wrapper"];
      if (_this42.afterMounted) {
        fastdom.mutate(function() {
          tableBodyElem && Object.assign(tableBodyElem, {
            scrollLeft: 0,
            scrollTop: 0
          });
        });
      }
    });
  },
  // 更新表尾合计
  updateFooter: function updateFooter() {
    var afterFullData = this.afterFullData, footerMethod = this.footerMethod, showFooter = this.showFooter, summaryConfig = this.summaryConfig, tableColumn2 = this.tableColumn, rowHeight = this.rowHeight;
    if (!isArray(afterFullData) || afterFullData.length <= 0 || !isArray(tableColumn2) || tableColumn2.length <= 0) {
      return;
    }
    if (footerMethod && showFooter) {
      var data7 = footerMethod({
        columns: tableColumn2,
        data: afterFullData
      });
      if (data7.length && data7.some(function(value) {
        return !isArray(value);
      })) {
        data7 = [data7];
      }
      this.footerData = tableColumn2.length ? data7 : [];
    }
    if (summaryConfig) {
      var fields = summaryConfig.fields, fraction = summaryConfig.fraction, text = summaryConfig.text, _truncate = summaryConfig.truncate;
      var summary = tableColumn2.map(function(column, columnIndex) {
        if (columnIndex === 0) {
          return text || "";
        }
        if (~fields.indexOf(column.property)) {
          return toDecimal(sum(afterFullData, column.property), fraction, _truncate);
        }
        return null;
      });
      this.footerData = [summary];
    }
    this.footerHeight = this.footerData.length * rowHeight;
    return this.$nextTick();
  },
  // 更新列状态：如果组件值v-model发生change，调用该函数更新列的编辑状态。如果单元格配置了校验规则，则进行校验
  updateStatus: function updateStatus(scope, cellValue, renderOpts) {
    var _this43 = this;
    var $refs = this.$refs, editRules = this.editRules, tableData = this.tableData, validStore = this.validStore;
    var tableBody = $refs.tableBody;
    if (!scope || !tableBody) {
      return this.$nextTick();
    }
    var column = scope.column, row2 = scope.row;
    var type = "change";
    var refreshStatus = function refreshStatus2() {
      if (!isUndefined(cellValue)) {
        var _this43$$refs$tableBo;
        _this43.updateRowStatus(row2);
        (_this43$$refs$tableBo = _this43.$refs.tableBody) == null ? void 0 : _this43$$refs$tableBo.$forceUpdate();
      }
    };
    if (!editRules || !this.hasCellRules(type, row2, column)) {
      refreshStatus();
      return this.$nextTick();
    }
    if (renderOpts && renderOpts.isValidAlways) {
      validStore.visible = true;
    }
    var rowIndex = tableData.indexOf(row2);
    getCell(this, {
      row: row2,
      rowIndex,
      column
    }).then(function(cell) {
      if (!cell) {
        return;
      }
      return _this43.validCellRules(type, row2, column, cellValue).then(function() {
        refreshStatus();
        _this43.clearValidate();
      }).catch(function(_ref169) {
        var rule = _ref169.rule;
        refreshStatus();
        _this43.showValidTooltip({
          rule,
          row: row2,
          column,
          cell
        });
      });
    });
    return this.$nextTick();
  },
  /* X/Y 方向滚动 */
  updateScrollStatus: function updateScrollStatus() {
    var _this44 = this;
    if (!this.tasks.updateScrollStatus) {
      this.tasks.updateScrollStatus = debounce(AsyncCollectTimeout, function() {
        var scrollXLoad = _this44.scrollXLoad, scrollYLoad = _this44.scrollYLoad, isAsyncColumn = _this44.isAsyncColumn;
        if (isAsyncColumn && (scrollXLoad || scrollYLoad)) {
          var tableData = _this44.tableData, scrollXStore = _this44.scrollXStore, scrollYStore = _this44.scrollYStore, tableFullData = _this44.tableFullData, _this44$scrollDirecti = _this44.scrollDirection, scrollDirection = _this44$scrollDirecti === void 0 ? "N" : _this44$scrollDirecti;
          var isInit = scrollXLoad && scrollXStore.visibleIndex === 0 || scrollYLoad && scrollYStore.visibleIndex === 0;
          if (isInit || scrollDirection !== "N") {
            _this44.handleResolveColumn(tableFullData, _this44.collectAsyncColumn(tableData));
          }
        }
      });
    }
    this.tasks.updateScrollStatus();
  },
  // 获取异步列唯一ID
  getAsyncColumnUniqueKey: function getAsyncColumnUniqueKey(property, row2) {
    return property + "_" + row2[this.rowId];
  },
  // 获取异步列名称
  getAsyncColumnName: function getAsyncColumnName(property) {
    return GlobalConfig$1.constant.asyncPrefix + property;
  },
  // 收集异步列
  collectAsyncColumn: function collectAsyncColumn(tableData) {
    var _this45 = this;
    var fetchColumns = [];
    var rowId = this.rowId, asyncRenderMap = this.asyncRenderMap, tableColumn2 = this.tableColumn;
    if (!rowId) {
      warn("The (grid-props:rowId) is required for the asynchronous column.");
      return fetchColumns;
    }
    tableColumn2.forEach(function(col) {
      var _ref170 = col.format || {}, async = _ref170.async;
      var _ref171 = async || {}, fetch = _ref171.fetch, _ref171$splitConfig = _ref171.splitConfig, splitConfig = _ref171$splitConfig === void 0 ? {} : _ref171$splitConfig;
      if (typeof fetch === "function") {
        var columnValues = [];
        tableData.forEach(function(row2) {
          var cellValue = row2[col.property];
          if (typeof cellValue !== "string" || typeof cellValue === "string" && !cellValue) {
            cellValue = " ";
          }
          var cellValuesCount = 1;
          var cellValues = [cellValue];
          var uniqueKey = _this45.getAsyncColumnUniqueKey(col.property, row2);
          if (splitConfig.enabled === true) {
            cellValues = cellValue.split(splitConfig.valueSplit || ",");
            cellValuesCount = cellValues.length;
          }
          if (!asyncRenderMap[uniqueKey]) {
            asyncRenderMap[uniqueKey] = cellValuesCount;
            cellValues.forEach(function(value) {
              return columnValues.push(value);
            });
          }
        });
        if (columnValues.length) {
          fetchColumns.push(_extends({}, col, {
            columnValues
          }));
        }
      }
    });
    return fetchColumns;
  },
  // fetchData 执行
  handleAsyncColumn: function handleAsyncColumn(tableData) {
    if (this.isAsyncColumn && tableData.length) {
      this.asyncRenderMap = {};
      this.handleResolveColumn(tableData, this.collectAsyncColumn(tableData));
    }
  },
  // 查询异步列
  handleResolveColumn: function handleResolveColumn(tableData, fetchColumns) {
    var tableColumn2 = this.tableColumn, scrollYStore = this.scrollYStore, asyncRenderMap = this.asyncRenderMap, scrollXLoad = this.scrollXLoad, scrollYLoad = this.scrollYLoad;
    var startIndex = scrollYStore.startIndex;
    var isScrollLoad = scrollXLoad || scrollYLoad;
    if (fetchColumns.length === 0) {
      return;
    }
    var promises = mapFetchColumnPromise({
      _vm: this,
      fetchColumns,
      tableColumn: tableColumn2
    });
    Promise.all(promises).then(handleAllColumnPromises({
      startIndex,
      fetchColumns,
      tableData,
      asyncRenderMap,
      isScrollLoad
    }, this));
  },
  // Publish methods 与工具栏对接
  connect: function connect(_ref172) {
    var toolbar = _ref172.toolbar;
    this.$toolbar = toolbar;
  },
  // 检查触发源是否属于目标节点
  getEventTargetNode,
  // 可见性改变事件处理
  handleVisibilityChange: function handleVisibilityChange(visible3, entry) {
    this._tableVisible = visible3;
    if (visible3) {
      var _this$$grid$_delayAct, _this$$grid2;
      (_this$$grid$_delayAct = (_this$$grid2 = this.$grid)._delayActivateAnchor) == null ? void 0 : _this$$grid$_delayAct.call(_this$$grid2);
      this.updateParentHeight();
      this.recalculate();
    }
    emitEvent(this, "visible-change", [{
      $table: this,
      visible: visible3,
      entry
    }]);
  },
  // 按顺序切换列的排序状态（null --> asc --> desc --> null --> ...）
  toggleColumnOrder: function toggleColumnOrder(column) {
    return column.order ? column.order === "asc" ? "desc" : null : "asc";
  },
  handleDataChange: function handleDataChange() {
    if (Array.isArray(this.rawData)) {
      !this._isUpdateData && this.loadTableData(this.rawData);
      this._isUpdateData = false;
    }
  },
  getVm: function getVm(name) {
    return this.$grid.getVm(name);
  },
  assembleColumns: function assembleColumns() {
    if (!this.isTagUsageSence) return;
    assemColumn(this);
  },
  isValidCustomColumn: function isValidCustomColumn(columnName) {
    return columnName && this.columnNames.includes(columnName);
  },
  computeCollectKey: function computeCollectKey() {
    var columnIds = [];
    var _traverse2 = function traverse(columns2) {
      if (Array.isArray(columns2) && columns2.length > 0) {
        columns2.forEach(function(column) {
          columnIds.push(column.columnConfig.id);
          _traverse2(column.childColumns);
        });
      }
    };
    _traverse2(this.childColumns);
    return columnIds.join(",");
  },
  // 获取所有多选数据状态
  getAllSelection: function getAllSelection() {
    return this.selection;
  },
  // 尝试恢复滚动位置，规范了最大滚动位置的取值
  attemptRestoreScroll: function attemptRestoreScroll(options) {
    var _this46 = this;
    var _ref173 = options || this, lastScrollTop = _ref173.lastScrollTop, lastScrollLeft = _ref173.lastScrollLeft;
    var scrollXLoad = this.scrollXLoad, scrollYLoad = this.scrollYLoad, elemStore = this.elemStore;
    var tableBodyElem = elemStore["main-body-wrapper"];
    if ((lastScrollTop || lastScrollLeft) && tableBodyElem) {
      fastdom.measure(function() {
        var maxScrollTop = tableBodyElem.scrollHeight - tableBodyElem.offsetHeight;
        var maxScrollLeft = tableBodyElem.scrollWidth - tableBodyElem.offsetWidth;
        lastScrollTop = Math.min(lastScrollTop, maxScrollTop);
        lastScrollLeft = Math.min(lastScrollLeft, maxScrollLeft);
        fastdom.mutate(function() {
          _this46.restoreScollFlag = true;
          _this46.scrollTo(lastScrollLeft, lastScrollTop);
          requestAnimationFrame(function() {
            var _this46$$refs$tableBo;
            return (_this46$$refs$tableBo = _this46.$refs.tableBody) == null ? void 0 : _this46$$refs$tableBo.resetStickyWrapperScrollPos();
          });
          scrollXLoad && _this46.triggerScrollXEvent();
          scrollYLoad && _this46.triggerScrollYEvent({
            target: {
              scrollTop: lastScrollTop
            }
          });
        });
      });
    }
    return this.$nextTick();
  },
  updateRowStatus: function updateRowStatus2(row2) {
    _updateRowStatus(this, row2);
  },
  getCellStatus: function getCellStatus2(row2, column) {
    return _getCellStatus(this, row2, column);
  }
};
funcs.forEach(function(name) {
  Methods$d[name] = function() {
    return this["_" + name] ? this["_" + name].apply(this, arguments) : null;
  };
});
var fnField = function fnField2(field) {
  return typeof field === "string" ? field : field[0];
};
var fnFields = function fnFields2(fields) {
  return fields.map(fnField);
};
var fnFieldConfig = function fnFieldConfig2(field) {
  return Array.isArray(field) && field.length > 1 ? field[1] : {};
};
var _sfc_main$9 = defineComponent({
  props: _extends({}, $props, {
    datas: Object,
    row: Object
  }),
  render: function render7() {
    var datas = this.datas, row2 = this.row;
    var selectionColumn = datas.selectionColumn, genParams2 = datas.genParams, config2 = datas.config;
    var params = genParams2(selectionColumn, row2);
    var tableVm = config2.tableVm;
    if (~tableVm.selection.indexOf(row2)) ;
    var _tableVm$selectConfig = tableVm.selectConfig, selectConfig = _tableVm$selectConfig === void 0 ? {} : _tableVm$selectConfig;
    var checkMethod = selectConfig.checkMethod;
    if (typeof checkMethod === "function" && !checkMethod(params)) ;
    return h("div", {
      attrs: {
        "data-tag": "tiny-table-column-type"
      },
      class: "py-px"
    }, selectionColumn.renderCell(h, params));
  }
});
var _sfc_main$8 = defineComponent({
  inject: ["$mftable"],
  props: _extends({}, $props, {
    datas: Object,
    row: Object
  }),
  render: function render8() {
    var _this47 = this;
    var datas = this.datas, row2 = this.row;
    var primaryColumn = datas.primaryColumn, contentColumns = datas.contentColumns, genParams2 = datas.genParams, config2 = datas.config, cardView2 = datas.cardView;
    var _config$cardConfig = config2.cardConfig, cardConfig = _config$cardConfig === void 0 ? {} : _config$cardConfig;
    var props = {
      attrs: {
        "data-tag": "tiny-table-column-content"
      },
      class: "my-0.5 truncate space-y-2 sm:space-y-1 [&_[data-tag=tiny-input]]:leading-7 [&_[data-tag=tiny-input-textarea]>span]:pt-0"
    };
    var ons = function ons2() {
      return {
        mouseenter: _this47.handleMouseenter,
        mouseleave: _this47.handleMouseleave
      };
    };
    if (!primaryColumn || !primaryColumn.renderCell) {
      return h("div", props, []);
    }
    var primaryVnode = null;
    var contentVnodes = [];
    if (cardView2) {
      var hasLogo = typeof cardConfig.logoField !== "undefined";
      var hasTag = false;
      var tagVnodes = [];
      var logoField = "";
      var logoFieldConfig = {};
      var logoVnode = null;
      if (cardConfig.tagFields && cardConfig.tagFields.length) {
        var tagFields = cardConfig.tagFields;
        hasTag = true;
        tagVnodes = fnFields(tagFields).map(function(field, i) {
          if (!row2[field] || typeof row2[field] !== "string") {
            return null;
          }
          var fieldConfig = fnFieldConfig(tagFields[i]);
          var colorField = "";
          if (cardConfig.tagColorFields && cardConfig.tagColorFields.length && cardConfig.tagColorFields[i]) {
            colorField = fnField(cardConfig.tagColorFields[i]);
          }
          var type = row2[colorField] || "";
          var tagProps = {
            props: {
              size: "mini",
              customClass: "inline-block truncate max-w-[theme(spacing.28)] h-4.5"
            }
          };
          if (type) {
            Object.assign(tagProps.props, {
              type
            });
          }
          if (fieldConfig.render) {
            return fieldConfig.render({
              h,
              row: row2,
              field,
              config: fieldConfig,
              value: row2[field],
              color: type
            });
          }
          return h(Tag, tagProps, row2[field] || "");
        });
      }
      if (cardConfig.logoField) {
        logoField = fnField(cardConfig.logoField);
        logoFieldConfig = fnFieldConfig(cardConfig.logoField);
      }
      var gridCls = ["grid mb-3", hasLogo ? "grid-cols-[theme(spacing.11)_auto]" : "grid-cols-[auto]"];
      var logoCls = "w-11 h-11 overflow-hidden rounded";
      var primCls = ["text-base sm:text-sm text-color-text-primary font-medium leading-5", "overflow-hidden text-ellipsis whitespace-normal line-clamp-2 sm:line-clamp-1", {
        "ml-3": hasLogo
      }];
      var tagCls = ["truncate h-4.5", {
        "ml-3": hasLogo
      }];
      if (logoFieldConfig.render) {
        var params = {
          h,
          row: row2,
          field: logoField,
          config: logoFieldConfig,
          value: row2[logoField]
        };
        logoVnode = logoFieldConfig.render(params);
      } else {
        logoVnode = h("img", {
          class: "w-full h-full",
          attrs: {
            src: row2[logoField] || ""
          }
        });
      }
      primaryVnode = h("div", {
        class: gridCls
      }, [hasLogo ? h("div", {
        class: logoCls
      }, [logoVnode]) : null, h("div", {
        class: "truncate space-y-1 relative -top-0.5"
      }, [h("div", {
        class: primCls,
        on: ons()
      }, [primaryColumn.renderCell(h, genParams2(primaryColumn, row2))]), hasTag ? h("div", {
        class: tagCls
      }, tagVnodes) : null])]);
    } else {
      var cls = ["w-full text-base sm:text-sm text-color-text-primary font-medium leading-5", "overflow-hidden text-ellipsis whitespace-normal line-clamp-2 sm:line-clamp-1"];
      primaryVnode = h("div", {
        class: cls,
        on: ons()
      }, [primaryColumn.renderCell(h, genParams2(primaryColumn, row2))]);
    }
    contentVnodes = contentColumns.map(function(contentColumn) {
      var params2 = genParams2(contentColumn, row2);
      var cls2 = ["w-full text-sm sm:text-xs text-color-fill-9 font-normal sm:font-normal leading-5 sm:leading-5", "overflow-hidden text-ellipsis whitespace-normal "];
      var cellNode = Cell.renderCell(h, params2);
      var cellIsVnode = false;
      if (Array.isArray(cellNode)) {
        cellIsVnode = isVnode(cellNode[0]);
      }
      var VnodeCls = cellIsVnode ? "inline-block flex-grow [&_span]:leading-5 [&_div[data-tag=tiny-input]]:leading-5 ml-1" : "text-color-text-primary break-all";
      var divCls = cellIsVnode ? "flex" : "line-clamp-2 sm:line-clamp-1";
      return h("div", {
        class: cls2 + divCls,
        on: ons()
      }, [
        // 这里需要兼容AUI与TinyVue表头实现方式的差异，此处勿同步
        h("span", {
          class: "shrink-0"
        }, [Cell.renderHeader(h, params2, "card")]),
        h("span", {}, cardConfig.split || ": "),
        h("span", {
          class: VnodeCls
        }, [cellNode])
      ]);
    });
    return h("div", props, [primaryVnode].concat(contentVnodes));
  },
  methods: {
    handleMouseenter: function handleMouseenter(e) {
      var $mftable = this.$mftable;
      $mftable.showTooltip(e);
    },
    handleMouseleave: function handleMouseleave() {
      var $mftable = this.$mftable;
      $mftable.hideTooltip();
    }
  }
});
var _sfc_main$7 = defineComponent({
  props: _extends({}, $props, {
    datas: Object,
    row: Object
  }),
  render: function render9() {
    var datas = this.datas, row2 = this.row;
    var slotLink = datas.slotLink, operationColumn = datas.operationColumn, hasOperation = datas.hasOperation, genParams2 = datas.genParams;
    var params = genParams2(operationColumn, row2);
    var vnode;
    if (hasOperation) {
      var _operationColumn$oper = operationColumn.operationConfig, operationConfig = _operationColumn$oper === void 0 ? {} : _operationColumn$oper;
      var _operationConfig$butt2 = operationConfig.buttons, buttons = _operationConfig$butt2 === void 0 ? [] : _operationConfig$butt2, _operationConfig$disa2 = operationConfig.disabledClass, disabledClass = _operationConfig$disa2 === void 0 ? "" : _operationConfig$disa2;
      var visibleButtons = [];
      var isDisabled = function isDisabled2(buttonConfig) {
        var _buttonConfig$disable2 = buttonConfig.disabled, disabled = _buttonConfig$disable2 === void 0 ? false : _buttonConfig$disable2;
        return typeof disabled === "boolean" && disabled || typeof disabled === "function" && disabled(row2);
      };
      var isHidden = function isHidden2(buttonConfig) {
        var _buttonConfig$hidden2 = buttonConfig.hidden, hidden = _buttonConfig$hidden2 === void 0 ? false : _buttonConfig$hidden2;
        return typeof hidden === "boolean" && hidden || typeof hidden === "function" && hidden(row2);
      };
      var handleItemClick = function handleItemClick2(name) {
        var buttonConfig = visibleButtons.find(function(_ref174) {
          var buttonName = _ref174.name;
          return buttonName === name;
        });
        buttonConfig.click(window.event || {}, _extends({
          buttonConfig
        }, params));
      };
      buttons.forEach(function(buttonConfig) {
        return !isHidden(buttonConfig) && visibleButtons.push(buttonConfig);
      });
      if (visibleButtons.length === 0) {
        vnode = null;
      } else if (visibleButtons.length === 1) {
        vnode = h("div", {
          attrs: {
            title: visibleButtons[0].name
          },
          "class": "w-5 h-5 sm:w-4 sm:w-4 " + (isDisabled(visibleButtons[0]) ? "fill-color-icon-disabled" : "fill-color-icon-secondary") + " "
        }, [h(visibleButtons[0].icon, {
          on: {
            click: function click(event) {
              if (!isDisabled(visibleButtons[0])) {
                handleItemClick(visibleButtons[0].name);
              }
              event.stopPropagation();
            }
          },
          "attrs": {
            "custom-class": "w-5 h-5 sm:w-4 sm:w-4"
          }
        })]);
      } else {
        var scopedSlots = {
          default: function _default29() {
            return h(IconEllipsis(), {
              class: "mf-table-more outline-none text-base cursor-pointer"
            });
          },
          dropdown: function dropdown() {
            return h(DropdownMenu, {
              slot: "dropdown"
            }, visibleButtons.map(function(buttonConfig) {
              var _class3;
              return h(DropdownItem, {
                class: (_class3 = {}, _class3[disabledClass || ""] = isDisabled(buttonConfig), _class3),
                props: {
                  itemData: buttonConfig.name,
                  disabled: isDisabled(buttonConfig),
                  icon: buttonConfig.icon
                }
              }, buttonConfig.name);
            }));
          }
        };
        vnode = h(Dropdown, {
          on: {
            "item-click": handleItemClick
          },
          props: {
            trigger: "hover",
            showSelfIcon: true,
            tiny_mode: "mobile-first",
            tiny_mode_root: true
          },
          scopedSlots
        });
      }
    } else {
      vnode = slotLink({
        row: row2,
        h
      });
    }
    return h("div", {
      attrs: {
        "data-tag": "tiny-table-column-link"
      },
      class: "flex flex-col items-end mt-px sm:mt-0"
    }, [vnode]);
  }
});
var _sfc_main$6 = defineComponent({
  props: _extends({}, $props, {
    datas: Object,
    row: Object
  }),
  components: {
    ColumnType: _sfc_main$9,
    ColumnContent: _sfc_main$8,
    ColumnLink: _sfc_main$7
  },
  computed: {
    showLink: function showLink() {
      var datas = this.datas;
      return datas.hasLink || datas.hasOperation;
    }
  }
});
function _sfc_render$2(_ctx, _cache, $props2, $setup, $data, $options) {
  var _component_column_type = resolveComponent("column-type");
  var _component_column_content = resolveComponent("column-content");
  var _component_column_link = resolveComponent("column-link");
  return openBlock(), createElementBlock(
    "div",
    {
      ref: "layout",
      "data-tag": "tiny-table-column-layout",
      class: normalizeClass(["grid", [_ctx.datas.hasType && _ctx.showLink ? "grid-cols-[theme(spacing.7)_auto_theme(spacing.7)]" : "", _ctx.datas.hasType && !_ctx.showLink ? "grid-cols-[theme(spacing.7)_auto]" : "", !_ctx.datas.hasType && _ctx.showLink ? "grid-cols-[auto_theme(spacing.7)]" : "", !_ctx.datas.hasType && !_ctx.showLink ? "grid-cols-[auto]" : ""]])
    },
    [_ctx.datas.hasType ? (openBlock(), createBlock(_component_column_type, {
      key: 0,
      ref: "type",
      datas: _ctx.datas,
      row: _ctx.row
    }, null, 8, ["datas", "row"])) : createCommentVNode("v-if", true), createVNode(_component_column_content, {
      datas: _ctx.datas,
      row: _ctx.row
    }, null, 8, ["datas", "row"]), _ctx.showLink ? (openBlock(), createBlock(_component_column_link, {
      key: 1,
      ref: "link",
      datas: _ctx.datas,
      row: _ctx.row
    }, null, 8, ["datas", "row"])) : createCommentVNode("v-if", true)],
    2
    /* CLASS */
  );
}
var ColumnLayout = /* @__PURE__ */ _export_sfc(_sfc_main$6, [["render", _sfc_render$2]]);
var _sfc_main$5 = defineComponent({
  components: {
    ColumnLayout
  },
  props: _extends({}, $props, {
    datas: Object,
    currentRowId: String
  }),
  emits: ["card-click"],
  data: function data2() {
    return {
      mergeClass
    };
  },
  computed: {
    row: function row() {
      var datas = this.datas;
      return datas.row;
    },
    rowid: function rowid() {
      var datas = this.datas;
      var config2 = datas.config, row2 = datas.row;
      var tableVm = config2.tableVm;
      return getRowid(tableVm, row2);
    },
    isCardType: function isCardType() {
      var _config$tableVm;
      var datas = this.datas;
      var config2 = datas.config;
      var _config$tableVm$$grid = config2 == null ? void 0 : (_config$tableVm = config2.tableVm) == null ? void 0 : _config$tableVm.$grid, viewType2 = _config$tableVm$$grid.viewType;
      return viewType2 === "card";
    }
  },
  methods: {
    handleClick: function handleClick(e) {
      if (!getEventTargetNode(e, this.$el, "tiny-grid-checkbox").flag && !getEventTargetNode(e, this.$el, "mf-table-more").flag) {
        this.$emit("card-click", this.row, e);
      }
    }
  }
});
var _hoisted_1 = ["data-rowid"];
function _sfc_render$1(_ctx, _cache, $props2, $setup, $data, $options) {
  var _component_column_layout = resolveComponent("column-layout");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(_ctx.mergeClass(_ctx.datas.rowClass, _ctx.currentRowId === _ctx.rowid && _ctx.isCardType ? "border sm:border-2 sm:border-color-brand" : "")),
    "data-tag": "tiny-table-row",
    "data-rowid": _ctx.rowid,
    onClick: _cache[0] || (_cache[0] = function() {
      return _ctx.handleClick && _ctx.handleClick.apply(_ctx, arguments);
    })
  }, [createVNode(_component_column_layout, {
    datas: _ctx.datas,
    row: _ctx.row
  }, null, 8, ["datas", "row"])], 10, _hoisted_1);
}
var TableRow = /* @__PURE__ */ _export_sfc(_sfc_main$5, [["render", _sfc_render$1]]);
var _sfc_main$4 = defineComponent({
  inject: ["$mftable"],
  props: _extends({}, $props),
  render: function render10() {
    var $mftable = this.$mftable;
    var params = {
      $table: $mftable.config.tableVm,
      $mftable,
      config: $mftable.listConfig,
      rows: $mftable.tableData,
      columns: $mftable.config.tableVm.tableColumn,
      h
    };
    return typeof $mftable.slotList === "function" ? $mftable.slotList(params) : null;
  }
});
var _sfc_main$3 = defineComponent({
  inject: ["$mftable"],
  props: _extends({}, $props),
  render: function render11() {
    var $mftable = this.$mftable;
    var params = {
      $table: $mftable.config.tableVm,
      $mftable,
      config: $mftable.ganttConfig,
      rows: $mftable.tableData,
      columns: $mftable.config.tableVm.tableColumn,
      h
    };
    return typeof $mftable.slotGantt === "function" ? $mftable.slotGantt(params) : null;
  }
});
var _sfc_main$2 = defineComponent({
  inject: ["$mftable"],
  props: _extends({}, $props),
  render: function render12() {
    var $mftable = this.$mftable;
    var params = {
      $table: $mftable.config.tableVm,
      $mftable,
      config: $mftable.customConfig,
      rows: $mftable.tableData,
      columns: $mftable.config.tableVm.tableColumn,
      h
    };
    return typeof $mftable.slotCustom === "function" ? $mftable.slotCustom(params) : null;
  }
});
var _sfc_main$1 = defineComponent({
  inject: ["$mftable"],
  props: _extends({}, $props),
  render: function render13() {
    var $mftable = this.$mftable;
    var slotEmpty = $mftable.slotEmpty;
    var renderEmpty = $mftable.renderEmpty;
    var $table = $mftable.config.tableVm;
    var content = null;
    if (typeof slotEmpty === "function") {
      content = slotEmpty({
        $table
      });
    } else if (typeof renderEmpty === "function") {
      content = renderEmpty(h, $table);
    }
    return h("div", {
      attrs: {
        "data-tag": "tiny-grid-empty-wrapper"
      },
      class: "flex t-0 justify-center items-center w-full h-full bg-transparent sm:bg-color-bg-1 text-center"
    }, [content]);
  }
});
var _sfc_main = defineComponent({
  components: {
    TableRow,
    Tooltip: Tooltip$1,
    Exception,
    ListView: _sfc_main$4,
    GanttView: _sfc_main$3,
    CustomView: _sfc_main$2,
    CustomEmpty: _sfc_main$1
  },
  provide: function provide() {
    return {
      $mftable: this
    };
  },
  props: {
    tableData: Array,
    cardConfig: Object,
    listConfig: Object,
    ganttConfig: Object,
    customConfig: Object
  },
  data: function data3() {
    return {
      primaryColumn: null,
      contentColumns: null,
      operationColumn: null,
      selectionColumn: null,
      slotLink: null,
      rowKey: 0,
      wrapperHeight: 0,
      tooltipVisible: false,
      tooltipContent: "",
      currentRow: null,
      currentRowId: ""
    };
  },
  computed: {
    config: function config() {
      var vm = this.$parent, cardConfig = this.cardConfig, listConfig = this.listConfig;
      return {
        tableVm: hooks.markRaw(vm),
        cardConfig,
        listConfig
      };
    },
    cardView: function cardView() {
      var _config$tableVm2;
      var config2 = this.config;
      var _config$tableVm$$grid2 = config2 == null ? void 0 : (_config$tableVm2 = config2.tableVm) == null ? void 0 : _config$tableVm2.$grid, viewType2 = _config$tableVm$$grid2.viewType, mfShow = _config$tableVm$$grid2.mfShow;
      var _GlobalConfig$1$viewC = GlobalConfig$1.viewConfig, CARD = _GlobalConfig$1$viewC.CARD, MF = _GlobalConfig$1$viewC.MF, MF_SHOW_CARD = _GlobalConfig$1$viewC.MF_SHOW_CARD;
      return viewType2 === CARD || viewType2 === MF && mfShow === MF_SHOW_CARD;
    },
    listView: function listView() {
      var _config$tableVm3;
      var config2 = this.config;
      var _config$tableVm$$grid3 = config2 == null ? void 0 : (_config$tableVm3 = config2.tableVm) == null ? void 0 : _config$tableVm3.$grid, viewType2 = _config$tableVm$$grid3.viewType;
      var LIST = GlobalConfig$1.viewConfig.LIST;
      return viewType2 === LIST;
    },
    ganttView: function ganttView() {
      var _config$tableVm4;
      var config2 = this.config;
      var _config$tableVm$$grid4 = config2 == null ? void 0 : (_config$tableVm4 = config2.tableVm) == null ? void 0 : _config$tableVm4.$grid, viewType2 = _config$tableVm$$grid4.viewType;
      var GANTT = GlobalConfig$1.viewConfig.GANTT;
      return viewType2 === GANTT;
    },
    customView: function customView() {
      var _config$tableVm5;
      var config2 = this.config;
      var _config$tableVm$$grid5 = config2 == null ? void 0 : (_config$tableVm5 = config2.tableVm) == null ? void 0 : _config$tableVm5.$grid, viewType2 = _config$tableVm$$grid5.viewType;
      var CUSTOM = GlobalConfig$1.viewConfig.CUSTOM;
      return viewType2 === CUSTOM;
    },
    wrapperClass: function wrapperClass() {
      var _config$tableVm6;
      var config2 = this.config;
      return mergeClass("w-full h-full overflow-y-auto", config2 == null ? void 0 : (_config$tableVm6 = config2.tableVm) == null ? void 0 : _config$tableVm6.viewCls("mfTable"), this.exceptionVisible ? "sm:border sm:border-solid sm:border-color-border-separator rounded-sm" : "");
    },
    tableClass: function tableClass() {
      var cardView2 = this.cardView;
      var tableCls = "w-full px-3.5 sm:px-0";
      return cardView2 ? mergeClass(tableCls, "grid gap-3") : tableCls;
    },
    cardClass: function cardClass() {
      var listView2 = this.listView, ganttView2 = this.ganttView, cardConfig = this.cardConfig, customView2 = this.customView;
      var smallCls = "grid-cols-[repeat(auto-fill,minmax(theme(spacing.64),1fr))]";
      var defaultCls = "grid-cols-[repeat(auto-fill,minmax(theme(spacing.80),1fr))]";
      return listView2 || ganttView2 || customView2 ? "" : (cardConfig == null ? void 0 : cardConfig.cardSize) === "small" ? smallCls : defaultCls;
    },
    rowClass: function rowClass() {
      var cardView2 = this.cardView, cardConfig = this.cardConfig;
      var rowCls = "border-b-0.5 sm:border-b border-solid border-color-border-separator last:border-color-bg-1 py-3";
      if (cardView2) {
        rowCls = "border-0 sm:border border-solid border-color-border-separator p-3 rounded hover:shadow-lg bg-color-bg-1";
        rowCls += (cardConfig == null ? void 0 : cardConfig.cardSize) === "small" ? " min-w-[theme(spacing.64)]" : " min-w-[theme(spacing.80)]";
      }
      return rowCls;
    },
    wrapperStyle: function wrapperStyle() {
      var _config$tableVm7, _config$tableVm7$$gri, _config$tableVm8, _config$tableVm8$$gri;
      var config2 = this.config, wrapperHeight = this.wrapperHeight;
      var defaultView = (config2 == null ? void 0 : (_config$tableVm7 = config2.tableVm) == null ? void 0 : (_config$tableVm7$$gri = _config$tableVm7.$grid) == null ? void 0 : _config$tableVm7$$gri.viewType) === GlobalConfig$1.viewConfig.DEFAULT;
      var displayStyle = defaultView ? "display:none;" : "";
      var heightStyle = wrapperHeight ? "height:" + wrapperHeight + "px;" : "";
      var maxHeight = config2 == null ? void 0 : (_config$tableVm8 = config2.tableVm) == null ? void 0 : (_config$tableVm8$$gri = _config$tableVm8.$grid) == null ? void 0 : _config$tableVm8$$gri.maxHeight;
      var maxHeightStyle;
      if (isScale(maxHeight)) {
        maxHeightStyle = "max-height:" + maxHeight + ";";
      } else if (toNumber(maxHeight)) {
        maxHeightStyle = "max-height:" + toNumber(maxHeight) + "px;";
      } else {
        maxHeightStyle = "";
      }
      return "" + displayStyle + heightStyle + maxHeightStyle;
    },
    exceptionVisible: function exceptionVisible() {
      var tableData = this.tableData;
      var isException = tableData.length === 0;
      return isException;
    },
    isLoading: function isLoading() {
      var _config$tableVm9, _config$tableVm9$$gri;
      var config2 = this.config;
      return (config2 == null ? void 0 : (_config$tableVm9 = config2.tableVm) == null ? void 0 : (_config$tableVm9$$gri = _config$tableVm9.$grid) == null ? void 0 : _config$tableVm9$$gri.loading) || false;
    }
  },
  watch: {
    "config.tableVm.tableColumn": {
      handler: function handler() {
        this.mapColumns();
      },
      immediate: true
    },
    "config.tableVm.viewType": {
      handler: function handler2() {
        this.rowKey++;
      },
      immediate: true
    },
    "config.tableVm.height": {
      handler: function handler3() {
        this.getWrapperHeight();
      },
      immediate: true
    },
    "config.tableVm.parentHeight": {
      handler: function handler4() {
        this.getWrapperHeight();
      },
      immediate: true
    },
    "config.tableVm.currentRow": {
      handler: function handler5(value) {
        this.currentRow = value;
        this.setCurrentRowId();
      },
      immediate: true
    }
  },
  mounted: function mounted() {
    this.$nextTick(this.getWrapperHeight);
  },
  methods: {
    mapColumns: function mapColumns() {
      var _config$tableVm0, _config$tableVm1, _config$tableVm1$$gri, _config$tableVm1$$gri2, _config$tableVm10, _config$tableVm10$$gr, _config$tableVm10$$gr2, _config$tableVm11, _config$tableVm11$$gr, _config$tableVm11$$gr2, _config$tableVm12, _config$tableVm12$$gr, _config$tableVm12$$gr2, _config$tableVm13, _config$tableVm13$$gr, _config$tableVm13$$gr2, _config$tableVm14, _config$tableVm14$$gr;
      var config2 = this.config, typeColumns2 = this.typeColumns, firstFewPropertyColumn2 = this.firstFewPropertyColumn;
      var _this$cardConfig = this.cardConfig, cardConfig = _this$cardConfig === void 0 ? {} : _this$cardConfig, _this$listConfig = this.listConfig, listConfig = _this$listConfig === void 0 ? {} : _this$listConfig, _this$ganttConfig = this.ganttConfig, ganttConfig = _this$ganttConfig === void 0 ? {} : _this$ganttConfig, _this$customConfig = this.customConfig, customConfig = _this$customConfig === void 0 ? {} : _this$customConfig;
      var tableColumn2 = config2 == null ? void 0 : (_config$tableVm0 = config2.tableVm) == null ? void 0 : _config$tableVm0.tableColumn;
      var primaryField = cardConfig.primaryField, contentFields = cardConfig.contentFields, selectable = cardConfig.selectable, renderLink = cardConfig.renderLink, operable = cardConfig.operable, _cardConfig$few = cardConfig.few, few = _cardConfig$few === void 0 ? 4 : _cardConfig$few;
      var renderList2 = listConfig.renderList;
      var renderGantt = ganttConfig.renderGantt;
      var renderCustom = customConfig.renderCustom;
      var fieldName = "";
      var fieldNames = [];
      var propCols = firstFewPropertyColumn2(tableColumn2, few);
      var primaryColumn;
      var contentColumns;
      var operationColumn;
      var selectionColumn;
      var slotLink;
      var slotList;
      var slotGantt;
      var slotCustom;
      var slotEmpty;
      if (primaryField) {
        fieldName = fnField(primaryField);
        primaryColumn = typeColumns2(tableColumn2, [fieldName]).find(function(column) {
          return column.property === fieldName;
        });
      } else {
        primaryColumn = propCols && propCols.length > 0 ? propCols[0] : void 0;
      }
      if (contentFields && contentFields.length) {
        fieldNames = fnFields(contentFields);
        contentColumns = typeColumns2(tableColumn2, fieldNames);
      } else {
        contentColumns = propCols && propCols.length > 1 ? propCols.slice(1) : [];
      }
      if (typeof operable === "undefined" || operable) {
        operationColumn = typeColumns2(tableColumn2, ["operation"], "type").find(function(column) {
          return column.type === "operation";
        });
      }
      if (typeof selectable === "undefined" || selectable) {
        selectionColumn = typeColumns2(tableColumn2, ["selection"], "type").find(function(column) {
          return column.type === "selection";
        });
      }
      slotLink = (config2 == null ? void 0 : (_config$tableVm1 = config2.tableVm) == null ? void 0 : (_config$tableVm1$$gri = _config$tableVm1.$grid) == null ? void 0 : (_config$tableVm1$$gri2 = _config$tableVm1$$gri.slots) == null ? void 0 : _config$tableVm1$$gri2.link) || renderLink;
      slotList = (config2 == null ? void 0 : (_config$tableVm10 = config2.tableVm) == null ? void 0 : (_config$tableVm10$$gr = _config$tableVm10.$grid) == null ? void 0 : (_config$tableVm10$$gr2 = _config$tableVm10$$gr.slots) == null ? void 0 : _config$tableVm10$$gr2.list) || renderList2;
      slotGantt = (config2 == null ? void 0 : (_config$tableVm11 = config2.tableVm) == null ? void 0 : (_config$tableVm11$$gr = _config$tableVm11.$grid) == null ? void 0 : (_config$tableVm11$$gr2 = _config$tableVm11$$gr.slots) == null ? void 0 : _config$tableVm11$$gr2.gantt) || renderGantt;
      slotCustom = (config2 == null ? void 0 : (_config$tableVm12 = config2.tableVm) == null ? void 0 : (_config$tableVm12$$gr = _config$tableVm12.$grid) == null ? void 0 : (_config$tableVm12$$gr2 = _config$tableVm12$$gr.slots) == null ? void 0 : _config$tableVm12$$gr2.custom) || renderCustom;
      slotEmpty = config2 == null ? void 0 : (_config$tableVm13 = config2.tableVm) == null ? void 0 : (_config$tableVm13$$gr = _config$tableVm13.$grid) == null ? void 0 : (_config$tableVm13$$gr2 = _config$tableVm13$$gr.slots) == null ? void 0 : _config$tableVm13$$gr2.empty;
      var renderEmpty = config2 == null ? void 0 : (_config$tableVm14 = config2.tableVm) == null ? void 0 : (_config$tableVm14$$gr = _config$tableVm14.$grid) == null ? void 0 : _config$tableVm14$$gr.renderEmpty;
      Object.assign(this, {
        primaryColumn,
        contentColumns,
        operationColumn,
        selectionColumn
      });
      Object.assign(this, {
        slotLink,
        slotList,
        slotGantt,
        slotCustom,
        slotEmpty,
        renderEmpty
      });
    },
    typeColumns: function typeColumns(columns2, types, field) {
      var cols = types.map(function(type) {
        return columns2.find(function(column) {
          return column.visible && column[field || "property"] === type;
        });
      });
      var res = [];
      cols.forEach(function(col) {
        return col && res.push(col);
      });
      return res;
    },
    firstFewPropertyColumn: function firstFewPropertyColumn(columns2, few) {
      return columns2.filter(function(column) {
        return column.visible && !column.type && column.property;
      }).slice(0, few);
    },
    genParams: function genParams(column, row2) {
      var config2 = this.config, tableData = this.tableData;
      var $table = config2.tableVm;
      var tableColumn2 = $table.tableColumn;
      var $columnIndex = tableColumn2.indexOf(column);
      var columnIndex = $table.getColumnIndex(column);
      var p = {
        column,
        $table,
        $columnIndex,
        columnIndex,
        data: tableData
      };
      if (row2) {
        var $rowIndex = tableData.indexOf(row2);
        var rowIndex = $table.getRowIndex(row2);
        var seq = $rowIndex + 1;
        Object.assign(p, {
          row: row2,
          $rowIndex,
          rowIndex,
          $seq: "",
          seq
        });
      }
      return p;
    },
    emitEvent: function emitEvent$1(type, args) {
      var config2 = this.config;
      emitEvent(config2 == null ? void 0 : config2.tableVm, type, args);
    },
    handleCardClick: function handleCardClick(row2, e) {
      var config2 = this.config, emitEvent2 = this.emitEvent;
      var _config$tableVm15 = config2 == null ? void 0 : config2.tableVm, highlightCurrentRow = _config$tableVm15.highlightCurrentRow;
      if (highlightCurrentRow) {
        var _config$tableVm16;
        this.currentRow = row2;
        this.setCurrentRowId();
        config2 == null ? void 0 : (_config$tableVm16 = config2.tableVm) == null ? void 0 : _config$tableVm16.triggerCurrentRowEvent(e, {
          $table: config2 == null ? void 0 : config2.tableVm,
          row: row2
        });
      }
      emitEvent2.call(this, "card-click", [row2, e]);
    },
    scrollEvent: function scrollEvent(event) {
      var _this$config;
      var _this$$refs$warpperEl = this.$refs.warpperElem, scrolLeft = _this$$refs$warpperEl.scrolLeft, scrollTop = _this$$refs$warpperEl.scrollTop;
      this.emitEvent("scroll", [{
        type: "body",
        $table: (_this$config = this.config) == null ? void 0 : _this$config.tableVm,
        scrolLeft,
        scrollTop
      }, event]);
    },
    cfg: function cfg(row2) {
      var config2 = this.config, cardView2 = this.cardView, selectionColumn = this.selectionColumn, slotLink = this.slotLink, primaryColumn = this.primaryColumn;
      var operationColumn = this.operationColumn, contentColumns = this.contentColumns, genParams2 = this.genParams, rowClass2 = this.rowClass;
      var hasType = !!selectionColumn;
      var hasLink = !!slotLink;
      var hasOperation = !!operationColumn;
      var res = {
        row: row2,
        config: config2,
        cardView: cardView2,
        selectionColumn,
        slotLink,
        primaryColumn,
        operationColumn,
        contentColumns,
        genParams: genParams2,
        hasType,
        hasOperation,
        hasLink,
        rowClass: rowClass2
      };
      return res;
    },
    getWrapperHeight: function getWrapperHeight() {
      var _config$tableVm17, _config$tableVm18;
      var config2 = this.config;
      var $grid = config2 == null ? void 0 : (_config$tableVm17 = config2.tableVm) == null ? void 0 : _config$tableVm17.$grid;
      var height2 = ($grid == null ? void 0 : $grid.height) === "auto" ? "100%" : $grid == null ? void 0 : $grid.height;
      var parentHeight2 = config2 == null ? void 0 : (_config$tableVm18 = config2.tableVm) == null ? void 0 : _config$tableVm18.parentHeight;
      this.wrapperHeight = isScale(height2) ? Math.floor(parseInt(height2) / 100 * parentHeight2) : toNumber(height2);
    },
    showTooltip: function showTooltip(e) {
      var $refs = this.$refs, _this$cardConfig2 = this.cardConfig, cardConfig = _this$cardConfig2 === void 0 ? {} : _this$cardConfig2;
      var showTip = cardConfig.showTip;
      if (!showTip) return;
      var tooltip = $refs.tooltip;
      var dom = e.target;
      var text = dom.textContent;
      var rect = dom.getBoundingClientRect();
      var range = document.createRange();
      range.setStart(dom, 0);
      range.setEnd(dom, dom.childNodes.length);
      var rangeRect = range.getBoundingClientRect();
      if (rangeRect.x + rangeRect.width < rect.x || rangeRect.x + rangeRect.width > rect.x + rect.width || rangeRect.y + rangeRect.height < rect.y || rangeRect.y + rangeRect.height > rect.y + rect.height) {
        tooltip.state.referenceElm = dom;
        tooltip.state.popperElm && (tooltip.state.popperElm.style.display = "none");
        tooltip.doDestroy();
        this.tooltipContent = text;
        this.tooltipVisible = true;
        setTimeout(tooltip.updatePopper, 20);
      }
    },
    hideTooltip: function hideTooltip() {
      this.tooltipVisible = false;
      this.tooltipContent = "";
    },
    setCurrentRowId: function setCurrentRowId() {
      var config2 = this.config, currentRow = this.currentRow;
      if (currentRow) {
        this.currentRowId = getRowid(config2.tableVm, currentRow);
      }
    }
  }
});
function _sfc_render(_ctx, _cache, $props2, $setup, $data, $options) {
  var _component_custom_empty = resolveComponent("custom-empty");
  var _component_exception = resolveComponent("exception");
  var _component_list_view = resolveComponent("list-view");
  var _component_gantt_view = resolveComponent("gantt-view");
  var _component_custom_view = resolveComponent("custom-view");
  var _component_table_row = resolveComponent("table-row");
  var _component_tooltip = resolveComponent("tooltip");
  return openBlock(), createElementBlock(
    "div",
    {
      ref: "warpperElem",
      "data-tag": "tiny-table-wrapper",
      class: normalizeClass(["scrollbar-size-0", _ctx.wrapperClass]),
      style: normalizeStyle(_ctx.wrapperStyle),
      onScroll: _cache[1] || (_cache[1] = function() {
        return _ctx.scrollEvent && _ctx.scrollEvent.apply(_ctx, arguments);
      })
    },
    [_ctx.exceptionVisible && (_ctx.slotEmpty || _ctx.renderEmpty) ? (openBlock(), createBlock(_component_custom_empty, {
      key: 0,
      class: normalizeClass(_ctx.isLoading ? "invisible" : "")
    }, null, 8, ["class"])) : _ctx.exceptionVisible ? (openBlock(), createBlock(_component_exception, {
      key: 1,
      tiny_mode: "mobile-first",
      tiny_mode_root: "",
      class: normalizeClass(["min-h-[theme(spacing.72)]", _ctx.isLoading ? "invisible" : ""]),
      "component-page": "",
      type: "nodata"
    }, null, 8, ["class"])) : (openBlock(), createElementBlock(
      "div",
      {
        key: 2,
        "data-tag": "tiny-table",
        class: normalizeClass([_ctx.tableClass, _ctx.cardClass]),
        ref: "table"
      },
      [_ctx.listView ? (openBlock(), createBlock(_component_list_view, {
        key: 0
      })) : _ctx.ganttView ? (openBlock(), createBlock(_component_gantt_view, {
        key: 1
      })) : _ctx.customView ? (openBlock(), createBlock(_component_custom_view, {
        key: 2
      })) : (openBlock(true), createElementBlock(
        Fragment,
        {
          key: 3
        },
        renderList(_ctx.tableData, function(row2, i) {
          return openBlock(), createBlock(_component_table_row, {
            key: "" + _ctx.rowKey + i,
            datas: _ctx.cfg(row2),
            "current-row-id": _ctx.currentRowId,
            onCardClick: _ctx.handleCardClick
          }, null, 8, ["datas", "current-row-id", "onCardClick"]);
        }),
        128
        /* KEYED_FRAGMENT */
      ))],
      2
      /* CLASS */
    )), createVNode(_component_tooltip, {
      ref: "tooltip",
      modelValue: _ctx.tooltipVisible,
      "onUpdate:modelValue": _cache[0] || (_cache[0] = function($event) {
        return _ctx.tooltipVisible = $event;
      }),
      content: _ctx.tooltipContent,
      manual: ""
    }, null, 8, ["modelValue", "content"])],
    38
    /* CLASS, STYLE, NEED_HYDRATION */
  );
}
var MfTable = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
var themes$1 = GlobalConfig$1.themes, viewConfig$1 = GlobalConfig$1.viewConfig, columnLevelKey$1 = GlobalConfig$1.columnLevelKey, defaultColumnName$1 = GlobalConfig$1.defaultColumnName;
var T_TINY = themes$1.TINY, T_SAAS$1 = themes$1.SAAS;
var V_DEFAULT$1 = viewConfig$1.DEFAULT, V_MF$1 = viewConfig$1.MF;
var V_MF_LIST = viewConfig$1.MF_SHOW_LIST;
var hiddenContainerClass = "tiny-grid-hidden-column";
function verifyConfig(_vm) {
  if (!getRowkey(_vm)) {
    error("ui.grid.error.rowIdEmpty");
  }
  if (!Grid$1._edit && _vm.editConfig) {
    throw new Error(template(error("ui.grid.error.reqModule"), {
      name: "Edit"
    }));
  }
  if (!Grid$1._valid && _vm.editRules) {
    throw new Error(template(error("ui.grid.error.reqModule"), {
      name: "Validator"
    }));
  }
  if (!Grid$1._keyboard && (_vm.keyboardConfig || _vm.mouseConfig)) {
    throw new Error(template(error("ui.grid.error.reqModule"), {
      name: "Keyboard"
    }));
  }
  if (!Grid$1._resize && _vm.autoResize) {
    throw new Error(template(error("ui.grid.error.reqModule"), {
      name: "Resize"
    }));
  }
}
function mergeScrollDirStore(scrollDir, scrollDirStore) {
  if (scrollDir) {
    Object.assign(scrollDirStore, {
      startIndex: 0,
      visibleIndex: 0,
      adaptive: isBoolean(scrollDir.adaptive) ? scrollDir.adaptive : true,
      renderSize: toNumber$1(scrollDir.rSize),
      offsetSize: toNumber$1(scrollDir.oSize)
    });
  }
}
function loadStatic(data7, _vm) {
  if (data7 && data7.length > 0) {
    _vm.updateRawData(data7);
    _vm.updateStyle();
  }
}
function mergeTreeConfig(_vm) {
  if (_vm.treeConfig) {
    var ordered = _vm.treeConfig.ordered;
    _vm.treeOrdered = isNull(ordered) ? true : Boolean(ordered);
  }
}
function bindEvent(ctx) {
  GlobalEvent.on(ctx, "mousedown", ctx.handleGlobalMousedownEvent);
  GlobalEvent.on(ctx, "mousedown", ctx.handleGlobalMousedownCaptureEvent, true);
  GlobalEvent.on(ctx, "blur", ctx.handleGlobalBlurEvent);
  GlobalEvent.on(ctx, "mousewheel", ctx.handleGlobalMousewheelEvent);
  GlobalEvent.on(ctx, "keydown", ctx.handleGlobalKeydownEvent);
  GlobalEvent.on(ctx, "resize", ctx.handleGlobalResizeEvent);
  GlobalEvent.on(ctx, "contextmenu", ctx.handleGlobalContextmenuEvent);
}
function unbindEvent(table) {
  GlobalEvent.off(table, "mousedown");
  GlobalEvent.off(table, "mousedown", true);
  GlobalEvent.off(table, "blur");
  GlobalEvent.off(table, "mousewheel");
  GlobalEvent.off(table, "keydown");
  GlobalEvent.off(table, "resize");
  GlobalEvent.off(table, "contextmenu");
}
var Table = defineComponent({
  name: $prefix + "GridTable",
  props: _extends({}, $props, {
    // 所有的列对齐方式
    align: {
      type: String,
      default: function _default() {
        return GlobalConfig$1.align;
      }
    },
    // 是否自动监听父容器变化去更新响应式表格宽高
    autoResize: Boolean,
    // 是否带有纵向边框
    border: {
      type: Boolean,
      default: function _default2() {
        return GlobalConfig$1.border;
      }
    },
    // 给单元格附加 className
    cellClassName: [String, Function],
    // 主键配置
    columnKey: Boolean,
    // 所有列最小宽度，把剩余宽度按比例分配
    columnMinWidth: [Number, String],
    // 所有列宽度
    columnWidth: [Number, String],
    // 快捷菜单配置项
    contextMenu: Object,
    // 初始化绑定动态列
    customs: Array,
    // 数据
    data: [Array, Object],
    // 行拖拽和列拖拽的配置
    dropConfig: {
      type: Object,
      default: function _default3() {
        return {};
      }
    },
    // 编辑配置项
    editConfig: [Object, Boolean],
    // 校验规则配置项
    editRules: Object,
    // 展开行配置项
    expandConfig: Object,
    // 列的宽度是否自撑开
    fit: {
      type: Boolean,
      default: function _default4() {
        return GlobalConfig$1.fit;
      }
    },
    // 所有的表尾列的对齐方式
    footerAlign: {
      type: String,
      default: function _default5() {
        return GlobalConfig$1.footerAlign;
      }
    },
    // 给表尾的单元格附加 className
    footerCellClassName: [String, Function],
    // 表尾合计的计算方法
    footerMethod: Function,
    // 给表尾的行附加 className
    footerRowClassName: [String, Function],
    // 表尾合并行或列
    footerSpanMethod: Function,
    // 所有的表头列的对齐方式
    headerAlign: {
      type: String,
      default: function _default6() {
        return GlobalConfig$1.headerAlign;
      }
    },
    // 给表头的单元格附加 className
    headerCellClassName: [String, Function],
    // 给表头的行附加 className
    headerRowClassName: [String, Function],
    // 表头后置图标是否绝对定位
    headerSuffixIconAbsolute: Boolean,
    // 表格的高度
    height: [Number, String],
    // 激活单元格编辑时是否高亮显示
    highlightCell: Boolean,
    // 是否要高亮当前选中列
    highlightCurrentColumn: {
      type: Boolean,
      default: function _default7() {
        return GlobalConfig$1.highlightCurrentColumn;
      }
    },
    // 是否要高亮当前选中行
    highlightCurrentRow: {
      type: Boolean,
      default: function _default8() {
        return GlobalConfig$1.highlightCurrentRow;
      }
    },
    // 鼠标移到列是否要高亮显示
    highlightHoverColumn: {
      type: Boolean,
      default: function _default9() {
        return GlobalConfig$1.highlightHoverColumn;
      }
    },
    // 鼠标移到行是否要高亮显示
    highlightHoverRow: {
      type: Boolean,
      default: function _default10() {
        return GlobalConfig$1.highlightHoverRow;
      }
    },
    // 是否开启异步列功能
    isAsyncColumn: Boolean,
    isCenterEmpty: Boolean,
    // 是否拖拽表头调整列顺序
    isDragHeaderSorting: Boolean,
    // 按键配置项
    keyboardConfig: {
      type: Object,
      default: function _default11() {
        return {};
      }
    },
    // 表格是否加载中
    loading: Boolean,
    loadingComponent: Object,
    // 表格的最大高度
    maxHeight: [Number, String],
    // 表格的最小高度
    minHeight: [Number, String],
    // 鼠标配置项
    mouseConfig: {
      type: Object,
      default: function _default12() {
        return {};
      }
    },
    // 优化配置项
    optimization: Object,
    // 额外的参数
    params: Object,
    // 单选配置
    radioConfig: Object,
    // 是否所有服务端筛选
    remoteFilter: Boolean,
    // 是否所有服务端排序
    remoteSort: Boolean,
    // 空数据渲染
    renderEmpty: Function,
    // 自定义表格行渲染后的逻辑
    renderRowAfter: Function,
    // 所有列是否允许拖动列宽调整大小
    resizable: {
      type: Boolean,
      default: function _default13() {
        return GlobalConfig$1.resizable;
      }
    },
    // 可调整列宽的配置
    resizableConfig: Object,
    // 给行附加 className
    rowClassName: [String, Function],
    // 行分组配置映射表
    rowGroup: Object,
    rowId: {
      type: String,
      default: function _default14() {
        return GlobalConfig$1.rowId;
      }
    },
    rowKey: Boolean,
    // 行合并
    rowSpan: Array,
    // 滚动加载(滚动分页加载)
    scrollLoad: Object,
    // 多选配置项
    selectConfig: Object,
    // 是否显示表尾合计
    showFooter: Boolean,
    // 是否显示表头
    showHeader: {
      type: Boolean,
      default: function _default15() {
        return GlobalConfig$1.showHeader;
      }
    },
    // 设置表头所有内容过长时显示为省略号
    showHeaderOverflow: {
      type: [Boolean, String],
      default: function _default16() {
        return GlobalConfig$1.showHeaderOverflow;
      }
    },
    // 设置所有内容过长时显示为省略号
    showOverflow: {
      type: [Boolean, String],
      default: function _default17() {
        return GlobalConfig$1.showOverflow;
      }
    },
    // 表格的尺寸
    size: {
      type: String,
      default: function _default18() {
        return GlobalConfig$1.size;
      }
    },
    // 排序配置项
    sortConfig: Object,
    // 自定义所有列的排序方法
    sortMethod: Function,
    // 全局开关控制所有的列是否可以排列
    sortable: {
      type: Boolean,
      default: true
    },
    // 合并行或列
    spanMethod: Function,
    // 只对 type=index 时有效，自定义序号的起始值
    startIndex: {
      type: Number,
      default: 0
    },
    // 是否带有斑马纹
    stripe: {
      type: Boolean,
      default: function _default19() {
        return GlobalConfig$1.stripe;
      }
    },
    // saas下是否带有斑马纹
    stripeSaas: {
      type: Boolean,
      default: function _default20() {
        return GlobalConfig$1.stripeSaas;
      }
    },
    // saas下是否带有下边框线
    borderSaas: {
      type: Boolean,
      default: function _default21() {
        return GlobalConfig$1.borderSaas;
      }
    },
    // saas下配置竖线
    borderVertical: {
      type: Boolean,
      default: function _default22() {
        return GlobalConfig$1.borderVertical;
      }
    },
    // 默认统计配置
    summaryConfig: Object,
    // 是否自动根据状态属性去更新响应式表格宽高
    syncResize: Boolean,
    tableLayout: String,
    // tooltip 配置项
    tooltipConfig: {
      type: Object,
      default: function _default23() {
        return {};
      }
    },
    // 树形结构配置项
    treeConfig: Object,
    // 校验配置项
    validConfig: Object,
    // 多端卡片配置
    cardConfig: Object,
    // 视图类型
    viewType: {
      type: String,
      default: function _default24() {
        return V_DEFAULT$1;
      }
    },
    // 移动优先视图下展示类型
    mfShow: {
      type: String,
      default: function _default25() {
        return V_MF_LIST;
      }
    },
    // 列锚点
    columnAnchor: Array,
    // 表尾自定义渲染
    renderFooter: Function,
    // 多端列表配置
    listConfig: Object,
    // 多端甘特配置
    ganttConfig: Object,
    // 多端custom配置
    customConfig: Object,
    // 数据预取配置
    prefetch: [Boolean, Array],
    // 相交配置
    intersectionOption: Object,
    // 值比较方法
    equals: Function,
    // 自动清空鼠标选中
    autoClearMouseChecked: {
      type: Boolean,
      default: true
    },
    // 操作列（type为index或radio或selection的列）是否可拖动列宽
    operationColumnResizable: {
      type: Boolean,
      default: function _default26() {
        return GlobalConfig$1.operationColumnResizable;
      }
    },
    // 自动清空键盘复制
    autoClearKeyboardCopy: {
      type: Boolean,
      default: false
    },
    // 自定义列组件名称（列表）
    customColumnNames: {
      type: [String, Array],
      default: GlobalConfig$1.defaultColumnName
    }
  }),
  provide: function provide2() {
    return {
      $table: this,
      // 嵌套表格屏蔽父表格列
      $column: null
    };
  },
  inject: {
    $grid: {
      default: null
    }
  },
  data: function data4() {
    return {
      // 存储异步加载过的行\列数据
      asyncRenderMap: {},
      // 存放快捷菜单的信息
      ctxMenuStore: {
        list: [],
        selectChild: null,
        selected: null,
        showChild: false,
        style: null,
        visible: false
      },
      // 当前行
      currentRow: null,
      // 存放可编辑相关信息
      editStore: {
        editorAutoRefreshKey: 0,
        // 激活
        actived: {
          column: null,
          row: null
        },
        // 所有选中
        checked: {
          columns: [],
          rows: [],
          tColumns: [],
          tRows: []
        },
        // 已复制源
        copyed: {
          columns: [],
          cut: false,
          rows: []
        },
        indexs: {
          columns: []
        },
        insertList: [],
        insertMap: /* @__PURE__ */ new Map(),
        removeList: [],
        // 选中源
        selected: {
          column: null,
          row: null
        },
        titles: {
          columns: []
        }
      },
      // 已展开的行
      expandeds: [],
      // 当前选中的筛选列
      filterStore: {
        column: null,
        condition: {
          input: "",
          relation: "equals"
        },
        id: "",
        multi: false,
        options: [],
        visible: false,
        searchValue: ""
      },
      // 所有列已禁用
      headerCheckDisabled: false,
      // 是否全选
      isAllSelected: false,
      // 多选属性，有选中且非全选状态
      isIndeterminate: false,
      // 是否存在横向滚动条
      overflowX: false,
      // 是否存在纵向滚动条
      overflowY: true,
      // 存储滚动加载，上次滚动的位置
      scrollLoadStore: {
        bodyHeight: 0,
        scrollHeight: 0
      },
      // 横向滚动条的高度
      scrollbarHeight: 0,
      // 纵向滚动条的宽度
      scrollbarWidth: 0,
      // 单选属性，选中行
      selectRow: null,
      // 存放多选工具栏相关信息
      selectToolbarStore: {
        layout: {
          height: 0,
          left: 0,
          top: 0,
          width: 0,
          zIndex: 1
        },
        visible: false
      },
      // 多选属性，已选中的列
      selection: [],
      // tooltip提示内容
      tooltipContent: "",
      // tooltip提示内容是否处理换行字符
      tooltipContentPre: false,
      // 已展开树节点
      treeExpandeds: [],
      // 树节点不确定状态的列表
      treeIndeterminates: [],
      // 存放数据校验相关信息
      validStore: {
        column: null,
        content: "",
        isArrow: false,
        row: null,
        rule: null,
        visible: false
      },
      // 校验tip提示内容
      validTipContent: "",
      // 在编辑模式下 单元格在失去焦点验证的状态
      validatedMap: {},
      // 表格父容器的高度
      parentHeight: 0
    };
  },
  computed: {
    bodyCtxMenu: function bodyCtxMenu() {
      return this.ctxMenuOpts.body && this.ctxMenuOpts.body.options ? this.ctxMenuOpts.body.options : [];
    },
    ctxMenuList: function ctxMenuList() {
      var rest = [];
      this.ctxMenuStore.list.forEach(function(list) {
        return list.forEach(function(item) {
          return rest.push(item);
        });
      });
      return rest;
    },
    ctxMenuOpts: function ctxMenuOpts() {
      return extend(true, {}, GlobalConfig$1.menu, this.contextMenu);
    },
    hasFilter: function hasFilter() {
      return this.tableColumn.some(function(column) {
        return isObject(column.filter) && !isEmptyObject(column.filter);
      });
    },
    hasTip: function hasTip() {
      return Grid$1._tooltip;
    },
    headerCtxMenu: function headerCtxMenu() {
      return this.ctxMenuOpts.header && this.ctxMenuOpts.header.options ? this.ctxMenuOpts.header.options : [];
    },
    isCtxMenu: function isCtxMenu() {
      return this.headerCtxMenu.length || this.bodyCtxMenu.length;
    },
    // 是否使用了分组表头
    isGroup: function isGroup() {
      return this.collectColumn.some(function(column) {
        return hasChildrenList(column);
      });
    },
    isResizable: function isResizable() {
      return this.resizable || this.tableFullColumn.some(function(column) {
        return column.resizable;
      });
    },
    sortOpts: function sortOpts() {
      return extend(true, {}, GlobalConfig$1.sortConfig, this.sortConfig);
    },
    tooltipContentOpts: function tooltipContentOpts() {
      return extend(true, {
        content: this.tooltipContent,
        pre: this.tooltipContentPre,
        renderContent: this.tooltipConfig.renderContent,
        placement: "right",
        type: this.tooltipConfig.effect ? void 0 : "normal"
      }, this.tooltipConfig);
    },
    vaildTipOpts: function vaildTipOpts() {
      return extend(true, {
        isArrow: false,
        placement: "top",
        type: "error",
        content: this.validTipContent
      }, this.tooltipConfig);
    },
    validOpts: function validOpts() {
      var _this$$grid3, _this$$grid3$designCo;
      var config2 = Object.assign({
        message: "tooltip"
      }, GlobalConfig$1.validConfig, (_this$$grid3 = this.$grid) == null ? void 0 : (_this$$grid3$designCo = _this$$grid3.designConfig) == null ? void 0 : _this$$grid3$designCo.validConfig, this.validConfig);
      config2.isMessageTooltip = config2.message === "tooltip";
      config2.isMessageDefault = config2.message === "default";
      config2.isMessageInline = config2.message === "inline";
      return config2;
    },
    isThemeTiny: function isThemeTiny() {
      return this.tinyTheme === T_TINY;
    },
    isThemeSaas: function isThemeSaas() {
      return this.tinyTheme === T_SAAS$1;
    },
    isViewDefault: function isViewDefault() {
      return this.viewType === V_DEFAULT$1;
    },
    isShapeTable: function isShapeTable() {
      return this.isViewDefault || this.viewType === V_MF$1 && this.$grid.currentBreakpoint !== "default";
    },
    columnNames: function columnNames() {
      var customColumnNames = this.customColumnNames;
      var columnNames2 = [defaultColumnName$1];
      var pushIfNot = function pushIfNot2(columnName) {
        if (typeof columnName === "string" && !columnNames2.includes(columnName)) {
          columnNames2.push(columnName);
        }
      };
      if (Array.isArray(customColumnNames) && customColumnNames.length > 0) {
        customColumnNames.forEach(pushIfNot);
      } else if (typeof customColumnNames === "string") {
        pushIfNot(customColumnNames);
      }
      return columnNames2;
    }
  },
  watch: {
    collectColumn: function collectColumn(value) {
      this.watchColumn(value);
    },
    customs: function customs(value) {
      !this.isUpdateCustoms && this.mergeCustomColumn(value);
      this.isUpdateCustoms = false;
    },
    height: function height() {
      this.$nextTick(this.recalculate);
    },
    syncResize: function syncResize(value) {
      value && this.$nextTick(this.recalculate);
    },
    tableColumn: function tableColumn() {
      this.handleSelectionHeader();
    },
    parentHeight: function parentHeight() {
      this.recalculate();
    },
    // 选项式监控在vue2可以检测到顶层数组splice项替换/$set项替换
    // array.splice(index, 1, newItem)
    // this.$set(array, index, newItem)
    // 在数组中的行对象上动态添加字段也会触发此选项式监听
    data: function data5(array1, array2) {
      if (isVue2 && array1 === array2 && array1.length === array2.length) {
        this.updateRawData(this.data);
      }
    }
  },
  created: function created() {
    var _Object$assign = Object.assign(this, {
      // 条件处理后数据
      afterFullData: [],
      // 分组表场景全量数据（包含虚拟行）
      groupFullData: [],
      elemStore: {},
      // 缓存数据集 columnId --> { colid: column.id, column, index }
      fullColumnIdData: {},
      // 缓存数据集 column --> { colid: column.id, column, index }
      fullColumnMap: /* @__PURE__ */ new Map(),
      fullDataRowIdData: {},
      fullDataRowMap: /* @__PURE__ */ new Map(),
      // 最后滚动位置
      lastScrollLeft: 0,
      lastScrollTop: 0,
      scrollDirection: "N",
      // N,X,Y （滚动方向）
      // 存放横向 X 虚拟滚动相关的信息
      scrollXStore: {},
      // 存放纵向 Y 虚拟滚动相关信息
      scrollYStore: {},
      // 完整数据
      tableFullData: [],
      // 表格高度
      tableHeight: 0,
      // 可见列总宽度
      totalWidth: 0,
      // 存放 tooltip 相关信息
      tooltipStore: {},
      // 表格已挂载完成
      afterMounted: false,
      // 临时任务
      tasks: {},
      // 列初始就绪
      isColumnInitReady: false,
      // 列就绪
      isColumnReady: false,
      // 分组表场景是否具有虚拟行
      hasVirtualRow: false,
      // 是否是标签式用法场景
      isTagUsageSence: false,
      // 收集列信息（列数量和列顺序）
      columnCollectKey: "",
      // treeConfig.ordered的取值处理
      treeOrdered: void 0,
      // 水平滚动时新进入渲染的列
      addedColumn: [],
      // 单元格状态
      cellStatus: /* @__PURE__ */ new Map(),
      // 是否具有type selection列
      isCheckable: false
    }), scrollXStore = _Object$assign.scrollXStore, scrollYStore = _Object$assign.scrollYStore, optimizeOpts = _Object$assign.optimizeOpts, data7 = _Object$assign.data;
    var scrollX = optimizeOpts.scrollX, scrollY = optimizeOpts.scrollY;
    verifyConfig(this);
    mergeScrollDirStore(scrollX, scrollXStore);
    mergeScrollDirStore(scrollY, scrollYStore);
    mergeTreeConfig(this);
    loadStatic(data7, this);
    bindEvent(this);
    this.$grid.connect({
      name: "table",
      vm: this
    });
  },
  setup: function setup6(props, context) {
    var slots = context.slots, attrs = context.attrs, listeners = context.listeners;
    var id3 = hooks.ref(uniqueId());
    var collectColumn2 = hooks.ref([]);
    var tableFullColumn = hooks.ref([]);
    var visibleColumn = hooks.ref([]);
    var tableColumn2 = hooks.ref([]);
    var tableNode = hooks.ref([]);
    var tableData = hooks.ref([]);
    var footerData = hooks.ref([]);
    var scrollXLoad = hooks.ref(false);
    var scrollYLoad = hooks.ref(false);
    var headerHeight = hooks.ref(0);
    var footerHeight = hooks.ref(0);
    var isColumnWidthAssigned = hooks.ref(false);
    var bodyWrapperHeight = hooks.ref();
    var bodyWrapperMinHeight = hooks.ref();
    var bodyWrapperMaxHeight = hooks.ref();
    var containerScrollWidth = hooks.ref(0);
    var containerScrollHeight = hooks.ref(0);
    var bodyTableWidth = hooks.ref();
    var scrollLoadScrollHeight = hooks.ref();
    var columnStore = hooks.ref({
      // 自适应的列表集合
      autoList: [],
      centerList: [],
      // 左侧冻结列表集合
      leftList: [],
      // 右侧冻结列表集合
      rightList: [],
      // 固定像素宽度列表集合
      pxList: [],
      // 设置了最小宽度列表集合
      pxMinList: [],
      // 可调整列宽列表集合
      resizeList: [],
      // 百分比宽度列表集合
      scaleList: [],
      // 百分比最小宽度列表集合
      scaleMinList: []
    });
    var horizonScroll = hooks.ref({
      fixed: false,
      threshold: 2,
      max: 0,
      isLeft: true,
      isRight: false
    });
    var bodyProps = hooks.computed(function() {
      return {
        collectColumn: collectColumn2.value,
        tableColumn: tableColumn2.value,
        tableNode: tableNode.value,
        tableData: tableData.value,
        footerData: footerData.value
      };
    });
    var mfTableProps = hooks.computed(function() {
      return {
        tableData: tableData.value,
        cardConfig: props.cardConfig,
        listConfig: props.listConfig,
        ganttConfig: props.ganttConfig,
        customConfig: props.customConfig
      };
    });
    var tableWrapper = hooks.ref();
    var _useNormalData = useNormalData({
      props,
      tableFullColumn
    }), rawData = _useNormalData.rawData, rawDataVersion = _useNormalData.rawDataVersion;
    var tinyTheme = hooks.ref(resolveTheme(props, context));
    var $table = hooks.getCurrentInstance().proxy;
    var $grid = hooks.inject("$grid", null);
    var vSize3 = hooks.computed(function() {
      return props.size || ($grid == null ? void 0 : $grid.size) || ($grid == null ? void 0 : $grid.vSize);
    });
    var optimizeOpts = hooks.computed(function() {
      return extend(true, {}, GlobalConfig$1.optimization, props.optimization);
    });
    var rowHeight = hooks.computed(function() {
      var _optimizeOpts$value, _optimizeOpts$value$s, _GlobalConfig$1$rowHe;
      return ((_optimizeOpts$value = optimizeOpts.value) == null ? void 0 : (_optimizeOpts$value$s = _optimizeOpts$value.scrollY) == null ? void 0 : _optimizeOpts$value$s.rHeight) || ((_GlobalConfig$1$rowHe = GlobalConfig$1.rowHeight[tinyTheme.value]) == null ? void 0 : _GlobalConfig$1$rowHe[vSize3.value || "default"]) || 40;
    });
    var headerRowHeight = hooks.computed(function() {
      var _GlobalConfig$1$heade;
      return ((_GlobalConfig$1$heade = GlobalConfig$1.headerRowHeight[tinyTheme.value]) == null ? void 0 : _GlobalConfig$1$heade[vSize3.value || "default"]) || 40;
    });
    useInstanceSlots();
    useRelation({
      relationKey: columnLevelKey$1 + "-" + id3.value,
      childrenKey: "childColumns",
      relationContainer: function relationContainer() {
        return !isServer ? $table.$el.querySelector("." + hiddenContainerClass) : null;
      },
      onChange: function onChange() {
        var collectKey = $table.computeCollectKey();
        if (collectKey !== $table.columnCollectKey) {
          $table.columnCollectKey = collectKey;
          $table.assembleColumns();
        }
      }
    });
    useDrag({
      props,
      collectColumn: collectColumn2,
      tableColumn: tableColumn2
    });
    useRowGroup({
      props,
      visibleColumn,
      tableFullColumn,
      tableColumn: tableColumn2,
      columnStore
    });
    var _useData = useData(props), tiledLength = _useData.tiledLength;
    var updateFixedClasses = function updateFixedClasses2(isLeft, isRight) {
      if (!$table.$el) return;
      var el = $table.$el;
      var hasLeftClass = el.classList.contains("tiny-grid-fixed__left");
      var hasRightClass = el.classList.contains("tiny-grid-fixed__right");
      var shouldHaveLeft = !isLeft;
      var shouldHaveRight = !isRight;
      if (hasLeftClass !== shouldHaveLeft) {
        if (shouldHaveLeft) {
          el.classList.add("tiny-grid-fixed__left");
        } else {
          el.classList.remove("tiny-grid-fixed__left");
        }
      }
      if (hasRightClass !== shouldHaveRight) {
        if (shouldHaveRight) {
          el.classList.add("tiny-grid-fixed__right");
        } else {
          el.classList.remove("tiny-grid-fixed__right");
        }
      }
    };
    var resolveMap = {};
    hooks.watch(function() {
      var _horizonScroll$value, _horizonScroll$value2;
      return [(_horizonScroll$value = horizonScroll.value) == null ? void 0 : _horizonScroll$value.isLeft, (_horizonScroll$value2 = horizonScroll.value) == null ? void 0 : _horizonScroll$value2.isRight];
    }, function(_ref175) {
      var newIsLeft = _ref175[0], newIsRight = _ref175[1];
      hooks.nextTick(function() {
        return updateFixedClasses(newIsLeft, newIsRight);
      });
    });
    hooks.onMounted(function() {
      $table.addIntersectionObserver();
      if (tableWrapper.value) {
        document.body.appendChild(tableWrapper.value);
      }
      hooks.nextTick(function() {
        $table.afterMounted = true;
        setTimeout(function() {
          if ($table.$el && $table.horizonScroll) {
            var _horizonScroll$value3, _horizonScroll$value4;
            var el = $table.$el;
            if (!((_horizonScroll$value3 = horizonScroll.value) != null && _horizonScroll$value3.isLeft)) {
              el.classList.add("tiny-grid-fixed__left");
            }
            if (!((_horizonScroll$value4 = horizonScroll.value) != null && _horizonScroll$value4.isRight)) {
              el.classList.add("tiny-grid-fixed__right");
            }
          }
        });
        if (props.autoResize && Grid$1._resize) {
          $table.bindResize();
        }
      });
    });
    hooks.onBeforeUnmount(function() {
      var _tableWrapper$value;
      if ((_tableWrapper$value = tableWrapper.value) != null && _tableWrapper$value.parentNode) {
        var _tableWrapper$value2;
        (_tableWrapper$value2 = tableWrapper.value) == null ? void 0 : _tableWrapper$value2.parentNode.removeChild(tableWrapper.value);
      }
      $table.removeIntersectionObserver();
      if (Grid$1._resize) {
        $table.unbindResize();
      }
      $table.closeFilter();
      $table.closeMenu();
      var ySpaceElem = $table.elemStore["main-body-ySpace"];
      if (ySpaceElem) {
        ySpaceElem.onscroll = null;
      }
      unbindEvent($table);
      $table._tileInfo = $table._graphInfo = null;
      $table.rowidCacheMap = null;
    });
    hooks.onActivated(function() {
      var lastScrollLeft = $table.lastScrollLeft, lastScrollTop = $table.lastScrollTop;
      if (lastScrollLeft || lastScrollTop) {
        $table.scrollTo(lastScrollLeft, lastScrollTop);
        scrollXLoad.value && $table.triggerScrollXEvent();
        scrollYLoad.value && $table.triggerScrollYEvent({
          target: {
            scrollTop: lastScrollTop
          }
        });
      }
      bindEvent($table);
    });
    hooks.onDeactivated(function() {
      unbindEvent($table);
    });
    var tableListeners = getListeners(attrs, listeners);
    var markColumnIndex = hooks.ref(0);
    var rowidCacheMap = /* @__PURE__ */ new Map();
    var columnSlotsWeakMap = /* @__PURE__ */ new WeakMap();
    var operationMap = /* @__PURE__ */ new Map();
    return {
      slots,
      tableListeners,
      tinyTheme,
      id: id3,
      collectColumn: collectColumn2,
      tableFullColumn,
      visibleColumn,
      tableColumn: tableColumn2,
      tableNode,
      tableData,
      footerData,
      bodyProps,
      mfTableProps,
      vSize: vSize3,
      optimizeOpts,
      rowHeight,
      headerRowHeight,
      tableWrapper,
      scrollXLoad,
      scrollYLoad,
      headerHeight,
      footerHeight,
      isColumnWidthAssigned,
      bodyWrapperHeight,
      bodyWrapperMinHeight,
      bodyWrapperMaxHeight,
      containerScrollWidth,
      containerScrollHeight,
      bodyTableWidth,
      scrollLoadScrollHeight,
      columnStore,
      tiledLength,
      rawDataVersion,
      rawData,
      markColumnIndex,
      rowidCacheMap,
      horizonScroll,
      resolveMap,
      columnSlotsWeakMap,
      operationMap
    };
  },
  render: function render14() {
    var _class4;
    var $grid = this.$grid, bodyProps = this.bodyProps, border = this.border, borderSaas = this.borderSaas, borderVertical = this.borderVertical;
    var ctxMenuStore = this.ctxMenuStore, dropConfig = this.dropConfig, editConfig = this.editConfig, editRules = this.editRules, filterStore = this.filterStore;
    var hasFilter2 = this.hasFilter, hasTip2 = this.hasTip, height2 = this.height, highlightCell = this.highlightCell, highlightHoverColumn = this.highlightHoverColumn, highlightHoverRow = this.highlightHoverRow;
    var horizonScroll = this.horizonScroll, id3 = this.id, instanceSlots = this.instanceSlots, isCtxMenu2 = this.isCtxMenu, isGroup2 = this.isGroup, isResizable2 = this.isResizable;
    var isShapeTable2 = this.isShapeTable, isThemeTiny2 = this.isThemeTiny, isThemeSaas3 = this.isThemeSaas, loading = this.loading, loadingComponent = this.loadingComponent, mfTableProps = this.mfTableProps;
    var mouseConfig = this.mouseConfig, optimizeOpts = this.optimizeOpts, overflowX = this.overflowX, overflowY = this.overflowY, rowSpan = this.rowSpan;
    var scrollbarHeight = this.scrollbarHeight, selectToolbarStore = this.selectToolbarStore, showFooter = this.showFooter, showHeader = this.showHeader;
    var showHeaderOverflow = this.showHeaderOverflow, showOverflow = this.showOverflow, spanMethod = this.spanMethod, stripe = this.stripe, stripeSaas = this.stripeSaas, tableData = this.tableData;
    var tooltipContentOpts2 = this.tooltipContentOpts, vSize3 = this.vSize, vaildTipOpts2 = this.vaildTipOpts, validOpts2 = this.validOpts;
    var isMessageDefault = validOpts2.isMessageDefault, isMessageTooltip = validOpts2.isMessageTooltip;
    return h("div", {
      class: (_class4 = {
        "tiny-grid h-full sm:h-auto !bg-transparent sm:!bg-color-bg-1 after:border-none sm:after:border-solid": true
      }, _class4["row__valid-" + validOpts2.message] = true, _class4["size__" + vSize3] = vSize3, _class4["tiny-grid-editable"] = editConfig, _class4["show__head"] = showHeader, _class4["show__foot"] = showFooter, _class4["scroll__y"] = overflowY, _class4["scroll__x"] = overflowX, _class4["all-overflow"] = showOverflow, _class4["all-head-overflow"] = showHeaderOverflow, _class4["tiny-grid-cell__highlight"] = highlightCell, _class4["tiny-grid__animat"] = optimizeOpts.animat, _class4["tiny-grid__stripe"] = !isThemeSaas3 && stripe, _class4["tiny-grid__stripe-saas"] = isThemeSaas3 && stripeSaas, _class4["tiny-grid__border"] = border || isGroup2, _class4["tiny-grid__border-saas"] = isThemeSaas3 && borderSaas, _class4["tiny-grid__group-saas"] = isThemeSaas3 && isGroup2, _class4["tiny-grid__border-vertical"] = borderVertical, _class4["tiny-grid__checked"] = mouseConfig.checked, _class4["tiny-grid__hover-align"] = mouseConfig.hover, _class4["mark-insert"] = editConfig && editConfig.markInsert, _class4["edit__no-border"] = editConfig && editConfig.showBorder === false, _class4["is__loading"] = loading, _class4["row__highlight"] = highlightHoverRow, _class4["column__highlight"] = highlightHoverColumn, _class4["is__row-span"] = rowSpan && rowSpan.length > 0 || typeof spanMethod === "function", _class4["row__drop-handle--index"] = dropConfig.rowHandle === "index", _class4["fixed__sticky"] = horizonScroll.fixed, _class4)
    }, [
      // 隐藏列
      h("div", {
        class: hiddenContainerClass,
        ref: "hideColumn"
      }, instanceSlots.default && instanceSlots.default()),
      // 主内容
      h(Body, {
        ref: "tableBody",
        props: bodyProps,
        class: this.viewCls("tableBody")
      }),
      // 边框线
      h("div", {
        class: ["tiny-grid__border-line", this.viewCls("borderLine")],
        key: "tinyGridBorderLine"
      }),
      // 列宽线
      isResizable2 ? h("div", {
        class: ["tiny-grid__resizable-bar", this.viewCls("resizeBar")],
        style: overflowX ? {
          "padding-bottom": scrollbarHeight + "px"
        } : null,
        ref: "resizeBar",
        key: "tinyGridResizeBar"
      }) : null,
      // 加载中
      h(Loading, {
        props: {
          visible: loading,
          loadingComponent
        },
        class: this.viewCls("gridLoading")
      }),
      // 筛选、快捷菜单、Tip提示、校验提示
      h("div", {
        class: ["tiny-grid" + id3 + "-wrapper " + (this.$attrs.staticClass || ""), this.viewCls("tableWrapper")],
        ref: "tableWrapper"
      }, [
        // 筛选
        hasFilter2 ? h(Panel$1, {
          props: {
            optimizeOpts,
            filterStore
          },
          ref: "filterWrapper"
        }) : null,
        // 快捷菜单
        isCtxMenu2 ? h(Panel, {
          props: {
            ctxMenuStore
          },
          ref: "ctxWrapper"
        }) : null,
        // Ellipsis tooltip
        hasTip2 ? h(Tooltip$1, {
          ref: "tooltip",
          props: tooltipContentOpts2
        }) : null,
        // valid error tooltip
        hasTip2 && editRules && (isMessageDefault ? !height2 : isMessageTooltip) ? h(Tooltip$1, {
          class: "tiny-grid__valid-error",
          props: isMessageTooltip || tableData.length === 1 ? vaildTipOpts2 : null,
          ref: "validTip"
        }) : null
      ]),
      // 多选工具栏
      $grid.selectToolbar ? h("div", {
        ref: "selectToolbar",
        class: ["tiny-grid__select-toolbar", this.viewCls("selectToolbar")],
        style: [selectToolbarStore.layout, {
          visibility: selectToolbarStore.visible ? "visible" : "hidden"
        }]
      }, $grid.renderedToolbar ? $grid.renderedToolbar : [null]) : null,
      // 多端表格（默认主题或默认视图/mf大屏下，不渲染）
      !isThemeTiny2 && !isShapeTable2 ? h(MfTable, {
        ref: "mfTable",
        props: mfTableProps
      }) : null
    ]);
  },
  methods: _extends({}, Methods$d, {
    viewCls: function viewCls(module) {
      return this.$grid.viewCls(module);
    }
  })
});
Table.install = function(Vue) {
  Grid$1.Vue = Vue;
  Grid$1.Table = Table;
  Vue.component(Table.name, Table);
};
var columnLevelKey = GlobalConfig$1.columnLevelKey, defaultColumnName = GlobalConfig$1.defaultColumnName;
var Column = defineComponent({
  name: defaultColumnName,
  componentName: "GridColumn",
  props: _extends({}, $props, {
    // 列对齐方式
    align: String,
    // 给单元格附加 className
    className: [String, Function],
    // 单元格编辑渲染配置项
    editor: [Object, Function],
    // 列属性
    field: String,
    filter: {},
    // 自定义筛选方法
    filterMethod: Function,
    // 筛选是否允许多选
    filterMultiple: {
      type: Boolean,
      default: true
    },
    // 筛选模板配置项
    filterRender: Object,
    // 配置筛选条件数组
    filters: Array,
    // 将列固定在左侧或者右侧
    fixed: String,
    // 表尾列的对齐方式
    footerAlign: String,
    // 给表尾单元格附加 className
    footerClassName: [String, Function],
    // 格式化配置
    formatConfig: {
      type: Object,
      default: function _default27() {
        return {
          data: []
        };
      }
    },
    // 格式化显示内容
    formatText: [Function, String],
    // 格式化单元格编辑器组件的传入值
    formatValue: Function,
    group: {
      type: Boolean,
      default: false
    },
    // 表头对齐方式
    headerAlign: String,
    // 给表头单元格附加 className
    headerClassName: [String, Function],
    // 自定义索引方法
    indexMethod: Function,
    // 在 v3.0 中废弃 label
    label: String,
    // 列最小宽度，把剩余宽度按比例分配
    minWidth: [Number, String],
    // 额外的参数
    params: Object,
    // 在 v3.0 中废弃 prop
    prop: String,
    // 是否服务端排序
    remoteSort: {
      type: Boolean,
      default: null
    },
    // 单元格渲染配置项
    renderer: [Object, Function],
    // 是否必填，如不设置，则会根据校验规则自动生成
    required: {
      type: Boolean,
      default: false
    },
    // 是否允许拖动列宽调整大小
    resizable: {
      type: Boolean,
      default: null
    },
    // 表单的验证功能
    rules: Object,
    // 当表头内容过长时显示为省略号
    showHeaderOverflow: {
      type: [Boolean, String],
      default: null
    },
    // 显示列头提示
    showHeaderTip: {
      type: Boolean,
      default: null
    },
    // 是否显示列头编辑图标
    showIcon: {
      type: Boolean,
      default: true
    },
    // 当内容过长时显示为省略号
    showOverflow: {
      type: [Boolean, String],
      default: null
    },
    // 显示列内容提示
    showTip: {
      type: Boolean,
      default: null
    },
    // 自定义排序的属性
    sortBy: [String, Array],
    // 自定义排序方法
    sortMethod: Function,
    // 是否允许排序
    sortable: {
      type: Boolean,
      default: false
    },
    // 列标题
    title: [String, Function],
    // 指定为树节点
    treeNode: Boolean,
    // 渲染类型: index,radio,selection,expand,operation,默认值空串
    type: String,
    // 列宽度
    width: [Number, String],
    // 操作列配置
    operationConfig: Object,
    // 值比较方法
    equals: Function
  }),
  provide: function provide3() {
    return {
      $column: this
    };
  },
  inject: {
    $table: {
      default: null
    }
  },
  setup: function setup7(props, _ref176) {
    var slots = _ref176.slots;
    var reactive = hooks.reactive, inject = hooks.inject, getCurrentInstance = hooks.getCurrentInstance, onUpdated = hooks.onUpdated, watch = hooks.watch, nextTick = hooks.nextTick;
    var currentInstance = getCurrentInstance();
    var instance = currentInstance.proxy;
    var $grid = inject("$grid");
    var $table = inject("$table");
    var slotsCache = {};
    !$table.isTagUsageSence && ($table.isTagUsageSence = true);
    useInstanceSlots();
    useRelation({
      relationKey: columnLevelKey + "-" + $table.id
    });
    var state = reactive({
      // 创建表格列实例化对象
      columnConfig: Cell.createColumn($table, props),
      slots,
      // 如果是静态数据源，就拿第一行数据
      firstRow: !$grid.fetchOption && $grid.data && $grid.data.length ? $grid.data[0] : {}
    });
    watch(function() {
      return props.formatConfig;
    }, function() {
      return state.columnConfig.format = props.formatConfig;
    });
    onUpdated(function() {
      var noProxySlots = instance.$scopedSlots || instance.slots;
      var slotsChange = Object.keys(noProxySlots || {}).some(function(key) {
        var _slotsCache;
        return !(((_slotsCache = slotsCache) == null ? void 0 : _slotsCache[key]) === noProxySlots[key]);
      });
      if (slotsChange) {
        slotsCache = _extends({}, noProxySlots);
        state.columnConfig.slots = slotsCache;
      }
    });
    nextTick(function() {
      return state.columnConfig.slots = instance.instanceSlots;
    });
    return state;
  },
  render: function render15() {
    var $table = this.$table, firstRow = this.firstRow, columnConfig = this.columnConfig, instanceSlots = this.instanceSlots;
    var slotVnode;
    try {
      slotVnode = instanceSlots.default && instanceSlots.default({
        row: firstRow,
        column: columnConfig,
        skip: true
      });
    } catch (e) {
      slotVnode = null;
      warn("ui.grid.error.chainCallError");
    }
    var hasSubColumn = findTree(slotVnode, function(node) {
      var _ref177 = node || {}, componentOptions = _ref177.componentOptions, type = _ref177.type;
      var componentName = type && type.name || componentOptions && componentOptions.Ctor.extendOptions.name;
      return $table.isValidCustomColumn(componentName);
    }, null, null);
    var columnProps = {
      key: columnConfig.id,
      attrs: {
        "data-colid": columnConfig.id
      }
    };
    return h("div", columnProps, hasSubColumn && slotVnode);
  },
  methods: Cell
});
Column.install = function(Vue) {
  Vue.component(Column.name, Column);
};
var themes = GlobalConfig$1.themes, viewConfig = GlobalConfig$1.viewConfig;
var T_SAAS = themes.SAAS;
var V_GANTT = viewConfig.GANTT, V_MF = viewConfig.MF, V_CARD = viewConfig.CARD; viewConfig.DEFAULT; var V_CUSTOM = viewConfig.CUSTOM;
var propKeys = Object.keys(Table.props);
function createRender(opt) {
  var _class5;
  var h2 = opt.h, _vm = opt._vm, vSize3 = opt.vSize, props = opt.props, selectToolbar = opt.selectToolbar, slots = opt.slots, $slots = opt.$slots, tableOns = opt.tableOns, renderedToolbar = opt.renderedToolbar, loading = opt.loading, pagerConfig = opt.pagerConfig, pager = opt.pager, tableLoading = opt.tableLoading, viewType2 = opt.viewType, columnAnchorParams = opt.columnAnchorParams, columnAnchor = opt.columnAnchor, fullScreenClass = opt.fullScreenClass;
  return h2("div", {
    class: (_class5 = {}, _class5["tiny-grid__wrapper tiny-grid view_" + viewType2] = true, _class5["!bg-transparent sm:!bg-color-bg-1"] = viewType2 === V_MF || viewType2 === V_CARD, _class5["size__" + vSize3] = vSize3, _class5["tiny-grid__animat"] = props.optimization.animat, _class5[fullScreenClass] = true, _class5)
  }, [
    selectToolbar ? null : renderedToolbar(),
    columnAnchor ? _vm.renderColumnAnchor(columnAnchorParams, _vm) : null,
    // 这里会渲染tiny-grid-column插槽内容，从而获取列配置
    h2(Table, {
      props,
      on: tableOns,
      ref: "tinyTable"
    }, slots.default && slots.default()),
    _vm.renderPager({
      $slots,
      _vm,
      loading,
      pager,
      pagerConfig,
      tableLoading,
      vSize: vSize3
    })
  ]);
}
var Grid = defineComponent({
  name: $prefix + "Grid",
  provide: function provide4() {
    return {
      $grid: this
    };
  },
  props: _extends({}, $props, {
    columns: Array,
    proxyConfig: Object,
    fetchData: Object,
    saveData: Object,
    deleteData: Object,
    toolbar: Object,
    pager: Object,
    dataset: Object,
    autoLoad: {
      type: Boolean,
      default: true
    },
    seqSerial: {
      type: Boolean,
      default: false
    },
    events: Object
  }, Table.props, {
    isBeforePageChange: Boolean,
    showSaveMsg: {
      type: Boolean,
      default: false
    },
    isMultipleHistory: Boolean,
    selectToolbar: [Boolean, Object],
    filterPopperOptions: {
      type: Object,
      default: function _default28() {
        return {};
      }
    }
  }),
  data: function data6() {
    return {
      emitter: emitter(),
      fetchOption: null,
      filterData: [],
      listeners: {},
      pagerConfig: null,
      // 存放标记为删除的行数据
      pendingRecords: [],
      sortData: {},
      tableCustoms: [],
      tableData: [],
      tableLoading: false,
      tablePage: {
        total: 0,
        pageSize: 10,
        currentPage: 1
      },
      tablePageLoading: false,
      realTimeTablePage: {
        total: 0,
        pageSize: 10,
        currentPage: 1
      },
      columnAnchorParams: {},
      columnAnchorKey: "",
      tasks: {},
      fullScreenClass: "",
      isInitialLoading: true,
      // 是否首次加载数据
      _delayActivateAnchor: void 0
    };
  },
  computed: {
    // 工具栏按钮保存和删除时是否弹出提示信息
    isMsg: function isMsg() {
      return this.proxyOpts.message !== false;
    },
    tableProps: function tableProps() {
      var _this48 = this;
      var rest = {};
      propKeys.forEach(function(key) {
        return rest[key] = _this48[key];
      });
      return rest;
    },
    proxyOpts: function proxyOpts() {
      return extend(true, {}, GlobalConfig$1.grid.proxyConfig, this.proxyConfig);
    },
    vSize: function vSize2() {
      return this.size || this.$parent && this.$parent.size || this.$parent && this.$parent.vSize;
    },
    seqIndex: function seqIndex() {
      var seqSerial = this.seqSerial, scrollLoad = this.scrollLoad, oldPage = this.pagerConfig, startIndex = this.startIndex, tablePageLoading = this.tablePageLoading, realTimeTablePage = this.realTimeTablePage;
      var seqIndexValue = startIndex;
      var pagerConfig = tablePageLoading ? realTimeTablePage : oldPage;
      if ((seqSerial || scrollLoad) && pagerConfig) {
        seqIndexValue = (pagerConfig.currentPage - 1) * pagerConfig.pageSize + startIndex;
      }
      return seqIndexValue;
    },
    isThemeSaas: function isThemeSaas2() {
      return this.tinyTheme === T_SAAS;
    },
    isModeMobileFirst: function isModeMobileFirst() {
      return this.tinyMode === "mobile-first";
    },
    isViewGantt: function isViewGantt() {
      return this.viewType === V_GANTT;
    },
    isViewCustom: function isViewCustom() {
      return this.viewType === V_CUSTOM;
    },
    optimizOpt: function optimizOpt() {
      return Object.assign(this.initOptimization, GlobalConfig$1.optimization, this.optimization);
    },
    editConfigOpt: function editConfigOpt() {
      return this.editConfig ? Object.assign(this.initEditConfig, GlobalConfig$1.editConfig, this.editConfig, {
        activeMethod: this.handleActiveMethod
      }) : null;
    },
    tooltipOpt: function tooltipOpt() {
      var _this$designConfig;
      return Object.assign(this.initTooltipConfig, GlobalConfig$1.tooltip, (_this$designConfig = this.designConfig) == null ? void 0 : _this$designConfig.tooltip, this.tooltipConfig);
    }
  },
  watch: {
    // 监听配置式columns数组
    columns: function columns(cols) {
      this.loadColumn(cols);
    },
    tableCustoms: function tableCustoms() {
      this.toolbar && this.$refs.toolbar && this.$refs.toolbar.loadStorage();
    },
    viewType: function viewType(value) {
      var _this49 = this;
      if (value === V_MF) {
        this.$nextTick(function() {
          return _this49.recalculate();
        });
      }
    }
  },
  created: function created2() {
    this.vmStore = /* @__PURE__ */ Object.create(null);
    this.fetchOption = this.initFetchOption();
    this.pagerConfig = this.initPagerConfig();
    var customs2 = this.customs, events = this.events, prefetch = this.prefetch, fetchOption = this.fetchOption, autoLoad = this.autoLoad;
    if (customs2) {
      this.tableCustoms = customs2;
    }
    if (events) {
      var listeners = {};
      for (var event in events) {
        if (Object.prototype.hasOwnProperty.call(events, event)) {
          var evkey = event.replace(/\B([A-Z])/g, "-$1").toLowerCase();
          listeners[evkey] = events[event];
          if (~["pageChange", "sortChange", "filterChange", "toolbarButtonClick"].indexOf(event)) {
            this.emitter.on(evkey, events[event]);
          }
        }
      }
      this.listeners = listeners;
    }
    if (prefetch && fetchOption && autoLoad !== false) {
      if (Array.isArray(prefetch)) {
        this.commitProxy("prefetch", prefetch);
      } else {
        this.commitProxy("prefetch");
      }
    }
  },
  mounted: function mounted2() {
    var columns2 = this.columns, fetchOption = this.fetchOption, autoLoad = this.autoLoad, pagerSlot = this.pagerSlot, prefetch = this.prefetch;
    if (pagerSlot) {
      var _pagerSlot$componentO = pagerSlot.componentOptions.listeners, listeners = _pagerSlot$componentO === void 0 ? {} : _pagerSlot$componentO, componentInstance = pagerSlot.componentInstance;
      if (!listeners["size-change"]) {
        componentInstance.$on("size-change", this.pageSizeChange);
      }
      if (!listeners["current-change"]) {
        componentInstance.$on("current-change", this.pageCurrentChange);
      }
    }
    if (columns2 && columns2.length) {
      this.loadColumn(this.columns);
    }
    if (!prefetch && fetchOption) {
      if (this._pageSizeChangeCallback) {
        this._pageSizeChangeCallback();
        this._pageSizeChangeCallback = null;
      } else if (autoLoad) {
        var toolbarVm = this.getVm("toolbar");
        this.commitProxy("query", toolbarVm && toolbarVm.orderSetting());
      }
    }
    if (this.isMultipleHistory) {
      this.initMultipleHistory();
    }
  },
  setup: function setup8(props, context) {
    var listeners = context.listeners, attrs = context.attrs;
    var initEditConfig = hooks.ref({});
    var initOptimization = hooks.ref({});
    var initTooltipConfig = hooks.ref({});
    var tableListeners = getListeners(attrs, listeners);
    var tinyTheme = hooks.ref(resolveTheme(props, context));
    var tinyMode = hooks.ref(resolveMode(props, context));
    var breakpoint = useBreakpoint();
    var renderless = function renderless2(props2, hooks2, _ref178) {
      var _ref178$designConfig = _ref178.designConfig, designConfig = _ref178$designConfig === void 0 ? null : _ref178$designConfig;
      return {
        tableListeners,
        designConfig,
        tinyTheme,
        tinyMode,
        currentBreakpoint: breakpoint.current,
        initEditConfig,
        initOptimization,
        initTooltipConfig
      };
    };
    hooks.onBeforeUnmount(function() {
      var gridVm = hooks.getCurrentInstance().proxy;
      gridVm.vmStore = null;
    });
    return setup$1({
      props,
      context,
      renderless,
      api: ["designConfig", "tableListeners", "tinyTheme", "tinyMode", "currentBreakpoint", "initEditConfig", "initOptimization", "initTooltipConfig"]
    });
  },
  render: function render16() {
    var _this50 = this;
    var fetchOption = this.fetchOption, listeners = this.listeners, loading = this.loading, optimizOpt2 = this.optimizOpt, editConfigOpt2 = this.editConfigOpt, tooltipOpt2 = this.tooltipOpt, pager = this.pager, pagerConfig = this.pagerConfig, remoteFilter = this.remoteFilter, remoteSort = this.remoteSort, selectToolbar = this.selectToolbar;
    var seqIndex2 = this.seqIndex, $slots = this.slots, tableCustoms2 = this.tableCustoms, tableData = this.tableData, tableListeners = this.tableListeners, tableLoading = this.tableLoading, tableProps2 = this.tableProps, toolbar = this.toolbar, vSize3 = this.vSize, designConfig = this.designConfig, viewType2 = this.viewType;
    var columnAnchor = this.columnAnchor, columnAnchorParams = this.columnAnchorParams, fullScreenClass = this.fullScreenClass;
    if (designConfig != null && designConfig.icons) {
      Object.assign(GlobalConfig$1.icon, designConfig.icons);
    }
    var props = Object.assign(tableProps2, {
      optimization: optimizOpt2,
      startIndex: seqIndex2,
      editConfig: editConfigOpt2,
      tooltipConfig: tooltipOpt2
    });
    if ((designConfig == null ? void 0 : designConfig.stripe) !== void 0 && !props.stripe) {
      props.stripe = designConfig == null ? void 0 : designConfig.stripe;
    }
    var tableOns = Object.assign(listeners, tableListeners);
    var rowClassName = this.handleRowClassName, sortChangeEvent2 = this.sortChangeEvent, filterChangeEvent2 = this.filterChangeEvent;
    if (fetchOption) {
      Object.assign(props, {
        loading: loading || tableLoading,
        data: tableData,
        rowClassName
      });
      remoteSort && (tableOns["sort-change"] = sortChangeEvent2);
      remoteFilter && (tableOns["filter-change"] = filterChangeEvent2);
    }
    toolbar && !(toolbar.setting && toolbar.setting.storage) && (props.customs = tableCustoms2);
    toolbar && (tableOns["update:customs"] = function(value) {
      return _this50.tableCustoms = value;
    });
    tableOns["column-init-ready"] = this.handleColumnInitReady;
    var renderedToolbar = function renderedToolbar2() {
      return _this50.getRenderedToolbar({
        $slots,
        _vm: _this50,
        loading,
        tableLoading,
        toolbar
      });
    };
    return createRender({
      h,
      _vm: this,
      vSize: vSize3,
      props,
      selectToolbar,
      renderedToolbar,
      tableOns,
      slots: this.slots,
      $slots,
      loading,
      pager,
      pagerConfig,
      tableLoading,
      viewType: viewType2,
      columnAnchorParams,
      columnAnchor,
      fullScreenClass
    });
  },
  methods: {
    // 配置高度减去（表格锚点+工具栏+分页）计算得出表格高度
    updateParentHeight: function updateParentHeight2() {
      var _this51 = this;
      if (!this.tasks.updateParentHeight) {
        this.tasks.updateParentHeight = debounce(10, function() {
          var $el = _this51.$el, $refs = _this51.$refs;
          var tinyTable = $refs.tinyTable, tinyGridColumnAnchor = $refs.tinyGridColumnAnchor;
          var toolbarVm = _this51.getVm("toolbar");
          if (tinyTable) {
            var columnAnchorHeight = 0;
            if (tinyGridColumnAnchor) {
              var _getComputedStyle = getComputedStyle(tinyGridColumnAnchor), height2 = _getComputedStyle.height, marginTop = _getComputedStyle.marginTop, marginBottom = _getComputedStyle.marginBottom;
              columnAnchorHeight = toNumber$1(height2) + toNumber$1(marginTop) + toNumber$1(marginBottom);
            }
            tinyTable.parentHeight = $el.parentNode.clientHeight - (toolbarVm ? toolbarVm.$el.clientHeight : 0) - columnAnchorHeight - ($refs.pager ? $refs.pager.$el.clientHeight : 0);
          }
        });
      }
      this.tasks.updateParentHeight();
    },
    // 向缓存添加实例
    connect: function connect2(_ref179) {
      var name = _ref179.name, vm = _ref179.vm;
      if (name && typeof name === "string" && vm) {
        this.vmStore[name] = vm;
      }
    },
    createJob: function createJob(type, callback) {
      if (type === "pageSizeChangeCallback") {
        this._pageSizeChangeCallback = callback;
      } else if (type === "updateCustomsCallback") {
        this._updateCustomsCallback = this._updateCustomsCallback || [];
        this._updateCustomsCallback.push(callback);
      }
    },
    // 从缓存获取实例
    getVm: function getVm2(name) {
      if (name && typeof name === "string" && this.vmStore) {
        return this.vmStore[name];
      }
    },
    // 列就绪时的处理
    handleColumnInitReady: function handleColumnInitReady() {
      if (this._updateCustomsCallback) {
        this._updateCustomsCallback.forEach(function(fn) {
          fn();
        });
        this._updateCustomsCallback = [];
      }
    },
    handleRowClassName: function handleRowClassName(params) {
      var rowClassName = this.rowClassName;
      var clss = [];
      if (this.pendingRecords.includes(params.row)) {
        clss.push("row__pending");
      }
      return clss.concat(rowClassName ? rowClassName(params) : []);
    },
    handleActiveMethod: function handleActiveMethod(params) {
      return !~this.pendingRecords.indexOf(params.row) && (!this.editConfig.activeMethod || this.editConfig.activeMethod(params));
    },
    sortChangeEvent: function sortChangeEvent(params) {
      var remoteSort = this.remoteSort;
      var column = params.column;
      var eventParams = extend(false, {
        $grid: this
      }, params);
      if (isBoolean(column.remoteSort) ? column.remoteSort : remoteSort) {
        this.sortData = params;
        this.tablePage.currentPage = 1;
        this.commitProxy("query");
      }
      emitEvent(this, "sort-change", eventParams);
      this.emitter.emit("sort-change", eventParams);
    },
    viewCls: function viewCls2(module) {
      return GlobalConfig$1.viewConfig[module][this.viewType] || "";
    },
    filterChangeEvent: function filterChangeEvent(params) {
      var eventParams = extend(false, {
        $grid: this
      }, params);
      if (this.remoteFilter) {
        this.filterData = params.filters;
        this.commitProxy("query");
      }
      emitEvent(this, "filter-change", eventParams);
      this.emitter.emit("filter-change", eventParams);
    }
  }
});
Grid.install = function(Vue) {
  Vue.component(Grid.name, Grid);
};
function operArrs(_ref180) {
  var _vm = _ref180._vm, editStore = _ref180.editStore, newRecords = _ref180.newRecords, row2 = _ref180.row, tableFullData = _ref180.tableFullData;
  if (row2 === -1) {
    Array.prototype.push.apply(tableFullData, newRecords);
  }
  if (row2 && row2 !== -1) {
    var insertIndex = tableFullData.indexOf(row2);
    if (insertIndex === -1) {
      throw new Error(error("ui.grid.error.unableInsert"));
    }
    Array.prototype.splice.apply(tableFullData, [insertIndex, 0].concat(newRecords));
  }
  if (!row2) {
    Array.prototype.unshift.apply(tableFullData, newRecords);
  }
  Array.prototype.unshift.apply(editStore.insertList, newRecords);
  newRecords.forEach(function(row22) {
    return editStore.insertMap.set(getRowid(_vm, row22), row22);
  });
}
var _setActiveCell = function _setActiveCell2(row2, field) {
  var _this52 = this;
  var next1 = function next12() {
    if (!row2 || !field) {
      return _this52.$nextTick();
    }
    var column = find$1(_this52.visibleColumn, function(column2) {
      return column2.property === field;
    });
    if (!column || !column.editor) {
      return _this52.$nextTick();
    }
    var next2 = function next22(cell) {
      if (!cell) {
        return;
      }
      var rowIndex = _this52.getRowIndex(row2);
      var columnIndex = _this52.getColumnIndex(column);
      _this52.handleActived({
        row: row2,
        rowIndex,
        column,
        columnIndex,
        cell,
        $table: _this52
      });
      _this52.lastCallTime = Date.now();
    };
    getCell(_this52, {
      row: row2,
      column
    }).then(next2);
    return _this52.$nextTick();
  };
  return this.scrollToRow(row2, true).then(next1);
};
var Methods$c = {
  _insert: function _insert(records) {
    return this.insertAt(records);
  },
  // 根据位置从指定行添加数据
  _insertAt: function _insertAt(records, row2) {
    var _this53 = this;
    var editStore = this.editStore, isAsyncColumn = this.isAsyncColumn, scrollYLoad = this.scrollYLoad, tableFullData = this.tableFullData, treeConfig = this.treeConfig, visibleColumn = this.visibleColumn;
    if (treeConfig) {
      throw new Error(error("ui.grid.error.treeInsert"));
    }
    if (!isArray(records)) {
      records = [records];
    }
    var isColumnFormat = isAsyncColumn && visibleColumn.some(function(column) {
      var _column$format, _column$format$async;
      return (_column$format = column.format) == null ? void 0 : (_column$format$async = _column$format.async) == null ? void 0 : _column$format$async.fetch;
    });
    var defaultRowId = "_RID";
    var newRecords = records.map(function(record) {
      isColumnFormat && (record[GlobalConfig$1.constant.insertedField] = true);
      record == null ? true : delete record[defaultRowId];
      return hooks.reactive(_this53.defineField(Object.assign({}, record)));
    });
    operArrs({
      _vm: this,
      editStore,
      row: row2,
      tableFullData,
      newRecords
    });
    this.updateCache();
    this.handleTableData(true);
    this.checkSelectionStatus();
    this.updateFooter();
    if (scrollYLoad) {
      this.updateScrollYSpace();
    }
    var res = {
      row: newRecords.length ? newRecords[newRecords.length - 1] : null,
      rows: newRecords
    };
    return this.$nextTick().then(function() {
      _this53.recalculate();
      return res;
    });
  },
  /**
   * 删除指定行数据
   * 如果传 row 则删除一行
   * 如果传 rows 则删除多行
   */
  _remove: function _remove(rows) {
    var _this54 = this;
    var afterFullData = this.afterFullData, _this$selectConfig3 = this.selectConfig, selectConfig = _this$selectConfig3 === void 0 ? {} : _this$selectConfig3;
    var selection = this.selection, tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    var _this$editStore = this.editStore, insertList = _this$editStore.insertList, insertMap = _this$editStore.insertMap, removeList = _this$editStore.removeList;
    var checkField = selectConfig.checkField;
    var nowData = afterFullData;
    var rest = [];
    var rowsOld = rows;
    var inArr = function inArr2(row3, rows2) {
      return ~rows2.indexOf(row3);
    };
    if (treeConfig) {
      throw new Error(error("ui.grid.error.treeRemove"));
    }
    if (!rowsOld) {
      rows = tableFullData;
    }
    if (rowsOld && !isArray(rowsOld)) {
      rows = [rowsOld];
    }
    for (var i = 0; i < rows.length; i++) {
      var row2 = rows[i];
      if (this.isTemporaryRow(row2)) continue;
      removeList.push(row2);
    }
    if (!checkField) {
      remove(selection, function(row3) {
        return inArr(row3, rows);
      });
    }
    if (tableFullData === rows) {
      rows = tableFullData.slice(0);
      tableFullData.length = 0;
      nowData.length = 0;
    } else {
      rest = remove(tableFullData, function(row3) {
        return inArr(row3, rows);
      });
      remove(nowData, function(row3) {
        return inArr(row3, rows);
      });
    }
    remove(insertList, function(row3) {
      var exist = inArr(row3, rows);
      exist && insertMap.delete(getRowid(_this54, row3));
      return exist;
    });
    this.updateCache();
    this.handleTableData(true);
    this.checkSelectionStatus();
    this.updateFooter();
    var res = {
      row: rows && rows.length ? rows[rows.length - 1] : null,
      rows: rest
    };
    return this.$nextTick().then(function() {
      _this54.recalculate();
      return res;
    });
  },
  /**
   * 删除选中数据
   */
  _removeSelecteds: function _removeSelecteds() {
    var _this55 = this;
    var selectRecords = this.getSelectRecords(true);
    var callback = function callback2(params) {
      _this55.clearSelection();
      return params;
    };
    return this.remove(selectRecords).then(callback);
  },
  _revert: function _revert() {
    warn("ui.grid.error.delRevert");
    return this.revertData.apply(this, arguments);
  },
  /**
   * 对数据进行还原
   * 如果不传任何参数，则还原整个表格；
   * 如果传row则还原一行；
   * 如果传rows则还原多行；
   * 如果还额外传了field则还原指定单元格。
   */
  _revertData: function _revertData(rows, field) {
    var rawData = this.rawData;
    if (arguments.length && rows && !isArray(rows)) {
      rows = [rows];
    }
    if (!arguments.length) {
      rows = rawData || [];
    }
    for (var i = 0; i < rows.length; i++) {
      var row2 = rows[i];
      var oRow = this.getOriginRow(row2);
      if (oRow && row2) {
        if (field) {
          set(row2, field, get(oRow, field));
        } else {
          destructuring(row2, oRow);
        }
      }
      this.updateRowStatus(row2);
    }
    if (arguments.length) {
      return this.$nextTick();
    }
    return this.reloadData(rawData || []);
  },
  /**
   * 获取表格操作数据集
   */
  _getRecordset: function _getRecordset() {
    var res = {};
    res.insertRecords = this.getInsertRecords();
    res.removeRecords = this.getRemoveRecords();
    res.updateRecords = this.getUpdateRecords();
    return res;
  },
  /**
   * 获取删除数据列表
   */
  _getRemoveRecords: function _getRemoveRecords() {
    return this.editStore.removeList;
  },
  /**
   * 获取新增数据列表
   */
  _getInsertRecords: function _getInsertRecords() {
    return this.editStore.insertList;
  },
  /**
   * 获取更新数据列表
   * 只精准匹配row的更改。如果是树表格，子节点更改状态不会影响父节点的更新状态
   */
  _getUpdateRecords: function _getUpdateRecords() {
    var _this56 = this;
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    var handler6 = function handler7(row2) {
      return !_this56.isTemporaryRow(row2) && _this56.hasRowChange(row2);
    };
    var updateRecords = treeConfig ? filterTree(tableFullData, handler6, treeConfig) : tableFullData.filter(handler6);
    return updateRecords;
  },
  /**
   * 处理激活编辑
   */
  handleActived: function handleActived(params, event) {
    var _this57 = this;
    return _asyncToGenerator(/* @__PURE__ */ _regenerator().m(function _callee() {
      var editConfig, editStore, tableColumn2, cell, column, row2, editor, actived, isActiveCell, type, canActive, args;
      return _regenerator().w(function(_context) {
        while (1) switch (_context.n) {
          case 0:
            editConfig = _this57.editConfig, editStore = _this57.editStore, tableColumn2 = _this57.tableColumn;
            cell = params.cell, column = params.column, row2 = params.row;
            editor = column.editor;
            actived = editStore.actived;
            isActiveCell = handleActivedCheckCell({
              actived,
              column,
              editConfig,
              row: row2
            });
            if (!(editor && cell && isActiveCell && editConfig)) {
              _context.n = 2;
              break;
            }
            type = "edit-disabled";
            _context.n = 1;
            return handleActivedCanActive({
              editConfig,
              params
            });
          case 1:
            canActive = _context.v;
            args = {
              _vm: _this57,
              actived,
              canActive,
              cell,
              column,
              editConfig
            };
            Object.assign(args, {
              event,
              params,
              row: row2,
              tableColumn: tableColumn2,
              type
            });
            type = handleActivedDoActive(args);
            handleActivedClearActive({
              _vm: _this57,
              canActive,
              event
            });
            emitEvent(_this57, type, [params, event]);
          case 2:
            handleActivedTryActive({
              _vm: _this57,
              actived,
              cell,
              column,
              editor,
              event,
              isActiveCell,
              params,
              row: row2
            });
            return _context.a(2, _this57.$nextTick());
        }
      }, _callee);
    }))();
  },
  _getColumnModel: function _getColumnModel(row2, column) {
    var model = column.model, editor = column.editor;
    if (editor) {
      model.value = getCellValue(row2, column);
      model.update = false;
    }
  },
  _setColumnModel: function _setColumnModel(row2, column) {
    var model = column.model, editor = column.editor;
    if (editor && model.update) {
      setCellValue(row2, column, model.value);
      model.update = false;
      model.value = null;
    }
  },
  _getActiveRow: function _getActiveRow() {
    var $el = this.$el, editStore = this.editStore, tableData = this.tableData;
    var actived = editStore.actived;
    var args = actived.args, row2 = actived.row;
    var length = $el.querySelectorAll(".tiny-grid-body__column.col__actived").length;
    return args && ~tableData.indexOf(row2) && length ? _extends({}, args) : null;
  },
  /**
   * 清除已激活的编辑
   */
  _clearActived: function _clearActived(event) {
    var _this58 = this;
    var _this$editConfig3 = this.editConfig, editConfig = _this$editConfig3 === void 0 ? {} : _this$editConfig3, editStore = this.editStore, tableColumn2 = this.tableColumn;
    var actived = editStore.actived;
    var args = actived.args, column = actived.column, row2 = actived.row;
    var isActived = row2 || column;
    if (isActived && editConfig.mode === "row") {
      tableColumn2.forEach(function(column22) {
        return _this58._setColumnModel(row2, column22);
      });
    }
    if (isActived && editConfig.mode !== "row") {
      this._setColumnModel(row2, column);
    }
    if (isActived) {
      var _editor$attrs;
      this.updateRowStatus(actived.row);
      this.updateFooter();
      var row22 = args.row, column2 = args.column;
      var _ref181 = column2 || {}, editor = _ref181.editor;
      if ((editor == null ? void 0 : editor.component) === "input" && (editor == null ? void 0 : (_editor$attrs = editor.attrs) == null ? void 0 : _editor$attrs.type) === "number") {
        row22[column2.property] = +row22[column2.property];
      }
      emitEvent(this, "edit-closed", [args, event]);
    }
    actived.args = null;
    actived.column = null;
    actived.row = null;
    return this.clearValidate().then(this.recalculate);
  },
  _hasActiveRow: function _hasActiveRow(row2) {
    return this.editStore.actived.row === row2;
  },
  /**
   * 处理聚焦
   */
  handleFocus: function handleFocus(params) {
    var cell = params.cell, column = params.column;
    var editor = column.editor;
    if (!editor) {
      return;
    }
    var inputElem;
    var compRender = Renderer.get(editor.component);
    var autofocus = editor.autofocus, autoselect = editor.autoselect, focusDelay = editor.focusDelay;
    var handler6 = function handler7() {
      if (autofocus) {
        inputElem = cell.querySelector(autofocus);
      }
      var isAutofocus = compRender && compRender.autofocus;
      if (isAutofocus && !inputElem) {
        inputElem = cell.querySelector(isAutofocus);
      }
      if (!inputElem) {
        return;
      }
      var type = inputElem.type;
      inputElem.type = "text";
      inputElem.selectionEnd = inputElem.value.length;
      inputElem.selectionStart = inputElem.selectionEnd;
      inputElem.type = type;
      inputElem[autoselect ? "select" : "focus"]();
      if (browserInfo.name !== "ie") {
        return;
      }
      var textRange = inputElem.createTextRange();
      textRange.collapse(false);
      textRange.select();
    };
    setTimeout(handler6, focusDelay || 50);
  },
  /**
   * 激活单元格编辑
   */
  _setActiveCell,
  /**
   * 激活行编辑
   */
  _setActiveRow: function _setActiveRow(row2) {
    var editColumn = find$1(this.visibleColumn, function(column) {
      return column.editor;
    });
    return this.setActiveCell(row2, editColumn.property);
  },
  /**
   * 只对trigger为dblclick有效，选中单元格
   */
  _setSelectCell: function _setSelectCell(row2, field) {
    var _this59 = this;
    var editConfig = this.editConfig, tableData = this.tableData, visibleColumn = this.visibleColumn;
    if (!row2 || !field || editConfig.trigger === "manual") {
      return this.$nextTick();
    }
    var column = find$1(visibleColumn, function(column2) {
      return column2.property === field;
    });
    var rowIndex = tableData.indexOf(row2);
    if (!~rowIndex || !column) {
      return this.$nextTick();
    }
    var next = function next2(cell) {
      var columnIndex = visibleColumn.indexOf(column);
      var params = {
        row: row2,
        rowIndex,
        column,
        columnIndex,
        cell
      };
      _this59.handleSelected(params, {});
    };
    getCell(this, {
      row: row2,
      rowIndex,
      column
    }).then(next);
    return this.$nextTick();
  },
  updateSelectedCls: function updateSelectedCls(clear2) {
    var editStore = this.editStore, elemStore = this.elemStore, _this$mouseConfig2 = this.mouseConfig, mouseConfig = _this$mouseConfig2 === void 0 ? {} : _this$mouseConfig2;
    var _editStore$selected = editStore.selected, column = _editStore$selected.column, row2 = _editStore$selected.row;
    clear2 && this.clearSelected(true);
    if (!mouseConfig.selected || !row2 || !column) {
      return;
    }
    var listElem = elemStore["main-body-list"];
    var rowid2 = getRowid(this, row2);
    var trElem = listElem.querySelector('[data-rowid="' + rowid2 + '"]');
    if (!trElem) {
      return;
    }
    addClass(trElem.querySelector("." + column.id), "col__selected");
  },
  /**
   * 处理选中源
   */
  handleSelected: function handleSelected(params, event, noDebounce) {
    var _this60 = this;
    var editConfig = this.editConfig, editStore = this.editStore, elemStore = this.elemStore, _this$mouseConfig3 = this.mouseConfig, mouseConfig = _this$mouseConfig3 === void 0 ? {} : _this$mouseConfig3;
    var actived = editStore.actived, selected = editStore.selected;
    var _ref182 = params || {}, cell = _ref182.cell, column = _ref182.column, row2 = _ref182.row;
    var selectMethod = function selectMethod2() {
      var _cell$parentNode;
      if (selected.row === row2 && selected.column === column || actived.row === row2 && (editConfig.mode !== "cell" || actived.column === column)) {
        return _this60.$nextTick();
      }
      if (_this60.keyboardConfig || _this60.mouseConfig) {
        _this60.clearChecked(event);
        _this60.clearIndexChecked();
        _this60.clearHeaderChecked();
        _this60.clearSelected(event);
      }
      _this60.clearActived(event);
      selected.args = params;
      selected.row = row2;
      selected.column = column;
      _this60.updateSelectedCls();
      if (!mouseConfig.checked) {
        return _this60.$nextTick();
      }
      var headerElem = elemStore["main-body-headerList"];
      _this60.handleChecked([[cell]]);
      if (!headerElem) {
        return _this60.$nextTick();
      }
      _this60.handleHeaderChecked([[headerElem == null ? void 0 : headerElem.querySelector("." + (column == null ? void 0 : column.id))]]);
      _this60.handleIndexChecked([[cell == null ? void 0 : (_cell$parentNode = cell.parentNode) == null ? void 0 : _cell$parentNode.querySelector(".col__index")]]);
      return _this60.$nextTick();
    };
    selectMethod = noDebounce ? selectMethod : debounce(20, selectMethod);
    return selectMethod();
  }
};
var Edit = {
  install: function install(Table2) {
    Grid$1._edit = 1;
    Object.assign(Table2.methods, Methods$c);
  }
};
var Export = {
  install: function install2(Table2) {
    Grid$1._export = 1;
    Object.assign(Table2.methods, Methods$g);
  }
};
function findLeft(params) {
  var checkColumn = params.checkColumn, columnIndex = params.columnIndex, isLeft = params.isLeft, rowIndex = params.rowIndex, tableData = params.tableData;
  var targetColumn = params.targetColumn, targetColumnIndex = params.targetColumnIndex, targetRow = params.targetRow, targetRowIndex = params.targetRowIndex, visibleColumn = params.visibleColumn;
  if (!isLeft) {
    return {
      targetColumn,
      targetColumnIndex,
      targetRow,
      targetRowIndex
    };
  }
  for (var _index5 = columnIndex - 1; _index5 >= 0; _index5--) {
    if (checkColumn(visibleColumn[_index5])) {
      targetColumnIndex = _index5;
      targetColumn = visibleColumn[_index5];
      break;
    }
  }
  if (targetColumn || rowIndex <= 0) {
    return {
      targetColumn,
      targetColumnIndex,
      targetRow,
      targetRowIndex
    };
  }
  targetRowIndex = rowIndex - 1;
  targetRow = tableData[targetRowIndex];
  for (var _index6 = visibleColumn.length - 1; _index6 >= 0; _index6--) {
    if (checkColumn(visibleColumn[_index6])) {
      targetColumnIndex = _index6;
      targetColumn = visibleColumn[_index6];
      break;
    }
  }
  return {
    targetColumn,
    targetColumnIndex,
    targetRow,
    targetRowIndex
  };
}
function findRight(params) {
  var checkColumn = params.checkColumn, columnIndex = params.columnIndex, isLeft = params.isLeft, rowIndex = params.rowIndex, tableData = params.tableData;
  var targetColumn = params.targetColumn, targetColumnIndex = params.targetColumnIndex, targetRow = params.targetRow, targetRowIndex = params.targetRowIndex, visibleColumn = params.visibleColumn;
  if (isLeft) {
    return {
      targetColumn,
      targetColumnIndex,
      targetRow,
      targetRowIndex
    };
  }
  for (var _index7 = columnIndex + 1; _index7 < visibleColumn.length; _index7++) {
    if (checkColumn(visibleColumn[_index7])) {
      targetColumnIndex = _index7;
      targetColumn = visibleColumn[_index7];
      break;
    }
  }
  if (targetColumn || rowIndex >= tableData.length - 1) {
    return {
      targetColumn,
      targetColumnIndex,
      targetRow,
      targetRowIndex
    };
  }
  targetRowIndex = rowIndex + 1;
  targetRow = tableData[targetRowIndex];
  for (var _index8 = 0; _index8 < visibleColumn.length; _index8++) {
    if (checkColumn(visibleColumn[_index8])) {
      targetColumnIndex = _index8;
      targetColumn = visibleColumn[_index8];
      break;
    }
  }
  return {
    targetColumn,
    targetColumnIndex,
    targetRow,
    targetRowIndex
  };
}
function processTarget(args1) {
  var _vm = args1._vm, args = args1.args, edit = args1.edit, editConfig = args1.editConfig, event = args1.event, isLeft = args1.isLeft, params = args1.params, rowIndex = args1.rowIndex;
  var targetColumn = args1.targetColumn, targetColumnIndex = args1.targetColumnIndex, targetRow = args1.targetRow, targetRowIndex = args1.targetRowIndex;
  if (targetColumn) {
    if (targetRow) {
      params.rowIndex = targetRowIndex;
      params.row = targetRow;
    } else {
      params.rowIndex = rowIndex;
    }
    params.columnIndex = targetColumnIndex;
    params.column = targetColumn;
    getCell(_vm, params).then(function(resCell) {
      params.cell = resCell;
      if (editConfig) {
        if (editConfig.trigger === "click" || editConfig.trigger === "dblclick") {
          if (editConfig.mode === "row" || edit) {
            _vm.handleActived(params, event);
          } else {
            _vm.handleSelected(params, event);
            _vm.scrollToRow(params.row, params.column, false, {
              isLeftArrow: isLeft,
              isRightArrow: !isLeft,
              from: args.column
            });
          }
        }
      }
    });
  }
}
function onCellMousedownGridEl(args) {
  var _vm = args._vm, bodyList = args.bodyList, cell = args.cell, cellFirstElementChild = args.cellFirstElementChild, cellLastElementChild = args.cellLastElementChild, flag = args.flag, headStart = args.headStart, headerList = args.headerList, isIndex = args.isIndex, startCellNode = args.startCellNode, targetElem = args.targetElem;
  if (flag) {
    if (isIndex) {
      var firstCell = targetElem.parentNode.firstElementChild;
      _vm.handleChecked(getRowNodes(bodyList, getCellNodeIndex(firstCell.nextElementSibling), getCellNodeIndex(cellLastElementChild)));
      _vm.handleIndexChecked(getRowNodes(bodyList, getCellNodeIndex(firstCell), getCellNodeIndex(cell)));
    } else if (!hasClass(targetElem, "col__index")) {
      var _firstCell = targetElem.parentNode.firstElementChild;
      var colIndex = [].indexOf.call(targetElem.parentNode.children, targetElem);
      var head = headerList[0].children[colIndex];
      _vm.handleHeaderChecked(getRowNodes(headerList, getCellNodeIndex(head), getCellNodeIndex(headStart)));
      _vm.handleIndexChecked(getRowNodes(bodyList, getCellNodeIndex(_firstCell), getCellNodeIndex(cellFirstElementChild)));
      _vm.handleChecked(getRowNodes(bodyList, startCellNode, getCellNodeIndex(targetElem)));
    }
  }
}
function handleCellMousedownEvent(args1) {
  var $el = args1.$el, _vm = args1._vm, bodyList = args1.bodyList, cell = args1.cell, cellFirstElementChild = args1.cellFirstElementChild;
  var cellLastElementChild = args1.cellLastElementChild, headStart = args1.headStart, headerList = args1.headerList, isIndex = args1.isIndex, startCellNode = args1.startCellNode;
  var _vm$mouseConfig2 = _vm.mouseConfig, mouseConfig = _vm$mouseConfig2 === void 0 ? {} : _vm$mouseConfig2;
  var _ref183 = mouseConfig || {}, _ref183$updateInterva = _ref183.updateInterval, updateInterval = _ref183$updateInterva === void 0 ? 10 : _ref183$updateInterva;
  var oldMousemove = document.onmousemove;
  var oldMouseup = document.onmouseup;
  var updateEvent = function updateEvent2(event) {
    event.preventDefault();
    var _getEventTargetNode = getEventTargetNode(event, $el, "tiny-grid-body__column"), flag = _getEventTargetNode.flag, targetElem = _getEventTargetNode.targetElem;
    var args = {
      _vm,
      bodyList,
      cell,
      cellFirstElementChild,
      cellLastElementChild,
      flag
    };
    Object.assign(args, {
      headStart,
      headerList,
      isIndex,
      startCellNode,
      targetElem
    });
    onCellMousedownGridEl(args);
  };
  var updateEventThrot = throttle(updateInterval, false, updateEvent, true);
  document.onmousemove = updateEventThrot;
  document.onmouseup = function() {
    document.onmousemove = oldMousemove;
    document.onmouseup = oldMouseup;
  };
}
function onCellMousedownSelectEditable(args) {
  var _vm = args._vm, actived = args.actived, cell = args.cell, checked = args.checked, column = args.column, editConfig = args.editConfig;
  var event = args.event, isLeftBtn = args.isLeftBtn, mouseConfig = args.mouseConfig, params = args.params, row2 = args.row;
  if (editConfig && (actived.row !== row2 || !(editConfig.mode === "cell" && actived.column === column)) && !(isLeftBtn && mouseConfig.checked) && mouseConfig.selected && editConfig.trigger === "dblclick" && (!checked.rowNodes || !checked.rowNodes.some(function(list) {
    return ~list.indexOf(cell);
  }))) {
    _vm.handleSelected(params, event);
  }
}
function onCellMousedownSelect(_ref184) {
  var _vm = _ref184._vm, editConfig = _ref184.editConfig, event = _ref184.event, mouseConfig = _ref184.mouseConfig, params = _ref184.params;
  if (!editConfig && mouseConfig.selected) {
    _vm.handleSelected(params, event);
  }
}
function onCellMousedownIndexColumn(args) {
  var _vm = args._vm, bodyList = args.bodyList, cell = args.cell, cellLastElementChild = args.cellLastElementChild, event = args.event;
  var headerList = args.headerList, isIndex = args.isIndex, params = args.params, visibleColumn = args.visibleColumn;
  if (isIndex) {
    var firstCell = cell.parentNode.firstElementChild;
    params.columnIndex++;
    params.column = visibleColumn[params.columnIndex];
    params.cell = cell.nextElementSibling;
    _vm.handleSelected(params, event);
    _vm.handleChecked(getRowNodes(bodyList, getCellNodeIndex(firstCell.nextElementSibling), getCellNodeIndex(cellLastElementChild)));
    _vm.handleHeaderChecked([headerList[0].querySelectorAll(".tiny-grid-header__column:not(.col__index)")]);
    _vm.handleIndexChecked(getRowNodes(bodyList, getCellNodeIndex(firstCell), getCellNodeIndex(cell)));
  }
}
function onCellMousedownNotIndexColumn(_ref185) {
  var _vm = _ref185._vm, cell = _ref185.cell, column = _ref185.column, event = _ref185.event, headerList = _ref185.headerList, isIndex = _ref185.isIndex, params = _ref185.params;
  if (!isIndex) {
    var firstCell = cell.parentNode.firstElementChild;
    _vm.handleSelected(params, event);
    _vm.handleHeaderChecked([[headerList[0].querySelector("." + column.id)]]);
    _vm.handleIndexChecked([[firstCell]]);
  }
}
function handleHeaderCellMousedownEvent(_ref186) {
  var $el = _ref186.$el, _vm = _ref186._vm, bodyList = _ref186.bodyList, cell = _ref186.cell, headerList = _ref186.headerList, startCell = _ref186.startCell;
  var oldMousemove = document.onmousemove;
  var oldMouseup = document.onmouseup;
  var updateEvent = function updateEvent2(event) {
    event.preventDefault();
    var _getEventTargetNode2 = getEventTargetNode(event, $el, "tiny-grid-header__column"), flag = _getEventTargetNode2.flag, targetElem = _getEventTargetNode2.targetElem;
    if (!flag) {
      var tmp = getEventTargetNode(event, $el, "tiny-grid-body__column");
      flag = tmp.flag;
      targetElem = tmp.targetElem;
    }
    if (flag && !hasClass(targetElem, "col__index")) {
      var colIndex = [].concat(targetElem.parentNode.children).indexOf(targetElem);
      var lastCell = bodyList[bodyList.length - 1].children[colIndex];
      var headCell = headerList[0].children[colIndex];
      _vm.handleHeaderChecked(getRowNodes(headerList, getCellNodeIndex(headCell), getCellNodeIndex(cell)));
      _vm.handleChecked(getRowNodes(bodyList, getCellNodeIndex(startCell), getCellNodeIndex(lastCell)));
    }
  };
  var updateEventThrot = throttle(80, false, updateEvent, true);
  addClass($el, "tiny-grid-cell__checked");
  document.onmousemove = updateEventThrot;
  document.onmouseup = function() {
    removeClass($el, "tiny-grid-cell__checked");
    document.onmousemove = oldMousemove;
    document.onmouseup = oldMouseup;
  };
}
var removeCellClass = function removeCellClass2(bodyRef, clazz) {
  return arrayEach(bodyRef.$el.querySelectorAll("." + clazz), function(elem) {
    return removeClass(elem, clazz);
  });
};
var getCellIndex = function getCellIndex2(_ref187) {
  var cell = _ref187.cell, bodyList = _ref187.bodyList;
  var trElem = cell.parentNode;
  var cIndex = arrayIndexOf(trElem.children, cell);
  var rIndex = arrayIndexOf(bodyList, trElem);
  return {
    rIndex,
    cIndex
  };
};
var getModify = function getModify2(_ref188) {
  var offsetTop = _ref188.offsetTop, offsetLeft = _ref188.offsetLeft, cWidth = _ref188.cWidth, cHeight = _ref188.cHeight;
  var modifyDomStyle = function modifyDomStyle2(dom, styleOptions) {
    return dom && Object.assign(dom.style, styleOptions);
  };
  return function(top, right, bottom, left) {
    modifyDomStyle(top, {
      top: offsetTop + "px",
      left: offsetLeft + "px",
      width: cWidth + "px"
    });
    modifyDomStyle(right, {
      top: offsetTop + "px",
      left: offsetLeft + cWidth + "px",
      height: cHeight + "px"
    });
    modifyDomStyle(bottom, {
      top: offsetTop + cHeight + "px",
      left: offsetLeft + "px",
      width: cWidth + "px"
    });
    modifyDomStyle(left, {
      top: offsetTop + "px",
      left: offsetLeft + "px",
      height: cHeight + "px"
    });
  };
};
var writeClipboardText = function writeClipboardText2(_ref189) {
  var $table = _ref189.$table, columns2 = _ref189.columns, rows = _ref189.rows, rowNodes = _ref189.rowNodes;
  var _$table$keyboardConfi = $table.keyboardConfig, keyboardConfig = _$table$keyboardConfi === void 0 ? {} : _$table$keyboardConfi;
  var _keyboardConfig$clipb = keyboardConfig.clipboard, clipboard = _keyboardConfig$clipb === void 0 ? {} : _keyboardConfig$clipb;
  var writeMethod = clipboard.writeMethod, _clipboard$cellSplit = clipboard.cellSplit, cellSplit = _clipboard$cellSplit === void 0 ? "," : _clipboard$cellSplit, _clipboard$rowSplit = clipboard.rowSplit, rowSplit = _clipboard$rowSplit === void 0 ? ";" : _clipboard$rowSplit;
  if (!clipboard) return;
  var value;
  if (typeof writeMethod === "function") {
    value = writeMethod({
      $table,
      columns: columns2,
      rows
    });
  } else {
    var rowValues = [];
    rowNodes.forEach(function(row2) {
      var cellValues = [];
      row2.forEach(function(col) {
        cellValues.push(col && col.innerText);
      });
      rowValues.push(cellValues.join(cellSplit));
    });
    value = rowValues.join(rowSplit);
  }
  var writeFallback = function writeFallback2() {
    var input = document.createElement("input");
    input.value = value;
    document.body.appendChild(input);
    input.select();
    document.execCommand("Copy");
    document.body.removeChild(input);
  };
  if (isSecureContext && navigator.clipboard) {
    navigator.clipboard.writeText(value).catch(function(reason) {
      warn("ui.grid.error.clipboardWriteError", reason);
      writeFallback();
    });
  } else {
    writeFallback();
  }
};
var Methods$b = {
  // Tab键移动处理
  moveTabSelected: function moveTabSelected(args, isLeft, event, edit) {
    var editConfig = this.editConfig, hasIndexColumn2 = this.hasIndexColumn, tableData = this.tableData, visibleColumn = this.visibleColumn;
    var params = _extends({}, args);
    var columnIndex = visibleColumn.indexOf(params.column);
    var rowIndex = tableData.indexOf(params.row);
    var _ref190 = {}, targetColumn = _ref190.targetColumn, targetColumnIndex = _ref190.targetColumnIndex, targetRow = _ref190.targetRow, targetRowIndex = _ref190.targetRowIndex;
    event.preventDefault();
    var checkColumn = function checkColumn2(column) {
      return !hasIndexColumn2(column) && (edit ? column.editor : true);
    };
    var args1 = {
      columnIndex,
      checkColumn,
      isLeft,
      tableData,
      rowIndex,
      targetColumn
    };
    Object.assign(args1, {
      targetRowIndex,
      targetColumnIndex,
      targetRow,
      visibleColumn
    });
    var ret = findLeft(args1);
    targetColumn = ret.targetColumn;
    targetColumnIndex = ret.targetColumnIndex;
    targetRow = ret.targetRow;
    targetRowIndex = ret.targetRowIndex;
    args1 = {
      checkColumn,
      columnIndex,
      isLeft,
      rowIndex,
      tableData,
      targetColumn
    };
    Object.assign(args1, {
      targetColumnIndex,
      targetRow,
      targetRowIndex,
      visibleColumn
    });
    ret = findRight(args1);
    targetColumn = ret.targetColumn;
    targetColumnIndex = ret.targetColumnIndex;
    targetRow = ret.targetRow;
    targetRowIndex = ret.targetRowIndex;
    args1 = {
      _vm: this,
      args,
      edit,
      editConfig,
      event,
      isLeft,
      params,
      rowIndex
    };
    Object.assign(args1, {
      targetColumn,
      targetColumnIndex,
      targetRow,
      targetRowIndex
    });
    processTarget(args1);
  },
  // 当前行方向键移动处理
  moveCurrentRow: function moveCurrentRow(isUpArrow, isDwArrow, event) {
    var _this61 = this;
    var afterFullData = this.afterFullData, currentRow = this.currentRow, treeConfig = this.treeConfig;
    var targetRow;
    event.preventDefault();
    if (treeConfig) {
      var _findTree2 = findTree$1(afterFullData, function(row2) {
        return row2 === currentRow;
      }, treeConfig), curIndex = _findTree2.index, neighbors = _findTree2.items;
      if (isUpArrow && curIndex > 0) {
        targetRow = neighbors[curIndex - 1];
      } else if (isDwArrow && curIndex < neighbors.length - 1) {
        targetRow = neighbors[curIndex + 1];
      }
    } else {
      var _curIndex = afterFullData.indexOf(currentRow);
      if (isUpArrow && _curIndex > 0) {
        targetRow = afterFullData[_curIndex - 1];
      } else if (isDwArrow && _curIndex < afterFullData.length - 1) {
        targetRow = afterFullData[_curIndex + 1];
      }
    }
    if (targetRow) {
      this.scrollToRow(targetRow).then(function() {
        return _this61.triggerCurrentRowEvent(event, {
          $table: _this61,
          row: targetRow
        });
      });
    }
  },
  // 可编辑方向键移动处理
  moveSelected: function moveSelected(_ref191) {
    var _this62 = this;
    var args = _ref191.args, isLeftArrow = _ref191.isLeftArrow, isUpArrow = _ref191.isUpArrow, isRightArrow = _ref191.isRightArrow, isDownArrow = _ref191.isDownArrow, event = _ref191.event;
    var hasIndexColumn2 = this.hasIndexColumn, visibleColumn = this.tableColumn, tableData = this.tableFullData;
    var params = _extends({}, args);
    var column = params.column;
    var columnIndex = visibleColumn.indexOf(column);
    event.preventDefault();
    if (isUpArrow && params.rowIndex) {
      params.row = tableData[--params.rowIndex];
    } else if (isDownArrow && params.rowIndex < tableData.length - 1) {
      params.row = tableData[++params.rowIndex];
    } else if (isLeftArrow && columnIndex) {
      for (var pos = columnIndex - 1; pos >= 0; pos--) {
        if (!hasIndexColumn2(visibleColumn[pos])) {
          params.columnIndex = pos;
          params.column = visibleColumn[pos];
          break;
        }
      }
    } else if (isRightArrow) {
      for (var _pos = columnIndex + 1; _pos < visibleColumn.length; _pos++) {
        if (!hasIndexColumn2(visibleColumn[_pos])) {
          params.columnIndex = _pos;
          params.column = visibleColumn[_pos];
          break;
        }
      }
    }
    getCell(this, params).then(function(resCell) {
      params.cell = resCell;
      _this62.handleSelected(params, event, true);
      _this62.scrollToRow(params.row, params.column, false, {
        isLeftArrow,
        isRightArrow,
        from: column
      });
    });
  },
  // 表头按下事件
  triggerHeaderCellMousedownEvent: function triggerHeaderCellMousedownEvent(event, params) {
    var _this63 = this;
    var $el = this.$el, elemStore = this.elemStore, _this$mouseConfig4 = this.mouseConfig, mouseConfig = _this$mouseConfig4 === void 0 ? {} : _this$mouseConfig4, tableData = this.tableData;
    var headerList = elemStore["main-body-headerList"].children;
    var bodyList = elemStore["main-body-list"].children;
    var cell = params.cell;
    var column = params.column;
    var isIndex = column.type === "index";
    var startCell = bodyList[0].querySelector("." + column.id);
    var button = event.button;
    var isLeftBtn = button === 0;
    if (!isLeftBtn || !mouseConfig.checked) {
      return;
    }
    if (isIndex) {
      this.handleAllChecked(event);
      this.closeMenu();
      return;
    }
    event.preventDefault();
    event.stopPropagation();
    this.clearSelected(event);
    this.clearHeaderChecked();
    this.clearIndexChecked();
    handleHeaderCellMousedownEvent({
      $el,
      _vm: this,
      bodyList,
      cell,
      headerList,
      startCell
    });
    this.handleHeaderChecked([[cell]]);
    if (!bodyList.length) {
      this.closeMenu();
      return;
    }
    var firstTr = bodyList[0];
    var lastTr = bodyList[bodyList.length - 1];
    var firstCell = firstTr.querySelector(".col__index");
    var lastCell = lastTr.querySelector("." + column.id);
    params.rowIndex = 0;
    params.row = tableData[0];
    getCell(this, params).then(function(cellRes) {
      params.cell = cellRes;
      _this63.handleSelected(params, event);
      var rowNodes = getRowNodes(bodyList, getCellNodeIndex(firstCell), getCellNodeIndex(lastTr.querySelector(".col__index")));
      _this63.handleIndexChecked(rowNodes);
      _this63.handleChecked(getRowNodes(bodyList, getCellNodeIndex(startCell), getCellNodeIndex(lastCell)));
    });
    this.closeMenu();
  },
  // 单元格按下事件
  triggerCellMousedownEvent: function triggerCellMousedownEvent(event, params) {
    var $el = this.$el, editConfig = this.editConfig, editStore = this.editStore, elemStore = this.elemStore, _this$mouseConfig5 = this.mouseConfig, mouseConfig = _this$mouseConfig5 === void 0 ? {} : _this$mouseConfig5, visibleColumn = this.visibleColumn;
    var actived = editStore.actived, checked = editStore.checked;
    var _mouseConfig$excludes = mouseConfig.excludes, excludeClo = _mouseConfig$excludes === void 0 ? [] : _mouseConfig$excludes;
    var button = event.button;
    var cell = params.cell, column = params.column, row2 = params.row;
    var isLeftBtn = button === 0;
    var args;
    if (excludeClo.includes(column.type || column.property)) {
      return;
    }
    if (editConfig && (actived.row !== row2 || !(editConfig.mode === "cell" && actived.column === column)) && isLeftBtn && mouseConfig.checked) {
      event.preventDefault();
      event.stopPropagation();
      this.clearHeaderChecked();
      this.clearIndexChecked();
      var isIndex = column.type === "index";
      var startCellNode = getCellNodeIndex(cell);
      var headerList = elemStore["main-body-headerList"].children;
      var bodyList = elemStore["main-body-list"].children;
      var cellFirstElementChild = cell.parentNode.firstElementChild;
      var cellLastElementChild = cell.parentNode.lastElementChild;
      var colIndex = Array.from(cell.parentNode.children).indexOf(cell);
      var headStart = headerList == null ? void 0 : headerList[0].children[colIndex];
      args = {
        $el,
        _vm: this,
        bodyList,
        cell,
        cellFirstElementChild
      };
      Object.assign(args, {
        cellLastElementChild,
        headStart,
        headerList,
        isIndex,
        startCellNode
      });
      handleCellMousedownEvent(args);
      args = {
        _vm: this,
        bodyList,
        cell,
        cellLastElementChild
      };
      Object.assign(args, {
        event,
        headerList,
        isIndex,
        params,
        visibleColumn
      });
      onCellMousedownIndexColumn(args);
      onCellMousedownNotIndexColumn({
        _vm: this,
        cell,
        column,
        event,
        headerList,
        isIndex,
        params
      });
      this.closeFilter();
      this.closeMenu();
    }
    args = {
      _vm: this,
      actived,
      cell,
      checked,
      column,
      editConfig
    };
    Object.assign(args, {
      event,
      isLeftBtn,
      mouseConfig,
      params,
      row: row2
    });
    onCellMousedownSelectEditable(args);
    onCellMousedownSelect({
      _vm: this,
      editConfig,
      event,
      mouseConfig,
      params
    });
  },
  // 清除所选中源状态
  _clearSelected: function _clearSelected(keep) {
    var selected = this.editStore.selected, elemStore = this.elemStore;
    if (!keep) {
      selected.row = null;
      selected.column = null;
    }
    var bodyElem = elemStore["main-body-list"];
    var headerElem = elemStore["main-body-headerList"];
    if (bodyElem) {
      var elem = bodyElem.querySelector(".col__selected");
      removeClass(elem, "col__selected");
    }
    if (headerElem) {
      arrayEach(headerElem.querySelectorAll(".col__title-selected"), function(elem2) {
        return removeClass(elem2, "col__title-selected");
      });
    }
    return this.$nextTick();
  },
  // 清除所有选中状态
  _clearChecked: function _clearChecked() {
    var $refs = this.$refs, editStore = this.editStore, mouseConfig = this.mouseConfig;
    var checked = editStore.checked;
    if (!mouseConfig || !mouseConfig.checked) {
      return this.$nextTick();
    }
    Object.assign(checked, {
      columns: [],
      rows: [],
      tColumns: [],
      tRows: [],
      rowNodes: []
    });
    var tableBody = $refs.tableBody;
    tableBody.$refs.checkBorders.style.display = "none";
    removeCellClass(tableBody, "col__checked");
    return this.$nextTick();
  },
  _getMouseCheckeds: function _getMouseCheckeds() {
    var _this64 = this;
    var _this$editStore$check = this.editStore.checked.rowNodes, rowNodes = _this$editStore$check === void 0 ? [] : _this$editStore$check;
    var _ref192 = {}, _ref192$rows = _ref192.rows, rows = _ref192$rows === void 0 ? [] : _ref192$rows, _ref192$columns = _ref192.columns, columns2 = _ref192$columns === void 0 ? [] : _ref192$columns;
    var res = {
      rows,
      columns: columns2,
      rowNodes
    };
    if (rowNodes && rowNodes.length) {
      res.rows = rowNodes.map(function(arr) {
        return _this64.getRowNode(arr[0].parentNode).item;
      });
      res.columns = rowNodes[0].map(function(col) {
        return _this64.getColumnNode(col).item;
      });
    }
    return res;
  },
  _getMouseSelecteds: function _getMouseSelecteds() {
    var _this$editStore$selec = this.editStore.selected, args = _this$editStore$selec.args, column = _this$editStore$selec.column;
    if (!args || !column) {
      return null;
    }
    return extend(true, {}, args);
  },
  // 处理所有选中
  handleChecked: function handleChecked(rowNodes) {
    var $refs = this.$refs, mouseConfig = this.mouseConfig;
    if (!mouseConfig || !mouseConfig.checked) {
      return;
    }
    var _ref193 = {}, cHeight = _ref193.cHeight, cWidth = _ref193.cWidth, offsetLeft = _ref193.offsetLeft, offsetTop = _ref193.offsetTop;
    cWidth = cHeight = -2;
    offsetTop = offsetLeft = 0;
    this.clearChecked();
    arrayEach(rowNodes, function(rowNode, rowIndex) {
      arrayEach(rowNode, function(colNode, colIndex) {
        var firstRow = rowIndex === 0;
        var firstCol = colIndex === 0;
        if (firstRow) {
          if (firstCol) {
            offsetTop = colNode.offsetTop;
            offsetLeft = colNode.offsetLeft;
          }
          cWidth += colNode.offsetWidth;
        }
        if (firstCol) {
          cHeight += colNode.offsetHeight;
        }
        addClass(colNode, "col__checked");
      });
    });
    var modify = getModify({
      offsetTop,
      offsetLeft,
      cWidth,
      cHeight
    });
    var tableBody = $refs.tableBody;
    var _tableBody$$refs = tableBody.$refs, checkBorders = _tableBody$$refs.checkBorders, checkTop = _tableBody$$refs.checkTop, checkRight = _tableBody$$refs.checkRight, checkBottom = _tableBody$$refs.checkBottom, checkLeft = _tableBody$$refs.checkLeft;
    checkBorders.style.display = "none";
    modify(checkTop, checkRight, checkBottom, checkLeft);
    checkBorders.style.display = "block";
    this.editStore.checked.rowNodes = rowNodes;
  },
  handleAllChecked: function handleAllChecked(event) {
    var _this65 = this;
    var elemStore = this.elemStore, _this$mouseConfig6 = this.mouseConfig, mouseConfig = _this$mouseConfig6 === void 0 ? {} : _this$mouseConfig6, tableData = this.tableData, visibleColumn = this.visibleColumn;
    if (!mouseConfig.checked) {
      return;
    }
    event.preventDefault();
    var column = find$1(visibleColumn, function(col) {
      return col.type === "index";
    }) || visibleColumn[0];
    var selectorColumnId = "." + column.id;
    var headerListElem = elemStore["main-body-headerList"];
    var headerList = headerListElem == null ? void 0 : headerListElem.children;
    var cell = headerListElem == null ? void 0 : headerListElem.querySelector(selectorColumnId);
    var bodyList = elemStore["main-body-list"].children;
    var firstTrElem = bodyList[0];
    var firstCell = firstTrElem.querySelector(selectorColumnId);
    var lastTrElem = bodyList[bodyList.length - 1];
    var params = {
      $table: this,
      rowIndex: 0,
      row: tableData[0]
    };
    params.column = find$1(visibleColumn, function(col) {
      return col.property;
    });
    params.columnIndex = this.getColumnIndex(params.column);
    getCell(this, params).then(function(resCell) {
      params.cell = resCell;
      _this65.handleSelected(params, event, true);
      _this65.handleHeaderChecked(getRowNodes(headerList, getCellNodeIndex(cell.nextElementSibling), getCellNodeIndex(cell.parentNode.lastElementChild)));
      _this65.handleIndexChecked(getRowNodes(bodyList, getCellNodeIndex(firstCell), getCellNodeIndex(lastTrElem.querySelector(selectorColumnId))));
      _this65.handleChecked(getRowNodes(bodyList, getCellNodeIndex(firstCell.nextElementSibling), getCellNodeIndex(lastTrElem.lastElementChild)));
    });
  },
  handleIndexChecked: function handleIndexChecked(rowNodes) {
    this.clearIndexChecked();
    arrayEach(rowNodes, function(rowNode) {
      arrayEach(rowNode, function(colNode) {
        addClass(colNode, "col__index-checked");
      });
    });
    this.editStore.indexs.rowNodes = rowNodes;
  },
  _clearIndexChecked: function _clearIndexChecked() {
    var tbody = this.elemStore["main-body-list"];
    if (tbody) {
      var indexCheckeds = tbody.querySelectorAll(".col__index-checked");
      var eachHandler = function eachHandler2(colNode) {
        return removeClass(colNode, "col__index-checked");
      };
      arrayEach(indexCheckeds, eachHandler);
    }
    Object.assign(this.editStore.indexs, {
      rowNodes: []
    });
    return this.$nextTick();
  },
  handleHeaderChecked: function handleHeaderChecked(rowNodes) {
    this.clearHeaderChecked();
    arrayEach(rowNodes, function(rowNode) {
      arrayEach(rowNode, function(colNode) {
        addClass(colNode, "col__title-checked");
      });
    });
    this.editStore.titles.rowNodes = rowNodes;
  },
  _clearHeaderChecked: function _clearHeaderChecked() {
    var headerElem = this.elemStore["main-body-headerList"];
    if (headerElem) {
      var eachHandler = function eachHandler2(colNode) {
        return removeClass(colNode, "col__title-checked");
      };
      arrayEach(headerElem.querySelectorAll(".col__title-checked"), eachHandler);
    }
    return this.$nextTick();
  },
  // 清空已复制的内容
  _clearCopyed: function _clearCopyed() {
    var $refs = this.$refs, editStore = this.editStore, keyboardConfig = this.keyboardConfig;
    var editStoreCopyed = editStore.copyed;
    if (!keyboardConfig || !keyboardConfig.isCut) return this.$nextTick();
    var tableBody = $refs.tableBody;
    editStoreCopyed.cut = false;
    editStoreCopyed.rows = [];
    editStoreCopyed.columns = [];
    tableBody.$refs.copyBorders.style.display = "none";
    removeCellClass(tableBody, "col__copyed");
    return this.$nextTick();
  },
  // 处理复制
  handleCopyed: function handleCopyed(cut) {
    var editStore = this.editStore, tableColumn2 = this.tableColumn, tableData = this.tableData;
    var copyed = editStore.copyed;
    var rowNodes = editStore.checked.rowNodes;
    var _ref194 = {}, cHeight = _ref194.cHeight, cWidth = _ref194.cWidth, offsetLeft = _ref194.offsetLeft, offsetTop = _ref194.offsetTop, _ref194$rows = _ref194.rows, rows = _ref194$rows === void 0 ? [] : _ref194$rows, _ref194$columns = _ref194.columns, columns2 = _ref194$columns === void 0 ? [] : _ref194$columns;
    cWidth = cHeight = -3;
    offsetTop = offsetLeft = 0;
    this.clearCopyed();
    if (!rowNodes) return;
    if (rowNodes.length) {
      var firstRows = rowNodes[0];
      var firstCell = firstRows[0];
      var firstRowsLength = firstRows.length;
      var _getCellNodeIndex = getCellNodeIndex(firstCell), rowIndex = _getCellNodeIndex.rowIndex, columnIndex = _getCellNodeIndex.columnIndex;
      columns2 = tableColumn2.slice(columnIndex, columnIndex + firstRowsLength);
      rows = tableData.slice(rowIndex, rowIndex + rowNodes.length);
      writeClipboardText({
        $table: this,
        columns: columns2,
        rows,
        rowNodes
      });
    }
    arrayEach(rowNodes, function(rowNode, rowIndex2) {
      arrayEach(rowNode, function(colNode, colIndex) {
        var isTop = rowIndex2 === 0;
        var isLeft = colIndex === 0;
        if (isTop) {
          if (isLeft) {
            offsetTop = colNode.offsetTop;
            offsetLeft = colNode.offsetLeft;
          }
          cWidth += colNode.offsetWidth;
        }
        if (isLeft) {
          cHeight += colNode.offsetHeight;
        }
        addClass(colNode, "col__copyed");
      });
    });
    var modify = getModify({
      offsetTop,
      offsetLeft,
      cWidth,
      cHeight
    });
    var tableBody = this.$refs.tableBody;
    var _tableBody$$refs2 = tableBody.$refs, copyBorders = _tableBody$$refs2.copyBorders, copyTop = _tableBody$$refs2.copyTop, copyRight = _tableBody$$refs2.copyRight, copyBottom = _tableBody$$refs2.copyBottom, copyLeft = _tableBody$$refs2.copyLeft;
    modify(copyTop, copyRight, copyBottom, copyLeft);
    copyBorders.style.display = "block";
    copyed.cut = cut;
    copyed.columns = columns2;
    copyed.rows = rows;
    copyed.rowNodes = rowNodes;
  },
  // 处理粘贴
  handlePaste: function handlePaste() {
    var editStore = this.editStore, elemStore = this.elemStore, tableData = this.tableData, visibleColumn = this.visibleColumn;
    var copyed = editStore.copyed, selected = editStore.selected;
    var columns2 = copyed.columns, cut = copyed.cut, rows = copyed.rows;
    if (!rows.length || !columns2.length || !selected.row || !selected.column) return;
    var _selected$args = selected.args, columnIndex = _selected$args.columnIndex, rowIndex = _selected$args.rowIndex;
    arrayEach(rows, function(row2, rIndex2) {
      var targetRow = tableData[rowIndex + rIndex2];
      if (targetRow) {
        arrayEach(columns2, function(column, cIndex2) {
          var targetColumn = visibleColumn[columnIndex + cIndex2];
          targetColumn && setCellValue(targetRow, targetColumn, getCellValue(row2, column));
          cut && setCellValue(row2, column, null);
        });
      }
    });
    cut && this.clearCopyed();
    var cell = selected.args.cell;
    var bodyList = elemStore["main-body-list"].children;
    var _getCellIndex = getCellIndex({
      cell,
      bodyList
    }), rIndex = _getCellIndex.rIndex, cIndex = _getCellIndex.cIndex;
    var maxIndex = bodyList.length - 1;
    var curIndex = rIndex + rows.length - 1;
    var targetTrElem = bodyList[curIndex > maxIndex ? maxIndex : curIndex];
    maxIndex = targetTrElem.children.length - 1;
    curIndex = cIndex + columns2.length - 1;
    var targetCell = targetTrElem.children[curIndex > maxIndex ? maxIndex : curIndex];
    var targetCellNode = getCellNodeIndex(targetCell);
    var cellNode = getCellNodeIndex(cell);
    var rowNodes = getRowNodes(bodyList, cellNode, targetCellNode);
    this.handleChecked(rowNodes);
  },
  handleClearMouseChecked: function handleClearMouseChecked(event) {
    var $grid = this.$grid, $refs = this.$refs, autoClearMouseChecked = this.autoClearMouseChecked, autoClearKeyboardCopy = this.autoClearKeyboardCopy;
    var tableWrapper = $refs.tableWrapper, tooltip = $refs.tooltip, validTip = $refs.validTip;
    var equalOrContain = function equalOrContain2(elm, target) {
      return elm && (elm === target || elm.contains(target));
    };
    var actualTarget = getActualTarget(event);
    if (!equalOrContain($grid.$el, actualTarget) && !equalOrContain(tableWrapper, actualTarget) && !equalOrContain(tooltip && tooltip.state.popperElm, actualTarget) && !equalOrContain(validTip && validTip.state.popperElm, actualTarget)) {
      if (autoClearMouseChecked) {
        this.clearChecked();
        this.clearSelected();
      }
      if (autoClearKeyboardCopy) {
        this.clearCopyed();
      }
    }
  }
};
var Keyboard = {
  install: function install3(Table2) {
    Grid$1._keyboard = 1;
    Object.assign(Table2.methods, Methods$b);
  }
};
function adjustParams(rows, cb, vaildDatas) {
  if (rows) {
    if (isFunction(rows)) {
      cb = rows;
    } else {
      vaildDatas = isArray(rows) ? rows : [rows];
    }
  }
  return {
    cb,
    vaildDatas
  };
}
var columnHandler = function columnHandler2(_ref195) {
  var _vm = _ref195._vm, colValidPromiseArr = _ref195.colValidPromiseArr, editRules = _ref195.editRules, isAll = _ref195.isAll, row2 = _ref195.row, validRest = _ref195.validRest;
  return function(column, columnIndex) {
    if (has(editRules, column.property)) {
      colValidPromiseArr.push(new Promise(function(resolve, reject) {
        _vm.validCellRules("all", row2, column).then(resolve).catch(function(_ref196) {
          var rule = _ref196.rule, rules = _ref196.rules;
          var rowIndex = _vm.getRowIndex(row2);
          var rest = {
            rule,
            rules,
            rowIndex,
            row: row2,
            columnIndex,
            column,
            $table: _vm
          };
          if (isAll) {
            if (!validRest[column.property]) {
              validRest[column.property] = [];
            }
            validRest[column.property].push(rest);
            resolve();
          }
          reject(rest);
        });
      }));
    }
  };
};
function validTree(_ref197) {
  var treeConfig = _ref197.treeConfig, handleVaild = _ref197.handleVaild, hasTreeExpand2 = _ref197.hasTreeExpand, vaildDatas = _ref197.vaildDatas, treeOpts = _ref197.treeOpts;
  if (treeConfig.validHidden === false) {
    var _recurValid = function recurValid(row2) {
      var children = row2[treeConfig.children || "children"];
      handleVaild(row2);
      if (hasTreeExpand2(row2) && children && children.length) {
        children.forEach(_recurValid);
      }
    };
    vaildDatas.forEach(_recurValid);
  } else {
    eachTree(vaildDatas, handleVaild, treeOpts);
  }
}
var realValid = function realValid2(_ref198) {
  var _vm = _ref198._vm, editRules = _ref198.editRules, isAll = _ref198.isAll, validRest = _ref198.validRest, treeConfig = _ref198.treeConfig, hasTreeExpand2 = _ref198.hasTreeExpand, vaildDatas = _ref198.vaildDatas, treeOpts = _ref198.treeOpts;
  var rowValids = [];
  var columns2 = _vm.getColumns();
  var handleVaild = function handleVaild2(row2) {
    var colValidPromiseArr = [];
    columns2.forEach(columnHandler({
      _vm,
      colValidPromiseArr,
      editRules,
      isAll,
      row: row2,
      validRest
    }));
    rowValids.push(Promise.all(colValidPromiseArr));
  };
  if (treeConfig) {
    validTree({
      treeConfig,
      handleVaild,
      hasTreeExpand: hasTreeExpand2,
      vaildDatas,
      treeOpts
    });
  } else {
    vaildDatas.forEach(handleVaild);
  }
  return rowValids;
};
var Rule = /* @__PURE__ */ (function() {
  function Rule2(rule) {
    Object.assign(this, {
      $options: rule,
      max: rule.min,
      maxWidth: rule.maxWidth,
      min: rule.min,
      pattern: rule.pattern,
      required: rule.required,
      trigger: rule.trigger,
      type: rule.type,
      validator: rule.validator
    });
  }
  return _createClass(Rule2, [{
    key: "message",
    get: function get2() {
      return getFuncText(this.$options.message);
    }
  }]);
})();
var onRejected = function onRejected2(opt, _this) {
  var isAll = opt.isAll, validRest = opt.validRest, cb = opt.cb, afterFullData = opt.afterFullData, treeConfig = opt.treeConfig;
  return function(params) {
    var _ref199;
    var args = isAll ? validRest : (_ref199 = {}, _ref199[params.column.property] = params, _ref199);
    var funcFinish = function funcFinish2(args2, reject, resolve) {
      return function() {
        opt.status = false;
        cb && cb(opt.status, args2);
        cb ? resolve() : reject(args2);
      };
    };
    var funcPosAndFinish = function funcPosAndFinish2(params2, finish) {
      return /* @__PURE__ */ _asyncToGenerator(/* @__PURE__ */ _regenerator().m(function _callee2() {
        return _regenerator().w(function(_context2) {
          while (1) switch (_context2.n) {
            case 0:
              if (!_this.scrollXLoad) {
                _context2.n = 1;
                break;
              }
              _context2.n = 1;
              return _this.scrollToColumn(params2.column, true);
            case 1:
              return _context2.a(2, getCell(_this, params2).then(function(activeCell) {
                params2.cell = activeCell;
                _this.handleValidError(params2);
                finish();
              }));
          }
        }, _callee2);
      }));
    };
    var getLocatRow = function getLocatRow2(params2) {
      var row2 = params2.row;
      var rowIndex = afterFullData.indexOf(row2);
      return rowIndex > 0 ? afterFullData[rowIndex - 1] : row2;
    };
    return new Promise(function(resolve, reject) {
      var finish = funcFinish(args, reject, resolve);
      var posAndFinish = funcPosAndFinish(params, finish);
      var locatRow = getLocatRow(params);
      var isAutoPosFalse = _this.validOpts.autoPos === false;
      isAutoPosFalse && finish();
      !isAutoPosFalse && treeConfig && _this.scrollToTreeRow(locatRow).then(posAndFinish);
      !isAutoPosFalse && !treeConfig && _this.scrollToRow(locatRow, params.column, true).then(posAndFinish);
    });
  };
};
var Methods$a = {
  // 对表格数据进行校验
  _validate: function _validate(rows, cb) {
    return this.beginValidate(rows, cb);
  },
  // 与validate一致行为，区别就是会校验所有并返回所有不通过的列
  _fullValidate: function _fullValidate(rows, cb) {
    return this.beginValidate(rows, cb, true);
  },
  // 聚焦到校验通过的单元格并弹出校验错误提示
  handleValidError: function handleValidError(params) {
    var _this66 = this;
    var event = {
      type: "valid-error",
      trigger: "call"
    };
    this.handleActived(params, event).then(function() {
      return _this66.showValidTooltip(params);
    });
  },
  validatePromise: function validatePromise(row2, column, columnIndex, isAll, validRest) {
    var _this67 = this;
    function onrejected(_ref201) {
      var _vm = _ref201._vm, reject = _ref201.reject, resolve = _ref201.resolve;
      return function(_ref202) {
        var rule = _ref202.rule, rules = _ref202.rules;
        var rest = {
          $table: _vm,
          column,
          columnIndex,
          row: row2,
          rule,
          rules
        };
        rest.rowIndex = _vm.getRowIndex(row2);
        if (!isAll) {
          return reject(rest);
        }
        validRest[column.property] = validRest[column.property] || [];
        validRest[column.property].push(rest);
        return resolve();
      };
    }
    return new Promise(function(resolve, reject) {
      _this67.validCellRules("all", row2, column).then(resolve).catch(onrejected({
        _vm: _this67,
        reject,
        resolve
      }));
    });
  },
  /**
   * 对表格数据进行校验
   * 如果传 row 指定行记录，则只验证传入的行
   * 如果传 rows 为多行记录，则只验证传入的行
   * 如果只传 callback 否则默认验证整个表格数据
   * isAll: 是否全量校验，如果为true会校验所有并返回所有不通过的列
   * 返回 Promise 对象，或者使用回调方式
   */
  beginValidate: function beginValidate(rows, callback, isAll) {
    var afterFullData = this.afterFullData, editRules = this.editRules, hasTreeExpand2 = this.hasTreeExpand, treeConfig = this.treeConfig, treeOpts = this.treeOpts;
    var _ref203 = {}, _ref203$status = _ref203.status, status = _ref203$status === void 0 ? true : _ref203$status, _ref203$validRest = _ref203.validRest, validRest = _ref203$validRest === void 0 ? {} : _ref203$validRest;
    var _adjustParams = adjustParams(rows, callback, afterFullData), vaildDatas = _adjustParams.vaildDatas, cb = _adjustParams.cb;
    var opt = {
      isAll,
      validRest,
      cb,
      afterFullData,
      treeConfig,
      status
    };
    this.lastCallTime = Date.now();
    this.clearValidate();
    if (!editRules) {
      if (cb) {
        cb(opt.status);
      }
      return Promise.resolve(opt.status);
    }
    var validParams = {
      _vm: this,
      editRules,
      isAll,
      validRest,
      treeConfig,
      hasTreeExpand: hasTreeExpand2,
      vaildDatas,
      treeOpts
    };
    var rowValids = realValid(validParams);
    var onFulfilled = function onFulfilled2() {
      var ruleKeys = Object.keys(validRest);
      if (ruleKeys.length) {
        return Promise.reject(validRest[ruleKeys[0]][0]);
      }
      cb && cb(opt.status);
    };
    return Promise.all(rowValids).then(onFulfilled).catch(onRejected(opt, this));
  },
  hasCellRules: function hasCellRules(type, row2, _ref204) {
    var property = _ref204.property;
    if (!property || !this.editRules) {
      return false;
    }
    var rules = get(this.editRules, property);
    var handler6 = function handler7(rule) {
      return type === "all" || !rule.trigger || type === rule.trigger;
    };
    rules = !Array.isArray(rules) && (isObject$1(rules) || isFunction(rules)) ? [rules] : rules;
    return rules && find$1(rules, handler6);
  },
  /**
   * 校验数据：
   * 按表格行顺序、列顺序依次校验（同步或异步）；
   * 根据校验规则的索引顺序依次校验，如果是异步则会等待校验完成才会继续校验下一列；
   * 如果校验失败，则触发回调或者Promise，结果返回一个Boolean值；
   * 如果是传回调方式这返回一个Boolean值和校验不通过列的错误消息；
   *
   * rule 配置：
   *  required为Boolean表示是否必填；
   *  max为Number表示最大长度；
   *  min为Number表示最小长度；
   *  validator为Function(rule, value, callback, {rules, row, column, rowIndex, columnIndex})进行自定义校验；
   *  trigger为blur|change表示触发方式（默认为空就行，除非特殊场景）；
   *  @param {'change' | 'all'} type 校验单元格的触发方式
   *  @param { IRow }  row 表格的行数据
   *  @param { IColumnConfig }  column 表格的行数据
   *  @param { any }  defaultValue 需要校验的默认值
   */
  validCellRules: function validCellRules(type, row2, column, defaultValue) {
    var _this68 = this;
    var editRules = this.editRules, rowId = this.rowId;
    var property = column.property;
    var _ref205 = {}, _ref205$descriptor = _ref205.descriptor, descriptor = _ref205$descriptor === void 0 ? {} : _ref205$descriptor, _ref205$model = _ref205.model, model = _ref205$model === void 0 ? {} : _ref205$model;
    if (property && editRules) {
      var rules = get(editRules, property);
      var cellValue = isUndefined(defaultValue) ? get(row2, property) : defaultValue;
      if (Array.isArray(rules)) {
        rules.forEach(function(rule, index2) {
          model[property + index2] = cellValue;
          descriptor[property + index2] = rule;
        });
      } else {
        model[property] = cellValue;
        descriptor[property] = rules;
      }
    }
    var _descriptor = extend(true, {}, descriptor);
    var validator = new Validator$1(_descriptor, t);
    var executor = function executor2(resolve, reject) {
      var validArgs = {
        firstFields: true,
        first: true,
        custom: {
          row: row2,
          column
        }
      };
      var onRejected3 = function onRejected32(_ref206) {
        var fields = _ref206.fields;
        var cellErrors = Object.keys(fields).map(function(prop) {
          var rules2 = _descriptor[prop];
          _descriptor[prop] = !rules2.message ? Object.assign(rules2, {
            message: fields[prop][0].message
          }) : rules2;
          return new Rule(_descriptor[prop]);
        });
        reject({
          rules: cellErrors,
          rule: cellErrors[0]
        });
      };
      validator.validate(model, validArgs).then(resolve).catch(onRejected3);
    };
    var onFulfilled = function onFulfilled2() {
      _this68.validatedMap[column.id + "-" + row2[rowId]] = false;
      return Promise.resolve();
    };
    var onRejected22 = function onRejected23(errors) {
      _this68.validatedMap[column.id + "-" + row2[rowId]] = true;
      return Promise.reject(errors);
    };
    return new Promise(executor).then(onFulfilled).catch(onRejected22);
  },
  _clearValidate: function _clearValidate() {
    Object.assign(this.validStore, {
      column: null,
      content: "",
      isArrow: false,
      row: null,
      rule: null,
      visible: false
    });
    this.clostValidTooltip(void 0);
    return this.$nextTick();
  },
  clearValidateMap: function clearValidateMap() {
    if (this.validatedMap) {
      for (var key in this.validatedMap) {
        this.validatedMap[key] = false;
      }
    }
  },
  // 触发校验
  triggerValidate: function triggerValidate(type) {
    var _this69 = this;
    var editConfig = this.editConfig, editRules = this.editRules, editStore = this.editStore, validStore = this.validStore;
    var actived = editStore.actived;
    if (!actived.row || !editRules) {
      return Promise.resolve();
    }
    var _actived$args = actived.args, cell = _actived$args.cell, column = _actived$args.column, row2 = _actived$args.row;
    if (!this.hasCellRules(type, row2, column)) {
      return Promise.resolve();
    }
    var onfulfilled = function onfulfilled2() {
      editConfig.mode === "row" && validStore.visible && validStore.row === row2 && validStore.column === column && _this69.clearValidate();
    };
    var onrejected = function onrejected2(_ref207) {
      var rule = _ref207.rule;
      if (rule.trigger && type !== rule.trigger) {
        return Promise.resolve();
      }
      var rest = {
        cell,
        column,
        row: row2,
        rule
      };
      _this69.showValidTooltip(rest);
      return Promise.reject(rest);
    };
    return this.validCellRules(type, row2, column).then(onfulfilled).catch(onrejected);
  },
  // 弹出校验错误提示
  showValidTooltip: function showValidTooltip(params) {
    var _this70 = this;
    var $refs = this.$refs, height2 = this.height, tableData = this.tableData, validOpts2 = this.validOpts;
    var cell = params.cell, column = params.column, row2 = params.row, rule = params.rule;
    var content = rule.message;
    var validTip = $refs.validTip;
    var isMessageTooltip = validOpts2.isMessageTooltip, isMessageDefault = validOpts2.isMessageDefault, isMessageInline = validOpts2.isMessageInline;
    var showMsg = isMessageTooltip || isMessageDefault && !height2 && tableData.length < 2;
    this.$nextTick(function() {
      Object.assign(_this70.validStore, {
        row: row2,
        column,
        rule,
        content,
        visible: true
      });
      if (validTip && showMsg) {
        _this70.clostValidTooltip();
        _this70.validTipContent = content;
        validTip.state.referenceElm = cell;
        validTip.$refs.popper && (validTip.$refs.popper.style.display = "none");
        validTip.doDestroy();
        validTip.setExpectedState(true);
        _this70.activateTooltipValid(validTip);
      } else if (isMessageInline) {
        _this70.$nextTick(function() {
          return _this70.recalculate();
        });
      }
      emitEvent(_this70, "valid-error", [params]);
    });
  },
  // 关闭 validTip
  clostValidTooltip: function clostValidTooltip() {
    var validTip = this.$refs.validTip;
    if (validTip) {
      validTip.setExpectedState(false);
      validTip.handleClosePopper();
    }
    return this.$nextTick();
  }
};
var Validator = {
  install: function install4(Table2) {
    Grid$1._valid = 1;
    Object.assign(Table2.methods, Methods$a);
  }
};
var Methods$9 = {
  bindResize: function bindResize() {
    var _this71 = this;
    var parentElem = this.getParentElem();
    if ((parentElem == null ? void 0 : parentElem.nodeType) === Node.ELEMENT_NODE) {
      (this.$resize = new Resize$1(function() {
        _this71.updateParentHeight();
        _this71.recalculate();
      }, GlobalConfig$1.resizeInterval)).observe(parentElem);
    }
  },
  unbindResize: function unbindResize() {
    var _this$$resize;
    (_this$$resize = this.$resize) == null ? void 0 : _this$$resize.disconnect();
  }
};
var Resize = {
  install: function install5(Table2) {
    Grid$1._resize = 1;
    Object.assign(Table2.methods, Methods$9);
  }
};
var Methods$8 = {
  /**
   * 初始化fetch-data配置项
   * @returns {object}
   */
  initFetchOption: function initFetchOption() {
    var _this$fetchData = this.fetchData, fetchData = _this$fetchData === void 0 ? {} : _this$fetchData, _this$dataset = this.dataset, dataset = _this$dataset === void 0 ? {} : _this$dataset;
    if (fetchData.api || dataset.source || dataset.value || dataset.api) {
      var _ref208 = fetchData || dataset.source || dataset.api || {}, loading = _ref208.loading, fields = _ref208.fields, api = _ref208.api, reloadConfig = _ref208.reloadConfig;
      var isReloadFilter = false;
      var isReloadScroll = false;
      if (reloadConfig) {
        isReloadFilter = Boolean(reloadConfig.filter);
        isReloadScroll = Boolean(reloadConfig.scroll);
      }
      return {
        api,
        dataset,
        fields,
        loading,
        isReloadFilter,
        isReloadScroll
      };
    }
  },
  /* 第二个参数sortArg参数可以移除，fetchData.args可以传递更加灵活的参数
     但为避免破坏性变更，暂保留
  */
  handleFetch: function handleFetch(code, sortArg) {
    var _this72 = this;
    var pager = this.pager, sortData = this.sortData, filterData = this.filterData, pagerConfig = this.pagerConfig, fetchOption = this.fetchOption, fetchData = this.fetchData, dataset = this.dataset;
    var _fetchData$reloadConf = fetchData.reloadConfig, reloadConfig = _fetchData$reloadConf === void 0 ? {} : _fetchData$reloadConf;
    var _reloadConfig$scroll = reloadConfig.scroll, scroll = _reloadConfig$scroll === void 0 ? false : _reloadConfig$scroll;
    if (this.isInitialLoading) {
      this.isInitialLoading = false;
    } else {
      this.columnAnchor && this.clearActiveAnchor();
    }
    if (code !== "prefetch") {
      this.clearRadioRow();
      !scroll && this.resetScrollTop();
    }
    if (!fetchOption) {
      error("ui.grid.error.notQuery");
      return this.$nextTick();
    }
    var _ref209 = fetchData || dataset.source || dataset.api || {}, args = _ref209.args, loading = _ref209.loading;
    var field = sortData.field, order = sortData.order, prop = sortData.prop, property = sortData.property;
    var sortByData = {
      field,
      order,
      prop,
      property
    };
    var params = _extends({
      $grid: this,
      sort: sortData,
      sortBy: sortByData,
      filters: filterData
    }, args);
    var search;
    var _fetchOption$isReload3 = fetchOption.isReloadFilter, isReloadFilter = _fetchOption$isReload3 === void 0 ? false : _fetchOption$isReload3;
    this.tableLoading = loading;
    if (pagerConfig) {
      params.page = pagerConfig;
    }
    if (code === "reload") {
      if (pager || args.page) {
        pagerConfig.currentPage = 1;
      }
      this.sortData = params.sort = {};
      if (!isReloadFilter) {
        params.filters = [];
        this.filterData = params.filters;
      }
      this.pendingRecords = [];
      this.clearAll();
    }
    if (sortArg && sortArg.length > 0) {
      params.sortBy = sortArg;
    }
    if (fetchData && fetchData.api) {
      search = fetchData.api(params);
    } else {
      search = getDataset({
        dataset,
        service: this.$service
      }, params);
    }
    return search.then(this.loadFetchData).catch(function(error2) {
      _this72.tableLoading = false;
      throw error2;
    });
  },
  clearActiveAnchor: function clearActiveAnchor() {
    var columnAnchor = this.columnAnchor, _this$columnAnchorPar = this.columnAnchorParams, columnAnchorParams = _this$columnAnchorPar === void 0 ? {} : _this$columnAnchorPar;
    var _columnAnchorParams$a = columnAnchorParams.anchors, anchors = _columnAnchorParams$a === void 0 ? [] : _columnAnchorParams$a;
    if (!columnAnchor || anchors.length <= 0) return;
    anchors.forEach(function(anchor) {
      return anchor.active = false;
    });
  },
  loadFetchData: function loadFetchData(rest) {
    if (!rest) {
      this.tableData = [];
      this.tableLoading = false;
      return;
    }
    var _this$fetchOption$fie = this.fetchOption.fields, fields = _this$fetchOption$fie === void 0 ? {} : _this$fetchOption$fie, pagerConfig = this.pagerConfig, pagerSlot = this.pagerSlot;
    if (pagerConfig && !Array.isArray(rest)) {
      var _rest$result;
      var total = getObj(rest, fields.total || "page.total") || (rest == null ? void 0 : (_rest$result = rest.result) == null ? void 0 : _rest$result.length) || 0;
      var data7 = getObj(rest, fields.result || fields.data || "result") || [];
      this.tableData = data7;
      pagerConfig.total = total;
      var setTotal = pagerSlot && pagerSlot.componentInstance.setTotal;
      setTotal && setTotal(total);
    } else {
      this.tableData = (fields.list ? getObj(rest, fields.list) : rest) || [];
    }
    this.tableLoading = false;
  }
};
var FetchData = {
  host: "grid",
  install: function install6(host) {
    Object.assign(host.methods, Methods$8);
  }
};
var Methods$7 = {
  // 初始化表格分页配置
  initPagerConfig: function initPagerConfig() {
    var $slots = this.$slots, fetchOption = this.fetchOption, _this$scrollLoad = this.scrollLoad, scrollLoad = _this$scrollLoad === void 0 ? {} : _this$scrollLoad;
    var pagerProps = {};
    if (fetchOption) {
      var pagerSlot = $slots.pager && $slots.pager[0];
      if (pagerSlot) {
        var componentOptions = pagerSlot.componentOptions, children = pagerSlot.children;
        if (componentOptions && !children) {
          this.pagerSlot = pagerSlot;
          pagerProps = componentOptions.propsData;
        }
      } else if (this.pager) {
        pagerProps = this.pager.attrs;
      }
      if (this.pager || $slots.pager || this.scrollLoad) {
        return Object.assign(this.tablePage, {
          pageSize: scrollLoad.pageSize
        }, pagerProps);
      }
      return fetchOption.args && fetchOption.args.page;
    }
  },
  // 表格内置分页渲染器
  renderPager: function renderPager(_ref210) {
    var $slots = _ref210.$slots, _vm = _ref210._vm, loading = _ref210.loading, pager = _ref210.pager, pagerConfig = _ref210.pagerConfig, tableLoading = _ref210.tableLoading, vSize3 = _ref210.vSize;
    var res = null;
    var fetchData = _vm.fetchData;
    if ($slots.pager) {
      res = $slots.pager();
    } else if (pager) {
      pager.component = pager.component || (fetchData && fetchData.api ? Pager$1 : null);
      res = h(hooks.toRaw(pager.component), {
        props: _extends({
          // tiny-grid 的 size 为 small/mini 时，pager 传 mini；为 medium 时，pager 传空字符串
          size: vSize3 === "medium" ? "" : ["small", "mini"].includes(vSize3) ? "mini" : "",
          // 兼容 grid 的 size 传递
          loading: loading || tableLoading,
          isBeforePageChange: _vm.isBeforePageChange || _vm.showSaveMsg,
          accurateJumper: _vm.autoLoad
        }, pagerConfig),
        on: {
          "size-change": _vm.pageSizeChange,
          "current-change": _vm.pageCurrentChange,
          "before-page-change": _vm.beforePageChangeHandler
        },
        ref: "pager"
      });
    }
    return res;
  },
  pageChangeEvent: function pageChangeEvent(params) {
    var _this73 = this;
    this.tablePageLoading = true;
    if (!this.tasks.updatePage) {
      this.tasks.updatePage = debounce(200, function() {
        var eventParams = _extends({
          $grid: _this73
        }, params);
        var toolbarVm = _this73.getVm("toolbar");
        emitEvent(_this73, "page-change", eventParams);
        _this73.emitter.emit("page-change", eventParams);
        _this73.handleFetch("query").then(function() {
          _this73.realTimeTablePage = _extends({}, _this73.tablePage);
          _this73.tablePageLoading = false;
        });
        if (toolbarVm) {
          toolbarVm.orderSetting();
        }
      });
    }
    this.tasks.updatePage();
  },
  // size为页大小，load为false则触发change事件与查询，在个性化初始化时根据autoload控制是否加载数据
  pageSizeChange: function pageSizeChange(size, load) {
    this.tablePage.pageSize = size;
    this.tablePage.currentPage = 1;
    load || this.pageChangeEvent(this.tablePage);
  },
  pageCurrentChange: function pageCurrentChange(current) {
    if (this.tablePage.currentPage !== current) {
      this.tablePage.currentPage = current;
      this.pageChangeEvent(this.tablePage);
    }
  },
  beforePageChangeHandler: function beforePageChangeHandler(params) {
    var _this74 = this;
    if (!this.showSaveMsg) {
      var eventParams = extend(false, {
        $grid: this
      }, params);
      emitEvent(this, "before-page-change", eventParams);
      this.emitter.emit("before-page-change", eventParams);
      return;
    }
    var callback = params.callback, rollback = params.rollback;
    var _this$getRecordset = this.getRecordset(), insertRecords = _this$getRecordset.insertRecords, removeRecords = _this$getRecordset.removeRecords, updateRecords = _this$getRecordset.updateRecords;
    if (insertRecords.length || removeRecords.length || updateRecords.length) {
      var next = function next2(res) {
        if (res === "confirm") {
          rollback && rollback();
          emitEvent(_this74, "cancel-page-change", _this74);
          _this74.emitter.emit("cancel-page-change", _this74);
        } else {
          callback && callback();
        }
      };
      Modal.confirm(GlobalConfig$1.i18n("ui.grid.isSaveMsg")).then(next);
    } else {
      callback && callback();
    }
  }
};
var Pager = {
  host: "grid",
  install: function install7(host) {
    Object.assign(host.methods, Methods$7);
  }
};
function setBodyRecords(_ref211) {
  var body = _ref211.body, insertRecords = _ref211.insertRecords, pendingRecords = _ref211.pendingRecords;
  if (insertRecords.length) {
    body.pendingRecords = pendingRecords.filter(function(row2) {
      return !insertRecords.includes(row2);
    });
  }
  if (pendingRecords.length) {
    body.insertRecords = insertRecords.filter(function(row2) {
      return !pendingRecords.includes(row2);
    });
  }
}
function canInvokeSaveDataApi(body, removeRecords, updateRecords) {
  return body.insertRecords.length || removeRecords.length || updateRecords.length || body.pendingRecords.length;
}
function doRemoveOrShowMsg(_ref212) {
  var _vm = _ref212._vm, canInvoke = _ref212.canInvoke, code = _ref212.code, isMsg2 = _ref212.isMsg, pendingRecords = _ref212.pendingRecords, resolve = _ref212.resolve, valid = _ref212.valid;
  if (valid && !canInvoke) {
    if (isMsg2) {
      if (pendingRecords.length) {
        _vm.remove(pendingRecords);
      } else {
        Modal.message({
          id: code,
          message: GlobalConfig$1.i18n("ui.grid.dataUnchanged"),
          status: "info"
        });
      }
    }
    resolve();
  }
}
function invokeSaveDataApi(_ref213) {
  var _vm = _ref213._vm, args = _ref213.args, body = _ref213.body, code = _ref213.code, removeRecords = _ref213.removeRecords, resolve = _ref213.resolve, saveData = _ref213.saveData, updateRecords = _ref213.updateRecords, valid = _ref213.valid;
  var canInvoke = false;
  if (valid) {
    canInvoke = canInvokeSaveDataApi(body, removeRecords, updateRecords);
  }
  if (valid && canInvoke) {
    _vm.tableLoading = true;
    resolve(saveData.api.apply(_vm, [{
      $grid: _vm,
      changeRecords: body
    }].concat(args)).then(function() {
      Modal.message({
        id: code,
        message: GlobalConfig$1.i18n("ui.grid.saveSuccess"),
        status: "success"
      });
      _vm.tableLoading = false;
    }).catch(function() {
      _vm.tableLoading = false;
    }).then(function() {
      return _vm.commitProxy("reload");
    }));
  }
  return canInvoke;
}
var Methods$6 = {
  // 表格工具栏渲染器
  getRenderedToolbar: function getRenderedToolbar(_ref214) {
    var $slots = _ref214.$slots, _vm = _ref214._vm, loading = _ref214.loading, tableLoading = _ref214.tableLoading, toolbar = _ref214.toolbar;
    return _vm.renderedToolbar = (function() {
      var res = null;
      if ($slots.toolbar) {
        res = $slots.toolbar();
      } else if (toolbar) {
        res = h(hooks.toRaw(toolbar.component), {
          ref: "toolbar",
          props: _extends({
            loading: loading || tableLoading
          }, toolbar),
          class: _vm.viewCls("toolbar"),
          scopedSlots: toolbar.slots || {}
        });
      }
      return res;
    })();
  },
  handleSave: function handleSave(code, args) {
    var _this75 = this;
    var saveData = this.saveData, isMsg2 = this.isMsg;
    if (!saveData) {
      error("ui.grid.error.notSave");
      return;
    }
    var body = extend(true, {
      pendingRecords: this.pendingRecords
    }, this.getRecordset());
    var insertRecords = body.insertRecords, removeRecords = body.removeRecords, updateRecords = body.updateRecords, pendingRecords = body.pendingRecords;
    var validRows = insertRecords.concat(updateRecords);
    var getCallback = function getCallback2(resolve) {
      return function(valid) {
        if (!valid) {
          resolve(valid);
          return;
        }
        var canInvoke = invokeSaveDataApi({
          _vm: _this75,
          args,
          body,
          code,
          removeRecords,
          resolve,
          saveData,
          updateRecords,
          valid
        });
        doRemoveOrShowMsg({
          _vm: _this75,
          canInvoke,
          code,
          isMsg: isMsg2,
          pendingRecords,
          resolve,
          valid
        });
      };
    };
    setBodyRecords({
      body,
      insertRecords,
      pendingRecords
    });
    return new Promise(function(resolve) {
      _this75.validate(validRows, getCallback(resolve));
    });
  },
  handleDelete: function handleDelete(code, args) {
    var _this76 = this;
    var deleteData = this.deleteData, isMsg2 = this.isMsg;
    if (!deleteData) {
      error("ui.grid.error.notDelete");
      return;
    }
    var selecteds = this.getSelectRecords(true);
    var afterRemove = function afterRemove2() {
      var removeds = _this76.getRemoveRecords();
      if (!removeds.length && isMsg2 && !selecteds.length) {
        Modal.message({
          id: code,
          message: GlobalConfig$1.i18n("ui.grid.selectOneRecord"),
          status: "warning"
        });
      }
      if (removeds.length) {
        var apiArgs = [{
          $grid: _this76,
          changeRecords: {
            removeRecords: removeds
          }
        }].concat(args);
        var stopLoading = function stopLoading2() {
          _this76.tableLoading = false;
        };
        _this76.tableLoading = true;
        return deleteData.api.apply(_this76, apiArgs).then(stopLoading).catch(stopLoading).then(function() {
          return _this76.commitProxy("reload");
        });
      }
    };
    this.remove(selecteds).then(afterRemove);
  },
  handleFullScreen: function handleFullScreen(_ref215) {
    var _this77 = this;
    var show2 = _ref215[0];
    this.fullScreenClass = show2 ? "tiny-fullscreen-full" : "";
    this.$nextTick(function() {
      _this77.recalculate();
      emitEvent(_this77, "fullscreen", show2);
      _this77.emitter.emit("fullscreen", show2);
    });
  },
  commitProxy: function commitProxy(code) {
    var _this78 = this;
    for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      args[_key2 - 1] = arguments[_key2];
    }
    var btnMethod = Buttons.get(code);
    if (code === "insert") {
      this.insert();
    } else if (code === "insert_actived") {
      this.insert().then(function(_ref216) {
        var row2 = _ref216.row;
        return _this78.setActiveRow(row2);
      });
    } else if (code === "mark_cancel") {
      this.triggerPendingEvent(code);
    } else if (code === "delete_selection") {
      this.handleDeleteRow(code, "ui.grid.deleteSelectRecord", function() {
        return _this78.commitProxy(["delete"].concat(args));
      });
    } else if (code === "remove_selection") {
      this.handleDeleteRow(code, "ui.grid.removeSelectRecord", function() {
        return _this78.removeSelecteds();
      });
    } else if (code === "export") {
      this.exportCsv();
    } else if (code === "reset_custom") {
      this.resetAll();
    } else if (~["reload", "query", "prefetch"].indexOf(code)) {
      this.handleFetch(code, args);
    } else if (code === "delete") {
      this.handleDelete(code, args);
    } else if (code === "save") {
      this.handleSave();
    } else if (code === "fullscreen") {
      this.handleFullScreen(args);
    } else if (btnMethod) {
      btnMethod.call.apply(btnMethod, [this, {
        code,
        $grid: this
      }].concat(args));
    }
    return this.$nextTick();
  },
  handleDeleteRow: function handleDeleteRow(code, i18nKey, callback) {
    var selecteds = this.getSelectRecords();
    if (this.isMsg && selecteds.length) {
      Modal.confirm(GlobalConfig$1.i18n(i18nKey)).then(function(type) {
        type === "confirm" && callback();
      });
    }
    if (this.isMsg && !selecteds.length) {
      Modal.message({
        id: code,
        message: GlobalConfig$1.i18n("ui.grid.selectOneRecord"),
        status: "warning"
      });
    }
    if (!this.isMsg && selecteds.length) {
      callback();
    }
  },
  getPendingRecords: function getPendingRecords() {
    return this.pendingRecords;
  },
  triggerToolbarBtnEvent: function triggerToolbarBtnEvent(button, event) {
    var _this$events = this.events, events = _this$events === void 0 ? {} : _this$events, tableListeners = this.tableListeners;
    var code = button.code;
    if (!events.toolbarButtonClick && !tableListeners["toolbar-button-click"]) {
      this.commitProxy(code, event);
    }
    emitEvent(this, "toolbar-button-click", [{
      code,
      button,
      $grid: this
    }, event]);
    this.emitter.emit("toolbar-button-click", {
      code,
      button,
      $grid: this
    }, event);
  },
  triggerPendingEvent: function triggerPendingEvent(code) {
    var isMsg2 = this.isMsg, pendings = this.pendingRecords;
    var selectColumn = this.getColumns().filter(function(col) {
      return ~["selection", "radio"].indexOf(col.type);
    });
    var isSelection = selectColumn.length && selectColumn[0].type === "selection";
    var isRadio = selectColumn.length && selectColumn[0].type === "radio";
    var selecteds = isSelection ? this.getSelectRecords(true) : isRadio ? [this.getRadioRow()] : [];
    if (!selecteds.length && isMsg2) {
      Modal.message({
        id: code,
        message: GlobalConfig$1.i18n("ui.grid.selectOneRecord"),
        status: "warning"
      });
    }
    if (selecteds.length) {
      var _ref217 = {}, _ref217$plus = _ref217.plus, plus = _ref217$plus === void 0 ? [] : _ref217$plus, _ref217$minus = _ref217.minus, minus = _ref217$minus === void 0 ? [] : _ref217$minus, tmp = _ref217.tmp;
      selecteds.forEach(function(data7) {
        var selectedPending = pendings.includes(data7);
        tmp = selectedPending ? minus : plus;
        tmp.push(data7);
      });
      tmp = minus.length ? pendings.filter(function(item) {
        return !~minus.indexOf(item);
      }) : pendings;
      this.pendingRecords = tmp.concat(plus);
      isSelection && this.clearSelection();
      isRadio && this.clearRadioRow();
    }
  }
};
var Toolbar = {
  host: "grid",
  install: function install8(host) {
    Object.assign(host.methods, Methods$6);
  }
};
var Methods$5 = {
  renderColumnAnchor: function renderColumnAnchor(params, _vm) {
    var _ref218 = params || {}, _ref218$anchors = _ref218.anchors, anchors = _ref218$anchors === void 0 ? [] : _ref218$anchors, _ref218$action = _ref218.action, action = _ref218$action === void 0 ? function() {
    } : _ref218$action;
    var viewType2 = _vm.viewType;
    return h("div", {
      class: ["tiny-grid__column-anchor", _vm.viewCls("columnAnchor")],
      style: viewType2 === "default" ? "display:flex" : "",
      key: _vm.columnAnchorKey,
      ref: "tinyGridColumnAnchor"
    }, anchors.map(function(anchor) {
      var _anchor$active = anchor.active, active = _anchor$active === void 0 ? false : _anchor$active, _anchor$label = anchor.label, label = _anchor$label === void 0 ? "" : _anchor$label, _anchor$field = anchor.field, field = _anchor$field === void 0 ? "" : _anchor$field, render17 = anchor.render;
      if (typeof render17 === "function") {
        return render17({
          h,
          anchor,
          action
        });
      }
      var itemClass = {
        "tiny-grid__column-anchor-item": true,
        "tiny-grid__column-anchor-item--active": active
      };
      var itemOn = {
        click: function click(e) {
          return action(field, e);
        }
      };
      var iconVnode = active ? h(iconMarkOn(), {
        class: "tiny-grid__column-anchor-item-icon"
      }) : null;
      var spanVnode = h("span", label);
      return h("div", {
        class: itemClass,
        on: itemOn
      }, [iconVnode, spanVnode]);
    }));
  },
  buildColumnAnchor: function buildColumnAnchor(_ref219) {
    var property = _ref219.property, label = _ref219.label, anchors = _ref219.anchors, activeAnchor = _ref219.activeAnchor;
    var visibleColumn = this.getColumns();
    var column = visibleColumn.find(function(col) {
      return !col.type && col.property === property;
    });
    var anchorName = "";
    var anchorRender = null;
    if (typeof label !== "undefined") {
      if (typeof label === "string") {
        anchorName = label;
      } else if (Array.isArray(label)) {
        if (label.length > 1) {
          anchorName = String(label[0]);
          anchorRender = label[1];
        }
      } else if (label && typeof label === "object") {
        anchorName = label.label;
        anchorRender = label.render;
        if (label.active) {
          activeAnchor.field = property;
          if (label.delay) activeAnchor.delay = label.delay;
        }
      }
    }
    if (column) {
      anchors.push({
        label: anchorName || (typeof column.title === "string" ? column.title : ""),
        field: property,
        active: false,
        render: anchorRender
      });
    }
  },
  buildColumnAnchorParams: function buildColumnAnchorParams() {
    var _this79 = this;
    var columnAnchor = this.columnAnchor;
    var anchors = [];
    var activeAnchor = {
      field: "",
      delay: 30
    };
    if (Array.isArray(columnAnchor) && columnAnchor.length) {
      columnAnchor.forEach(function(item) {
        if (typeof item === "string") {
          _this79.buildColumnAnchor({
            property: item,
            anchors
          });
        } else if (Array.isArray(item)) {
          if (item.length > 1) _this79.buildColumnAnchor({
            property: item[0],
            label: item[1],
            anchors
          });
        } else if (item && typeof item === "object") {
          var field = item.field, other = _objectWithoutPropertiesLoose(item, _excluded2);
          _this79.buildColumnAnchor({
            property: field,
            label: other,
            anchors,
            activeAnchor
          });
        }
      });
    }
    if (!activeAnchor.field && anchors.length) {
      activeAnchor.field = anchors[0].field;
    }
    this.columnAnchorParams = {
      anchors,
      activeAnchor,
      action: function action(field, e) {
        return _this79.anchorAction({
          field,
          anchors,
          _vm: _this79,
          e
        });
      }
    };
    this._delayActivateAnchor = function() {
      _this79._delayActivateAnchor = void 0;
      setTimeout(function() {
        return _this79.anchorAction({
          field: activeAnchor.field,
          anchors,
          _vm: _this79
        });
      }, activeAnchor.delay);
    };
  },
  anchorAction: function anchorAction(_ref220) {
    var field = _ref220.field, anchors = _ref220.anchors, _vm = _ref220._vm;
    var fromAnchor = anchors.find(function(anchor) {
      return anchor.active;
    });
    var toAnchor = anchors.find(function(anchor) {
      return anchor.field === field;
    });
    if (toAnchor && fromAnchor !== toAnchor) {
      if (fromAnchor && fromAnchor.active) {
        fromAnchor.active = false;
      }
      if (!toAnchor.active) {
        toAnchor.active = true;
        _vm.columnAnchorKey = field;
        _vm.$nextTick(function(found) {
          if (found === void 0) {
            found = false;
          }
          var visibleColumn = _vm.getColumns();
          var column = visibleColumn.find(function(col) {
            return !col.type && col.property === field;
          });
          var width = visibleColumn.filter(function(col) {
            return !col.fixed;
          }).map(function(col) {
            if (col === column) {
              found = true;
            }
            return found ? 0 : col.renderWidth;
          }).reduce(function(p, c) {
            return p + c;
          }, 0);
          if (column) {
            _vm.scrollTo(width);
          }
        });
      }
    }
  }
};
var ColumnAnchor = {
  host: "grid",
  install: function install9(host) {
    Object.assign(host.methods, Methods$5);
  }
};
var createHandlerOnEnd = function createHandlerOnEnd2(_ref221) {
  var _vm = _ref221._vm, refresh = _ref221.refresh;
  return function(event) {
    var insertRecords = _vm.getInsertRecords();
    if (insertRecords.length) {
      return false;
    }
    var options = {
      children: (_vm.treeConfig || {}).children || "children"
    };
    var targetTrElem = event.item;
    var wrapperElem = targetTrElem.parentNode, prevEl = targetTrElem.previousElementSibling, nextEl = targetTrElem.nextElementSibling;
    var prevTrElem = prevEl && prevEl.classList.contains("tiny-grid-body__row") ? prevEl : prevEl && prevEl.previousElementSibling;
    var tableTreeData = _vm.rawData || _vm.tableData;
    var selfRow = _vm.getRowNode(targetTrElem).item;
    var selfNode = findTree$1(tableTreeData, function(row2) {
      return row2 === selfRow;
    }, options);
    selfRow._isDraging = true;
    if (prevTrElem) {
      var prevRow = _vm.getRowNode(prevTrElem).item;
      var prevNode = findTree$1(tableTreeData, function(row2) {
        return row2 === prevRow;
      }, options);
      if (findTree$1(selfRow[options.children], function(row2) {
        return prevRow === row2;
      }, options)) {
        var oldTrElem = wrapperElem.children[event.oldIndex];
        wrapperElem.insertBefore(targetTrElem, oldTrElem);
        return Modal.message({
          message: GlobalConfig$1.i18n("ui.grid.error.notAllowDragSelf"),
          status: "error"
        });
      }
      var currRow = selfNode.items.splice(selfNode.index, 1)[0];
      if (_vm.hasTreeExpand(prevRow)) {
        prevRow[options.children].splice(0, 0, currRow);
      } else {
        prevNode.items.splice(prevNode.index + (selfNode.index < prevNode.index ? 0 : 1), 0, currRow);
        prevNode.items = [].concat(prevNode.items);
      }
    } else if (nextEl && nextEl.classList.contains("tiny-grid-body__row")) {
      var _currRow = selfNode.items.splice(selfNode.index, 1)[0];
      tableTreeData.unshift(_currRow);
      _vm.tableFullData = [].concat(tableTreeData);
    }
    _vm.$emit("row-drop-end", event, _vm, _vm.scrollYLoad ? tableTreeData : _vm.tableFullData);
    refresh && _vm.rawData && _vm.refreshData(_vm.rawData);
  };
};
var getSortColumns = function getSortColumns2(columns2) {
  var left = [];
  var right = [];
  var center = [];
  columns2.forEach(function(col) {
    var fixed = col.fixed;
    if (fixed === "left") {
      left.push(col);
    } else if (fixed === "right") {
      right.push(col);
    } else {
      center.push(col);
    }
  });
  return left.concat(center).concat(right);
};
var Methods$4 = {
  // 处理列拖拽
  columnDrop: function columnDrop(headerEl) {
    var _this80 = this;
    var _ref222 = this.dropConfig || {}, plugin = _ref222.plugin, onBeforeMove = _ref222.onBeforeMove, filter2 = _ref222.filter;
    var columnDropContainer = headerEl.querySelector(".tiny-grid-header__row");
    var columnDropOptions = _extends({}, this.dropConfig, {
      handle: ".tiny-grid-header__column:not(.col__fixed)",
      filter: filter2,
      onEnd: function onEnd(event) {
        var item = event.item, newIndex = event.newIndex, oldIndex = event.oldIndex;
        var _this80$getTableColum = _this80.getTableColumn(), fullColumn = _this80$getTableColum.fullColumn, tableColumn2 = _this80$getTableColum.tableColumn;
        var sortVisibleCols = getSortColumns(tableColumn2);
        var targetThElem = item;
        var wrapperElem = targetThElem.parentNode;
        var newColumn = sortVisibleCols[newIndex];
        if (newColumn.fixed) {
          if (newIndex > oldIndex) {
            for (var i = newIndex; i >= oldIndex; i--) {
              wrapperElem.insertBefore(targetThElem, wrapperElem.children[i]);
            }
          } else {
            for (var _i3 = newIndex; _i3 <= oldIndex; _i3++) {
              wrapperElem.insertBefore(targetThElem, wrapperElem.children[_i3]);
            }
            wrapperElem.insertBefore(wrapperElem.children[oldIndex], targetThElem);
          }
          return Modal.message({
            message: GlobalConfig$1.i18n("ui.grid.error.notAllowDragFixed"),
            status: "error"
          });
        }
        var oldColumnIndex = _this80.getColumnIndex(sortVisibleCols[oldIndex]);
        var newColumnIndex = _this80.getColumnIndex(sortVisibleCols[newIndex]);
        var currCol = fullColumn.splice(oldColumnIndex, 1)[0];
        fullColumn.splice(newColumnIndex, 0, currCol);
        _this80.loadColumn(fullColumn);
        _this80.$emit("column-drop-end", event, _this80);
        var toolbarVm = _this80.getVm("toolbar");
        _this80.isDragHeaderSorting && toolbarVm && toolbarVm.updateSetting();
      },
      onStart: function onStart(event) {
        _this80.$emit("column-drop-start", event, _this80);
      },
      onMove: function onMove(event) {
        var cancel = typeof onBeforeMove === "function" ? onBeforeMove("column", null, event, _this80) : true;
        _this80.$emit("column-drop-move", event, _this80);
        return cancel === void 0 || cancel;
      }
    });
    return plugin.create(columnDropContainer, columnDropOptions);
  },
  // 处理行拖拽
  rowDrop: function rowDrop(bodyEl) {
    var _this81 = this;
    var _this$dropConfig = this.dropConfig, plugin = _this$dropConfig.plugin, onBeforeMove = _this$dropConfig.onBeforeMove, filter2 = _this$dropConfig.filter, _this$dropConfig$refr = _this$dropConfig.refresh, refresh = _this$dropConfig$refr === void 0 ? true : _this$dropConfig$refr, rowHandle = _this$dropConfig.rowHandle, trigger = _this$dropConfig.trigger;
    var rowDropContainer = bodyEl.querySelector(".tiny-grid__body tbody");
    var handle = trigger || ".tiny-grid-body__row";
    if (rowHandle === "index") {
      handle = ".tiny-grid-body__row>td.col__index>.row__drop-handle";
    }
    var rowDropOptions = _extends({}, this.dropConfig, {
      handle,
      filter: filter2,
      onEnd: createHandlerOnEnd({
        _vm: this,
        refresh
      }),
      onStart: function onStart(event) {
        _this81.$emit("row-drop-start", event, _this81);
      },
      onMove: function onMove(event) {
        var insertRecords = _this81.getInsertRecords();
        if (insertRecords.length) return false;
        var dragged = event.dragged;
        var selfRow = _this81.getRowNode(dragged).item;
        var cancel = typeof onBeforeMove === "function" ? onBeforeMove("row", selfRow, event, _this81) : true;
        _this81.$emit("row-drop-move", event, _this81);
        return cancel === void 0 || cancel;
      }
    });
    return plugin.create(rowDropContainer, rowDropOptions);
  }
};
var Dragger = {
  host: "table",
  install: function install10(host) {
    Object.assign(host.methods, Methods$4);
  }
};
var Methods$3 = {};
var Sort = {
  host: "table",
  install: function install11(host) {
    Object.assign(host.methods, Methods$3);
  }
};
function createTooltipRange(_ref223) {
  var _vm = _ref223._vm, cell = _ref223.cell, column = _ref223.column, isHeader = _ref223.isHeader;
  var range = document.createRange();
  var rangeEnd;
  range.setStart(cell, 0);
  if (_vm.headerSuffixIconAbsolute) {
    if (column.sortable && column.filter && isHeader) {
      rangeEnd = cell.childNodes.length - 2;
    } else if ((column.sortable || column.filter) && isHeader) {
      rangeEnd = cell.childNodes.length - 1;
    } else {
      rangeEnd = cell.childNodes.length;
    }
  } else {
    rangeEnd = column.sortable && isHeader ? cell.childNodes.length - 1 : cell.childNodes.length;
  }
  range.setEnd(cell, rangeEnd);
  return range;
}
function processContentMethod(_ref224) {
  var _vm = _ref224._vm, column = _ref224.column, content = _ref224.content, contentMethod = _ref224.contentMethod, event = _ref224.event, isHeader = _ref224.isHeader, row2 = _ref224.row, showTip = _ref224.showTip;
  if (contentMethod) {
    _vm.tooltipContent = contentMethod({
      event,
      column,
      row: row2,
      showTip,
      isHeader,
      content
    }, h);
  } else {
    _vm.tooltipContent = content;
  }
}
var focusSingle = null;
var Methods$2 = {
  // 显示 tooltip 依赖的 popper 组件
  activateTooltip: function activateTooltip(tooltip, isValid) {
    if (!this.tasks.activateTooltip) {
      this.tasks.activateTooltip = debounce(300, function() {
        var sign = isValid !== void 0 ? isValid : focusSingle;
        if (sign) {
          tooltip.state.popperElm && (tooltip.state.popperElm.style.display = "none");
          tooltip.doDestroy();
          tooltip.show();
          setTimeout(tooltip.updatePopper);
        }
      });
    }
    this.tasks.activateTooltip();
  },
  // 显示 tooltip 依赖的 popper 组件
  activateTooltipValid: function activateTooltipValid(tooltip) {
    if (!this.tasks.activateTooltipValid) {
      this.tasks.activateTooltipValid = debounce(50, function() {
        tooltip.handleShowPopper();
        setTimeout(function() {
          return tooltip.updatePopper();
        });
      });
    }
    this.tasks.activateTooltipValid();
  },
  // 显示 tooltip
  handleTooltip: function handleTooltip(event, column, row2, showTip, isHeader) {
    var cell = this.hoverText;
    if (!cell) {
      return;
    }
    var tooltip = this.$refs.tooltip;
    var wrapperElem = cell;
    var content = cell.innerText.trim();
    var contentMethod = this.tooltipConfig.contentMethod;
    var range = createTooltipRange({
      _vm: this,
      cell,
      column,
      isHeader
    });
    var rangeWidth = range.getBoundingClientRect().width;
    var padding = (parseInt(getStyle(cell, "paddingLeft"), 10) || 0) + (parseInt(getStyle(cell, "paddingRight"), 10) || 0);
    var isOverflow = (
      // 浏览器缩放情况下，会存在细微的像素无法，因此设置0.1像素作为误差量
      rangeWidth + padding > cell.getBoundingClientRect().width + 0.1 || wrapperElem.scrollWidth > wrapperElem.clientWidth
    );
    if ((contentMethod || content) && (showTip || isOverflow)) {
      Object.assign(this.tooltipStore, {
        row: row2,
        column,
        visible: true
      });
      if (tooltip) {
        processContentMethod({
          _vm: this,
          column,
          content,
          contentMethod,
          event,
          isHeader,
          row: row2,
          showTip
        });
        tooltip.state.referenceElm = cell;
        tooltip.state.popperElm && (tooltip.state.popperElm.style.display = "none");
        focusSingle = true;
        this.activateTooltip(tooltip);
      }
    }
    return this.$nextTick();
  },
  // 提供关闭tips提示的方法
  clostTooltip: function clostTooltip() {
    var tooltip = this.$refs.tooltip;
    Object.assign(this.tooltipStore, {
      content: null,
      row: null,
      visible: false,
      column: null
    });
    focusSingle = false;
    if (tooltip && typeof tooltip.setExpectedState === "function") {
      tooltip.setExpectedState(false);
      this.debounceClose(tooltip);
    }
    return this.$nextTick();
  },
  // 添加代码让用户代码可以划入popper
  debounceClose: function debounceClose(tooltip) {
    if (!this.tasks.debounceClose) {
      this.tasks.debounceClose = debounce(50, function() {
        tooltip.handleClosePopper();
      });
    }
    this.tasks.debounceClose();
  },
  // 触发表头 tooltip 事件
  triggerHeaderTooltipEvent: function triggerHeaderTooltipEvent(event, params) {
    var tooltipStore = this.tooltipStore;
    var column = params.column, showHeaderTip = params.showHeaderTip;
    if (tooltipStore.column !== column || !tooltipStore.visible) {
      this.handleTooltip(event, column, null, showHeaderTip, true);
    }
  },
  // 触发表尾 tooltip 事件
  triggerFooterTooltipEvent: function triggerFooterTooltipEvent(event, params) {
    var column = params.column;
    var tooltipStore = this.tooltipStore;
    if (tooltipStore.column !== column || !tooltipStore.visible) {
      this.handleTooltip(event, column);
    }
  },
  // 触发 tooltip 事件
  triggerTooltipEvent: function triggerTooltipEvent(event, params) {
    var editConfig = this.editConfig, editStore = this.editStore, tooltipStore = this.tooltipStore;
    var actived = editStore.actived;
    var row2 = params.row, column = params.column, showTip = params.showTip;
    if (editConfig) {
      if (editConfig.mode === "row" && actived.row === row2 && column.editor || actived.row === row2 && actived.column === column) {
        return;
      }
    }
    if (tooltipStore.column !== column || tooltipStore.row !== row2 || !tooltipStore.visible) {
      this.handleTooltip(event, column, row2, showTip);
    }
  }
};
var Tooltip = {
  host: "table",
  install: function install12(host) {
    Object.assign(host.methods, Methods$2);
  }
};
function onHalfSelectionProperty(_ref225) {
  var checkStrictly = _ref225.checkStrictly, property = _ref225.property, row2 = _ref225.row, treeConfig = _ref225.treeConfig, treeIndeterminates = _ref225.treeIndeterminates, value = _ref225.value;
  if (property && treeConfig && !checkStrictly && value === -1) {
    treeIndeterminates.push(row2);
    set(row2, property, false);
  }
}
function onFullSelectionProperty(_ref226) {
  var checkMethod = _ref226.checkMethod, checkStrictly = _ref226.checkStrictly, property = _ref226.property, row2 = _ref226.row, treeConfig = _ref226.treeConfig, treeIndeterminates = _ref226.treeIndeterminates, value = _ref226.value;
  if (property && treeConfig && !checkStrictly && value !== -1) {
    eachTree([row2], function(item, $rowIndex) {
      if (row2 === item || !checkMethod || checkMethod({
        row: item,
        $rowIndex
      })) {
        set(item, property, value);
        remove(treeIndeterminates, function(r) {
          return r === item;
        });
      }
    }, treeConfig);
  }
}
function getVItemsOnParentSlctProp(_ref227) {
  var checkMethod = _ref227.checkMethod, matchObj = _ref227.matchObj;
  var tinyItems;
  if (checkMethod) {
    tinyItems = matchObj.items.filter(function(item, $rowIndex) {
      return checkMethod({
        row: item,
        $rowIndex
      });
    });
  } else {
    tinyItems = matchObj.items;
  }
  return tinyItems;
}
function getParentStatusOnParentSlctProp(_ref228) {
  var indeterminatesItem = _ref228.indeterminatesItem, matchObj = _ref228.matchObj, property = _ref228.property, vItems = _ref228.vItems, value = _ref228.value;
  var parentStatus;
  if (indeterminatesItem) {
    parentStatus = -1;
  } else {
    var selectItemArr = matchObj.items.filter(function(item) {
      return get(item, property);
    });
    var _isEqual = selectItemArr.filter(function(item) {
      return ~vItems.indexOf(item);
    }).length === vItems.length;
    parentStatus = _isEqual ? true : selectItemArr.length || value === -1 ? -1 : false;
  }
  return parentStatus;
}
function onHalfSelection(_ref229) {
  var checkStrictly = _ref229.checkStrictly, property = _ref229.property, row2 = _ref229.row, selection = _ref229.selection, treeConfig = _ref229.treeConfig, treeIndeterminates = _ref229.treeIndeterminates, value = _ref229.value;
  if (!property && treeConfig && !checkStrictly && value === -1) {
    treeIndeterminates.push(row2);
    remove(selection, function(item) {
      return item === row2;
    });
  }
}
var addSelection = function addSelection2(selection, item) {
  return !selection.includes(item) && selection.push(item);
};
function onFullSelection(_ref230) {
  var checkMethod = _ref230.checkMethod, checkStrictly = _ref230.checkStrictly, property = _ref230.property, row2 = _ref230.row, selection = _ref230.selection, treeConfig = _ref230.treeConfig, treeIndeterminates = _ref230.treeIndeterminates, value = _ref230.value;
  if (!property && treeConfig && !checkStrictly && value !== -1) {
    eachTree([row2], function(item, $rowIndex) {
      if (row2 === item || !checkMethod || checkMethod({
        row: item,
        $rowIndex
      })) {
        if (value) {
          addSelection(selection, item);
        } else {
          remove(selection, function(select2) {
            return select2 === item;
          });
        }
        remove(treeIndeterminates, function(r) {
          return r === item;
        });
      }
    }, treeConfig);
  }
}
function getParentStatusOnParentSelection(_ref231) {
  var indeterminatesItem = _ref231.indeterminatesItem, matchObj = _ref231.matchObj, selection = _ref231.selection, vItems = _ref231.vItems, value = _ref231.value;
  var parentStatus;
  if (indeterminatesItem) {
    parentStatus = -1;
  } else {
    var selectItems = matchObj.items.filter(function(item) {
      return selection.includes(item);
    });
    var isEqualItem = selectItems.length === vItems.length;
    parentStatus = isEqualItem ? true : selectItems.length || value === -1 ? -1 : false;
  }
  return parentStatus;
}
function hasCheckField(_ref232, value, _vm) {
  var row2 = _ref232.row;
  var tableFullData = _vm.tableFullData, _vm$selectConfig = _vm.selectConfig, selectConfig = _vm$selectConfig === void 0 ? {} : _vm$selectConfig, treeConfig = _vm.treeConfig, treeIndeterminates = _vm.treeIndeterminates;
  var property = selectConfig.checkField, checkStrictly = selectConfig.checkStrictly, checkMethod = selectConfig.checkMethod;
  onHalfSelectionProperty({
    checkStrictly,
    property,
    row: row2,
    treeConfig,
    treeIndeterminates,
    value
  });
  onFullSelectionProperty({
    checkMethod,
    checkStrictly,
    property,
    row: row2,
    treeConfig,
    treeIndeterminates,
    value
  });
  if (property && treeConfig && !checkStrictly) {
    var matchObj = findTree$1(tableFullData, function(item) {
      return item === row2;
    }, treeConfig);
    if (matchObj && matchObj.parent) {
      var vItems = getVItemsOnParentSlctProp({
        checkMethod,
        matchObj
      });
      var indeterminatesItem = find$1(matchObj.items, function(item) {
        return treeIndeterminates.includes(item);
      });
      var parentStatus = getParentStatusOnParentSlctProp({
        indeterminatesItem,
        matchObj,
        property,
        vItems,
        value
      });
      return _vm.handleSelectRow({
        row: matchObj.parent
      }, parentStatus);
    }
  }
  if (property && !(treeConfig && !checkStrictly)) {
    set(row2, property, value);
  }
}
function onSelectTreeCheckStrictly(_ref233, value, _vm) {
  var row2 = _ref233.row;
  var selection = _vm.selection, tableFullData = _vm.tableFullData, _vm$selectConfig2 = _vm.selectConfig, selectConfig = _vm$selectConfig2 === void 0 ? {} : _vm$selectConfig2, treeConfig = _vm.treeConfig, treeIndeterminates = _vm.treeIndeterminates;
  var property = selectConfig.checkField, checkStrictly = selectConfig.checkStrictly, checkMethod = selectConfig.checkMethod;
  onHalfSelection({
    checkStrictly,
    property,
    row: row2,
    selection,
    treeConfig,
    treeIndeterminates,
    value
  });
  onFullSelection({
    checkMethod,
    checkStrictly,
    property,
    row: row2,
    selection,
    treeConfig,
    treeIndeterminates,
    value
  });
  if (!property && treeConfig && !checkStrictly) {
    var matchObj = findTree$1(tableFullData, function(item) {
      return item === row2;
    }, treeConfig);
    if (matchObj && matchObj.parent) {
      var vItems = matchObj.items;
      var indeterminatesItem = find$1(matchObj.items, function(item) {
        return treeIndeterminates.includes(item);
      });
      var parentStatus = getParentStatusOnParentSelection({
        indeterminatesItem,
        matchObj,
        selection,
        vItems,
        value
      });
      return _vm.handleSelectRow({
        row: matchObj.parent
      }, parentStatus);
    }
  }
}
function onSelectOther(_ref234, value, _vm) {
  var row2 = _ref234.row;
  var selection = _vm.selection, _vm$selectConfig3 = _vm.selectConfig, selectConfig = _vm$selectConfig3 === void 0 ? {} : _vm$selectConfig3, treeConfig = _vm.treeConfig;
  var property = selectConfig.checkField, checkStrictly = selectConfig.checkStrictly;
  if (!property && !(treeConfig && !checkStrictly)) {
    if (value) {
      addSelection(selection, row2);
    } else {
      remove(selection, function(item) {
        return item === row2;
      });
    }
  }
}
function hasNoCheckField(_ref235, value, _vm) {
  var row2 = _ref235.row;
  onSelectTreeCheckStrictly({
    row: row2
  }, value, _vm);
  onSelectOther({
    row: row2
  }, value, _vm);
}
function pushSelectRow(_ref236) {
  var afterFullData = _ref236.afterFullData, checkMethod = _ref236.checkMethod, checkStrictly = _ref236.checkStrictly; _ref236.property; var selectRows = _ref236.selectRows, selection = _ref236.selection, treeConfig = _ref236.treeConfig, value = _ref236.value;
  if (!checkStrictly && value) {
    eachTree(afterFullData, function(row2, $rowIndex) {
      if (!checkMethod || checkMethod({
        row: row2,
        $rowIndex
      })) {
        selectRows.push(row2);
      }
    }, treeConfig);
  }
  if (!checkStrictly && !value && checkMethod) {
    eachTree(afterFullData, function(row2, $rowIndex) {
      if (checkMethod({
        row: row2,
        $rowIndex
      }) ? 0 : selection.includes(row2)) {
        selectRows.push(row2);
      }
    }, treeConfig);
  }
}
function hasCheckFieldNoStrictly(_ref237) {
  var afterFullData = _ref237.afterFullData, checkMethod = _ref237.checkMethod, checkStrictly = _ref237.checkStrictly, property = _ref237.property, selection = _ref237.selection, treeConfig = _ref237.treeConfig, value = _ref237.value;
  if (!checkStrictly && property) {
    var indexKey = (treeConfig ? "$" : "") + "rowIndex";
    var setValFn = function setValFn2(row2, rowIndex) {
      var _checkMethod;
      if (!checkMethod || checkMethod((_checkMethod = {
        row: row2
      }, _checkMethod[indexKey] = rowIndex, _checkMethod))) {
        set(row2, property, value);
      }
    };
    var clearValFn = function clearValFn2(row2, rowIndex) {
      var _checkMethod2;
      if (!checkMethod || (checkMethod((_checkMethod2 = {
        row: row2
      }, _checkMethod2[indexKey] = rowIndex, _checkMethod2)) ? selection.includes(row2) : false)) {
        set(row2, property, value);
      }
    };
    if (treeConfig) {
      eachTree(afterFullData, value ? setValFn : clearValFn, treeConfig);
    } else {
      afterFullData.forEach(value ? setValFn : clearValFn);
    }
  }
}
function filterSelectRow(_ref238) {
  var afterFullData = _ref238.afterFullData, checkMethod = _ref238.checkMethod, checkStrictly = _ref238.checkStrictly, selectRows = _ref238.selectRows, selection = _ref238.selection, value = _ref238.value;
  if (!checkStrictly && value && checkMethod) {
    selectRows = afterFullData.filter(function(row2, rowIndex) {
      return selection.includes(row2) || checkMethod({
        row: row2,
        rowIndex
      });
    });
  }
  if (!checkStrictly && value && !checkMethod) {
    selectRows = afterFullData.slice(0);
  }
  if (!checkStrictly && !value && checkMethod) {
    selectRows = afterFullData.filter(function(row2, rowIndex) {
      return checkMethod({
        row: row2,
        rowIndex
      }) ? 0 : selection.includes(row2);
    });
  }
  return selectRows;
}
function hasNoCheckFieldNoStrictly(_ref239) {
  var afterFullData = _ref239.afterFullData, checkMethod = _ref239.checkMethod, checkStrictly = _ref239.checkStrictly, property = _ref239.property, selection = _ref239.selection, treeConfig = _ref239.treeConfig, value = _ref239.value;
  var selectRows = [];
  if (treeConfig) {
    pushSelectRow({
      afterFullData,
      checkMethod,
      checkStrictly,
      property,
      selectRows,
      selection,
      treeConfig,
      value
    });
  } else {
    selectRows = filterSelectRow({
      afterFullData,
      checkMethod,
      checkStrictly,
      selectRows,
      selection,
      value
    });
  }
  return selectRows;
}
function setSelectionNoStrictly(_ref240) {
  var _vm = _ref240._vm, checkStrictly = _ref240.checkStrictly, reserve = _ref240.reserve, selectRows = _ref240.selectRows, selection = _ref240.selection, value = _ref240.value, afterFullData = _ref240.afterFullData;
  if (!checkStrictly) {
    if (reserve) {
      var unCheckedRows = afterFullData.filter(function(row2) {
        return !selectRows.includes(row2);
      });
      _vm.selection = value ? selection.concat(selectRows.filter(function(row2) {
        return !selection.includes(row2);
      })) : selection.filter(function(row2) {
        return !unCheckedRows.includes(row2);
      });
    } else {
      _vm.selection = selectRows;
    }
  }
}
var Methods$1 = {
  // 处理默认勾选
  handleSelectionDefChecked: function handleSelectionDefChecked() {
    var fullDataRowIdData = this.fullDataRowIdData;
    var _ref241 = this.selectConfig || {}, checkAll = _ref241.checkAll, checkRowKeys = _ref241.checkRowKeys;
    if (checkAll) {
      this.setAllSelection(true);
      return;
    }
    if (checkRowKeys) {
      var defCheckedRowids = checkRowKeys.map(function(key) {
        return encodeURIComponent(key);
      });
      var defCheckedRows = [];
      defCheckedRowids.forEach(function(rowid2) {
        var rowCache = fullDataRowIdData[rowid2];
        if (rowCache) {
          defCheckedRows.push(rowCache.row);
        }
      });
      this.setSelection(defCheckedRows, true);
    }
  },
  setSelection: function setSelection(rows, value) {
    var _this82 = this;
    if (rows) {
      if (!isArray(rows)) {
        rows = [rows];
      }
      rows.forEach(function(row2) {
        return _this82.handleSelectRow({
          row: row2
        }, !!value);
      });
    }
    return this.$nextTick();
  },
  // 多选，行选中事件。value：选中true、不选false、不确定-1
  handleSelectRow: function handleSelectRow(_ref242, value) {
    var row2 = _ref242.row;
    hasCheckField({
      row: row2
    }, value, this);
    hasNoCheckField({
      row: row2
    }, value, this);
    this.checkSelectionStatus();
  },
  handleToggleCheckRowEvent: function handleToggleCheckRowEvent(params, event) {
    var selection = this.selection;
    var _ref243 = this.selectConfig || {}, checkField = _ref243.checkField;
    var row2 = params.row;
    var value = checkField ? !get(row2, checkField) : !~selection.indexOf(row2);
    if (event) {
      this.triggerCheckRowEvent(event, params, value);
    } else {
      this.handleSelectRow(params, value);
    }
  },
  triggerCheckRowEvent: function triggerCheckRowEvent(event, params, value) {
    var _this$selectConfig4 = this.selectConfig, selectConfig = _this$selectConfig4 === void 0 ? {} : _this$selectConfig4;
    var checkMethod = selectConfig.checkMethod;
    if (!checkMethod || checkMethod(params)) {
      var _this$$grid4;
      this.handleSelectRow(params, value);
      var eventParams = [_extends({
        selection: this.getSelectRecords(),
        checked: value,
        $table: this
      }, params), event];
      emitEvent(this, "select-change", eventParams);
      (_this$$grid4 = this.$grid) == null ? void 0 : _this$$grid4.emitter.emit("select-change", eventParams);
    }
  },
  // 多选，切换某一行的选中状态
  toggleRowSelection: function toggleRowSelection(row2) {
    this.handleToggleCheckRowEvent({
      row: row2
    });
    return this.$nextTick();
  },
  setAllSelection: function setAllSelection(value) {
    var afterFullData = this.afterFullData, _this$selectConfig5 = this.selectConfig, selectConfig = _this$selectConfig5 === void 0 ? {} : _this$selectConfig5, treeConfig = this.treeConfig, selection = this.selection;
    var property = selectConfig.checkField, reserve = selectConfig.reserve, checkStrictly = selectConfig.checkStrictly, checkMethod = selectConfig.checkMethod;
    checkStrictly = false;
    hasCheckFieldNoStrictly({
      afterFullData,
      checkMethod,
      checkStrictly,
      property,
      selection,
      treeConfig,
      value
    });
    var selectRows = hasNoCheckFieldNoStrictly({
      afterFullData,
      checkMethod,
      checkStrictly,
      property,
      selection,
      treeConfig,
      value
    });
    setSelectionNoStrictly({
      _vm: this,
      checkStrictly,
      reserve,
      selectRows,
      selection,
      value,
      afterFullData
    });
    this.treeIndeterminates = [];
    this.checkSelectionStatus();
  },
  // 根据表格选中状态，给头部复现框赋值状态（全选，半选，未选）
  checkSelectionStatus: function checkSelectionStatus() {
    var afterFullData = this.afterFullData, selection = this.selection, treeIndeterminates = this.treeIndeterminates;
    var _ref244 = this.selectConfig || {}, checkField = _ref244.checkField, checkMethod = _ref244.checkMethod;
    var _ref245 = {}, everyHandler = _ref245.everyHandler, someHandler = _ref245.someHandler;
    if (checkField) {
      everyHandler = checkMethod ? function(row2, rowIndex) {
        return !checkMethod({
          row: row2,
          rowIndex
        }) || get(row2, checkField);
      } : function(row2) {
        return get(row2, checkField);
      };
      someHandler = function someHandler2(row2) {
        return get(row2, checkField) || ~treeIndeterminates.indexOf(row2);
      };
      this.isAllSelected = false;
      this.headerCheckDisabled = checkMethod && afterFullData.length && afterFullData.every(function(row2, rowIndex) {
        return !checkMethod({
          row: row2,
          rowIndex
        });
      });
      afterFullData.length && (this.isAllSelected = afterFullData.every(everyHandler));
      this.isIndeterminate = !this.isAllSelected && afterFullData.some(someHandler);
    } else {
      everyHandler = function everyHandler2(row2, rowIndex) {
        return !checkMethod({
          row: row2,
          rowIndex
        });
      };
      this.headerCheckDisabled = checkMethod && afterFullData.length && afterFullData.every(everyHandler);
      everyHandler = checkMethod ? function(row2, rowIndex) {
        return !checkMethod({
          row: row2,
          rowIndex
        }) || ~selection.indexOf(row2);
      } : function(row2) {
        return ~selection.indexOf(row2);
      };
      someHandler = function someHandler2(row2) {
        return ~treeIndeterminates.indexOf(row2) || ~selection.indexOf(row2);
      };
      this.isAllSelected = false;
      afterFullData.length && (this.isAllSelected = afterFullData.every(everyHandler));
      this.isIndeterminate = !this.isAllSelected && afterFullData.some(someHandler);
    }
  },
  // 保留选中状态
  reserveCheckSelection: function reserveCheckSelection() {
    var fullDataRowIdData = this.fullDataRowIdData, selection = this.selection;
    var _ref246 = this.selectConfig || {}, reserve = _ref246.reserve;
    var rowkey = getRowkey(this);
    if (reserve && selection.length) {
      this.selection = selection.map(function(row2) {
        var rowCache = fullDataRowIdData["" + get(row2, rowkey)];
        return rowCache ? rowCache.row : row2;
      });
    }
  },
  // 多选，选中所有事件
  triggerCheckAllEvent: function triggerCheckAllEvent(event, value) {
    var _this$$grid5;
    this.setAllSelection(value);
    var eventParams = {
      selection: this.getSelectRecords(),
      checked: value,
      $table: this
    };
    emitEvent(this, "select-all", [eventParams, event]);
    (_this$$grid5 = this.$grid) == null ? void 0 : _this$$grid5.emitter.emit("select-all", [eventParams, event]);
  },
  // 多选，切换所有行的选中状态
  toggleAllSelection: function toggleAllSelection() {
    this.triggerCheckAllEvent(null, !this.isAllSelected);
    return this.$nextTick();
  },
  clearSelection: function clearSelection() {
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    var _ref247 = this.selectConfig || {}, checkField = _ref247.checkField;
    if (checkField) {
      treeConfig ? eachTree(tableFullData, function(item) {
        return set(item, checkField, false);
      }, treeConfig) : tableFullData.forEach(function(item) {
        return set(item, checkField, false);
      });
    }
    Object.assign(this, {
      isAllSelected: false,
      isIndeterminate: false,
      selection: [],
      treeIndeterminates: []
    });
    return this.$nextTick();
  },
  initMultipleHistory: function initMultipleHistory() {
    var isMultipleHistory = this.$grid.isMultipleHistory;
    var toolbarVm = this.getVm("toolbar");
    var storageKey = toolbarVm.settingOpts.storageKey, toolbarId = toolbarVm.id;
    var remoteSelectedMethod = toolbarVm.setting.multipleHistory.remoteSelectedMethod;
    var remoteSelectedPromise;
    if (isMultipleHistory && toolbarVm && toolbarVm.setting && toolbarVm.setting.multipleHistory && remoteSelectedMethod) {
      if (typeof remoteSelectedMethod === "function") {
        remoteSelectedPromise = remoteSelectedMethod();
        if (typeof remoteSelectedPromise.then === "function") {
          remoteSelectedPromise.then(function(storeStr) {
            var storeObj = toStringJSON(storeStr);
            storeObj = storeObj && storeObj[storageKey] || null;
            storeObj = (storeObj || {})[toolbarId] || {};
            var _storeObj = storeObj, columns2 = _storeObj.columns, pageSize = _storeObj.pageSize;
            toolbarVm.applySettings({
              columns: columns2,
              pageSize
            });
          });
        }
      }
    }
  },
  // 显示多选工具栏
  showSelectToolbar: function showSelectToolbar() {
    var _this$$grid6 = this.$grid, selectToolbar = _this$$grid6.selectToolbar, showHeader = _this$$grid6.showHeader, selectToolbarStore = this.selectToolbarStore;
    if (selectToolbar && showHeader) {
      selectToolbarStore.visible = false;
      var selectColumn = find$1(this.visibleColumn, function(item) {
        return item.type === "selection";
      });
      var selected = this.getSelectRecords();
      var position = typeof selectToolbar === "object" ? selectToolbar.position : "";
      if (selectColumn && selected && selected.length) {
        var _GlobalConfig$rowHeig;
        var tinyTheme = this.tinyTheme, vSize3 = this.vSize, $el = this.$el;
        var rowHeight = ((_GlobalConfig$rowHeig = GlobalConfig.rowHeight[tinyTheme]) == null ? void 0 : _GlobalConfig$rowHeig[vSize3 || "default"]) || 40;
        var selectTh = $el.querySelector("th.tiny-grid-header__column.col__selection");
        var tr = selectTh.parentNode;
        var thArr = toArray(tr.childNodes);
        var range = document.createRange();
        var rangeBoundingRect;
        var headerBoundingRect = {
          width: $el.getBoundingClientRect().width,
          height: rowHeight
        };
        var _layout = {
          width: 0,
          height: 0,
          left: 0,
          top: 0,
          zIndex: 20
        };
        var adjust = 1;
        if (selectColumn.fixed === "right") {
          range.setStart(tr, thArr.indexOf(selectTh));
          range.setEnd(tr, thArr.length);
          rangeBoundingRect = range.getBoundingClientRect();
          _layout.left = adjust + "px";
        } else {
          range.setStart(tr, 0);
          range.setEnd(tr, thArr.indexOf(selectTh) + 1);
          rangeBoundingRect = range.getBoundingClientRect();
          _layout.left = rangeBoundingRect.width + adjust + "px";
        }
        _layout.width = headerBoundingRect.width - rangeBoundingRect.width - 2 * adjust + "px";
        if (!selectColumn.fixed && position === "left") {
          range = document.createRange();
          range.setStart(tr, 0);
          range.setEnd(tr, thArr.indexOf(selectTh));
          rangeBoundingRect = range.getBoundingClientRect();
          _layout.left = adjust + "px";
          _layout.width = rangeBoundingRect.width - 2 * adjust + "px";
        }
        _layout.top = headerBoundingRect.height - rangeBoundingRect.height + adjust + "px";
        _layout.height = rangeBoundingRect.height - 2 * adjust + "px";
        return this.$nextTick().then(function() {
          selectToolbarStore.layout = _layout;
          selectToolbarStore.visible = true;
        });
      }
    }
    return this.$nextTick();
  },
  // 切换多选工具栏的显示
  toggleSelectToolbarVisible: function toggleSelectToolbarVisible() {
    this.selectToolbarStore.visible = !this.selectToolbarStore.visible;
    return this.$nextTick();
  },
  // 在空数据时Selection列表头复选框禁用，headerAutoDisabled设置为false就会和旧版本兼容
  handleSelectionHeader: function handleSelectionHeader() {
    var tableFullData = this.tableFullData, visibleColumn = this.visibleColumn, _this$selectConfig6 = this.selectConfig, selectConfig = _this$selectConfig6 === void 0 ? {} : _this$selectConfig6;
    var headerAutoDisabled = selectConfig.headerAutoDisabled;
    var selectionColumn = visibleColumn.find(function(column) {
      return column.type === "selection";
    });
    if ((typeof headerAutoDisabled === "undefined" || typeof headerAutoDisabled === "boolean" && headerAutoDisabled) && !tableFullData.length && selectionColumn) {
      this.headerCheckDisabled = true;
    }
  }
};
var Checkbox = {
  host: "table",
  install: function install13(host) {
    Object.assign(host.methods, Methods$1);
  }
};
var Methods = {
  // 展开树节点事件
  triggerTreeExpandEvent: function triggerTreeExpandEvent(event, _ref248) {
    var _this83 = this;
    var row2 = _ref248.row;
    var currentColumn = this.currentColumn, currentRow = this.currentRow;
    var rest = this.toggleTreeExpansion(row2);
    var eventParams = {
      $table: this,
      row: row2,
      rowIndex: this.getRowIndex(row2)
    };
    emitEvent(this, "toggle-tree-change", [eventParams, event]);
    this.$nextTick(function() {
      if (currentRow) {
        _this83.setCurrentRow(currentRow);
      } else if (currentColumn) {
        _this83.setCurrentColumn(currentColumn);
      }
    });
    return rest;
  },
  // 切换/展开树节点
  toggleTreeExpansion: function toggleTreeExpansion(row2) {
    return this.setTreeExpansion(row2);
  },
  // 处理默认展开树节点
  handleDefaultTreeExpand: function handleDefaultTreeExpand() {
    var _this84 = this;
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    if (!treeConfig) return;
    var children = treeConfig.children, expandAll = treeConfig.expandAll, rowids = treeConfig.expandRowKeys;
    var treeExpandeds = [];
    var rowkey = getRowkey(this);
    var isNonEmptyArr = function isNonEmptyArr2(arr) {
      return isArray(arr) && arr.length;
    };
    var doExpandAll = function doExpandAll2() {
      filterTree(tableFullData, function(row2) {
        return isNonEmptyArr(row2[children]) && treeExpandeds.push(row2);
      }, treeConfig);
      _this84.treeExpandeds = treeExpandeds;
    };
    var doExpandRows = function doExpandRows2() {
      rowids.forEach(function(rowid2) {
        var matchObj = findTree$1(tableFullData, function(item) {
          return rowid2 === get(item, rowkey);
        }, treeConfig);
        matchObj && isNonEmptyArr(matchObj.item[children]) && treeExpandeds.push(matchObj.item);
      });
      _this84.treeExpandeds = treeExpandeds;
    };
    expandAll ? doExpandAll() : rowids ? doExpandRows() : "";
    graphFullData(this);
    return this.handleTableData();
  },
  setAllTreeExpansion: function setAllTreeExpansion(expanded) {
    var tableFullData = this.tableFullData, treeConfig = this.treeConfig;
    var children = treeConfig.children;
    var treeExpandeds = [];
    if (expanded) {
      var rowHandler = function rowHandler2(row2) {
        if (row2[children] && row2[children].length) {
          treeExpandeds.push(row2);
        }
      };
      eachTree(tableFullData, rowHandler, treeConfig);
    }
    this.treeExpandeds = treeExpandeds;
    graphFullData(this);
    return this.handleTableData().then(this.recalculate);
  },
  // 设置展开树形节点，二个参数设置这一行展开与否：支持单行，支持多行
  setTreeExpansion: function setTreeExpansion(rows, expanded) {
    var treeConfig = this.treeConfig, treeExpandeds = this.treeExpandeds, tableFullData = this.tableFullData;
    var accordion = treeConfig.accordion, children = treeConfig.children;
    var isToggle = arguments.length === 1;
    if (!rows) {
      return this.$nextTick().then(this.recalculate);
    }
    if (!isArray(rows)) {
      rows = [rows];
    }
    if (accordion) {
      rows = rows.slice(rows.length - 1, rows.length);
    }
    var treeExpandedsCopy = [].concat(treeExpandeds);
    rows.forEach(function(row2) {
      if (row2[children] && row2[children].length) {
        var _index9 = treeExpandedsCopy.indexOf(row2);
        if (accordion) {
          var matchObj = findTree$1(tableFullData, function(item) {
            return item === row2;
          }, treeConfig);
          remove(treeExpandedsCopy, function(item) {
            return ~matchObj.items.indexOf(item);
          });
        }
        if (~_index9 && (isToggle || !expanded)) {
          treeExpandedsCopy.splice(_index9, 1);
          return;
        }
        if (!~_index9 && (isToggle || expanded)) {
          treeExpandedsCopy.push(row2);
        }
      }
    });
    this.treeExpandeds = treeExpandedsCopy;
    graphFullData(this);
    return this.handleTableData().then(this.recalculate);
  },
  hasTreeExpand: function hasTreeExpand(row2) {
    return ~this.treeExpandeds.indexOf(row2);
  },
  clearTreeExpand: function clearTreeExpand() {
    var _this85 = this;
    var hasExpand = this.treeExpandeds.length;
    this.treeExpandeds = [];
    graphFullData(this);
    return this.handleTableData().then(function() {
      return hasExpand ? _this85.recalculate() : 0;
    });
  },
  getTreeExpandeds: function getTreeExpandeds() {
    return this.treeExpandeds;
  }
};
var Tree = {
  host: "table",
  install: function install14(host) {
    Object.assign(host.methods, Methods);
  }
};
var version = "3.undefined";
var plugins = [Panel, Edit, Export, Keyboard, Validator, Resize, Panel$1, FetchData, Pager, Toolbar, ColumnAnchor, Dragger, Sort, Tooltip, Checkbox, Tree];
Grid$1.setup({
  i18n: t
});
Grid$1.t = t;
plugins.map(function(plugin) {
  return plugin.install(plugin.host === "grid" ? Grid : Table);
});
var getWrapFunc = function getWrapFunc2(name) {
  return function() {
    var tinyTable = this.$refs.tinyTable;
    if (tinyTable) {
      var _tinyTable$name;
      for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        args[_key3] = arguments[_key3];
      }
      return (_tinyTable$name = tinyTable[name]) == null ? void 0 : _tinyTable$name.apply(tinyTable, args);
    }
  };
};
Object.keys(Table.methods).forEach(function(name) {
  if (!Grid.methods[name]) {
    Grid.methods[name] = getWrapFunc(name);
  }
});
Grid.version = version;
Grid.install = function(Vue) {
  Vue.component(Grid.name, Grid);
};

export { Grid$1 as GridAdapter, Column as GridColumn, GlobalConfig$1 as GridConfig, Radio as GridRadio, index as GridTools, Grid as default };
